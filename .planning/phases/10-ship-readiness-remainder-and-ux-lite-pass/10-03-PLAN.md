---
phase: 10-ship-readiness-remainder-and-ux-lite-pass
plan: 03
type: execute
wave: 3
depends_on: [10-01, 10-02]
files_modified:
  - docs/environmental_restore_check.py
  - docs/router_ui_census.py
  - docs/sequence_dispatch_check.py
  - docs/phase6_self_check.py
  - src/PROSOCHE-Dumb.xml
autonomous: true
requirements: [AUDIT-03, AUDIT-04, SESS-07, CIRC-03, CIRC-05, SAFE-01, SAFE-02, SAFE-03, SAFE-05]

must_haves:
  truths:
    - "A future change that deletes dimming(), silence(), restore_managed_settings(), clear_snapshot(), device_detail(), the settings_snapshot subtree, or the brightness/volume entries in NUMERIC_OPERAND_FIELDS turns a check red rather than shipping silently."
    - "Emergency Restore still calls restore_managed_settings(), so SAFE-05 remains satisfied rather than deferred."
    - "Every volume write targets media audio only, never the ringer, and the dim target is strictly positive."
    - "Every user-facing action in the OPEN arm is enclosed by the Circle-0 silent-band conditional, so no future phase can reintroduce a surface into the silent band unnoticed."
    - "The CLOSE arm contains zero menus, and the MANUAL arm contains exactly one shownote and it is gated."
    - "Sequence entries that dispatch nothing are reported by name, including the known Circle 8 orphan, without failing the run and without hard-coding a condition code or a matching strategy."
    - "The structural self-check suite for the Dumb fork is green."
  artifacts:
    - "docs/environmental_restore_check.py"
    - "docs/router_ui_census.py"
    - "docs/sequence_dispatch_check.py"
    - "docs/phase6_self_check.py repaired"
  key_links:
    - "NUMERIC_OPERAND_FIELDS brightness/volume entries -> normalise_numeric_operands() -> the coercion aggrandizement on every variable-fed setbrightness site"
    - "manual_emergency_restore() -> restore_managed_settings() -> settings_snapshot leaves -> SAFE-05"
    - "the silent-band conditional group from 10-01 -> every OPEN-arm surface -> docs/router_ui_census.py"
    - "Config sequences arrays -> primitive_dispatch()'s Selected Primitive conditionals -> docs/sequence_dispatch_check.py orphan report"
---

<objective>
Turn three currently-unguarded properties into automated checks, and repair the one stale checker
that is red at HEAD.

Purpose: the brightness/volume cut was cancelled by user decision, which means `dimming()`,
`silence()`, `restore_managed_settings()` and the `settings_snapshot` subtree are load-bearing
safety machinery that a future subtractive pass could remove with nothing to stop it. Separately,
10-01's silent band is only as good as its enclosure, and the Config sequences table can name a
primitive that dispatches nothing with no error anywhere - which is exactly how Circle 8 shipped
dead.

Output: three new standalone checker scripts in the house style, plus a one-line repair to
`docs/phase6_self_check.py`, leaving the Dumb-fork check suite green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.claude/CLAUDE.md
@docs/CAPABILITY-DECISIONS.md
@.planning/phases/10-ship-readiness-remainder-and-ux-lite-pass/10-PATTERNS.md
@.planning/phases/10-ship-readiness-remainder-and-ux-lite-pass/10-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: docs/environmental_restore_check.py - pin the cancelled cut</name>

  <read_first>
    - tools/build_state_engine.py lines 391-475 (device_detail, set_brightness, set_media_volume, clear_snapshot, restore_managed_settings)
    - tools/build_state_engine.py lines 535-578 (dimming and silence, including their has-any-value guards and capture writes)
    - tools/build_state_engine.py lines 2343-2350 (NUMERIC_OPERAND_FIELDS)
    - tools/build_state_engine.py lines 1781-1860 and 2100-2140 (the snapshot seed constants, seed_settings_snapshot, verify_restore_gates)
    - tools/build_state_engine.py lines 1445-1455 (the Emergency Restore branch calling restore_managed_settings)
    - docs/CAPABILITY-DECISIONS.md BD-02 and BD-03, including BD-02's Phase 9 floor addendum
    - docs/phase7_self_check.py (all 47 lines - the require() and plistlib house skeleton)
  </read_first>

  <files>docs/environmental_restore_check.py</files>

  <action>
Write a new standalone checker in the house style established by docs/phase7_self_check.py: a
future-annotations import, ROOT resolved from parents[1], a local require() helper that raises
AssertionError with a message rather than a bare assert, a main() that ends with a single printed
passed line, and the standard name-equals-main guard. Follow
docs/state_engine_self_check.py's structural_check() for the read-only half - open the plist
directly with plistlib and do not shell out to rebuild - but do load the generator module by
importlib spec, as docs/phase7_self_check.py already does, so the source-level assertions can
inspect real module attributes rather than grep strings.

Open the script with a docstring stating plainly why it exists: the brightness/volume MVP cut was
proposed and then cancelled by user decision on 2026-08-16 and reaffirmed 2026-08-17; Dimming and
Silence stay, each as its own distinct Circle, with working capture-and-restore; this script is the
guard that makes a future re-attempt at that cut fail loudly instead of silently removing the
mechanism SAFE-03 and SAFE-05 depend on. Cite BD-02 and BD-03.

Assert, each with its own message:

Source-level, against the imported generator module. Every one of these attributes exists and is
callable: device_detail, set_brightness, set_media_volume, clear_snapshot, restore_managed_settings,
dimming, silence, seed_settings_snapshot, verify_state_seed, verify_restore_gates. The
NUMERIC_OPERAND_FIELDS mapping contains an entry for the set-brightness action identifier whose
value tuple contains the brightness parameter key, and an entry for the set-volume action
identifier whose value tuple contains the volume parameter key - this is the specific exemption the
cancelled cut would have introduced, and its absence is the property being pinned. Using
inspect.getsource on manual_emergency_restore, its body calls restore_managed_settings - this is
SAFE-05, Emergency Restore restoring recoverable environmental state, and it is the one requirement
the cut would have contradicted outright. Using inspect.getsource on close_pipeline, its body also
calls restore_managed_settings - this is SESS-07.

Artifact-level, against the parsed src/PROSOCHE-Dumb.xml. The count of set-brightness actions is
14, the count of set-volume actions is 14, and the count of get-device-details actions is 20;
state the derivation in a comment so a future reader can tell a legitimate change from a
regression - four restore_managed_settings expansions contribute one brightness plus one volume
each, and ten primitive_dispatch renderings contribute one dimming plus one silence each. Every
set-volume action carries a volume-setting parameter whose value selects media audio, never the
ringer - this is SAFE-02 enforced structurally. Every get-device-details action's detail parameter
names either the current-brightness or the current-volume case, so no unaudited device property is
being read. The bootstrap state template contains the settings_snapshot subtree, proving the seed
survived. Parsing the Config literal at action 7, the safety block's dim target is strictly greater
than zero and greater than or equal to the brightness floor, and the allow-volume-increase flag is
false - SAFE-01 as shipped and SAFE-02 as configured. Record in a comment that BD-02's Phase 9
addendum relaxes the historical 10-to-15-percent band on this fork, so the assertion is
strictly-positive rather than a specific floor, and that the real safety mechanism is
capture-and-restore rather than floor avoidance.

Cross-fork. Load tools/build_sentient.py by importlib spec and assert it imports without error -
this is the standing smoke test that the ten verify_* names it imports from build_state_engine all
still resolve.

Do not assert anything about the number of coercion aggrandizements: at HEAD 14 brightness sites
and 4 volume sites carry an explicit coercion while 10 volume sites are fed by an
already-numeric-typed variable that normalise_numeric_operands deliberately leaves alone. Pinning a
count there would encode an artifact of the current emitter rather than a safety property. Record
that measurement in a comment so a future reader does not mistake it for a gap.
  </action>

  <verify>
    <automated>python3 docs/environmental_restore_check.py && python3 -c "import sys; sys.path.insert(0,'tools'); import build_sentient; print('sentient import ok')"</automated>
  </verify>

  <acceptance_criteria>
    - `python3 docs/environmental_restore_check.py` exits 0 and prints a single passed line naming the script.
    - Negative control A: temporarily delete the set-volume entry from NUMERIC_OPERAND_FIELDS in a scratch copy of the generator and point the script at it, or temporarily edit the real table and revert - the script raises AssertionError naming that entry. Record the message in the SUMMARY.
    - Negative control B: temporarily rename `restore_managed_settings` to a different symbol - the script raises AssertionError naming it. Restore and confirm the script passes. Record both outcomes in the SUMMARY.
    - The script asserts all ten generator symbols by name, and `grep -v '^#' docs/environmental_restore_check.py | grep -c restore_managed_settings` returns at least 3 (the attribute assertion plus the two getsource assertions).
    - The script's docstring names the cancelled cut, the user decision dates 2026-08-16 and 2026-08-17, and BD-02 and BD-03.
    - The script does not invoke `subprocess` and does not rebuild `src/PROSOCHE-Dumb.xml`.
  </acceptance_criteria>

  <done>A standing guard exists that fails loudly if the brightness/volume machinery, its numeric-coercion table entries, its media-only volume scoping, its positive dim target, its bootstrap seed, or its two restore call sites are removed or weakened.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: docs/router_ui_census.py - per-arm surface census and the Circle-0 silence invariant</name>

  <read_first>
    - docs/state_engine_self_check.py lines 41-70 (structural_check - the read-only plistlib pattern with no rebuild)
    - docs/phase7_self_check.py lines 12-33 (ROOT, require, the plistlib parse and the menu-list assertion idiom)
    - tools/build_state_engine.py lines 1340-1360 (verify_router_shape - how the router's two exact Input Key tests are located without indices)
    - .planning/phases/10-ship-readiness-remainder-and-ux-lite-pass/10-RESEARCH.md Finding 1a (the arm boundary derivation and the per-arm UI census measured at the pre-Phase-10 artifact)
    - .planning/phases/10-ship-readiness-remainder-and-ux-lite-pass/10-01-SUMMARY.md (the silent-band group introduced in 10-01)
  </read_first>

  <files>docs/router_ui_census.py</files>

  <action>
Write a new read-only checker that censuses every user-facing action by router arm and asserts the
invariants 10-01 and 10-02 established. House skeleton as in Task 1; no rebuild, no subprocess.

Derive the three arm boundaries structurally, never from action indices, which shift on every
build. Walk the action list maintaining a stack of open control-flow GroupingIdentifiers. Locate
the two mode-0 conditionals whose WFInput variable is the normalised input key and whose
conditional-action-string is one of the two recognised router inputs; the OPEN arm is the span
between the first such conditional and its matching mode-1 otherwise, the CLOSE arm the same for
the second, and the MANUAL arm the span between the second conditional's otherwise and its matching
mode-2 end. Fail with a clear message if either conditional cannot be found, so a future router
restructure surfaces here rather than producing a silently empty census.

For each arm, count these identifiers: choosefrommenu at mode 0, ask, alert, notification,
choosefromlist, speaktext, shownote, filter.notes, and the Notes create action. Print the census as
a small table, so the script is useful as a report and not only as a gate.

Assert, each with its own message:
- The CLOSE arm contains zero mode-0 choosefrommenu actions, zero ask actions and zero
  choosefromlist actions. This is the standing evidence for the reported no-menu-on-close criterion:
  whatever the user saw, it did not come from the CLOSE arm.
- The OPEN arm contains zero notification actions, and the whole artifact contains exactly one -
  the CLOSE confirmation, which must lie inside the CLOSE arm.
- The MANUAL arm contains exactly one shownote, exactly one filter.notes and exactly one Notes
  create action.
- The Circle-0 silence invariant, which is the point of this script: every one of the counted
  user-facing actions inside the OPEN arm is enclosed by the silent-band conditional group - the
  mode-0 conditional whose WFInput variable is the next-Circle variable, with a greater-than
  numeric condition against zero. Report any violation by identifier and by the nearest preceding
  comment text, so the failure names the offending surface rather than an index. Give this
  assertion its own explanatory comment stating that it exists so a future phase cannot reintroduce
  a surface into the silent band, and that Circle 0 shows no UI at all is a product invariant, not
  an implementation detail.
- The shownote in the MANUAL arm is enclosed by a conditional whose WFInput variable is the
  show-note request flag - the same property docs/phase7_self_check.py pins, restated here so the
  census is self-contained as a regression guard for the whole of the UX pass.

Do not assert absolute counts for the OPEN or MANUAL arms beyond those named above. The MANUAL arm
legitimately carries many structurally duplicated alerts because the Test-a-Circle submenu renders
the full primitive dispatch nine times; pinning that number would make every future primitive edit
a check failure for no safety gain. Say so in a comment.
  </action>

  <verify>
    <automated>python3 docs/router_ui_census.py && python3 docs/phase7_self_check.py</automated>
  </verify>

  <acceptance_criteria>
    - `python3 docs/router_ui_census.py` exits 0, prints a per-arm census table with a row for each of the nine counted identifiers, and prints a single passed line.
    - The printed census shows zero mode-0 choosefrommenu actions in the CLOSE arm and zero notification actions in the OPEN arm.
    - Negative control: temporarily move any single OPEN-arm alert outside the silent-band conditional in the generator, rebuild, and run the script - it raises AssertionError naming that identifier and a nearby comment. Restore, rebuild, and confirm it passes. Record both outcomes in the SUMMARY.
    - The script locates arm boundaries by walking control-flow groups; `grep -v '^#' docs/router_ui_census.py | grep -c 'GroupingIdentifier'` returns at least 1, and the script contains no literal action-index constants above 10.
    - The script does not invoke `subprocess` and does not rebuild `src/PROSOCHE-Dumb.xml`.
    - Running the script twice in succession produces identical output.
  </acceptance_criteria>

  <done>A per-arm surface census exists that reports the full UI inventory and fails if the CLOSE arm grows a menu, if an OPEN notification returns, if the Note-showing action loses its gate, or if any OPEN-arm surface escapes the Circle-0 silent band.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: docs/sequence_dispatch_check.py plus the docs/phase6_self_check.py repair</name>

  <read_first>
    - tools/build_state_engine.py lines 644-668 (primitive_dispatch - the Selected Primitive lookup and the per-primitive conditional arms)
    - tools/build_state_engine.py lines 2638-2650 (normalize_open_apps - which deliberately re-emits only two parameters on openapp)
    - docs/phase6_self_check.py lines 55-75 (the exit-route shape assertions, including the stale one)
    - docs/CAPABILITY-DECISIONS.md BD-06 Decisions 4 and 5 (the slot table, the abolition of combined entries, and the move to exact matching)
    - .planning/ROADMAP.md Phase 10, the paragraph headed as the known open defect deliberately not fixed here
    - docs/state_engine_self_check.py lines 41-50 (the read-only plistlib pattern)
  </read_first>

  <files>docs/sequence_dispatch_check.py, docs/phase6_self_check.py, src/PROSOCHE-Dumb.xml</files>

  <action>
Two independent pieces of work, both small, grouped because both are checker hygiene.

Piece 1 - the sequence/dispatch reporter. Write docs/sequence_dispatch_check.py in the read-only
house style. Parse the Config JSON literal at action 7 of src/PROSOCHE-Dumb.xml and collect every
distinct entry from every array under the sequences object. Split each entry on the plus character
into component primitive names, so the script reads combined entries correctly today and continues
to read single-name entries correctly after BD-06 Decision 5 abolishes combined entries - the split
of a name with no plus is the name itself, so the code needs no branch.

Independently, derive the dispatch surface from the artifact rather than from any assumption about
how matching works. Collect every mode-0 conditional whose WFInput variable is the selected
primitive variable; for each, record the string it tests and its condition code. Determine match
semantics per conditional from its own code: a contains code means substring containment of the
tested string within a sequence entry, an exact-string code means equality. Any other code is
reported as unknown match semantics and its branch is counted as unresolved rather than causing a
failure. Do not hard-code either code as an invariant and do not assume substring matching - BD-06
Decision 5 moves dispatch to exact matching in a later phase, and this script must survive that
change without edits.

Report three lists and exit 0 in every case: sequence entry components that no dispatch branch
resolves (orphans); dispatch branches that no sequence entry names (unreachable); and any branch
with unknown match semantics. Print each orphan with the sequences and Circle positions it occupies,
so the report is directly actionable. Carry a module-level constant listing the known, accepted
orphans with the value that is accepted today - the Voice entry, which under contains-matching
resolves to no branch because the emitter deliberately skips rendering a separate Voice arm - and
print each known orphan on its own line marked as a known open defect with a pointer to the todo
that owns it. Exiting 0 on a known orphan is deliberate and required by the ROADMAP: Circle 8's dead
dispatch is a later phase's work, and this script must record it rather than block on it. Print a
clearly marked line for any orphan that is not in the known list, and still exit 0 - state in the
docstring that this is a reporting script, not a gate, and that a future phase may promote it to a
build guard once the roster and the matching strategy have settled under BD-06.

Piece 2 - the repair. docs/phase6_self_check.py is the one script red at HEAD. Its exit-route shape
assertion requires an app-name parameter on the open-app action, but normalize_open_apps()
deliberately clears the parameters and re-emits only the app identifier and the selected-app
parameter, so the assertion contradicts the generator by construction. Drop the app-name term from
that assertion, keeping the selected-app term, and add a comment citing normalize_open_apps() by
name and line so the next reader can see why the term went rather than assuming it was weakened to
get to green. Change nothing else in that file.
  </action>

  <verify>
    <automated>python3 docs/sequence_dispatch_check.py && python3 docs/phase6_self_check.py && python3 docs/phase5_self_check.py && python3 docs/phase7_self_check.py && python3 docs/phase9_self_check.py && python3 docs/state_engine_self_check.py && python3 docs/sentient_audit_check.py && python3 docs/environmental_restore_check.py && python3 docs/router_ui_census.py</automated>
  </verify>

  <acceptance_criteria>
    - `python3 docs/sequence_dispatch_check.py` exits 0 and prints three labelled lists plus a summary line.
    - Its output marks the Voice sequence entry as a known open defect, names the sequences and Circle positions it occupies, and points at the owning todo path.
    - `grep -v '^#' docs/sequence_dispatch_check.py | grep -cE '\b99\b'` and `grep -v '^#' docs/sequence_dispatch_check.py | grep -cE '\bcontains\b'` each show any occurrence only inside the per-conditional semantics dispatch, never in a comparison that presumes one code - confirm by reading the two call sites and record the confirmation in the SUMMARY.
    - Negative control: temporarily rename one primitive in a scratch copy of the Config sequences arrays so it matches no branch, run the script, and confirm it reports that name as an unknown orphan and still exits 0. Restore and re-run. Record both outcomes in the SUMMARY.
    - `python3 docs/phase6_self_check.py` exits 0 and prints its passed line; the repaired assertion still requires the selected-app parameter and carries a comment naming normalize_open_apps.
    - `git diff --stat docs/phase6_self_check.py` shows changes confined to the single assertion line plus its new comment.
    - All eight Dumb-fork checks named in the verify command exit 0 in one run. `docs/sentient_core_check.py` is deliberately excluded here - it compares the two forks and is expected red until 10-04 rebuilds Sentient.
  </acceptance_criteria>

  <done>A reporting script names every sequence entry that dispatches nothing, records the Circle 8 Voice orphan as known without failing on it, and does so without hard-coding a condition code or a matching strategy; and the Dumb-fork check suite is fully green.</done>
</task>

</tasks>

<artifacts_this_phase_produces>

Produced by this plan:

| Kind | Symbol / path | Notes |
|---|---|---|
| Checker script | `docs/environmental_restore_check.py` | Pins the cancelled brightness/volume cut, SAFE-01/02/03/05, SESS-07, AUDIT-03/04, CIRC-03/05 |
| Checker script | `docs/router_ui_census.py` | Per-arm UI census plus the Circle-0-shows-nothing invariant |
| Checker script | `docs/sequence_dispatch_check.py` | Sequence-to-dispatch coverage reporter; records the Voice orphan, never fails on it |
| Module constant | the known-accepted-orphan list in `docs/sequence_dispatch_check.py` | Currently one entry |
| Repair | `docs/phase6_self_check.py` open-app route assertion | Drops the stale app-name term |

Suite after this plan (Dumb fork): `state_engine`, `phase5`, `phase6`, `phase7`, `phase9`,
`sentient_audit`, `environmental_restore`, `router_ui_census`, `sequence_dispatch` - all green.
`sentient_core` is red until 10-04 rebuilds Sentient.

</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Shortcut -> device environment (brightness, volume, Color Filters) | The accessibility-stranding boundary. This plan does not cross it; it guards the code that does |
| Shortcut -> `state.json` `settings_snapshot` subtree | Holds the captured originals. If the seed or the readers vanish, a change becomes unrestorable |
| Future maintainer -> generator source | The realistic actor here. A subtractive pass with good intentions is the threat these checks exist to catch |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-14 | Denial of Service (accessibility stranding) | `restore_managed_settings()` removed from `manual_emergency_restore()` | high | mitigate | Emergency Restore is the last resort for a user left dark or silent; removing its restore call contradicts SAFE-05 outright. Task 1 asserts the call by source inspection with a demonstrated negative control |
| T-10-15 | Denial of Service (accessibility stranding) | `restore_managed_settings()` removed from `close_pipeline()` | high | mitigate | Without it a normal CLOSE leaves the change outstanding, contradicting SESS-07. Task 1 asserts the call by source inspection |
| T-10-16 | Tampering | `NUMERIC_OPERAND_FIELDS` losing its brightness or volume entry | high | mitigate | That exemption is precisely what the cancelled cut would have introduced; without it, variable-fed writes lose their coercion and silently no-op again, which is the Phase 9 defect. Task 1 pins both entries with a negative control |
| T-10-17 | Denial of Service | Volume written to the ringer, or a non-positive dim target | high | mitigate | A ringer write can produce startling output (SAFE-02); a zero or negative dim target is outside the range BD-02's addendum permits. Task 1 asserts media-only scoping on every volume write and a strictly positive dim target at or above the configured floor |
| T-10-18 | Denial of Service | A surface reintroduced into the Circle-0 silent band | medium | mitigate | The band's value collapses the moment one dialog escapes it, and nothing else would notice. Task 2 asserts every counted OPEN-arm surface is enclosed by the silent-band group, with a demonstrated negative control |
| T-10-19 | Denial of Service | A sequence entry that dispatches nothing | medium | accept | An unmatched entry is a silent no-op, not a crash or an unsafe state. The Circle 8 Voice orphan is a known open defect the ROADMAP assigns to a later phase, so Task 3 reports it and exits 0 by design rather than blocking this phase |
| T-10-20 | Repudiation | A checker weakened to reach green | medium | mitigate | `docs/phase6_self_check.py`'s stale term is dropped with a comment citing the generator function that contradicts it, and the diff is required to be confined to that line plus the comment - so the change is auditable as a correction rather than a concession |
</threat_model>

<verification>
- All nine Dumb-fork checks exit 0 in one run: `state_engine`, `phase5`, `phase6`, `phase7`, `phase9`, `sentient_audit`, `environmental_restore`, `router_ui_census`, `sequence_dispatch`.
- `python3 -c "import sys; sys.path.insert(0,'tools'); import build_sentient"` imports cleanly.
- Every negative control named in the acceptance criteria fails as expected and passes again after restoration, with both outcomes recorded in the SUMMARY.
- `docs/sentient_core_check.py` is expected red at the end of this plan and is not run as a gate; 10-04 rebuilds Sentient and restores it.
</verification>

<success_criteria>
- The cancelled brightness/volume cut is pinned by an automated guard covering every symbol, both restore call sites, the numeric-coercion table entries, media-only volume scoping, a positive dim target, and the bootstrap seed.
- The Circle-0 silent band is enforced structurally by a check with a demonstrated negative control.
- Sequence entries that dispatch nothing are reported by name with their Circle positions, the known Voice orphan is recorded rather than blocking, and no condition code or matching strategy is hard-coded.
- The Dumb-fork check suite is green.
</success_criteria>

<output>
Create `.planning/phases/10-ship-readiness-remainder-and-ux-lite-pass/10-03-SUMMARY.md` when done.
</output>
