---
phase: 15-circle-8-the-voice-primitive
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/build_state_engine.py
  - tools/build_sentient.py
  - src/PROSOCHE-Dumb.xml
  - src/PROSOCHE-Sentient.xml
  - src/CONFIG-BLOCK.md
  - docs/BUILD-NOTES.md
autonomous: true
requirements: [CIRC-08, CIRC-09, CIRC-14, DIST-01]

must_haves:
  truths:
    - "At Circle 8 with voice on, the user sees the reflection alert AND hears the same text spoken exactly once (D-01, D-03, CIRC-08)."
    - "At Circle 8 with voice off, the user still sees a Mirror-equivalent alert and hears nothing — the Circle is never empty (D-01)."
    - "At Circle 7 the user sees the reflection and hears nothing, whatever voice_enabled holds (D-02, CIRC-14)."
    - "Circle 8's words are the same 30 fact-gated Mirror templates as Circle 7's — the escalation is the modality, not the copy (D-03)."
    - "The `Spoken This Run` guard is byte-identical to what shipped before this phase; no reset, no second flag, no clear step was added (D-06)."
    - "Circle 9 still dispatches `Frozen` in all three sequences and `ice_start()` is untouched (CIRC-09)."
    - "Both forks pass validator gate A clean at --target-macos 26 --target-platform all (DIST-01)."
    - "Two distinct sequence-entry names never resolve to action-equal dispatch branch bodies, so an escalation on paper is an escalation in emitted actions (CIRC-14, edge: adjacency)."
    - "No sequence-entry component resolves to more than one dispatch branch name, so dispatch order carries no semantics and cannot silently reorder behaviour (CIRC-14, edge: ordering)."
    - statement: "Every Mirror template list carries at least 9 rows so `WFItemIndex = Circle Next` (1..9) is always in range, and no user-facing surface — alert or speech — is reachable from a Circle-0 OPEN."
      verification: backstop
  prohibitions:
    - "The Voice must not raise, unmute, or otherwise alter the device audio output level to make itself heard. Inaudibility is an accepted outcome; startling the user is not."
    - "The Voice must not speak when the user withheld speaking consent at import, and no path may reach the speech action other than through that consent gate."
    - "The spoken reflection must not use shaming, scolding, or accusatory framing — copy authored to be read silently must not become a reprimand when it is spoken aloud."
    - "Circle 8 must not become an escalation only on paper: if a user cannot tell Circle 8 from Circle 7 in the shipped configuration, that must be recorded as a named, accepted cost rather than passing silently as CIRC-14 satisfied."
  artifacts:
    - "tools/build_state_engine.py :: _mirror_body()"
    - "tools/build_state_engine.py :: mirror()"
    - "tools/build_state_engine.py :: voice()"
    - "tools/build_state_engine.py :: verify_speaktext_placement()"
    - "tools/build_sentient.py :: verify_speaktext_placement armed in the import list and called"
    - "src/PROSOCHE-Dumb.xml (regenerated)"
    - "src/PROSOCHE-Sentient.xml (regenerated)"
  key_links:
    - "primitive_dispatch()'s name-to-function tuple: ('Mirror', mirror) and ('Loud Mirror', voice) — the one line whose right-hand side changes, and the only place the 7-vs-8 difference becomes readable at the call site."
    - "_mirror_body() is the single consumer of mirror_text(), which is the single consumer of MIRROR_BASELINES / MIRROR_SUCCESSES / MIRROR_LAPSES — the 30-template surface stays single-sourced for DUMB-02/DUMB-03 and docs/phase7_self_check.py."
    - "verify_speaktext_placement() resolves branch spans through enclosing_groups(), so it stays correct if the dispatch condition code ever moves again."
---

<objective>
Split the interim `mirror_and_voice()` into two primitives — `mirror()` (Circle 7, shows) and
`voice()` (Circle 8, shows *and* speaks) — retarget the dispatch tuple, and prove the result
end to end: both forks rebuild, both pass validator gate A, and a new build guard holds the
placement invariant that makes Circle 8 an escalation rather than a replay.

Purpose: Circle 8 is a live dispatch today but it dispatches Circle 7's implementation
verbatim, which is exactly the CIRC-14 violation ("a stronger Circle does not necessarily
replay every weaker Circle's prompt") and leaves CIRC-08's "The Voice" without an identity.
Output: a Core and an Aware artifact in which Circle 7 shows and Circle 8 speaks, and a guard
that fails the build if that ever collapses back into one behaviour.

**THE ROADMAP ENTRY FOR THIS PHASE IS STALE — do not plan or verify against it.** Phase 11
executed on 2026-08-18 and already renamed the Circle-8 sequence entry to `Loud Mirror` in all
three sequences, emitted a real dispatch branch for it, moved dispatch from condition 99
("contains") to condition 4 ("string is"), and promoted `docs/sequence_dispatch_check.py` to a
hard gate with `KNOWN_ORPHANS = {}`. Six of the ROADMAP's seven factual claims are false;
`15-RESEARCH.md` § Pitfall 1 tabulates them. **There is no orphan exemption left to remove and
`KNOWN_ORPHANS` must stay an empty dict.** Treat `tools/build_state_engine.py`, `src/CONFIG-BLOCK.md`
and `docs/BUILD-NOTES.md` §34 as ground truth.

**Standing constraint for this whole phase (D-04 from CONTEXT.md, and it is not this plan's to
close):** the Mirror primitive carries an inherited axis-4 unfilled-picker defect, device-reproduced
three times across two installs and proven to follow the primitive rather than the Circle index.
Whatever this plan ships at Circle 8 inherits it. Plan 15-02 discriminates which action carries it.
A green build here does **not** mean Circle 8 runs on a phone, and no verification step in this plan
may claim it does.

**Standing constraint: `docs/manifest_check.py` goes red the moment either fork is rebuilt and stays
red until plan 15-05 re-signs both forks and re-derives the MANIFEST rows.** That is expected. Do
**not** "fix" it by editing MANIFEST rows without re-signing.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.claude/CLAUDE.md
@.planning/phases/15-circle-8-the-voice-primitive/15-CONTEXT.md
@.planning/phases/15-circle-8-the-voice-primitive/15-RESEARCH.md
@.planning/phases/15-circle-8-the-voice-primitive/15-PATTERNS.md
@.planning/phases/15-circle-8-the-voice-primitive/15-VALIDATION.md
</context>

## Artifacts this phase produces

Every symbol below is **created by Phase 15** and does not exist in the codebase before it. The
plan-review-convergence source-grounding pass must exclude these from drift verification.

| Artifact | File | Plan |
|---|---|---|
| `_mirror_body()` | `tools/build_state_engine.py` | 15-01 |
| `mirror()` | `tools/build_state_engine.py` | 15-01 |
| `voice()` | `tools/build_state_engine.py` | 15-01 |
| `verify_speaktext_placement()` | `tools/build_state_engine.py` | 15-01 |
| `VOICE_ENABLED_KEY`, `VOICE_ENABLED_VARIABLE`, `VOICE_ENABLED_SEED_TRUE`, `VOICE_ENABLED_SEED_FALSE`, `VOICE_ENABLED_LEGACY` | `tools/build_state_engine.py` | 15-03 |
| `normalise_voice_enabled_seed()` | `tools/build_state_engine.py` | 15-03 |
| `_voice_enabled_variables()` | `tools/build_state_engine.py` | 15-03 |
| `verify_voice_enabled_seed()` | `tools/build_state_engine.py` | 15-03 |
| `verify_voice_gates()` | `tools/build_state_engine.py` | 15-04 |
| `verify_voice_path_volume_silence()` | `tools/build_state_engine.py` | 15-04 |
| `branch_bodies()` + a fifth `require(...)` | `docs/sequence_dispatch_check.py` | 15-04 |
| four new `verify_*` names in the import list and call block | `tools/build_sentient.py` | 15-01, 15-03, 15-04 |
| `drafts/build_mirror_picker_probe.py`, the probe XML, the signed probe, `FINDINGS.md` | `.planning/spikes/011-mirror-primitive-picker-discriminator/` | 15-02 |
| `docs/BUILD-NOTES.md` §36 (Phase 15 recording duty) | `docs/BUILD-NOTES.md` | 15-05 |
| a new signed-artifact block in `artifacts/shortcuts/MANIFEST.md` | `artifacts/shortcuts/MANIFEST.md` | 15-05 |
| `COVERAGE.md` | `.planning/phases/15-circle-8-the-voice-primitive/` | (written at plan time) |

**Symbol retired by this phase:** `mirror_and_voice()` in `tools/build_state_engine.py`. It has
exactly five in-source references (its `def`, three comment mentions, and two tuple entries) and
zero references from any `docs/*_check.py`; `docs/environmental_restore_check.py`'s
`REQUIRED_SYMBOLS` tuple does **not** name it, measured 2026-08-18.

<assumption_delta_decision>
**Transition:** one primitive (`mirror_and_voice`) becomes two (`mirror`, `voice`) — a
singular→plural split.

**Now-primary noun:** **`voice`** (Circle 8's designed primitive), standing alongside **`mirror`**
(Circle 7's).

**Decision: `promote`.** `mirror_and_voice` is retired outright rather than kept alongside as a
compatibility shim or given a `mode` parameter.

**Rationale:** a single function whose body is two interleaved behaviours behind conditionals is
what let Circle 8 replay Circle 7 for four phases without anyone seeing it at the call site; the
split makes the 7-vs-8 difference legible in the dispatch tuple itself and keeps each primitive's
rendered action count independently measurable.

**Invariant guard against a future re-merge:** the "no two distinct sequence-entry names resolve to
action-equal branch bodies" assertion added to `docs/sequence_dispatch_check.py` in plan 15-04 goes
red if any later pass collapses them back together.
</assumption_delta_decision>

<flagged_assumptions>
Surfaced by the spec-less edge probe and **not** authored into `must_haves` (three `unclassified`
rows stay `unresolved` by protocol — they are never auto-backstopped and never silently dropped):

- **CIRC-08 — unclassified.** "The Voice speaks the Mirror at most once per run, only when voice is
  enabled, never at unsafe levels" resisted edge classification. Assumed decomposition, stated so it
  can be challenged: *once per run* = the run-scoped `Spoken This Run` variable plus condition-4
  dispatch making exactly one branch fire (D-06); *only when enabled* = the `voice_enabled > 0` gate
  enclosing every speech site; *never at unsafe levels* = writing no volume at all, because
  `is.workflow.actions.speaktext` exposes no volume parameter. If any of those three readings is
  wrong, CIRC-08 is not satisfied by this phase.
- **CIRC-09 — unclassified.** Assumed to be satisfied by **regression only** — `ice_start()` is not
  touched and Circle 9 remains `Frozen` in all three sequences. If CIRC-09 was expected to gain
  something in this phase, that expectation is not represented anywhere in the plan set.
- **DIST-01 — unclassified.** Assumed to mean gate A only (`--target-macos 26 --target-platform all`,
  exit 0, `Validation passed.`). Gate B is advisory, permanently waivered, and deliberately absent
  from every definition of done in this phase.

Additional flagged assumption specific to this plan:

- **The alert title at Circle 8 stays `"Mirror"`, identical to Circle 7's.** D-01 records that
  "with voice off, Circles 7 and 8 become indistinguishable to the user" and that distinct voice-off
  copy for Circle 8 was offered and declined. Giving Circle 8 a different alert title would make that
  recorded consequence false. The alternative (a `"The Voice"` title, as drawn in `15-RESEARCH.md`'s
  architecture diagram) is deliberately **not taken**; it is recorded in plan 15-05's BUILD-NOTES
  entry as considered-and-declined so a later reader does not mistake it for an oversight.
</flagged_assumptions>

<tasks>

<task type="tracer" tdd="false">
  <name>Task 1: End-to-end "Circle 7 shows, Circle 8 speaks" — one path through every layer</name>
  <files>tools/build_state_engine.py, tools/build_sentient.py, src/PROSOCHE-Dumb.xml, src/PROSOCHE-Sentient.xml</files>
  <precondition>`git merge-base --is-ancestor 7ca8ebbfe467da38e594bdd41687c094a1f0c678 HEAD` exits 0. Measured PASS on 2026-08-18; if it ever fails, ABORT the rebuild rather than proceeding (`.claude/CLAUDE.md` build-provenance gate).</precondition>
  <reversibility rating="reversible">Both new emitters live in one generator file behind a stable dispatch tuple; reverting is a one-commit restore and a rebuild. No on-disk format, no published contract, no external service.</reversibility>
  <read_first>
    - `tools/build_state_engine.py` — read `mirror_text()`, `mirror_templates()`, `mirror_and_voice()`, `primitive_dispatch()` and `silence()` in full. `silence()` is the gate-then-act analog for `voice()`'s speech arm. **Anchor on the symbol names, never on line numbers — this file is ~5,582 lines and every cited line shifts on edit.**
    - `tools/build_state_engine.py` — read the emitter helper vocabulary before writing any action dict: `uid()`, `action()`, `variable()`, `output()`, `text_token()`, `comment()`, `set_var()`, `read_value()`, `if_block()`, `otherwise()`, `end_if()`, `number()`, `alert()`, and `_list_row()`'s docstring.
    - `tools/build_state_engine.py` — read `main()`'s tail: the three `normalise_*` passes and the `verify_*` call block that runs between them and the single `SOURCE.write_bytes(...)`.
    - `tools/build_sentient.py` — read the `from build_state_engine import (...)` block and the site where the twenty imported guards are called.
    - `.planning/phases/15-circle-8-the-voice-primitive/15-PATTERNS.md` — § "`_mirror_body()` / `mirror()` / `voice()`", the four invariants list, and § "the dispatch tuple and its interim comment".
    - `.claude/CLAUDE.md` — § Conventions, the nine parameter-defect axes (axes 2, 5, 6, 8 and 9 all touch this span).
  </read_first>
  <action>
Split `mirror_and_voice()` into three symbols in `tools/build_state_engine.py`, placed where
`mirror_and_voice()` sits today, and retire `mirror_and_voice()`.

**`_mirror_body()`** — returns the shared template-selection actions and nothing else: the three
`mirror_templates()` calls over `MIRROR_BASELINES` / `MIRROR_SUCCESSES` / `MIRROR_LAPSES`, the
baseline `mirror_text(baseline, "Mirror Text")`, the `if_block("Previous Respected", 4, string="true")`
success arm, the `if_block("Previous Respected", 4, string="false")` lapse arm, and both closing
`otherwise(...) / action("is.workflow.actions.nothing") / end_if(...)` pairs. It leaves the named
variable `Mirror Text` set and emits **no** leading comment and **no** alert. It must remain the only
consumer of `mirror_text()`, which is the only consumer of the 30 templates (D-03) — never build a
`is.workflow.actions.list` / `is.workflow.actions.getitemfromlist` pair inline.

**`mirror()`** — Circle 7. Its own leading `comment()` (specific and bulleted, per the generator's
comment discipline) stating that the Mirror **shows** a fact-gated reflection and **does not speak**
— speech moved to Circle 8 per D-02 — followed by `_mirror_body()` and
`alert("Mirror", variable("Mirror Text"))`. It emits **no** `read_value("voice_enabled", ...)`, no
`Voice Enabled` conditional, no `Spoken This Run` conditional and no
`is.workflow.actions.speaktext`. This is the visible behaviour change D-02 accepts knowingly:
Circle 7 goes quiet for existing `voice_enabled = 1` users.

**`voice()`** — Circle 8. Its own leading `comment()` stating the four contract clauses in the
generator's own words: the same 30 fact-gated templates as the Mirror (D-03), speech only with the
user's import-time consent (D-01), at most once per run (D-06), and that no volume is written
anywhere in this path because the speech action exposes no volume parameter (CIRC-08 / SAFE-02).
Then `_mirror_body()`, then `alert("Mirror", variable("Mirror Text"))` — **the same title string
Circle 7 uses, deliberately**, per D-01's recorded cost that the two Circles are indistinguishable
with voice off; do not substitute a distinct title. Then, copied shape-for-shape from the retired
function so the gates stay byte-equivalent (D-06 — the guard stays verbatim, no reset, no second
flag, no clear step): `read_value("voice_enabled", variable("State"), "Voice Enabled")`,
`if_block("Voice Enabled", 2, number=0)`, `if_block("Spoken This Run", 101)`,
`action("is.workflow.actions.speaktext", WFText=variable("Mirror Text"))`,
`number(1, "Spoken This Run")`, and both closing
`otherwise(...) / action("is.workflow.actions.nothing") / end_if(...)` pairs in the same nesting
order. Emit `WFText` and nothing else: `WFSpeakTextWait`, `WFSpeakTextRate`, `WFSpeakTextPitch`,
`WFSpeakTextLanguage` and `WFSpeakTextVoice` are catalog-real but no donor shows how Shortcuts
serializes any of them, so omitting them is the safest fallback and the deviation is recorded in
plan 15-05. Because the alert is emitted **before** the consent gate, the voice-off path already
degrades to a Mirror-equivalent alert and its `otherwise` arm is `is.workflow.actions.nothing` —
that is D-01 implemented, and it is literally the current shape.

Never hand-write a plist dict anywhere in the split. `action()`, `variable()`, `if_block()`,
`text_token()` plus `main()`'s `normalise_string_envelopes` / `normalise_output_names` /
`normalise_numeric_operands` passes own axes 2, 5 and 6 at 4,300 sites and are the only thing that
has ever got all three right. Every control-flow block takes a fresh `uid()` and threads its group
to both `otherwise()` and `end_if()`.

In `primitive_dispatch()`, change the two tuple entries `("Mirror", mirror_and_voice)` and
`("Loud Mirror", mirror_and_voice)` to `("Mirror", mirror)` and `("Loud Mirror", voice)`. Change
nothing else in that function: the condition code stays `4` ("string is"), the seven other entries
stay as they are, and `"Loud Mirror"` remains the shipped Config entry name — do **not** rename any
sequence entry and do **not** touch the Config JSON literal inside `src/PROSOCHE-Dumb.xml`.

In the same edit, correct the generator's own comment block above that tuple. Delete the paragraph
that declares the Circle-8 entry a temporary stand-in awaiting this phase, and update the sentence
listing which primitives keep their Python identifiers so it no longer names the retired function.
**Keep the Circle-6 `Eject` paragraph immediately below it untouched — that stand-in is Phase 17's
and is still in force.** Plan 11-02's standing prohibition cuts both ways: an interim must be named
as interim in the generator's own comment text, so when it stops being an interim the comment must
stop saying so.

Then rebuild both forks in order — `tools/build_state_engine.py` first (it rewrites
`src/PROSOCHE-Dumb.xml` in place), then `tools/build_sentient.py` (it consumes that file and re-runs
twenty imported guards, so a Core change that breaks an invariant surfaces on the Aware side too).
  </action>
  <verify>
    <automated>git merge-base --is-ancestor 7ca8ebbfe467da38e594bdd41687c094a1f0c678 HEAD && python3 tools/build_state_engine.py && python3 tools/build_sentient.py && validate-shortcut src/PROSOCHE-Dumb.xml --target-macos 26 --target-platform all && validate-shortcut src/PROSOCHE-Sentient.xml --target-macos 26 --target-platform all && python3 docs/sequence_dispatch_check.py && python3 docs/phase5_self_check.py && python3 docs/phase7_self_check.py</automated>
  </verify>
  <acceptance_criteria>
    - Both builders exit 0 and both `validate-shortcut` invocations print `Validation passed.` and exit 0. Gate B (`--target-macos 27`) is advisory, permanently waivered, and is **not** part of this criterion.
    - `python3 -c "import plistlib,pathlib;
      [print(p, sum(1 for a in plistlib.loads(pathlib.Path(p).read_bytes())['WFWorkflowActions'] if a.get('WFWorkflowActionIdentifier')=='is.workflow.actions.speaktext')) for p in ('src/PROSOCHE-Dumb.xml','src/PROSOCHE-Sentient.xml')]"`
      prints `11` for each fork. It printed `22` for each before this task — measured 2026-08-18.
    - `python3 -c "import importlib.util,sys; s=importlib.util.spec_from_file_location('b','tools/build_state_engine.py'); m=importlib.util.module_from_spec(s); s.loader.exec_module(m); assert callable(m.mirror) and callable(m.voice) and callable(m._mirror_body); assert not hasattr(m,'mirror_and_voice'); print('split ok')"` prints `split ok`.
    - `python3 -c "import plistlib,pathlib; a=plistlib.loads(pathlib.Path('src/PROSOCHE-Dumb.xml').read_bytes())['WFWorkflowActions']; print(sum(1 for x in a if x.get('WFWorkflowActionIdentifier')=='is.workflow.actions.setvolume'))"` still prints `15` — the split added no volume write anywhere.
    - `grep -c 'DELIBERATE INTERIM' tools/build_state_engine.py` prints `1`. It printed `2` before this task; the surviving match is the Circle-6 `Eject` paragraph owned by Phase 17. *(This is a comment-content assertion — comments are the subject of the check, so the usual `grep -v '^#'` filter is deliberately not applied.)* <!-- planner-discipline-allow: DELIBERATE INTERIM -->
    - `python3 docs/sequence_dispatch_check.py` exits 0 and reports no orphan, no unreachable branch, no unknown strategy and no duplicate. `KNOWN_ORPHANS` in that file is still the empty dict `{}` — no exemption was added.
    - `python3 docs/phase5_self_check.py` exits 0: the nine shipped primitive names and the three sequence names are unchanged, and Circle 9 still dispatches `Frozen` in all three sequences (CIRC-09 by regression).
    - `python3 docs/phase7_self_check.py` exits 0: at least 30 distinct Mirror templates survive the split (DUMB-02).
    - `git status --short -- src/PROSOCHE-Dumb.xml src/PROSOCHE-Sentient.xml` shows **both** files modified — the Aware fork did not silently diverge by staying stale.
  </acceptance_criteria>
  <done>Circle 7 emits a show-only Mirror and Circle 8 emits a show-and-speak Voice; the dispatch tuple names two different functions; both forks rebuild, validate clean at gate A, and carry 11 speech sites each instead of 22; and the generator's own comment no longer calls Circle 8 an interim.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: verify_speaktext_placement() — the guard that makes the escalation structural</name>
  <files>tools/build_state_engine.py, tools/build_sentient.py, src/PROSOCHE-Dumb.xml, src/PROSOCHE-Sentient.xml</files>
  <precondition>Task 1 is committed: `mirror()` and `voice()` exist and `src/PROSOCHE-Dumb.xml` carries 11 `is.workflow.actions.speaktext` sites.</precondition>
  <read_first>
    - `tools/build_state_engine.py` — read `verify_dispatch_coverage()` in full. It is the primary analog: per-branch resolution, condition-code resolved per site rather than hardcoded, and an unrecognised strategy that **raises** rather than being skipped.
    - `tools/build_state_engine.py` — read `verify_circle_zero_silence()` (surface counting) and `enclosing_groups()` (the left-to-right control-flow stack walk; a mode-2 endpoint pops before it is recorded, a mode-0 start pushes after).
    - `tools/build_state_engine.py` — read `verify_panic_escape_isolation()` and `_panic_escape_variables()` for the two mandatory non-vacuity raises and the guard-docstring form.
    - `tools/build_sentient.py` — read the alphabetised `from build_state_engine import (...)` block and the call block for the twenty imported guards.
    - `.planning/phases/15-circle-8-the-voice-primitive/15-PATTERNS.md` — § "the three new build guards", Rules 1–4 and the error-message convention.
  </read_first>
  <action>
Add `verify_speaktext_placement(actions)` to `tools/build_state_engine.py`, placed immediately
beside `verify_dispatch_coverage()` with a comment saying why they are neighbours (both are
statements about the dispatch surface; one asks whether every named entry has a receiver, this one
asks whether two receivers actually differ).

Locate every dispatch branch the same way `verify_dispatch_coverage()` does — mode-0 conditionals
whose `WFInput` variable is `Selected Primitive`, resolving the match strategy **per site** from
that site's own `WFCondition`, never by filtering on a hardcoded code. An unrecognised strategy
raises; it is never excluded. Compute each branch's span from `enclosing_groups(actions)` using the
branch's own `GroupingIdentifier`.

Assert, in this order:

1. The resolved branch set is non-empty and contains at least one branch testing `Mirror` and at
   least one testing `Loud Mirror`. A guard that resolves nothing must fail, not pass vacuously.
2. The resolved `is.workflow.actions.speaktext` site set is non-empty. Zero speech sites is a
   failure for the same reason zero surfaces is.
3. Every speech site lies inside the span of a branch whose tested name is `Loud Mirror`. A speech
   site outside every dispatch branch, or inside any other branch, is a failure.
4. Exactly one speech site per `Loud Mirror` branch span — never zero (the Voice went mute), never
   two (the same utterance emitted twice).
5. Zero speech sites inside any `Mirror` branch span. This is the CIRC-14 clause: it is what stops
   a later pass from quietly re-adding speech to Circle 7 and collapsing the escalation.

Every `raise SystemExit` states, in the file's established convention: what was found, what is
required, **the user-visible failure it prevents**, and what to change. Use
`verify_dispatch_coverage()`'s orphan message as the model.

Register the call in `main()`'s `verify_*` block, after the three `normalise_*` passes and before
the single `SOURCE.write_bytes(...)`, sited next to `verify_dispatch_coverage()`. Add
`verify_speaktext_placement` to `tools/build_sentient.py`'s import list **in alphabetical position**
and call it where that file calls the other twenty. A guard armed on one fork only is the exact
silent-divergence risk the Aware fork exists to close.

**Negative-control duty (project standard, plan 11-10).** Prove the guard has teeth by mutating the
build and measuring the failure, then reverting the mutation. Two mutations, both required:
(a) point the `Loud Mirror` tuple entry back at `mirror` so Circle 8 emits no speech — assertion 4
must raise; (b) append a speech action to `mirror()` so Circle 7 speaks again — assertion 5 must
raise. Record both measured outcomes as a sentence in the guard's own docstring, in the form
`verify_panic_escape_isolation()`'s docstring uses. A guard proven only in the passing direction is
not proven.
  </action>
  <verify>
    <automated>python3 tools/build_state_engine.py && python3 tools/build_sentient.py && python3 -c "import importlib.util; s=importlib.util.spec_from_file_location('b','tools/build_state_engine.py'); m=importlib.util.module_from_spec(s); s.loader.exec_module(m); assert callable(m.verify_speaktext_placement); print('guard present')" && grep -c 'verify_speaktext_placement' tools/build_sentient.py</automated>
  </verify>
  <acceptance_criteria>
    - Both builders exit 0 with the guard armed.
    - `grep -c 'verify_speaktext_placement' tools/build_sentient.py` prints `2` — once in the import list, once at the call site.
    - Mutation (a): with the `Loud Mirror` tuple entry pointed back at `mirror`, `python3 tools/build_state_engine.py` exits non-zero and its message names the missing speech site in the `Loud Mirror` branch. Reverted afterwards; `git diff --stat tools/build_state_engine.py` shows the mutation gone.
    - Mutation (b): with a speech action appended to `mirror()`, `python3 tools/build_state_engine.py` exits non-zero and its message names the speech site found inside a `Mirror` branch. Reverted afterwards.
    - Both measured mutation outcomes appear as a sentence in `verify_speaktext_placement()`'s docstring.
    - The guard resolves the dispatch condition code per site: `grep -c 'WFCondition' tools/build_state_engine.py` is unchanged in meaning — specifically, `verify_speaktext_placement()`'s body contains no equality test against the literal `99`, and any strategy it cannot recognise raises rather than being skipped.
    - `python3 docs/sequence_dispatch_check.py && python3 docs/phase5_self_check.py` both exit 0.
  </acceptance_criteria>
  <done>The build fails if Circle 8 stops speaking or Circle 7 starts speaking, on either fork, and both failure directions have been measured rather than assumed.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: retire the interim declaration in the two documents that mirror it</name>
  <files>src/CONFIG-BLOCK.md, docs/BUILD-NOTES.md</files>
  <precondition>Task 1 is committed — the generator's own comment block no longer declares the Circle-8 entry an interim.</precondition>
  <read_first>
    - `src/CONFIG-BLOCK.md` — read the "Two entries here are deliberately INTERIM" preamble and both bullets beneath it. This file is a **mirror** of the live Config JSON literal, not the source; `verify_dispatch_coverage()` reads the literal inside `src/PROSOCHE-Dumb.xml`.
    - `docs/BUILD-NOTES.md` §34 "Phase 11 — the two interim stand-ins in force, and one review item already closed elsewhere" — read the whole section, including the Circle-8 subsection and the Circle-6 subsection.
    - `.planning/phases/15-circle-8-the-voice-primitive/15-RESEARCH.md` § Pitfall 7.
  </read_first>
  <action>
Bring the two documents that mirror the generator's interim declaration into agreement with the
code, in the same commit-adjacent pass as Task 1.

In `src/CONFIG-BLOCK.md`: rewrite the Circle-8 bullet so it states the shipped fact — Circle 8's
`Loud Mirror` dispatches the designed `voice()` primitive, distinct from Circle 7's `mirror()`, and
that the escalation is the modality rather than the copy (the same 30 fact-gated templates, D-03).
Change the preamble from "two entries" to one, and **leave the Circle-6 `Eject` bullet exactly as it
is** — that stand-in is Phase 17's and is still in force. Do not touch the Config JSON literal
reproduced below the bullets; the entry name `"Loud Mirror"` does not change. If any edit to the
live Config literal ever becomes necessary it goes through `tools/plist_text_edit.py`, never a hand
edit — a hand edit either gets overwritten by the next build or corrupts `attachmentsByRange`
offsets, which can crash Shortcuts on import.

In `docs/BUILD-NOTES.md` §34: add a supersession note to the Circle-8 subsection recording that the
stand-in is **discharged by Phase 15**, naming `voice()` as what replaced it and the date. Follow
the file's existing supersession form — strike or annotate rather than delete, so the correction has
something to point at, exactly as §9 and the DIST-03 entries do. Update §34's own heading or leading
sentence so it no longer claims two stand-ins are in force when one is. **The Circle-6 `Eject`
subsection stays.**

Do not open `docs/BUILD-NOTES.md` §19.7 in this task — plan 15-05 owns that supersession, and
splitting one file's edits across two waves is what causes merge churn.
  </action>
  <verify>
    <automated>python3 docs/sequence_dispatch_check.py && python3 docs/phase5_self_check.py && python3 -c "import pathlib; c=pathlib.Path('src/CONFIG-BLOCK.md').read_text(); assert 'Eject' in c and 'Phase 17' in c, 'the Circle-6 interim bullet was destroyed'; print('config-block ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Phase 15' src/CONFIG-BLOCK.md` prints `0` — that file no longer tells a reader a future phase will replace Circle 8's implementation. *(The literal is used freely elsewhere in this plan's prose; the gate is scoped to this one file.)* <!-- planner-discipline-allow: Phase 15 -->
    - `src/CONFIG-BLOCK.md` still contains the Circle-6 `Eject` bullet naming Phase 17, verbatim and unmoved (asserted by the automated command above).
    - The Config JSON literal block in `src/CONFIG-BLOCK.md` is byte-identical to before: `git diff -- src/CONFIG-BLOCK.md` shows no change inside the fenced ```json block.
    - `docs/BUILD-NOTES.md` §34 carries a dated supersession note naming `voice()` as the replacement for the Circle-8 stand-in, and still carries its Circle-6 `Eject` subsection.
    - `git diff --name-only` for this task lists exactly `src/CONFIG-BLOCK.md` and `docs/BUILD-NOTES.md` — no generator or artifact file moved.
  </acceptance_criteria>
  <done>No document in the repository still describes Circle 8's implementation as a placeholder awaiting this phase, and the one stand-in that is genuinely still in force (Circle 6 / Phase 17) is untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → `state.json` | The import-time speaking-consent answer and the Control Room `Toggle Voice` choice cross into a file the Shortcut later reads as a gate. |
| `state.json` → the running Shortcut | A type-confused or stale value read back here silently decides whether the device speaks. |
| the Shortcut → the device's audio and display | The only outward-facing effect this phase adds. There is no network, no credential, no external service, and no other process. |
| source XML → signed `.shortcut` | The signed AEA1 container is what a phone actually runs; it is asserted, never inferred. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-01 | Denial of Service (user harm) | `voice()` speech path | high | mitigate | Write no volume anywhere in the Voice path. `is.workflow.actions.speaktext` exposes no volume parameter, so "never at unsafe levels" is satisfied by absence, not by a setting. Held by `verify_voice_path_volume_silence()` (plan 15-04) and by the `setvolume` count staying at 15 in Task 1's acceptance criteria. |
| T-15-02 | Repudiation (consent boundary) | the `voice_enabled` gate around every speech site | high | mitigate | Every `is.workflow.actions.speaktext` site must be enclosed by both the `Voice Enabled > 0` group and the `Spoken This Run` condition-101 group, asserted structurally by `verify_voice_gates()` (plan 15-04), never by inspection. |
| T-15-04 | Denial of Service (of the product's own function) | the dispatch registry | medium | mitigate | `verify_dispatch_coverage()` plus `docs/sequence_dispatch_check.py` as a hard gate; `KNOWN_ORPHANS` stays `{}` and no exemption is added. Asserted in Task 1. |
| T-15-05 | Denial of Service (the escalation collapses back into a replay) | `Mirror` vs `Loud Mirror` branch bodies | medium | mitigate | `verify_speaktext_placement()` (Task 2) fails the build if Circle 7 gains speech or Circle 8 loses it, and plan 15-04's action-equality assertion catches the general case. |
| T-15-06 | Spoofing (build provenance) | any rebuild of either fork | high | mitigate | `git merge-base --is-ancestor 7ca8ebbfe467da38e594bdd41687c094a1f0c678 HEAD` as a task `<precondition>` that **aborts** rather than warns. |
| T-15-08 | Information Disclosure | the spoken reflection is audible to bystanders in a way an alert is not | low | accept | Inherent to the primitive. The user consented at import ("May PROSOCHĒ speak to you at the highest circles?") and can revoke at any time via Control Room → Toggle Voice. Recorded, not mitigated; no requirement asks for context detection and the product has no capability to do it. |
| T-15-SC | Tampering | npm / pip / cargo installs | low | accept | This phase installs no package in any ecosystem. `15-RESEARCH.md` § Package Legitimacy Audit records "not applicable"; there is no registry surface to audit and no legitimacy checkpoint is required. |
</threat_model>

<verification>
1. `git merge-base --is-ancestor 7ca8ebbfe467da38e594bdd41687c094a1f0c678 HEAD` exits 0 before either builder runs.
2. `python3 tools/build_state_engine.py && python3 tools/build_sentient.py` — both exit 0 with all guards armed, including the new one.
3. `validate-shortcut src/PROSOCHE-Dumb.xml --target-macos 26 --target-platform all` and the same for `src/PROSOCHE-Sentient.xml` — both print `Validation passed.` and exit 0 (DIST-01, gate A). Gate B is advisory, permanently waivered, and is not chained into this definition of done.
4. `python3 docs/sequence_dispatch_check.py`, `python3 docs/phase5_self_check.py`, `python3 docs/phase7_self_check.py`, `python3 docs/router_ui_census.py` — all exit 0.
5. Measured: 11 `is.workflow.actions.speaktext` sites per fork (down from 22), 15 `is.workflow.actions.setvolume` sites per fork (unchanged).
6. `docs/manifest_check.py` is **expected red** from the moment either fork is rebuilt and stays red until plan 15-05 re-signs both forks. Do not edit MANIFEST rows to make it green.
</verification>

<success_criteria>
- Circle 7 dispatches `mirror()` and shows without speaking; Circle 8 dispatches `voice()` and shows *and* speaks when consent is present.
- With `voice_enabled = 0`, Circle 8 still shows its alert — it never becomes an empty Circle (D-01).
- Both forks rebuild, validate clean at gate A, and are structurally distinguishable at Circles 7 and 8.
- `verify_speaktext_placement()` is armed on both forks and has been measured failing in both directions.
- No document in the repository still describes Circle 8's implementation as an interim.
- CIRC-08 remains recorded as **device-unproven** — the inherited axis-4 unfilled-picker defect is not closed by this plan and no step here claims it is.
</success_criteria>

<output>
Create `.planning/phases/15-circle-8-the-voice-primitive/15-01-SUMMARY.md` when done.
</output>
