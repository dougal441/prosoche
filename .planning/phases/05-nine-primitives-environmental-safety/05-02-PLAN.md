---
phase: 05-nine-primitives-environmental-safety
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified: [tools/build_state_engine.py, src/PROSOCHE-Dumb.xml]
autonomous: true
requirements: [CIRC-06, CIRC-07, CIRC-08, CIRC-09, CIRC-10, CIRC-11, CIRC-12, CIRC-13, CIRC-14]
must_haves:
  truths:
    - "Classic, Black Mirror, and Ambient read their nine entries from Config and dispatch the selected Circle entry."
    - "Exile immediately offers a deterministic route away; Mirror invents no fact; Voice runs at most once when enabled."
    - "Ice duration comes from the active profile, blocked opens do not inflate Heat, and expiry clears cooldown with relief."
  artifacts: [src/PROSOCHE-Dumb.xml, tools/build_state_engine.py]
  key_links:
    - "State.sequence + Circle -> Config sequences entry -> named primitive dispatcher."
    - "State.cooldown_until -> OPEN short-circuit -> redirect; expired cooldown -> clear + configured Heat relief."
---

<objective>
Complete config-driven primitive dispatch, the remaining interventions, and the deterministic Ice lifecycle.
</objective>

<context>
@.planning/phases/05-nine-primitives-environmental-safety/05-01-SUMMARY.md
@src/CONFIG-BLOCK.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve sequence entry and dispatch named primitives</name>
  <files>tools/build_state_engine.py, src/PROSOCHE-Dumb.xml</files>
  <action>Read State.sequence, select Config sequences.Classic/BlackMirror/Ambient, take the Circle item, and dispatch exact names including combined entries. Use nested iOS-26-compatible conditionals. A stronger Circle runs only its configured entry, not every earlier primitive.</action>
  <verify>Assert all three config paths, all nine positions, every primitive name, and combined entries are represented; validator passes.</verify>
  <done>Changing sequence visibly changes dispatch without changing arithmetic.</done>
</task>

<task type="auto">
  <name>Task 2: Add Exile, factual Mirror, and Voice-once</name>
  <files>tools/build_state_engine.py, src/PROSOCHE-Dumb.xml</files>
  <action>Exile immediately routes to a deterministic safe Close/home option without a permission gate; Phase 6 later replaces selection with learned exits. Mirror composes only guarded State facts and omits unavailable clauses. Voice consumes that same Mirror text only when voice_enabled is numeric true and a run-local spoken flag is false, then sets the flag.</action>
  <verify>Assert Exile has a route action, Mirror tokens reference guarded State outputs, and all Speak paths are nested under both voice-enabled and not-yet-spoken gates.</verify>
  <done>Exile, Mirror, and Voice meet their safety and factuality contracts.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Ice start, blocked-open short-circuit, and expiry</name>
  <files>tools/build_state_engine.py, src/PROSOCHE-Dumb.xml</files>
  <action>Read cooldown duration from Config.cooldown_seconds for the active profile. Ice writes cooldown_until once and routes away. OPEN during a live cooldown shows remaining time where available and exits before Heat/open-count mutation. Expiry clears cooldown, applies Config heat.ice_expiry_relief, clamps Heat, and continues; blocked attempts never compound Heat.</action>
  <verify>Structural control-flow check proves the live-cooldown branch contains no Heat/open-count mutation or writer that inflates them; profile durations are Config reads; expiry clears cooldown. Validator passes.</verify>
  <done>Ice is deterministic, finite, profile-aware, and never traps or endlessly penalizes.</done>
</task>

</tasks>

<verification>Run the builder, plist lint, validator, and a structural dispatcher/cooldown audit covering CIRC-06..14.</verification>
<success_criteria>All three sequences and remaining primitives execute correctly; Ice short-circuits safely and expires.</success_criteria>
<output>.planning/phases/05-nine-primitives-environmental-safety/05-02-SUMMARY.md</output>
