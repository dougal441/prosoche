---
phase: 05-nine-primitives-environmental-safety
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified: [tools/build_state_engine.py, src/PROSOCHE-Dumb.xml, docs/phase5_self_check.py, docs/BUILD-NOTES.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md]
autonomous: true
requirements: [SAFE-05, SAFE-06]
must_haves:
  truths:
    - "Emergency Restore is reachable independently of Circle and cooldown."
    - "It clears cooldown and active_session, restores only captured settings, and persists once."
    - "The complete Phase 5 graph passes plist lint, the Shortcuts Playground validator, and one runnable structural self-check."
  artifacts: [src/PROSOCHE-Dumb.xml, docs/phase5_self_check.py]
  key_links:
    - "Emergency Restore -> full State dictionary -> clear/restore mutations -> one final Save File."
---

<objective>
Finish the restoration escape hatch and leave one comprehensive runnable check for the non-trivial primitive graph.
</objective>

<context>
@.planning/phases/05-nine-primitives-environmental-safety/05-02-SUMMARY.md
@docs/BUILD-NOTES.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Emergency Restore as an always-reachable state-safe path</name>
  <files>tools/build_state_engine.py, src/PROSOCHE-Dumb.xml</files>
  <action>Add a semantic Emergency Restore entry reachable from manual control and the live-Ice branch. Root mutations in the full current State dictionary; clear cooldown_until and active_session, restore only values whose snapshots have data, omit unsupported restores, and save the final dictionary once. Never increase volume or blindly toggle accessibility configuration.</action>
  <verify>Assert both entry paths reach the restore block; its writer is inside the owner path; cleared fields and guarded restore inputs exist; validator passes.</verify>
  <done>Emergency Restore works during Ice and cannot destroy unrelated State.</done>
</task>

<task type="auto">
  <name>Task 2: Add the Phase 5 structural self-check and close requirements</name>
  <files>docs/phase5_self_check.py, docs/BUILD-NOTES.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <action>Create one stdlib plist self-check that fails on missing primitive names, sequence paths, cooldown/restore links, unknown Color Filters identifiers, unsafe brightness/volume literals, unbalanced control flow, lost pinned imports, or a non-idempotent builder. Run all checks, record the validator-clean Ash fallback and device-only identifier in build notes, then mark Phase 5 requirements complete only after success.</action>
  <verify>`python3 docs/phase5_self_check.py`; two builder runs produce the same hash; plutil, validator, and `git diff --check` pass.</verify>
  <done>SAFE-05/06 and all earlier Phase 5 requirements have executable evidence.</done>
</task>

</tasks>

<verification>Zero validator errors or waivers; all Phase 5 requirements have structural evidence and no manual-only claim is marked automated.</verification>
<success_criteria>Emergency Restore is always reachable, all recoverable state is restored safely, and the full Phase 5 graph is reproducible and validator-clean.</success_criteria>
<output>.planning/phases/05-nine-primitives-environmental-safety/05-03-SUMMARY.md</output>
