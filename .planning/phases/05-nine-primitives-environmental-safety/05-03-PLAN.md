---
phase: 05-nine-primitives-environmental-safety
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified: [tools/build_state_engine.py, src/PROSOCHE-Dumb.xml, docs/phase5_self_check.py, docs/BUILD-NOTES.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md]
autonomous: true
requirements: [SAFE-05, SAFE-06]
must_haves:
  truths:
    - "Emergency Restore is reachable independently of Circle and cooldown."
    - "Owning CLOSE, Ice expiry, and Emergency Restore restore managed settings; superseded CLOSE never does."
    - "The complete Phase 5 graph passes plist lint, the Shortcuts Playground validator, and one runnable structural self-check."
  artifacts: [src/PROSOCHE-Dumb.xml, docs/phase5_self_check.py]
  key_links:
    - "Each restore trigger -> full State dictionary -> guarded brightness/Media restore -> clear snapshots -> one final Save File."
---

<objective>
Finish the restoration escape hatch and leave one comprehensive runnable check for the non-trivial primitive graph.
</objective>

<context>
@.planning/phases/05-nine-primitives-environmental-safety/05-02-SUMMARY.md
@docs/BUILD-NOTES.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete all three restoration triggers and Emergency Restore</name>
  <files>tools/build_state_engine.py, src/PROSOCHE-Dumb.xml</files>
  <action>Replace the Phase 4 CLOSE restore hook inside the matching-owner branch: restore settings_snapshot.brightness to its captured original and settings_snapshot.volume as Media to its captured original; clear only successfully restored snapshot keys, then use the existing one final save. A superseded CLOSE must bypass all restore actions and writes. Apply the same guarded restoration sequence on Ice natural expiry. Add a semantic Emergency Restore entry reachable from manual control and the live-Ice branch; root mutations in the full State dictionary, clear cooldown_until and active_session, restore both captured settings, clear their snapshots, and save once. Never increase volume.</action>
  <verify>Assert owning CLOSE, Ice expiry, and both Emergency Restore entries reach guarded brightness/Media restoration and snapshot clearing; superseded CLOSE reaches none; each path preserves unrelated State and saves once. Target-26/platform-ios validator passes.</verify>
  <done>All restoration triggers work, including during Ice, without mutating a newer session or unrelated State.</done>
</task>

<task type="auto">
  <name>Task 2: Add the Phase 5 structural self-check and close requirements</name>
  <files>docs/phase5_self_check.py, docs/BUILD-NOTES.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <action>Create one stdlib plist self-check that fails on missing primitive names, sequence paths, cooldown/restore links, either Color Filters identifier, missing brightness/volume snapshot schema or no-overwrite guards, non-Media volume, unsafe brightness/volume targets, restore after superseded CLOSE, unbalanced control flow, lost pinned imports, or a non-idempotent builder. Run all checks, record the primary device evidence and the mandatory-validator fallback in build notes, then mark Phase 5 requirements complete only after success.</action>
  <verify>`python3 docs/phase5_self_check.py`; two builder runs produce the same hash; plutil, validator with `--target-macos 26 --target-platform ios`, and `git diff --check` pass.</verify>
  <done>SAFE-05/06 and all earlier Phase 5 requirements have executable evidence.</done>
</task>

</tasks>

<verification>Zero validator errors or waivers; all Phase 5 requirements have structural evidence and no manual-only claim is marked automated.</verification>
<success_criteria>Emergency Restore is always reachable, all recoverable state is restored safely, and the full Phase 5 graph is reproducible and validator-clean.</success_criteria>
<output>.planning/phases/05-nine-primitives-environmental-safety/05-03-SUMMARY.md</output>
