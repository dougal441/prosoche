---
phase: 05-nine-primitives-environmental-safety
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [tools/build_state_engine.py, src/PROSOCHE-Dumb.xml, docs/BUILD-NOTES.md]
autonomous: true
requirements: [CIRC-01, CIRC-02, CIRC-03, CIRC-04, CIRC-05, SAFE-01, SAFE-02, SAFE-03, SAFE-04]
must_haves:
  truths:
    - "Knock uses recorded telemetry and stays brief."
    - "Confession accepts free text and a 2/5/10/15/custom boundary."
    - "Ash is opt-in and uses explicit On/Off, while Silence and Dimming capture restorable originals; brightness never reaches zero and volume never increases."
  artifacts: [src/PROSOCHE-Dumb.xml, tools/build_state_engine.py]
  key_links:
    - "The Phase 3 Circle marker routes to a primitive tracer without changing Pressure."
    - "Brightness and volume changes require a captured original value; Ash requires Config opt-in and explicit set semantics."
---

<objective>
Build the production tracer for the primitive layer and the five core interventions, preserving the validated state/session graph.
</objective>

<context>
@.planning/phases/05-nine-primitives-environmental-safety/05-CONTEXT.md
@.planning/phases/03-deterministic-state-engine/03-01-SUMMARY.md
@.planning/phases/04-close-pipeline-session-race/04-01-SUMMARY.md
@src/CONFIG-BLOCK.md
@docs/CAPABILITY-DECISIONS.md
@docs/BUILD-NOTES.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the semantic plist builder with the primitive tracer</name>
  <files>tools/build_state_engine.py, src/PROSOCHE-Dumb.xml</files>
  <action>Extend the existing semantic builder; locate the Phase 5 marker by comment text, never by numeric index. Add validator-safe Comment blocks and a direct dispatcher scaffold that receives Circle, current factual telemetry, and State without altering Heat, Gravity, Pressure, or Circle. Preserve pinned import actions and every Phase 3/4 writer.</action>
  <verify>Run the builder twice and assert identical output hashes; run plutil and the Shortcuts Playground validator for target 26/platform ios.</verify>
  <done>The tracer is reproducible, validator-clean, and leaves deterministic arithmetic untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Knock, Ash fallback, Silence fallback, Confession, and Dimming fallback</name>
  <files>tools/build_state_engine.py, src/PROSOCHE-Dumb.xml, docs/BUILD-NOTES.md</files>
  <action>Implement Knock as a brief alert containing only available recorded values. Implement Ash behind Config safety.ash_managed_color_filters: true runs `com.apple.UniversalAccess.UASettingsShortcuts.UAToggleColorFiltersIntent` with `operation=turn`, explicit `state=On`, and `ShowWhenRun=Off`; false runs the low-salience pause. Never emit `operation=toggle`. For Dimming and Silence, use Get Device Details with `Current Brightness` / `Current Volume`, guard for a plausible numeric value, preserve an existing unrestored snapshot, otherwise store `{original_value, changed_at, changed_by_session_id}` beneath `settings_snapshot`, then set brightness within the Config 10–15% band or Media volume no higher than the captured original. If capture fails, run the message-only fallback. Confession asks for unrestricted free text, then a 2/5/10/15/custom time boundary; blank intent remains valid.</action>
  <verify>Structural checks prove the Ash opt-in and exact explicit-set parameters, no toggle, snapshot no-overwrite guards and schema, Media-only volume at or below original, no brightness below the configured floor, and both Confession prompts/presets. Run plutil and the target-26/platform-ios validator with zero errors.</verify>
  <done>Five primitives are reachable and all environmental behavior is reversible or safely degraded.</done>
</task>

</tasks>

<verification>
- `python3 tools/build_state_engine.py`
- `plutil -lint src/PROSOCHE-Dumb.xml`
- `python3 .../validate_shortcut.py src/PROSOCHE-Dumb.xml --target-macos 26 --target-platform ios`
- Structural assertions cover CIRC-01..05 and SAFE-01..04.
</verification>

<success_criteria>
The graph contains a safe primitive tracer, real Knock/Confession behavior, and validator-clean fallbacks wherever an environmental original cannot be restored.
</success_criteria>

<output>.planning/phases/05-nine-primitives-environmental-safety/05-01-SUMMARY.md</output>
