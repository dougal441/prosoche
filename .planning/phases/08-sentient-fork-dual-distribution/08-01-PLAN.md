---
phase: 08-sentient-fork-dual-distribution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [tools/build_sentient.py, src/PROSOCHE-Sentient.xml, docs/sentient_core_check.py]
autonomous: true
requirements: [SENT-01, SENT-02, SENT-03, SENT-12, SENT-15]
must_haves:
  truths: ["Sentient is an additive fork of frozen Dumb.", "Circle I and IX contain no model action; II–VIII use at most one audit call.", "Model output never writes Heat, Gravity, Pressure, thresholds, timers, exit selection, or Ice."]
  artifacts: [src/PROSOCHE-Dumb.xml, src/PROSOCHE-Sentient.xml, tools/build_sentient.py]
  key_links: ["Frozen Dumb checksum -> clone -> semantic model wrapper only at II–VIII contract gate."]
---
<objective>Create a reproducible additive Sentient fork while proving the deterministic core is unchanged.</objective>
<context>@.planning/phases/08-sentient-fork-dual-distribution/08-CONTEXT.md @docs/device-evidence/UseModel-OnDevice.xml @docs/BUILD-NOTES.md @src/PROSOCHE-Dumb.xml</context>
<tasks>
<task type="auto"><name>Task 1: Clone frozen Dumb and define an additive semantic builder</name><files>tools/build_sentient.py, src/PROSOCHE-Sentient.xml</files><action>Copy the verified Dumb plist, change only Sentient metadata/import AI preference and insert a named audit wrapper at the post-Confession contract marker. Use semantic markers/UUID generation and atomic serialization. Record hashes of every deterministic state/session/exit/Ice action before and after; fail if any differs. Never write src/PROSOCHE-Dumb.xml.</action><verify>`dumb_before=$(shasum -a 256 src/PROSOCHE-Dumb.xml); python3 tools/build_sentient.py; first=$(shasum -a 256 src/PROSOCHE-Sentient.xml); python3 tools/build_sentient.py; test "$first" = "$(shasum -a 256 src/PROSOCHE-Sentient.xml)"; test "$dumb_before" = "$(shasum -a 256 src/PROSOCHE-Dumb.xml)"; python3 docs/sentient_core_check.py` exits 0 and reports an unchanged core.</verify><done>The fork boundary is reproducible and non-mutating.</done></task>
<task type="auto"><name>Task 2: Gate model calls to Circles II–VIII with the recovered On-Device literal</name><files>tools/build_sentient.py, src/PROSOCHE-Sentient.xml, docs/sentient_core_check.py</files><action>Circle I follows Dumb immediately. Circle IX follows deterministic Ice with no model. Circles II–VIII may invoke exactly one `is.workflow.actions.askllm` per run with `WFLLMModel = Apple Intelligence on Device` and result type Text. Omit the OS-27-only `WFAllowWebSearch` and `FollowUp` keys entirely. Increasing involvement means progressively richer compact recorded facts/prompts, never broader control. AI-disabled/unavailable routes directly to Dumb.</action><verify>`plutil -lint src/PROSOCHE-Sentient.xml && python3 docs/sentient_core_check.py && python3 /Users/dougalhanson/.codex/plugins/cache/shortcuts-playground/shortcuts-playground/1.2.1/skills/shortcuts-playground/scripts/validate_shortcut.py src/PROSOCHE-Sentient.xml --target-macos 26 --target-platform all` exits 0; the self-check asserts exact model literal, call ancestry, no I/IX call, and raw absence of `WFAllowWebSearch`/`FollowUp`.</verify><done>On-Device auditing is additive and safety-neutral.</done></task>
</tasks>
<verification>Idempotence, deterministic-core checksum, plist lint, validator, and Dumb Phase 5/6/7 regressions pass.</verification>
<success_criteria>SENT-01/02/03/12/15 are structurally proven.</success_criteria>
<output>.planning/phases/08-sentient-fork-dual-distribution/08-01-SUMMARY.md</output>
