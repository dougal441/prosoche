---
phase: 08-sentient-fork-dual-distribution
plan: 03
type: execute
wave: 3
depends_on: ["08-02"]
files_modified: [src/PROSOCHE-Sentient.xml, artifacts/shortcuts, README.md, docs/BUILD-NOTES.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md]
autonomous: true
requirements: [DIST-01, DIST-02, DIST-03, DIST-04, DIST-05, DIST-06, DIST-07, DIST-08]
must_haves:
  truths: ["Both forks validate, archive, sign, and exist non-empty with distinct names.", "Unsigned XML and honest limitations/privacy docs are retained.", "No external analytics/network dependency exists; real-iPhone first run is not falsely claimed."]
  artifacts: [artifacts/shortcuts/PROSOCHĒ — Nine Circles — Dumb.shortcut, artifacts/shortcuts/PROSOCHĒ — Nine Circles — Sentient.shortcut]
  key_links: ["Validated unsigned sources -> required icon resolver -> sign_shortcut.sh archives + signed outputs -> size/checksum manifest."]
---
<objective>Ship two distinct signed shortcuts and honest distribution evidence.</objective>
<context>@.planning/phases/08-sentient-fork-dual-distribution/08-02-SUMMARY.md @README.md @docs/BUILD-NOTES.md</context>
<tasks>
<task type="auto"><name>Task 1: Validate and sign both forks through Shortcuts Playground</name><files>src/PROSOCHE-Sentient.xml, artifacts/shortcuts</files><action>Run the resolver separately with the full prompts. Verify the frozen Dumb XML already contains its resolved Phase-7 icon/color and never edit it; apply only the Sentient result if needed. Run target-26/all validator for each, archive/sign each with the final display name, verify signed files and raw archives are nonzero, and write a manifest containing source/archive/signed paths, sizes, and SHA-256 values.</action><verify>`for f in src/PROSOCHE-Dumb.xml src/PROSOCHE-Sentient.xml; do plutil -lint "$f" && python3 /Users/dougalhanson/.codex/plugins/cache/shortcuts-playground/shortcuts-playground/1.2.1/skills/shortcuts-playground/scripts/validate_shortcut.py "$f" --target-macos 26 --target-platform all; done` exits 0; both `artifacts/shortcuts/PROSOCHĒ — Nine Circles — *.shortcut` files and two dated archives pass `test -s`; manifest hashes are distinct.</verify><done>Two signed non-empty artifacts and their unsigned sources/archives are delivered without mutating Dumb.</done></task>
<task type="checkpoint:human-verify"><name>Task 2: Prove device import and finish distribution evidence</name><files>README.md, docs/BUILD-NOTES.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files><action>On a connected real iPhone, import each signed fork and complete its first Manual run. Record device/iOS version, artifact SHA-256, import result, run result, and timestamp. If no iPhone is connected, stop this criterion as blocked and leave DIST-03 unchecked; never substitute a Mac import or fabricated evidence. Independently document unsupported actions/fallbacks, validator limits, data on-device, no analytics/external network, model fallibility, self-directed/bypassable design, and exact artifact/source paths.</action><verify>`xcrun devicectl list devices` identifies the tested iPhone and the committed UAT record contains both signed hashes plus PASS for import and first Manual run; `python3 docs/phase5_self_check.py && python3 docs/phase6_self_check.py && python3 docs/phase7_self_check.py && python3 docs/sentient_core_check.py && python3 docs/sentient_audit_check.py && git diff --check` exits 0.</verify><done>DIST-03 is marked complete only with real-device evidence; all other distribution evidence is reproducible.</done></task>
</tasks>
<verification>All automated checks/signing pass; the device checkpoint either supplies real-iPhone evidence or explicitly blocks DIST-03 without a false completion claim.</verification>
<success_criteria>Both shortcuts and their unsigned sources/documentation are delivered.</success_criteria>
<output>.planning/phases/08-sentient-fork-dual-distribution/08-03-SUMMARY.md</output>
