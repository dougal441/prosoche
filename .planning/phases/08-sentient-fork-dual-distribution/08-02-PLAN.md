---
phase: 08-sentient-fork-dual-distribution
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified: [tools/build_sentient.py, src/PROSOCHE-Sentient.xml, docs/sentient_audit_check.py]
autonomous: true
requirements: [SENT-04, SENT-05, SENT-06, SENT-07, SENT-08, SENT-09, SENT-10, SENT-11, SENT-13, SENT-14]
must_haves:
  truths: ["Only ALLOW/CHALLENGE/DENY are accepted; malformed/empty falls back to Dumb.", "At most one challenge round; DENY only at high Circle and means redirect.", "Prompt uses compact recorded facts, permits bounded leisure, forbids lie/diagnosis/internal-app claims."]
  artifacts: [src/PROSOCHE-Sentient.xml, docs/sentient_audit_check.py]
  key_links: ["Model text -> strict normalized parser -> bounded audit branch -> existing Dumb contract/exit path."]
---
<objective>Implement the bounded contract-auditor protocol and its deterministic fallbacks.</objective>
<context>@.planning/phases/08-sentient-fork-dual-distribution/08-01-SUMMARY.md @PROSOCHE_Nine_Circles_Canonical_Strategy.md</context>
<tasks>
<task type="auto"><name>Task 1: Build compact privacy-safe prompt and strict parser</name><files>tools/build_sentient.py, src/PROSOCHE-Sentient.xml</files><action>Prompt includes only current intention/boundary, Circle, Heat/open counts, and guarded prior contract consistency—not the Note or app contents. System instruction requires exact first token ALLOW/CHALLENGE/DENY; says bounded deliberate leisure may ALLOW; forbids lying claims, diagnosis/banned moralizing vocabulary, feelings/app-content inference. Capture Current Date immediately before/after the model action and use the native date-difference action: if completed latency exceeds the documented threshold, discard the result and take the Dumb path. Normalize/Match output; unknown, empty, unavailable, or over-threshold output takes the same path. Shortcuts exposes no supported cancellation timeout for this action at target 26, so do not invent one; document that an indefinitely hung platform action cannot be intercepted.</action><verify>`python3 tools/build_sentient.py && python3 docs/sentient_audit_check.py` exits 0; the check asserts prompt allow/forbid tokens, permitted data sources, before/after clock ancestry, over-threshold fallback, and malformed/empty/unavailable fallback.</verify><done>Model context is compact/local and every completed but slow result fails safely.</done></task>
<task type="auto"><name>Task 2: Enforce one challenge and high-Circle-only DENY</name><files>tools/build_sentient.py, src/PROSOCHE-Sentient.xml, docs/sentient_audit_check.py</files><action>ALLOW persists the user contract unchanged. CHALLENGE asks once for revision/continuation, then commits through Dumb without a second model call. DENY is accepted only at configured sufficiently high Circles (VII–VIII), routes to existing enabled-exit flow, and never changes system settings or arithmetic; lower-Circle DENY degrades to CHALLENGE/Dumb.</action><verify>`python3 docs/sentient_audit_check.py && plutil -lint src/PROSOCHE-Sentient.xml && python3 /Users/dougalhanson/.codex/plugins/cache/shortcuts-playground/shortcuts-playground/1.2.1/skills/shortcuts-playground/scripts/validate_shortcut.py src/PROSOCHE-Sentient.xml --target-macos 26 --target-platform all` exits 0; reference checks assert one call/one challenge, high-Circle DENY redirect, and no model output reference in deterministic writers.</verify><done>The audit is bounded, honest, and non-punitive.</done></task>
</tasks>
<verification>Sentient audit self-check, plist lint, validator, deterministic-core checksum, and Dumb regressions pass.</verification>
<success_criteria>SENT-04..11/13/14 have executable evidence.</success_criteria>
<output>.planning/phases/08-sentient-fork-dual-distribution/08-02-SUMMARY.md</output>
