---
phase: 11-build-addendum-01-dante-circle-names-and-the-ten-primitive-r
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/plist_text_edit.py
  - tools/build_state_engine.py
  - src/PROSOCHE-Dumb.xml
  - src/PROSOCHE-Sentient.xml
  - src/CONFIG-BLOCK.md
  - docs/phase5_self_check.py
  - docs/note_identity_check.py
  - docs/BUILD-NOTES.md
  - artifacts/shortcuts/MANIFEST.md
autonomous: true
requirements: [CIRC-02, ROOM-01, DIST-01, DIST-02]

must_haves:
  truths:
    - "A rename driven from the generator's dispatch tuple and the plist Config literal reaches the signed artifact: the decrypted payload of both signed .shortcut files contains the sequence entry Pause and contains zero occurrences of the retired entry it replaced."
    - "Both builders remain idempotent after a hand edit to src/PROSOCHE-Dumb.xml: running tools/build_state_engine.py twice produces byte-identical output and git status --short reports no unexpected modification of the artifact."
    - "Every WFTextTokenString in both forks has attachmentsByRange keys whose leading offsets equal, exactly and in document order, the character offsets of U+FFFC in its own string — measured true for 775 (Dumb) and 779 (Sentient) token strings at the phase baseline and asserted mechanically from this plan onward."
    - "All eleven pre-existing docs/*.py structural checks exit 0 at every commit boundary in this plan, plus the twelfth (docs/note_identity_check.py) once it exists."
  artifacts:
    - "tools/plist_text_edit.py — the guarded plistlib round-trip helper, re-implementing the six-step method quick task 260817-au7 left only as prose"
    - "docs/note_identity_check.py — the twelfth structural checker: three Note-identity sites plus the global attachment-offset invariant, per fork"
    - "docs/BUILD-NOTES.md — a new appended section recording the round-trip method, the helper's API, and this plan's evidence table"
  key_links:
    - "tools/build_state_engine.py primitive_dispatch() name tuple -> the WFConditionalActionString literal on every emitted dispatch branch -> the sequences arrays in the Config literal at src/PROSOCHE-Dumb.xml. If these three disagree, a Circle silently does nothing."
    - "src/PROSOCHE-Dumb.xml -> tools/build_sentient.py (reads the built Dumb source) -> src/PROSOCHE-Sentient.xml. Every Dumb-side plist edit propagates; Sentient is never hand-edited."
    - "docs/phase5_self_check.py hardcoded primitive-name list -> the sequences arrays. The list goes red the moment an array moves without it."
  prohibitions:
    - "MUST NOT describe any renamed Circle, the Note lookup, or any intervention behaviour as device-verified while DIST-03 (no connected iPhone) is open; structural proof must be labelled structural in every artifact this plan writes."
    - "MUST NOT edit a WFTextTokenString by string substitution on the XML; every such edit goes through the guarded round-trip that recomputes attachmentsByRange, because an out-of-bounds range can crash Shortcuts on import."
---

<objective>
Prove the whole Build-Addendum-01 rename pipeline end to end on one primitive, and leave
behind the two tools every later plan in this phase depends on.

`Knock` becomes `Pause` — one BD-06 name, real and final, not a placeholder — driven from
the generator's dispatch tuple through the plist Config literal, through both builders,
through all eleven structural checks, through the validator, into both signed `.shortcut`
files, and read back out of the signed containers by decryption.

Purpose: this phase is a mass rename across three sequence arrays, ten dispatch renderings,
two forks and eleven checkers. The single largest risk is that a change validates, signs and
imports cleanly and is still wrong. Proving the full path on one name first turns that risk
into one commit instead of ten.

Output: `tools/plist_text_edit.py`, `docs/note_identity_check.py`, `Pause` live in all three
sequences and in the dispatch surface, both forks re-signed and decrypt-verified,
`MANIFEST.md` refreshed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.claude/CLAUDE.md
@.planning/phases/11-build-addendum-01-dante-circle-names-and-the-ten-primitive-r/11-RESEARCH.md
@.planning/phases/11-build-addendum-01-dante-circle-names-and-the-ten-primitive-r/11-PATTERNS.md
@docs/CAPABILITY-DECISIONS.md
</context>

<tasks>

<!-- The retired primitive name below is negative-grepped against the DECRYPTED SIGNED
     ARTIFACT, never against a file the executor authors, so the comment-echo failure mode
     the gate guards does not apply here. -->
<!-- planner-discipline-allow: Knock -->

<task type="tracer">
  <name>Task 1: End-to-end "Knock becomes Pause" — one primitive, generator to signed artifact</name>
  <precondition>`git merge-base --is-ancestor 7ca8ebbfe467da38e594bdd41687c094a1f0c678 HEAD` exits 0 — the rebuild provenance gate. If it fails, abort without running either builder.</precondition>
  <files>tools/plist_text_edit.py, tools/build_state_engine.py, src/PROSOCHE-Dumb.xml, src/PROSOCHE-Sentient.xml, src/CONFIG-BLOCK.md, docs/phase5_self_check.py, artifacts/shortcuts/MANIFEST.md</files>
  <read_first>
    - `.claude/CLAUDE.md` — the seven parameter-defect axes, the verified iOS runtime-semantics table, the evidence hierarchy, §5 (attachment offsets), §8 (AEA1 recovery)
    - `.planning/phases/11-.../11-RESEARCH.md` §2 (the generator is an in-place transformer), §3.2, §3.3, §6.3 (the six-step round-trip method), §10 (the pipeline and its invariants)
    - `.planning/phases/11-.../11-PATTERNS.md` §4 (the dispatch construction site, verbatim), §5 (the sequences arrays and `config_literal()`), §7 (the round-trip method and the evidence-table shape)
    - `tools/build_state_engine.py` — `primitive_dispatch()` at :644-670, `if_block()`, `main()` at :3086-3157
    - `docs/sequence_dispatch_check.py` — `config_literal()` at :95-103, the locate-by-content idiom this plan reuses everywhere
    - `docs/phase5_self_check.py` — the hardcoded name list at :65-67
    - `artifacts/shortcuts/MANIFEST.md` — the six-row table shape to refresh
  </read_first>
  <action>
    Build the reusable plist-edit helper and use it to move exactly one primitive name.

    **1. Create `tools/plist_text_edit.py`.** A standalone module (no third-party imports;
    `plistlib`, `pathlib`, `re` only) that re-implements the six-step method quick task
    `260817-au7` proved but left only as prose in `docs/BUILD-NOTES.md:1324-1385`. Required
    public surface, named exactly:
    - `load(path)` -> `(data, original_bytes)`
    - `assert_noop_roundtrip(data, original_bytes)` -> raises `SystemExit` unless
      `plistlib.dumps(data, fmt=plistlib.FMT_XML, sort_keys=False)` is byte-identical to
      `original_bytes`. This is step 1 and it licenses every later structured edit.
    - `assert_offsets_match(token)` -> for one `WFTextTokenString` dict, raises unless the
      leading integer of each `attachmentsByRange` key equals, in sorted document order, the
      character offsets of `U+FFFC` in `token["string"]`.
    - `replace_in_token(token, old, new, *, expected_count)` -> asserts `old` occurs exactly
      `expected_count` times, asserts `new` introduces no `U+FFFC`, performs the replacement,
      then rebuilds `attachmentsByRange` from the new `U+FFFC` offsets in document order,
      preserving each attachment's own value dict in that same order.
    - `replace_in_plain(action, key, old, new, *, expected_count)` -> the same guarded
      replacement for a parameter whose value is a plain `str` (the Config literal is one).
    - `save(path, data)` -> exactly one `plistlib.dumps(..., fmt=FMT_XML, sort_keys=False)`
      and one `write_bytes`.
    - `find_action(actions, predicate)` -> locate by content, never by index.
    Failure convention: `raise SystemExit(<message naming the consequence>)`, matching
    `tools/build_state_engine.py`'s `verify_*` family. Messages must name the consequence
    (an out-of-range attachment range can crash Shortcuts on import), not just the fact.

    **2. Generator edit.** In `tools/build_state_engine.py` `primitive_dispatch()`, change the
    first element of the branch tuple at :658 from the string `"Knock"` to `"Pause"`. The
    Python function `knock` is NOT renamed — `docs/environmental_restore_check.py:55-56`
    imports generator functions by name and renaming any of them is a separate, unrelated
    breakage; the tuple carries the shipped name and the function carries the internal name.
    Add a one-line comment above the tuple recording that mapping.

    **3. Config literal edit.** Using the helper, replace the three `Knock` cells in the
    `sequences` arrays of the Config literal in `src/PROSOCHE-Dumb.xml`: `Classic[0]`,
    `BlackMirror[0]`, `Ambient[3]`. Locate the literal by content — the
    `is.workflow.actions.gettext` whose `WFTextActionText` is a plain `str` containing
    `"config_version"` — exactly as `docs/sequence_dispatch_check.py:95-103` does. Never by
    index: action numbers shift on every rebuild. `expected_count` is 3. The JSON must stay
    parseable, because `is.workflow.actions.detect.dictionary` consumes it at runtime.

    **4. Mirror and checker.** Update the three matching cells in `src/CONFIG-BLOCK.md:45-49`
    and its prose at :82/:84/:86/:107-109. In `docs/phase5_self_check.py:65-67`, replace the
    first element of the required-name tuple with `Pause`. Do not touch any other element of
    that tuple in this plan.

    **5. Run the RESEARCH §10 pipeline in order.** Provenance gate, then
    `tools/build_state_engine.py`, then `tools/build_sentient.py` (always from the fresh Dumb
    source), then all eleven `docs/*.py` checks, then `validate-shortcut` on both forks at
    `--target-macos 26 --target-platform all` (never `27`, never `ios`), then `sign-shortcut`
    on both forks with an **explicit `--name`** set to their CURRENT canonical display names —
    `--name "PROSOCHĒ — Nine Circles — Dumb"` and `--name "PROSOCHĒ — Nine Circles — Sentient"`,
    plus `--mode anyone --output-dir artifacts/shortcuts`. The flag is not optional: without it
    the signer defaults the output basename to the *input* basename, producing
    `PROSOCHE-Dumb.shortcut`, which imports as a differently named library entry the user's two
    Personal Automations do not reference. The Core/Aware rename is plan **11-06**'s, not this
    plan's. Then decrypt both signed containers per `.claude/CLAUDE.md` §8, then refresh all six
    `MANIFEST.md` rows from disk.

    Do not use `timeout`. Do not use a pre-dated `--output-dir`. If `sign-shortcut` reports
    "The file doesn't exist." or "isn't in the correct format.", those are the two documented
    signer quirks and the wrapper retries them itself.
  </action>
  <verify>
    <automated>python3 tools/build_state_engine.py && python3 tools/build_sentient.py && python3 -c "import sys,json,plistlib;sys.path.insert(0,'docs');from sequence_dispatch_check import config_literal;a=plistlib.loads(open('src/PROSOCHE-Dumb.xml','rb').read())['WFWorkflowActions'];s=config_literal(a)['sequences'];t=json.dumps(s);assert 'Knock' not in t, s;assert sum(v.count('Pause') for v in s.values())==3, s;print('tracer: Pause x3, zero retired entry')"</automated>
  </verify>
  <acceptance_criteria>
    - `tools/plist_text_edit.py` exists and exposes exactly the seven public names listed in the action; `python3 -c "import sys;sys.path.insert(0,'tools');import plist_text_edit as p;[getattr(p,n) for n in ('load','assert_noop_roundtrip','assert_offsets_match','replace_in_token','replace_in_plain','save','find_action')]"` exits 0.
    - `python3 -c "import sys;sys.path.insert(0,'tools');import plist_text_edit as p;d,b=p.load('src/PROSOCHE-Dumb.xml');p.assert_noop_roundtrip(d,b);print('noop roundtrip byte-identical')"` exits 0.
    - `grep -c '"Pause"' tools/build_state_engine.py` returns at least 1, and `python3 -c "import sys;sys.path.insert(0,'tools');import build_state_engine as b;import inspect;src=inspect.getsource(b.primitive_dispatch);assert '\"Pause\"' in src and '\"Knock\"' not in src;print('dispatch tuple carries Pause')"` exits 0.
    - `python3 -c "import sys;sys.path.insert(0,'tools');import build_state_engine as b;assert callable(b.knock);print('generator function knock() still present')"` exits 0 — the Python symbol is deliberately unrenamed.
    - The verify command above exits 0: the Config literal's three sequences contain `Pause` exactly three times and zero occurrences of the retired entry.
    - All eleven checks exit 0: `for f in state_engine_self_check phase5_self_check phase6_self_check phase7_self_check phase9_self_check sentient_audit_check sentient_core_check environmental_restore_check router_ui_census sequence_dispatch_check manifest_check; do python3 docs/$f.py || exit 1; done`
    - `validate-shortcut src/PROSOCHE-Dumb.xml --target-macos 26 --target-platform all` and the same for `src/PROSOCHE-Sentient.xml` both print `Validation passed.` and exit 0.
    - `artifacts/shortcuts/PROSOCHĒ — Nine Circles — Dumb.shortcut` and `… — Sentient.shortcut` both exist with non-zero size and no filename suffix.
    - Both signed containers decrypt (`aea decrypt` + `aa extract` + `plutil -convert xml1`), both recovered plists pass `plutil -lint`, and `grep -c 'Pause' <recovered>.xml` returns at least 3 in each while `grep -c 'Knock' <recovered>.xml` returns 0 in each.
    - `python3 docs/manifest_check.py` exits 0 after the MANIFEST refresh.
    - `git status --short` shows no modification to `src/PROSOCHE-Dumb.xml` after a second consecutive run of `tools/build_state_engine.py` — the hand edit survives the generator byte-for-byte.
    - `--target-macos 27`, `--target-platform ios` and `timeout` appear nowhere in the commands run.
  </acceptance_criteria>
  <done>`Pause` is the live sequence entry and dispatch branch name in all three sequences, all eleven checks are green, both forks are validated, signed under their current canonical names, decrypt-verified to contain `Pause` and not the retired name, and `MANIFEST.md` matches disk.</done>
</task>

<task type="auto">
  <name>Task 2: Arm the Note-identity and attachment-offset invariants as the twelfth checker</name>
  <files>docs/note_identity_check.py, docs/BUILD-NOTES.md</files>
  <read_first>
    - `docs/sentient_audit_check.py` — the 24-line complete short-checker shape to copy: shebang, one-line docstring, `plistlib.loads(Path(...).read_bytes())["WFWorkflowActions"]`, bare `assert` with a message operand, one `print` on success, no `main()`
    - `docs/manifest_check.py` — the `require(value, message)` helper at :63-65 and the actionable-message idiom at :148-153
    - `docs/router_ui_census.py:40-42` — the three Notes identifier constants (`shownote`, `filter.notes`, `SharingExtension`)
    - `.planning/phases/11-.../11-RESEARCH.md` §6 — the three Note-identity sites and their parameter paths, and §6.3 — the offset hazard
    - `.planning/phases/11-.../11-PATTERNS.md` §2 — the analog checkers, and the "locate by content, never by index" shared pattern
    - `docs/BUILD-NOTES.md` — the append-only convention; read the tail to find the current highest section number
  </read_first>
  <action>
    Create `docs/note_identity_check.py`, the twelfth structural check. It must be green at
    this plan's HEAD against the **current** Note title, so that plan 11-03's rename has to
    change exactly one constant and nothing else, and so the offset invariant is already
    armed before any Note-body copy is edited.

    Module-level constant `EXPECTED_TITLE = "PROSOCHĒ — Control Room"` with a comment stating
    that plan 11-03 changes this one line to the bare product name and that the value is the
    user-facing title only — "Control Room" remains the internal name everywhere in code and
    docs, settled in commit `e84ee77`.

    For each of `src/PROSOCHE-Dumb.xml` and `src/PROSOCHE-Sentient.xml`, assert three
    identity sites, each located by content and never by action index:
    - the `is.workflow.actions.filter.notes` action whose `WFContentItemFilter` templates
      include a row with `Property == "Name"`: its `Values.String` is a `WFTextTokenString`
      whose `string` equals `EXPECTED_TITLE`. Also record and assert the row's current
      `Operator` value, so a later change to it is a deliberate, visible edit rather than a
      silent one.
    - the `is.workflow.actions.gettext` whose `WFTextActionText` is a `WFTextTokenString`
      whose `string` starts with `"# "` and contains the Note's `## READ THIS FIRST` heading:
      its first line equals `"# " + EXPECTED_TITLE`.
    - the `com.apple.mobilenotes.SharingExtension` action: its `name` parameter is a plain
      `str` equal to `EXPECTED_TITLE`.
    Assert there is exactly one of each in each fork, so a duplicate can never hide.

    Then assert the global offset invariant across the whole document, both forks: walk every
    nested dict, and for every dict that has both a `string` key holding a `str` and an
    `attachmentsByRange` key, require that the sorted leading integers of the range keys equal
    the sorted character offsets of `U+FFFC` in that string. Measured at the phase baseline:
    775 such token strings in Dumb, 779 in Sentient, zero mismatches. Print the per-fork count
    on success so a future drop in the count is visible in the output.

    Then append a new numbered section to `docs/BUILD-NOTES.md` — a pure append, zero
    deletions, matching the project's established convention. It records: the guarded
    round-trip method and `tools/plist_text_edit.py`'s public API; that the method was
    previously prose-only in §20 and is now executable; the twelfth checker and what it
    proves; and this plan's evidence table using the row shape from
    `.planning/quick/260817-au7-.../SUMMARY.md:156-170` — no-op round-trip byte count,
    `plutil -lint` results, provenance-gate exit code, both builder exit codes, retired-name
    count 0, new-name count, per-fork attachment-key-versus-recomputed-offset result,
    validator results, signed sizes, dated-archive SHA equality, and `plutil -lint` on both
    recovered plists. Label every row structural; state plainly that DIST-03 is open and that
    nothing in this phase is device-verified.
  </action>
  <verify>
    <automated>python3 docs/note_identity_check.py && for f in state_engine_self_check phase5_self_check phase6_self_check phase7_self_check phase9_self_check sentient_audit_check sentient_core_check environmental_restore_check router_ui_census sequence_dispatch_check manifest_check; do python3 docs/$f.py || exit 1; done</automated>
  </verify>
  <acceptance_criteria>
    - `python3 docs/note_identity_check.py` exits 0 and its output names both forks and a per-fork token-string count of at least 775.
    - `grep -c 'EXPECTED_TITLE' docs/note_identity_check.py` returns at least 4 — the constant is defined once and consumed by all three site assertions.
    - The checker fails when it should: `python3 - <<'PY'` style negative proof — copy `src/PROSOCHE-Dumb.xml` to a temp file, shift one `attachmentsByRange` key by one, point the checker at the copy, and confirm it raises. Record the raised message in the task SUMMARY.
    - `python3 -c "import re,pathlib;t=pathlib.Path('docs/note_identity_check.py').read_text();assert 'WFWorkflowActions[' not in t and not re.search(r'actions\[\d+\]',t), 'checker addresses an action by index';print('locate-by-content only')"` exits 0.
    - `git diff --numstat f4e47f9 -- docs/BUILD-NOTES.md` produces a **non-empty** row with a non-zero insertion count and a deletion count of exactly `0`. Diff against the phase-baseline commit, not the working tree — a bare `git diff` is empty once the task commits and would pass vacuously.
    - The new `docs/BUILD-NOTES.md` section contains the words `structural` and `DIST-03`, and contains no claim that any behaviour was observed on a device.
    - All twelve checks exit 0 (the eleven plus `note_identity_check`).
  </acceptance_criteria>
  <done>`docs/note_identity_check.py` exists, is green against the current title, is proven to fail on a deliberately shifted attachment offset, and the method plus this plan's evidence table are recorded in an append-only `docs/BUILD-NOTES.md` section.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build machine -> signed `.shortcut` -> user's iPhone | Everything this plan writes crosses into an artifact the user imports and cannot inspect. The only non-device read-back channel is AEA1 decryption. |
| generator (Python) -> plist literal regions | Large regions of `src/PROSOCHE-Dumb.xml` have no Python source. An edit assumed to be regenerated but actually hand-held silently reverts or silently persists. |
| plist `WFTextTokenString` -> Shortcuts import parser | Absolute `attachmentsByRange` offsets are consumed by the OS parser, not by anything this repo controls. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-01 | Tampering | `WFTextTokenString` `attachmentsByRange` in both forks | high | mitigate | Task 1's `assert_noop_roundtrip` + `replace_in_token` recompute offsets from `U+FFFC` positions; Task 2 arms `docs/note_identity_check.py`'s global invariant across all 775/779 token strings per fork; `plutil -lint` on both recovered plists |
| T-11-02 | Denial of service | dispatch surface vs `sequences` arrays | high | mitigate | The tracer moves the tuple and the arrays in one commit; the verify command asserts the retired entry is absent and `Pause` present exactly three times; plan 11-02 makes this a hard build guard |
| T-11-03 | Denial of service | signed artifact filename | high | mitigate | Signing keeps the current canonical display names with no suffix; `docs/manifest_check.py`'s DIST-04 assertion is run and must exit 0 |
| T-11-04 | Spoofing | provenance of the rebuilt artifact | medium | mitigate | The `git merge-base --is-ancestor` gate is a `<precondition>` on Task 1 and aborts the rebuild rather than warning |
| T-11-05 | Repudiation | "what actually shipped" | medium | mitigate | Both signed containers are decrypted and string-asserted; the evidence table is written into `docs/BUILD-NOTES.md` as an append |
| T-11-06 | Information disclosure | none | low | accept | No network dependency, no credential store, no telemetry. Nothing this plan writes leaves the device. |
| T-11-SC | Tampering | package-manager installs | low | accept | This plan installs no npm/pip/cargo package and adds no third-party import; the package-legitimacy gate is not triggered. `tools/plist_text_edit.py` uses only the Python standard library. |
</threat_model>

## Artifacts this phase produces

Symbols and paths **created** by this plan (excluded from drift verification because they do
not exist at the phase baseline):

| Kind | Name |
|---|---|
| new file | `tools/plist_text_edit.py` |
| new file | `docs/note_identity_check.py` |
| new functions (`tools/plist_text_edit.py`) | `load`, `assert_noop_roundtrip`, `assert_offsets_match`, `replace_in_token`, `replace_in_plain`, `save`, `find_action` |
| new constant (`docs/note_identity_check.py`) | `EXPECTED_TITLE` |
| new primitive name (live) | `Pause` |
| new doc section | `docs/BUILD-NOTES.md` — appended section recording the round-trip method, the twelfth checker, and this plan's evidence table |

Later plans in this phase add: `verify_dispatch_coverage`, `CIRCLE_NAMES`, `fix_fork_strings`,
the primitive names `Black and White` / `Intention` / `Dim` / `Eject` / `Loud Mirror` /
`Frozen` (and the reserved, unused `Redirect`), the state key `panic_escape_enabled`, the
MANUAL menu item `Panic Escape`, the Note sections `## PANIC ESCAPE` and `## OPTIONAL
HARDENING`, and the signed filenames `PROSOCHĒ — Nine Circles — Core.shortcut` /
`PROSOCHĒ — Nine Circles — Aware.shortcut`.

## Spec-less probe ledger (phase-wide — authored here, owned per plan)

This phase has no SPEC, so the deterministic edge probe ran over the eight phase requirement
IDs and returned nine items. **No-silent-drop equality: 9 surfaced == 3 authored into
`must_haves` + 6 surfaced as flagged assumptions.**

| # | Requirement | Category | Resolution | Where |
|---|---|---|---|---|
| 1 | ROOM-01 | idempotency | auto-**covered** -> plain `must_haves.truths` string | plan 11-03 |
| 2 | ROOM-01 | concurrency | auto-**backstop** -> structured `{ statement, verification: backstop }` marker | plan 11-03 |
| 3 | DIST-02 | concurrency | auto-**covered** -> plain `must_haves.truths` string | plan 11-06 |
| 4 | AUDIT-02 | unclassified | **unresolved** -> flagged assumption | plan 11-02 |
| 5 | CIRC-02 | unclassified | **unresolved** -> flagged assumption | this plan, below |
| 6 | CIRC-06 | unclassified | **unresolved** -> flagged assumption | plan 11-02 |
| 7 | CIRC-08 | unclassified | **unresolved** -> flagged assumption | plan 11-02 |
| 8 | ROOM-02 | unclassified | **unresolved** -> flagged assumption | plan 11-06 |
| 9 | DIST-01 | unclassified | **unresolved** -> flagged assumption | this plan, below |

`unclassified` rows are never auto-backstopped and are never silently dropped.

### Flagged assumptions owned by this plan

- **CIRC-02 (unclassified, unresolved).** The probe could not classify an edge category for
  "Ash applies the audited visual-salience reduction, or its documented fallback." This phase
  changes the primitive's **name only** — `ash()` at `tools/build_state_engine.py:486-490`
  keeps the alert-only fallback verbatim, and `docs/phase5_self_check.py:78`'s assertion that
  no Color Filters intent is emitted stays green. Real grayscale is Phase 14's. **Assumed:
  that a rename with no behaviour change satisfies CIRC-02 for this phase. Not machine-proven
  — review manually.**
- **DIST-01 (unclassified, unresolved).** The probe could not classify an edge category for
  "Both forks pass the validator at the iOS 26 target." The plan asserts the exit code and the
  exact invocation, but the validator's own coverage of this project's action set is a
  known-imperfect instrument (`docs/BUILD-NOTES.md` §13 DEV-01: the `ios` target rejects
  3675/3675 actions). **Assumed: that a green `--target-macos 26 --target-platform all` run is
  the strongest available structural signal, and that it is not a behavioural guarantee. Not
  machine-proven — review manually.**

## Recorded deviation — this plan moves three sequence cells before the coverage guard exists

The ROADMAP requires the dispatch-coverage guard to be written "during the rename, not after
it", and plan 11-02 satisfies that exactly: its Task 1 authors the guard, its Task 2 moves the
whole roster and arms it. **This plan is a deliberate, scoped exception, taken before the guard
exists, and it is recorded here rather than left to read as a contradiction.**

**What it does.** Task 1 step 3 rewrites three cells of the `sequences` arrays — `Classic[0]`,
`BlackMirror[0]` and `Ambient[3]` — in wave 1, together with the matching branch name in the
generator's dispatch tuple.

**Why it is done before the guard.** The tracer's entire purpose is to prove the rename pipeline
end to end — generator, plist, both forks, validator, signer, decrypt — on one name, so that a
pipeline defect surfaces after one commit instead of after the whole roster has moved. A tracer
that could not touch a sequence cell would not touch the plist layer at all, which is the layer
carrying the highest-risk edit mechanism in the phase. Writing the guard first instead would
invert the dependency: the guard's own correctness proof in 11-02 Task 1 requires the Circle-8
orphan to still be live, and it is a build-aborting `verify_*` that cannot be armed until the
roster is complete.

**What stands in for the guard during wave 1.** Three things, all mechanical:
- Task 1's `<automated>` verify carries an inline equivalent of the coverage assertion for the
  moved name — the retired entry is absent from the parsed `sequences` and the new name appears
  in exactly the three cells that moved.
- `docs/sequence_dispatch_check.py` is still present and still runs as one of the eleven checks.
  It is a reporter in wave 1, but it reports orphans and unreachable branches by name, so a
  mismatch introduced here would appear in its output rather than passing unseen.
- `docs/phase5_self_check.py`'s name list is updated in the same task, so the artifact and the
  checker cannot disagree about which name is live.

**Why it is safe for this specific name.** The move is name-for-name in both the tuple and all
three arrays in one commit, so coverage is complete at every point. Under the condition code
still in force in wave 1 the new name has no substring relationship with any other live branch
name in either direction — the collision that makes the exact-match move mandatory involves the
Circle-8 name, which this plan does not touch. And this plan leaves the Circle-8 orphan exactly
as it found it, which is what 11-02 Task 1 depends on to prove the guard has teeth.

**Scope of the exception.** One name, three cells, one wave. Every remaining cell in all three
arrays moves in 11-02 Task 2, with the guard authored and proven in Task 1 of that same plan and
armed as the last act of Task 2.

### Prohibition probe — canon referral

Injection, path traversal, prototype pollution and GDPR-class items are canon and are not
minted here — this product has no network dependency, no credential store, and one positively
identified input string. Breadcrumb: canon security items are owned by `/gsd-secure-phase`.

<verification>
- Provenance gate exits 0 before either builder runs.
- Both builders exit 0; a second consecutive `tools/build_state_engine.py` leaves
  `src/PROSOCHE-Dumb.xml` unmodified in `git status --short`.
- Twelve `docs/*.py` checks exit 0 (the eleven baseline plus `note_identity_check`).
- `validate-shortcut … --target-macos 26 --target-platform all` exits 0 on both forks.
- Both signed `.shortcut` files exist, are non-zero, carry the exact canonical display names
  with no suffix, decrypt successfully, and contain `Pause` and not the retired name.
- `docs/manifest_check.py` exits 0 against the refreshed `MANIFEST.md`.
- `docs/note_identity_check.py` is proven to fail on a deliberately shifted attachment offset.
</verification>

<success_criteria>
- One BD-06 name is live end to end, from the generator's dispatch tuple to the decrypted
  payload of both signed artifacts.
- `tools/plist_text_edit.py` and `docs/note_identity_check.py` exist and are green, so plans
  11-02 through 11-05 have both the edit tool and the offset invariant already armed.
- Nothing in this plan claims device-verified behaviour; DIST-03 remains open and is stated as
  such in the evidence record.
</success_criteria>

<output>
Create `.planning/phases/11-build-addendum-01-dante-circle-names-and-the-ten-primitive-r/11-01-SUMMARY.md` when done
</output>
