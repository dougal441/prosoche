---
phase: 11-build-addendum-01-dante-circle-names-and-the-ten-primitive-r
plan: 09
type: execute
wave: 9
depends_on: ["11-08"]
gap_closure: true
files_modified:
  - tools/build_sentient.py
  - src/PROSOCHE-Sentient.xml
  - docs/sentient_core_check.py
  - docs/sentient_audit_check.py
  - docs/BUILD-NOTES.md
  - artifacts/shortcuts/MANIFEST.md
  - artifacts/shortcuts/PROSOCHĒ — Nine Circles — Aware.shortcut
autonomous: true
requirements: [AUDIT-02, DIST-01, DIST-02]

must_haves:
  truths:
    - "The Aware fork's Use Model contract audit is present on every OPEN-arm primitive-dispatch rendering, so removing an unrelated bypass setting cannot silently turn Aware into Core."
    - "Each inserted audit block carries its own grouping identifiers and action UUIDs; no two renderings share one, because a reused GroupingIdentifier silently corrupts block boundaries."
    - "The insertion point is located structurally from the router's own OPEN test, never by taking the first marker in document order, so a future rendering added anywhere in the OPEN arm is covered automatically."
    - "The two Aware checkers assert the audit count as a value derived from the Core fork's measured OPEN-arm rendering count rather than pinned to a literal, and each inspects every audit block rather than the first."
    - "The Aware fork asserts the same silent-band and parameter-key properties the Core fork does, because it is the only artifact that inserts actions into the OPEN arm."
  artifacts:
    - "tools/build_sentient.py — audit_block() takes a per-rendering discriminator; insertion iterates every OPEN-arm contract marker in reverse"
    - "src/PROSOCHE-Sentient.xml — one audit block per OPEN-arm rendering, each with distinct grouping identifiers"
    - "docs/sentient_core_check.py — the model-count assertion derived from the Core fork; the additivity excision generalised to N blocks"
    - "docs/sentient_audit_check.py — every audit block inspected, and the block count reconciled against the model count"
  key_links:
    - "universal_leaving() renders primitive_dispatch() twice — once inside the Continue case and once in the otherwise arm taken when the bypass has been removed. Each rendering reaches Intention, which calls persist_contract(), which emits the contract marker the audit is inserted at. Two markers in the OPEN arm therefore means two audits."
    - "build_sentient.uid() is a deterministic uuid5 over a literal key, so a second audit_block() call with unchanged keys produces byte-identical GroupingIdentifiers and UUIDs. The discriminator is what makes a second rendering legal at all."
    - "sentient_core_check's fork-normalised additivity equality excises the audit span between its two markers; with more than one block the excision must remove every span or the equality compares the wrong thing and fails for the wrong reason."
    - "verify_circle_zero_silence()'s four properties are exactly the ones a Sentient-only OPEN-arm insertion can break, and the Aware fork is the only artifact where they were not asserted."
  prohibitions:
    - "MUST NOT record 'audit only the Panic-Escape-enabled path' as a deliberate product decision; see the resolution below for why that option is rejected rather than merely not taken."
    - "MUST NOT insert a second block without giving uid() a per-rendering discriminator; a shared GroupingIdentifier is the top documented real-world failure mode in this toolchain."
    - "MUST NOT pin either checker's audit count to a literal integer; the count is derived from the Core fork so a future rendering moves it automatically."
    - "MUST NOT claim the model call, the audit, or the Aware fork's behaviour has been observed on a device or on Apple-Intelligence-capable hardware."
---

<objective>
The Aware fork exists for one reason, and an unrelated setting currently deletes it.

`build_sentient.py` inserts the Use Model contract audit at the **first** contract marker in
document order and then breaks. Before this phase, "first marker" and "the OPEN-arm marker"
were the same action. Plan 11-05 added a second OPEN-arm dispatch rendering — the arm taken
when a user has removed the Panic Escape bypass — and the audit was not extended to it.
Measured on the shipped Aware fork: eleven dispatch renderings, eleven contract markers, two of
them in the OPEN arm, and exactly **one** Use Model action. A user who removes their bypass
reaches the Intention primitive with no contract audit at all, and there is nothing they can
observe that would tell them their fork just became Core. `sentient_core_check.py` asserts the
model count equals one, which pins the defect rather than detecting it.

Purpose: make the fork's differentiator unconditional on any setting unrelated to it, and make
the count self-deriving so the next rendering cannot silently halve it again.
Output: one audit per OPEN-arm rendering, per-rendering identifiers, and two checkers that
derive rather than pin.
</objective>

<gap3_resolution>
## Insert into every OPEN-arm rendering. The "deliberate product decision" option is rejected.

The verifier offered two legitimate resolutions and required one to be chosen and justified.

**Rejected: record "audit only the Panic-Escape path" in `docs/CAPABILITY-DECISIONS.md` and
surface it in the Note.** Three reasons.

1. **It is not a statable product rule.** Written out honestly it reads: *if you remove the
   easy-exit bypass, you also lose the on-device intelligence audit.* Those two features have no
   relationship. A user cannot predict it, cannot want it, and cannot undo one without the other.
   Recording it would be documenting a defect in the vocabulary of a feature.
2. **It inverts the escalation.** The bypass-removed path is the *harder* path — the user has
   deliberately given up their exit and goes straight into the intervention. That is exactly the
   moment the contract audit is most useful, and the current behaviour removes it precisely
   there.
3. **It is the same silent-degradation class this phase's own guard was written to eliminate.**
   BD-06 Decision 5's coverage guard exists because two well-formed halves stopped agreeing with
   each other and produced no error. This is that shape one level up: a fork that silently
   becomes the other fork, with nothing on device to see.

**Chosen: insert into every OPEN-arm rendering, located structurally.** The cost is real and is
paid explicitly — `uid()` must take a per-rendering discriminator, because `audit_block()` calls
it with fixed literal keys and `uid()` is a deterministic `uuid5`, so a second call would emit
byte-identical `GroupingIdentifier`s and `UUID`s. `.claude/CLAUDE.md` §4 names a reused
`GroupingIdentifier` as the top documented real-world failure mode in this toolchain, so that
discriminator is not a tidiness measure, it is the thing that makes the second block legal.

**Locating the OPEN arm structurally, rather than taking every marker in the file, is also a
choice and is deliberate.** The other nine markers are the Test-a-Circle submenu in the MANUAL
arm. Auditing those would put a model call behind a diagnostic menu item, which is neither the
fork's purpose nor safe to assume is cheap. The OPEN arm is located the same way
`verify_circle_zero_silence()` locates it — from the router's own OPEN literal test and its
grouping identifier — so a future rendering added anywhere inside that arm is covered without
another code change.
</gap3_resolution>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.claude/CLAUDE.md
@.planning/phases/11-build-addendum-01-dante-circle-names-and-the-ten-primitive-r/11-VERIFICATION.md
@.planning/phases/11-build-addendum-01-dante-circle-names-and-the-ten-primitive-r/11-REVIEW.md
@.planning/phases/11-build-addendum-01-dante-circle-names-and-the-ten-primitive-r/11-06-SUMMARY.md
</context>

<measured_baseline>
Taken at HEAD by the planner on 2026-08-17. Re-take anything a task depends on.

- Core fork: 4456 actions, 11 contract markers, of which **2 lie inside the OPEN arm** (the OPEN
  literal test at index 92, its otherwise endpoint at 1528; markers at 1082 and 1346).
- Aware fork: 4524 actions, 11 contract markers, 2 in the OPEN arm (markers at 1150 and 1414),
  and **exactly 1** `is.workflow.actions.askllm`.
- The nine markers outside the OPEN arm are the Test-a-Circle submenu renderings in the MANUAL
  arm and are deliberately not audit targets.
- `verify_circle_zero_silence()` and `verify_parameter_keys()` both run **clean** on the current
  Aware fork today — measured — so arming them cannot fail for an inherited reason and any raise
  after this plan's insertion is a genuine finding about the insertion.
- Phase 12 already armed four of the six previously-unasserted guards on the Aware chain; these
  two are the remainder.
- `docs/sentient_audit_check.py` locates the audit span from the **first** marker only, so with
  two blocks it would stay green while inspecting one — the same blindness class as the defect.
</measured_baseline>

<tasks>

<task type="auto">
  <name>Task 1: Per-rendering identifiers and structural insertion into every OPEN-arm rendering</name>
  <files>tools/build_sentient.py, src/PROSOCHE-Sentient.xml</files>
  <precondition>Plan 11-08 has landed and both forks rebuild clean; Phase 12 execution is not in flight. `src/PROSOCHE-Dumb.xml` is the input this builder forks, so it must be the post-11-08 build, not a stale one.</precondition>
  <read_first>
    - `tools/build_sentient.py`: `uid()`, `if_block()` and `audit_block()` in full. Note that
      `uid()` is a deterministic `uuid5` over a literal key and that `audit_block()` calls it
      eighteen times with fixed keys, four of them inline inside the returned list rather than in
      the tuple unpacking at the top — those inline calls are easy to miss and must take the
      discriminator too.
    - `tools/build_sentient.py`: `main()`'s insertion loop and its `else: raise SystemExit`
      clause. The clause is correct and must survive: no marker at all is still a hard failure.
    - `tools/build_state_engine.py`: `input_key_tests()`, `flow_index()`, and
      `verify_circle_zero_silence()`'s OPEN-arm derivation — the four lines that find the OPEN
      literal test, take its `GroupingIdentifier` and call `flow_index(..., 1)` for the endpoint.
      Reuse that exact derivation rather than writing a second one.
    - `tools/build_state_engine.py`: `persist_contract()`, and `universal_leaving()`'s docstring
      explaining Mechanism A and why the otherwise arm renders `primitive_dispatch()` verbatim.
      That verbatim rendering is why there are two OPEN-arm markers and why both need the audit.
    - `.claude/CLAUDE.md` §4 — every nesting level needs its own unique `GroupingIdentifier`, and
      a reused one silently corrupts block boundaries. This is the single highest-risk aspect of
      this task.
    - `.claude/CLAUDE.md` §5 — `attachmentsByRange` offsets and the display-versus-non-display
      parameter rule, because `audit_block()` builds a multi-variable prompt token string that is
      duplicated by this change.
  </read_first>
  <action>
    Give `audit_block()` a required ordinal parameter and thread it into every `uid()` call the
    block makes, by composing the discriminator with the existing literal key so each rendering
    yields a distinct deterministic identifier. Find every call — including the four made inline
    inside the returned list — and confirm by counting `uid(` occurrences within the function
    before and after that none was missed. A missed call is a shared identifier between two
    blocks, which validates, signs and imports cleanly and then corrupts a block boundary at run
    time.

    Replace `main()`'s first-marker-and-break loop with a structural one. Derive the OPEN arm's
    bounds exactly as `verify_circle_zero_silence()` does — locate the conditional testing the
    router's OPEN literal, take its grouping identifier, and take its otherwise endpoint — then
    collect every contract-marker index inside that span. Keep a hard failure when the collected
    set is empty, with a message naming the consequence: the Aware fork would be built with no
    audit anywhere and would be a byte-for-byte Core fork under a different name. Insert in
    reverse index order so earlier positions stay valid, passing each rendering's ordinal so its
    identifiers differ from its siblings'.

    Note the ordering constraint that already exists and must be preserved: the two import-
    preference actions are spliced in before this loop runs, so the marker indexes collected here
    are indexes into the already-spliced list. Derive them after that splice, not before.

    Arm the two remaining unasserted guards on the Aware chain while this file is open —
    `verify_circle_zero_silence` and `verify_parameter_keys` — each with a one-line justification
    in the file's established style. The first is directly load-bearing for this change: its four
    properties are precisely the ones an OPEN-arm insertion can break, and this fork is the only
    artifact that performs such an insertion. Both were measured clean on the pre-change Aware
    fork, so a raise after this change is a finding about the insertion and must be investigated,
    never suppressed.

    Rebuild the Aware fork only; `src/PROSOCHE-Dumb.xml` must be unchanged by this task and the
    builder's own frozen-source assertion proves it.
  </action>
  <verify>
    <automated>git merge-base --is-ancestor 7ca8ebbfe467da38e594bdd41687c094a1f0c678 HEAD &amp;&amp; python3 tools/build_state_engine.py &amp;&amp; python3 tools/build_sentient.py &amp;&amp; python3 docs/sentient_audit_check.py &amp;&amp; python3 docs/router_ui_census.py</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -c` over `src/PROSOCHE-Sentient.xml`: the count of `is.workflow.actions.askllm`
      equals the count of contract markers inside the **Core** fork's OPEN arm, measured in the
      same script from `src/PROSOCHE-Dumb.xml`. Both numbers printed. At the current baseline
      that is 2 and 2, but the assertion is the equality, not the literal.
    - `python3 -c` over `src/PROSOCHE-Sentient.xml`: the number of distinct `GroupingIdentifier`
      values inside the audit spans equals the number of `GroupingIdentifier` occurrences inside
      them divided by three (start, otherwise-or-end, end are the only modes emitted), and the
      intersection of the identifier sets of any two audit blocks is **empty**. Print the size of
      each block's identifier set and the size of their union; union must equal the sum.
    - `python3 -c` over `src/PROSOCHE-Sentient.xml`: no `UUID` value appears more than once
      anywhere in the artifact. This is the strongest available statement of the collision
      property and covers the four inline `uid()` calls that are easy to miss.
    - Negative control, run and recorded: temporarily drop the ordinal from one `uid()` call
      inside `audit_block()`, rebuild, and observe the duplicate-identifier assertion above
      failing; restore and confirm it passes. A collision that no check can see is the failure
      mode this criterion exists to rule out.
    - `python3 tools/build_sentient.py` prints its built line and exits 0 with both newly armed
      guards in the chain; `grep -c 'verify_circle_zero_silence' tools/build_sentient.py` returns
      at least 1 and the same for `verify_parameter_keys`.
    - `src/PROSOCHE-Dumb.xml` is unchanged: `git status --short -- src/PROSOCHE-Dumb.xml` is
      empty after the Aware rebuild.
    - `validate-shortcut src/PROSOCHE-Sentient.xml --target-macos 26 --target-platform all`
      prints `Validation passed.` and exits 0.
  </acceptance_criteria>
  <done>Every OPEN-arm rendering in the Aware fork carries its own audit block with its own identifiers, the insertion point is derived from the router rather than from document order, no UUID or grouping identifier is duplicated anywhere, and the two remaining guards are armed on the fork.</done>
</task>

<task type="auto">
  <name>Task 2: Make both Aware checkers derive the count and inspect every block</name>
  <files>docs/sentient_core_check.py, docs/sentient_audit_check.py</files>
  <read_first>
    - `docs/sentient_core_check.py` in full, and especially its module docstring, which explains
      why plain whole-list equality was replaced by a fork-normalised, counted equality and warns
      against restoring the stricter form. The generalisation below must preserve that reasoning:
      a bounded, counted normalisation cannot absorb an unrelated drift, and that property is the
      whole value of the check.
    - `docs/sentient_core_check.py`: the model assertion, the marker/end lookup, and the
      additivity line that slices around a single audit span. All three assume exactly one block.
    - `docs/sentient_audit_check.py` in full — 24 lines. It locates one span from the first
      marker and asserts a dozen properties inside it. With two blocks it inspects one and stays
      green, which is the same blindness that produced the defect.
    - `tools/build_sentient.py` after Task 1, for the ordinal scheme, so the checkers can reason
      about blocks without re-deriving identifiers.
    - `11-VERIFICATION.md` gap 3's `missing:` list — the third item asks specifically for a
      derivation comment beside the changed assertion.
  </read_first>
  <action>
    In `docs/sentient_core_check.py`, replace the pinned model-count assertion with a derived one:
    compute the number of contract markers inside the **Core** fork's OPEN arm from the already
    loaded Dumb action list, using the same structural derivation the builder uses, and assert
    the Aware fork's Use Model count equals it. Write the derivation as a comment naming why the
    two must agree — every OPEN-arm rendering reaches the Intention primitive, each such
    rendering gets one audit, and a mismatch means either a rendering lost its audit or an audit
    landed somewhere no rendering reaches. Then apply the existing per-model parameter assertions
    to **every** model rather than to the first: the pinned on-device model source, the text
    result type, and the absence of the two OS27-gated keys.

    Generalise the additivity equality to N audit spans. Collect every marker and every matching
    end marker, pair them, and build the Core-comparable list by removing all of those spans plus
    the two import-preference actions. Keep the counted fork-string normalisation exactly as it
    is — it is unrelated to this change and its expected counts must not move.

    In `docs/sentient_audit_check.py`, iterate every audit span rather than the first. Assert that
    the number of spans equals the number of Use Model actions in the fork, then apply the
    existing property set inside each span, so a second block that lost the latency gate, the
    parsed-token gate, the bounded prompt or the forbidden-action exclusions fails. Keep the
    per-block occurrence assertions per block rather than converting them to artifact-wide totals:
    a per-block count of one is the property that matters, and an artifact-wide total of two would
    pass even if both landed in the same block.

    Neither checker may pin a literal audit count. Both derive it, so the next OPEN-arm rendering
    moves them automatically instead of silently.
  </action>
  <verify>
    <automated>python3 docs/sentient_core_check.py &amp;&amp; python3 docs/sentient_audit_check.py &amp;&amp; for f in state_engine_self_check phase5_self_check phase6_self_check phase7_self_check phase9_self_check environmental_restore_check router_ui_census sequence_dispatch_check note_identity_check; do python3 docs/$f.py > /dev/null || echo "FAIL $f"; done</automated>
  </verify>
  <acceptance_criteria>
    - `python3 docs/sentient_core_check.py` exits 0 and prints its pass line; `grep -c 'len(models) == 1' docs/sentient_core_check.py` returns 0.
    - Negative control on `sentient_core_check`, run and recorded: on a scratch copy of the Aware
      source, delete one audit block's actions, run the checker, and observe it exiting non-zero
      with a message naming the count mismatch. Before this task the same deletion left it green
      as long as one block survived — record both observations.
    - Negative control on `sentient_audit_check`, run and recorded: on a scratch copy, remove the
      latency-gate conditional from the **second** audit block only and observe the checker
      exiting non-zero. Before this task the same mutation was invisible because only the first
      block was inspected.
    - `python3 -c` confirms neither checker contains a literal integer audit count: the model
      count and the block count are both computed from a parsed artifact in the same run.
    - All twelve structural checkers exit 0.
  </acceptance_criteria>
  <done>Both Aware checkers derive the audit count from the Core fork's OPEN-arm rendering count, inspect every block, and are each demonstrated failing on a defect that was invisible to them before.</done>
</task>

<task type="auto">
  <name>Task 3: Re-sign the Aware fork, decrypt-verify, and record the resolution</name>
  <files>artifacts/shortcuts/PROSOCHĒ — Nine Circles — Aware.shortcut, artifacts/shortcuts/MANIFEST.md, docs/BUILD-NOTES.md</files>
  <read_first>
    - `.claude/CLAUDE.md` §8 — the definition of done and the AEA1 recovery sequence, including
      passing `--name` explicitly because the signer defaults to the input basename.
    - `docs/manifest_check.py` — it validates the table rows and recomputes sizes and hashes from
      disk; the prose is not checked, which is why prose corrections are done by hand and
      verified by grep.
    - `docs/BUILD-NOTES.md` §25 — the most recent Aware-side divergence record, for the section
      format and the "what this does NOT establish" convention that closes each entry.
    - `docs/CAPABILITY-DECISIONS.md` BD-04-R2 — the Use Model model-source decision, so the record
      written here cites the existing decision rather than restating or re-deciding it.
  </read_first>
  <action>
    Sign the Aware fork under its exact canonical display name with no suffix, confirm non-zero
    bytes, decrypt it through the AEA1 recovery path, and assert the recovered action array equals
    the source. Re-sign the Core fork too if 11-08's build is not the one currently on disk, so
    the manifest rows describe the files that actually exist. Refresh
    `artifacts/shortcuts/MANIFEST.md` by recomputing every size and hash from disk.

    Append a `docs/BUILD-NOTES.md` section recording this resolution in the file's established
    shape: the measurement that exposed it (eleven renderings, eleven markers, two in the OPEN
    arm, one model call), the two resolutions available, the one chosen and the reasoning against
    the other, the identifier-collision hazard the choice created and how it was closed, the two
    guards armed on the fork, the negative controls observed, and a closing subsection stating
    what this does **not** establish — that no model call has been made on Apple-Intelligence-
    capable hardware, that DIST-03 is open, and that the audit's runtime behaviour is unproven.

    Add one sentence to the MANIFEST's Aware row context or its warning block stating that the
    Aware fork now carries one contract audit per OPEN-arm rendering, framed as a structural
    property. Do not add a new warning block; the existing block covering unproven-on-device
    behaviour already covers this and duplicating it dilutes all five.
  </action>
  <verify>
    <automated>python3 docs/manifest_check.py &amp;&amp; for f in state_engine_self_check phase5_self_check phase6_self_check phase7_self_check phase9_self_check sentient_audit_check sentient_core_check environmental_restore_check router_ui_census sequence_dispatch_check note_identity_check; do python3 docs/$f.py > /dev/null || echo "FAIL $f"; done</automated>
  </verify>
  <acceptance_criteria>
    - All twelve structural checkers exit 0.
    - `ls -l artifacts/shortcuts/` shows exactly two `.shortcut` files, both non-zero, basenames
      exactly the two canonical display names with no suffix.
    - Decrypt-verify of the Aware container: `aea decrypt` → `aa extract` → `plutil -convert xml1`
      succeeds, `plutil -lint` reports OK, and a `python3 -c` comparison reports the recovered
      `WFWorkflowActions` equal to `src/PROSOCHE-Sentient.xml`'s, `== True`.
    - `python3 -c` over the recovered Aware payload: the `is.workflow.actions.askllm` count equals
      the count in the source, and every one of them carries the pinned on-device model source
      literal and the text result type.
    - `grep -c 'DIST-03' docs/BUILD-NOTES.md` increases, and the new section's closing subsection
      states that no model call has been observed on Apple-Intelligence-capable hardware.
    - `validate-shortcut` at `--target-macos 26 --target-platform all` passes for both forks; gate
      B appears in no command in this plan.
  </acceptance_criteria>
  <done>The Aware container is re-signed and decrypt-verified with one audit per OPEN-arm rendering, the manifest is recomputed, and the resolution with its rejected alternative is recorded with an explicit no-behavioural-claim close.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User-editable bypass setting → fork identity | A Note-editable setting currently determines whether the Aware fork behaves as Aware. That coupling is the defect. |
| Recorded facts → on-device model | The audit sends a compact set of recorded facts to the on-device model; the boundary must not widen when the block is duplicated. |
| Generated identifiers → Shortcuts runtime | Duplicate grouping identifiers or UUIDs pass every file-level check and corrupt control flow only at run time. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-47 | Spoofing | Aware fork identity | high | mitigate | A fork that silently behaves as Core while presenting as Aware misrepresents itself to its user. Closed by auditing every OPEN-arm rendering and by a derived count that cannot be satisfied by one block. |
| T-11-48 | Tampering | Duplicate `GroupingIdentifier` / `UUID` across audit blocks | high | mitigate | Per-rendering discriminator on every `uid()` call, asserted by a whole-artifact uniqueness check and by a negative control that drops one discriminator and observes the failure. |
| T-11-49 | Information disclosure | The duplicated model prompt | medium | mitigate | The second block is the same bounded prompt as the first: recorded facts only, never the Note and never app contents. `sentient_audit_check` now asserts the bounded-prompt property inside **every** block rather than the first. |
| T-11-50 | Denial of service | A second model call on the bypass-removed path | low | accept | Each rendering is mutually exclusive at run time — a run takes the Continue case or the bypass-removed arm, never both — so no run makes two calls. The eight-second latency gate is unchanged and is asserted per block. |
| T-11-51 | Elevation of privilege | A model call reaching a MANUAL-arm diagnostic | medium | mitigate | Insertion is scoped to the structurally derived OPEN arm, so the nine Test-a-Circle renderings are excluded by construction rather than by an index. |
| T-11-52 | Repudiation | A simulator or structural result recorded as a model observation | high | mitigate | The record's closing subsection states that no model call has been made on capable hardware and that DIST-03 is open. |
| T-11-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install occurs in this plan; no dependency is added and the legitimacy gate has no input to audit. |
</threat_model>

<verification>
- Aware Use Model count equals the Core fork's measured OPEN-arm contract-marker count.
- No `UUID` and no `GroupingIdentifier` is duplicated anywhere in the Aware artifact; proven by a
  whole-artifact uniqueness check and by a negative control that removes one discriminator.
- `verify_circle_zero_silence` and `verify_parameter_keys` armed on the Aware chain and clean.
- Both Aware checkers demonstrated failing on a defect that was invisible to them beforehand.
- Twelve structural checkers exit 0; both forks validate; the Aware container decrypt-verifies
  equal to its source.

</verification>

<success_criteria>
- Removing the Panic Escape bypass no longer changes which fork the user is running.
- The audit count is derived in the builder and in both checkers, so the next OPEN-arm rendering
  is covered without a code change and a lost audit is a hard failure.
- The rejected alternative is recorded with its reasoning, so this is a decision rather than a
  default.
- No claim about model behaviour on capable hardware appears anywhere in this plan's output.
</success_criteria>

## Artifacts this phase produces

| Symbol / file | Kind | Where |
|---|---|---|
| `audit_block(ordinal)` with a per-rendering `uid()` discriminator | signature and body change | `tools/build_sentient.py` |
| Structural OPEN-arm marker collection and reverse insertion in `main()` | rewritten insertion | `tools/build_sentient.py` |
| `verify_circle_zero_silence` armed on the Aware chain | edit | `tools/build_sentient.py` |
| `verify_parameter_keys` armed on the Aware chain | edit | `tools/build_sentient.py` |
| Derived model-count assertion with its derivation comment | edit | `docs/sentient_core_check.py` |
| N-span additivity excision | edit | `docs/sentient_core_check.py` |
| Per-block iteration over every audit span | edit | `docs/sentient_audit_check.py` |
| BUILD-NOTES section: the resolution, the rejected alternative, the collision hazard | new section | `docs/BUILD-NOTES.md` |

<output>
Create `.planning/phases/11-build-addendum-01-dante-circle-names-and-the-ten-primitive-r/11-09-SUMMARY.md` when done
</output>
