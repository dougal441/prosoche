---
phase: 06-exits-exit-learning-contracts
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified: [tools/build_state_engine.py, src/PROSOCHE-Dumb.xml, docs/phase6_self_check.py, docs/BUILD-NOTES.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md]
autonomous: true
requirements: [LEARN-01, LEARN-02, LEARN-03, LEARN-04, LEARN-05]
must_haves:
  truths: ["The next tracked OPEN records time-away for the pending exit.", "Sparse data rotates enabled exits; sufficient data uses deterministic epsilon-greedy choice.", "Exploration rate and observation threshold come from Config; no model participates."]
  artifacts: [src/PROSOCHE-Dumb.xml, docs/phase6_self_check.py]
  key_links: ["pending_exit -> next genuine OPEN outcome -> exit_stats -> Selected Exit -> universal enabled-exit route/default."]
---

<objective>Add deterministic exit outcome learning and comprehensive Phase 6 regression evidence.</objective>
<context>@.planning/phases/06-exits-exit-learning-contracts/06-02-SUMMARY.md @src/CONFIG-BLOCK.md</context>
<tasks>
<task type="auto"><name>Task 1: Record pending-exit outcome on the next genuine OPEN</name><files>tools/build_state_engine.py, src/PROSOCHE-Dumb.xml</files><action>Before selecting a new exit and after debounce/cooldown guards, if a pending exit exists compute elapsed seconds since its timestamp, append/cap that exit's samples, increment count/sum, then clear pending. Duplicate or blocked OPEN must not complete an outcome.</action><verify>Control-flow ancestry and bounded-list checks prove only the next genuine OPEN records once.</verify><done>Time away becomes an honest per-exit outcome.</done></task>
<task type="auto"><name>Task 2: Select and route exits with exact deterministic state</name><files>tools/build_state_engine.py, src/PROSOCHE-Dumb.xml</files><action>Add guarded/migrated State fields `exit_selection_counter` (default 0) and `pending_exit` (default null). Increment the counter only when a matching-owner selection is committed. Filter `profile_snapshot.enabled_exits` in canonical order Capture, Coordinate, Create, Connect, Consult, Close. Sparse guard: unless every enabled exit has count at least Config.exits.exploit_min_observations, select counter modulo enabled count. Exploit rule: highest integer average `sum_return_seconds/count`, canonical order breaks ties. Exploration rule: if `(counter mod 100) < round(Config.exits.exploration_rate*100)`, select canonical next non-best enabled exit; otherwise best. Write `Selected Exit`. Present a concrete two-item menu: `Take suggested: <Selected Exit>` routes Selected Exit; `Choose another` opens the filtered six-exit menu and routes its chosen value. Both paths use the same recorder/router and can never select a disabled exit. Never call Use Model, Random, or a network request.</action><verify>Reference cases prove migration, exact counter ownership/increment, Config exploit_min_observations controls sparse-to-exploit transition, even sparse rotation, canonical tie break, best-average exploitation, configured exploration, disabled filtering, both concrete menu branches, and changed stats alter the real Selected Exit action feeding the route.</verify><done>Exit learning changes actual routing deterministically while preserving manual override.</done></task>
<task type="auto"><name>Task 3: Add Phase 6 self-check and close the phase</name><files>docs/phase6_self_check.py, docs/BUILD-NOTES.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files><action>Create one stdlib plist/reference check covering exact route identifiers/keys, universal Leaving ancestry, initial writer-before-interaction, reload/ownership/no-write stale branches, full-State preservation, record fields, contract ancestry, pending-outcome lifecycle, bounded samples, selector state/migration/config reads, selector-to-route wiring, and absence of send/call/message/model/random/download actions. Run all checks and update planning state only after success.</action><verify>Self-check, two identical builds, plutil, validator 26/all, and diff-check pass.</verify><done>All Phase 6 requirements have automated structural/reference evidence.</done></task>
</tasks>
<verification>Zero validator errors; any real-device route behavior is documented for final UAT without masking structural gaps.</verification>
<success_criteria>LEARN-01..05 pass and Phase 6 is independently verifiable.</success_criteria>
<output>.planning/phases/06-exits-exit-learning-contracts/06-03-SUMMARY.md</output>
