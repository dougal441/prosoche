---
phase: 06-exits-exit-learning-contracts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [tools/build_state_engine.py, src/PROSOCHE-Dumb.xml]
autonomous: true
requirements: [EXIT-01, EXIT-02, EXIT-03, EXIT-04, EXIT-05, EXIT-06, EXIT-07, EXIT-08, EXIT-09]
must_haves:
  truths: ["All six exits are reachable and Leaving is always available.", "Disabled exits are never selected.", "Every exit record contains type, timestamp, app, Circle, and Heat."]
  artifacts: [src/PROSOCHE-Dumb.xml]
  key_links: ["New OPEN session persisted -> universal Leaving menu before primitive dispatch -> enabled learned/manual exit -> bounded event -> route."]
---

<objective>Replace Phase 5's safe Exile tracer with the complete six-exit routing layer.</objective>
<context>@.planning/phases/06-exits-exit-learning-contracts/06-CONTEXT.md @.planning/phases/05-nine-primitives-environmental-safety/05-03-SUMMARY.md @src/CONFIG-BLOCK.md</context>
<tasks>
<task type="auto"><name>Task 1: Persist the session, then add universal Leaving and safe routes</name><files>tools/build_state_engine.py, src/PROSOCHE-Dumb.xml</files><action>Move the existing new-session persist before every interactive primitive/menu action. Replace the single Phase 5 dispatcher marker with one wrapper that offers Leaving before dispatch for every sequence/Circle, then continues to exactly the selected primitive. Filter six names against `profile_snapshot.enabled_exits`. Use verified shapes only: `openapp` with `WFSelectedApp`/`WFAppName` for Notes, Voice Memos, Camera, Reminders, Calendar; `searchweb` with `WFSearchWebDestination`/`WFInputText`; `searchmaps` with `WFInput`/`WFSearchMapsActionApp`; `returntohomescreen` for Close; Back is inert. Connect opens Contacts/Phone only and emits no send/call/message action. Create reads `profile_snapshot.create_target_url`; on first use/missing value, Ask for Input type URL, validate non-empty URL text, reload/confirm session ownership, persist it beneath profile_snapshot without losing enabled_exits, then route with `openurl`/`WFInput`; invalid/cancelled input shows a safe result and writes/routes nothing. Phase 7 Sync may later update the same field.</action><verify>Executable plist check asserts initial session Save File precedes universal menu and every Ask/Menu action; the universal menu is an ancestor of all nine Circle positions for all three sequences; exact route action keys are allowlisted; Create target capture/persistence/reuse and safe no-target fallback exist; no send/call/message/network-download action exists; `profile_snapshot.enabled_exits` filtering precedes selection. Validator 26/all passes.</verify><done>Every enabled exit is directly reachable without racing CLOSE or initiating contact.</done></task>
<task type="auto"><name>Task 2: Record a bounded owned exit event before routing</name><files>tools/build_state_engine.py, src/PROSOCHE-Dumb.xml</files><action>After interactive choice, reload State and compare active_session.id with the captured session ID. A stale owner writes/routes nothing. The matching owner appends a bounded event containing type, current epoch, triggering app when available, Circle, and Heat; sets pending_exit; mutates the full reloaded State dictionary; saves once before route.</action><verify>Self-check asserts owner ancestry, true no-write stale branch, record fields, bounded append, full-State preservation, writer-before-route, and no disabled exit path.</verify><done>All exit use is honestly persisted without reviving a closed/superseded session.</done></task>
</tasks>
<verification>Idempotent build, plist lint, validator, and structural exit check pass.</verification>
<success_criteria>EXIT-01..09 have executable graph evidence.</success_criteria>
<output>.planning/phases/06-exits-exit-learning-contracts/06-01-SUMMARY.md</output>
