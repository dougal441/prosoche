---
phase: 06-exits-exit-learning-contracts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [tools/build_state_engine.py, src/PROSOCHE-Dumb.xml]
autonomous: true
requirements: [EXIT-01, EXIT-02, EXIT-03, EXIT-04, EXIT-05, EXIT-06, EXIT-07, EXIT-08, EXIT-09]
must_haves:
  truths: ["All six exits are reachable and Leaving is always available.", "Disabled exits are never selected.", "Every exit record contains type, timestamp, app, Circle, and Heat."]
  artifacts: [src/PROSOCHE-Dumb.xml]
  key_links: ["Primitive/Leaving menu -> enabled exit selection -> route -> bounded exit event in State."]
---

<objective>Replace Phase 5's safe Exile tracer with the complete six-exit routing layer.</objective>
<context>@.planning/phases/06-exits-exit-learning-contracts/06-CONTEXT.md @.planning/phases/05-nine-primitives-environmental-safety/05-03-SUMMARY.md @src/CONFIG-BLOCK.md</context>
<tasks>
<task type="auto"><name>Task 1: Add the always-available enabled-exit menu</name><files>tools/build_state_engine.py, src/PROSOCHE-Dumb.xml</files><action>Extend the semantic builder at named Phase 5 Exile/Leaving markers. Filter the six exit names against State enabled exits and show Leaving at every Circle before optional intervention completion. Route Capture to Notes/Voice Memos/Camera choice, Coordinate to Reminders/Calendar choice, Create to a user-defined making target, Connect to a contact tool without sending/calling, Consult to query plus Web/Maps/Notes/Reminders/Calendar/Back, and Close to home/lock. No network API.</action><verify>Structural check proves all six names/routes, Back, no message/call action, and Leaving ancestry across every Circle. Validator 26/all passes.</verify><done>Every enabled exit is directly reachable and never coerces completion or contact.</done></task>
<task type="auto"><name>Task 2: Record a bounded exit event before routing</name><files>tools/build_state_engine.py, src/PROSOCHE-Dumb.xml</files><action>Before each route, append a bounded event containing type, current epoch, triggering app when available, Circle, and Heat; set the pending exit outcome marker used by 06-03. Mutate the full State dictionary and save once before the route.</action><verify>Self-check asserts record fields, bounded append, writer-before-route, and no disabled exit path.</verify><done>All exit use is honestly persisted.</done></task>
</tasks>
<verification>Idempotent build, plist lint, validator, and structural exit check pass.</verification>
<success_criteria>EXIT-01..09 have executable graph evidence.</success_criteria>
<output>.planning/phases/06-exits-exit-learning-contracts/06-01-SUMMARY.md</output>
