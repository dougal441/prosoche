---
phase: 13-red-operator-conditionals-and-the-wfitems-list-wrapper
plan: 03
type: execute
wave: 3
depends_on: ["13-02"]
files_modified:
  - .claude/CLAUDE.md
  - docs/CAPABILITY-DECISIONS.md
  - docs/BUILD-NOTES.md
  - .planning/ROADMAP.md
  - .planning/debug/HANDOFF.md
  - .planning/todos/pending/2026-08-15-fix-red-operator-and-list-wrapper-defects.md
  - .planning/todos/completed/2026-08-15-fix-red-operator-and-list-wrapper-defects.md
autonomous: true
requirements: [CIRC-07, CIRC-04, ROOM-03]

must_haves:
  truths:
    - "The numbered generator-authoring axis list in .claude/CLAUDE.md runs 1 through 9, its heading and the /ponytail row both name the same count, and no site in the file still asserts the old count."
    - "New axis 8 states the WFItems two-kind row rule — a literal row stays a bare string, a variable-bearing row takes {WFItemType: 0, WFValue: <WFTextTokenString>} — cites Donors 4 and 4.1 by path, names verify_list_item_wrappers() as its build guard, and states explicitly that it is a CONTAINER defect distinct from axis 2's string-envelope defect rather than an instance of it."
    - "New axis 9 states the compound-versus-scalar reader rule — get_value() for compound values consumed as a List, read_value() for scalars in text or numeric comparisons — names the four COMPOUND_STATE_KEYS members and verify_compound_value_reads() as its guard."
    - "Axis 7 is extended with the pending_exit container/leaf pattern: seed the container as a permanent invariant, write and clear only leaves, and gate on a string is-not-sentinel test or a numeric greater-than-zero test, never a condition-100 existence test over the container."
    - "docs/CAPABILITY-DECISIONS.md carries two new records: one settling the conditional TEXT-slot operand envelope as device-confirmed correct from Donor 5, and one recording the WFItems row wrapper from Donors 4 and 4.1 including the deliberately-unaudited status of WFItemType values other than 0."
    - "docs/BUILD-NOTES.md carries a Phase 13 section recording the three donor decrypts, the measured site inventory for both families in both forks, the refutation of the 14-site claim, the verbatim SystemExit texts from both guards' sensitivity demonstrations, the ordering mask inside verify_conditional_action_string(), and every open assumption A1 through A4."
    - "The ROADMAP Phase 13 Goal and Deliverables prose states the MEASURED figures, and the correction is dated and attributed to donor evidence rather than silently rewritten."
    - "The refuted counts are closed as a CLASS, not at two known sites: a whole-tree sweep of .planning/ and docs/ finds no surviving assertion of 14 defective conditional sites or 2 WFItems instances outside the two places where the historical wording is deliberately preserved — the closed todo and this phase's own planning directory — and every preserved occurrence sits within twelve lines of a REFUTED marker."
    - "The ROADMAP milestone checklist line for Phase 13, some 550 lines above the phase section and outside any scoped section edit, states the measured scope rather than the refuted counts."
    - ".planning/debug/HANDOFF.md carries a dated REFUTED annotation at every site that asserts a refuted figure — the WFItems instance count, the conditional site count, the axis-6 operand tally, the never-analysed status of Donor 5, and the false-lead attribution of if_block(\"Previous Respected\", 4, ...) — with the original wording preserved as history rather than deleted."
    - "The pending todo is closed by moving it into .planning/todos/completed/ with the refutation recorded in the file itself — a tombstone, not a deletion — because an unrecorded refutation gets re-litigated next cycle."
    - "The record states explicitly that CIRC-04 and ROOM-03 have no defect site in either family and are regression-protection requirements satisfied by docs/phase5_self_check.py and docs/note_identity_check.py staying green through the rebuild, rather than inventing work to make them look addressed."
  prohibitions:
    - "MUST NOT erase a refuted claim without leaving a tombstone — the refuted counts, the false-lead site attribution and the missing screenshot must each be recorded as corrected-with-evidence, never silently deleted, because this project's own convention is that an unrecorded refutation gets re-litigated next cycle."
    - "MUST NOT record an inference as evidence — every figure written into the docs must be one this phase measured or a donor showed, and every unsettled item (WFItemType values beyond 0, the cause of the 2026-08-14 red render, the raw-literal comparison target shape) must be written as explicitly unaudited rather than resolved."
  artifacts:
    - path: ".claude/CLAUDE.md"
      provides: "the numbered axis list extended to nine, with axis 7 extended and axes 8 and 9 added"
      contains: "WFItemType"
    - path: "docs/CAPABILITY-DECISIONS.md"
      provides: "BD-07 (Donor-5 conditional operand refutation) and BD-08 (Donor 4/4.1 WFItems row wrapper)"
      contains: "Donor 5"
    - path: "docs/BUILD-NOTES.md"
      provides: "the Phase 13 recording-duty section: decrypts, measurements, refutation, verbatim guard messages, assumptions"
      contains: "Phase 13"
    - path: ".planning/todos/completed/2026-08-15-fix-red-operator-and-list-wrapper-defects.md"
      provides: "the closed todo carrying its own refutation tombstone"
      contains: "REFUTED"
    - path: ".planning/debug/HANDOFF.md"
      provides: "dated REFUTED annotations at every site asserting a refuted figure, with the original wording preserved as history"
      contains: "REFUTED"
  key_links:
    - from: "13-RESEARCH.md's donor decrypts and measured inventory"
      to: ".claude/CLAUDE.md's numbered axis list and docs/CAPABILITY-DECISIONS.md"
      via: "the recording duty — a probe's result is recorded, not consumed — so the evidence survives outside the planning directory"
      pattern: "Donor 4"
    - from: "every record asserting a refuted count — the ROADMAP milestone checklist line, the ROADMAP Phase 13 section, .planning/debug/HANDOFF.md and the pending todo"
      to: "the measured figures"
      via: "each is corrected in place or annotated with a dated tombstone rather than deleted, and a whole-tree sweep proves no uncorrected source survives to re-assert them"
      pattern: "REFUTED"
---

<objective>
Do the recording duty, in one pass, so nothing settled here has to be settled again.

**This is the deliverable the phase was blocked on for three cycles.** The donor evidence was
on disk the whole time; nobody ran the decrypt. Now that it is run, the failure mode shifts from
"nobody looked" to "somebody looks at the stale record and re-litigates it". Three separate
project records — the ROADMAP Phase 13 section, the pending todo, and `HANDOFF.md` — still
assert that 14 conditional sites are defective and that the `WFItems` wrapper affects 2
instances. Both figures are refuted by measurement. Plan 13-02 put the refutation into the
guard's docstring so it travels with the code; this plan puts it into the prose record.

**Four updates, one pass, as the todo itself demanded.**

1. `.claude/CLAUDE.md`'s numbered generator-authoring axis list gains the `WFItems` row wrapper
   as a **new axis** — `13-RESEARCH.md` is explicit that it is a *container* defect, not an
   instance of the existing string-envelope axis — and gains the compound-versus-scalar reader
   rule as another. Axis 7 (state shape must exist before it is read) is extended with the
   `pending_exit` container/leaf pattern, which is the same rule refined rather than a new one.
2. `docs/CAPABILITY-DECISIONS.md` gains two decision records: the Donor-5 refutation, and the
   Donor 4 / 4.1 row wrapper with its unaudited boundary stated.
3. `docs/BUILD-NOTES.md` gains the Phase 13 recording-duty section: three decrypts, the measured
   inventory, the refutation, both guards' verbatim `SystemExit` texts and the ordering mask,
   and every open assumption.
4. Every record that still asserts a refuted count is corrected **as a class** — the ROADMAP
   milestone checklist bullet and the Phase 13 prose, `.planning/debug/HANDOFF.md`, and the
   pending todo, which is closed **with a tombstone** — and a whole-tree sweep proves no
   uncorrected source survived.

**Why the sweep, and not a list of sites.** A section-scoped edit reaches only the Phase 13
section, and plan review measured two refuted-count assertions outside it: the ROADMAP's own
milestone checklist bullet, roughly 550 lines above the section, and `HANDOFF.md` — which is
where the claim originated and the document a future cycle is most likely to read cold. Fixing
the two known sites would leave the class open. The task greps for every refuted figure across
`.planning/` and `docs/` and requires each surviving occurrence to sit beside a tombstone marker.

**Why a tombstone and not a delete.** `13-RESEARCH.md`'s anti-pattern list names this directly:
deleting a refuted claim without leaving a record is how it comes back. The closed todo carries
its own refutation, and `HANDOFF.md` keeps its original sentences with dated annotations beside
them — rewriting history in a handoff record would destroy the audit trail that makes the
refutation credible in the first place.

**Two things this plan must not do.** It must not invent work for CIRC-04 and ROOM-03 — neither
has a defect site in either family, and the honest record is that they are regression-protected
by `docs/phase5_self_check.py` and `docs/note_identity_check.py` staying green through the
rebuild. And it must not resolve what is unsettled: `WFItemType` values beyond `0`, the cause of
the 2026-08-14 red render, and the shape of a pure-literal comparison target all stay written as
explicitly unaudited.

**No build runs in this plan.** Nothing here touches a generator, an artifact or a checker, so
`docs/manifest_check.py` remains red for the same reason it has been since plan 13-01 — plan
13-04 owns it. Do not re-sign, re-archive or edit a MANIFEST row here.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-red-operator-conditionals-and-the-wfitems-list-wrapper/13-RESEARCH.md
@.planning/phases/13-red-operator-conditionals-and-the-wfitems-list-wrapper/13-01-SUMMARY.md
@.planning/phases/13-red-operator-conditionals-and-the-wfitems-list-wrapper/13-02-SUMMARY.md
@.claude/CLAUDE.md
</context>

## Artifacts this phase produces

New and changed artifacts introduced **by this plan** (exclude from drift verification):

| Symbol / path | Kind | File |
|---|---|---|
| axis 8 (`WFItems` two-kind row rule) | new numbered item | `.claude/CLAUDE.md` |
| axis 9 (compound-versus-scalar reader rule) | new numbered item | `.claude/CLAUDE.md` |
| `pending_exit` container/leaf paragraph | extension of axis 7 | `.claude/CLAUDE.md` |
| `BD-07` | new decision record | `docs/CAPABILITY-DECISIONS.md` |
| `BD-08` | new decision record | `docs/CAPABILITY-DECISIONS.md` |
| Phase 13 recording-duty section | new numbered section | `docs/BUILD-NOTES.md` |
| corrected Phase 13 Goal and Deliverables prose | changed prose | `.planning/ROADMAP.md` |
| corrected Phase 13 milestone checklist bullet | changed line | `.planning/ROADMAP.md` |
| dated `REFUTED` / `SUPERSEDED` annotations at five asserting sites | added annotations, originals preserved | `.planning/debug/HANDOFF.md` |
| the closed todo with its refutation tombstone | moved + extended file | `.planning/todos/completed/2026-08-15-fix-red-operator-and-list-wrapper-defects.md` |

Created by earlier plans in this phase: `_list_row`, `verify_list_item_wrappers`, the Aware
arming of that guard (13-01); the positive Donor-5 assertion inside
`verify_conditional_action_string()` and its provenance docstring (13-02). Created by plan
13-04: `13-UAT.md`, refreshed MANIFEST rows, new dated archives and re-signed artifacts.

## Flagged assumptions

The spec-less probe accounting for this phase is tabulated once in `13-01-PLAN.md`
§ "Flagged assumptions" — five edge rows authored into `must_haves`, three `unclassified` rows
carried as flagged assumptions. No probe row is assigned to this plan; nothing is dropped here.

<tasks>

<task type="auto">
  <name>Task 1: Extend the numbered axis list to nine and record both donor findings in CAPABILITY-DECISIONS</name>

  <reversibility rating="reversible">Documentation only; every edit is a scoped replacement inside a versioned Markdown file and `git revert` restores it exactly.</reversibility>

  <files>.claude/CLAUDE.md, docs/CAPABILITY-DECISIONS.md</files>

  <read_first>
    - `.claude/CLAUDE.md` § Conventions → the numbered generator-authoring rules (currently `:353-391`, but locate by heading text, not by line — line numbers shift on every edit, which is the project's own rule) and the adjacent § "Verified iOS Shortcuts runtime semantics" table. Read all seven existing items so the two new ones match their voice: each opens with a bold one-line rule, then explains the runtime failure it prevents, then names the guard that asserts it.
    - `.claude/CLAUDE.md` § 9 → the `/ponytail` row of the tooling table, which names the axis count in prose and must move with the heading.
    - `13-RESEARCH.md` § "Donor 4 / 4.1 — the `WFItems` List row, literal XML" and § "Donor 5 — the conditional operand, literal XML" — the verbatim device XML and both answer tables.
    - `13-RESEARCH.md` § "Documentation Updates Required (the single-pass deliverable)" — the four-row target table this task implements rows 1, 2, 3 and 4 of.
    - `13-RESEARCH.md` § "Assumptions Log" A2 — `WFItemType` values beyond `0` are deliberately unaudited and must not be guessed.
    - `tools/build_state_engine.py` — `get_value()` and `read_value()` and their docstrings, `COMPOUND_STATE_KEYS`, `verify_compound_value_reads()`, `seed_pending_exit()`, and `clear_snapshot()`'s docstring. Locate each by `def` name; do not trust a line number.
    - `docs/CAPABILITY-DECISIONS.md` — the table of contents at the top and one existing decision record end to end (BD-06-A4 is the most recent), so the two new records match the file's heading level, front-matter row style and evidence-citation habit.
  </read_first>

  <action>
**1 — Add the `WFItems` row wrapper as a new numbered axis** at the end of the numbered list in
`.claude/CLAUDE.md` § Conventions. Its bold one-line rule is the two-kind rule: a **literal** row
is emitted as a bare string directly in the `WFItems` array, and a **variable- or
attachment-bearing** row is wrapped as a dictionary whose `WFItemType` key holds the integer `0`
and whose `WFValue` key holds the complete, unchanged `WFTextTokenString` envelope.

The body must state four things. That the failure mode is a row rendering **empty** on device
while the plist validates, signs and imports perfectly — so the Mirror can select a blank
template. That it is a **container** defect and therefore a distinct axis, **not** an instance of
the existing string-envelope axis: the envelope inside `WFValue` is already correct in the
defective form; what is missing is the row framing around it. That the evidence is
`.planning/debug/Donor 4.shortcut` and `.planning/debug/Donor 4.1.shortcut`, decrypted in Phase
13, byte-identical on this action, showing both row kinds in one device-authored array. And that
`verify_list_item_wrappers()` in `tools/build_state_engine.py` is the build guard, armed on both
forks.

Add one closing sentence recording the boundary: only `WFItemType` `0` is observed, no donor
exercises a number, dictionary or file row, and any other value must be established from
evidence rather than inferred from `0`.

**2 — Add the compound-versus-scalar reader rule as a further numbered axis.** Its bold one-line
rule: a **compound** state value consumed as a List is read with `get_value()`; a **scalar** used
in a text or numeric comparison is read with `read_value()`. The body must name the four
`COMPOUND_STATE_KEYS` members verbatim from the source, explain that `read_value()`'s `gettext`
chain flattens a compound value so a downstream List consumer receives text rather than a list,
and name `verify_compound_value_reads()` as the guard. The rule is already fully written as a
docstring on `get_value()` and `read_value()`; lift it faithfully rather than paraphrasing from
memory.

**3 — Extend the existing state-shape axis with the `pending_exit` container/leaf pattern.** This
is a refinement of that axis, not a tenth one — it is the same "state shape must exist before it
is read" rule, made concrete. Add a paragraph inside that item stating: seed the container as a
**permanent invariant** at bootstrap; write and clear only its **leaves**, never the container
itself; and gate on a string is-not-sentinel test (condition code 5) or a numeric greater-than-zero
test, **never** a condition-100 existence test over the container — because a dotted read raises
on any missing segment, which makes a read-then-existence gate on a dotted path unimplementable.
Cite `seed_pending_exit()` and `clear_snapshot()`'s docstring in `tools/build_state_engine.py` as
the implementations.

**4 — Move the count word.** Steps 1 and 2 take the list from seven items to nine; step 3 refines
an existing item rather than adding a tenth. The list heading names the number of axes and the
`/ponytail` row in the § 9 tooling table repeats it, and both must end up carrying the exact
phrase `nine parameter-defect axes`. Search the whole file for any other prose that names the
count and update every occurrence — a single stale mention makes the list self-contradicting.

**5 — Add `BD-07` to `docs/CAPABILITY-DECISIONS.md`:** the conditional TEXT-slot operand envelope,
settled as **already correct**. Record: the question the ROADMAP asked; that
`.planning/debug/Donor 5.shortcut` was decrypted in Phase 13 (first analysis ever); the six
answers from the donor — the operand lives in `WFConditionalActionString` for a string-family
comparison, wrapped as a `WFTextTokenString` with a single `U+FFFC` string and an
`attachmentsByRange` keyed `{0, 1}`, whose inner attachment is a **bare** `{Type, VariableName}`
dict not re-wrapped in a serialization envelope, with `WFCondition` as an **integer**, with
`WFInput` present **simultaneously** carrying the opposite `WFTextTokenAttachment` envelope, and
with **no** coercion aggrandizement on either side; that `token()` emits a key-for-key identical
shape; the measured counts per fork; and the verdict — **REFUTED**, 0 defective sites, the
deliverable becomes a pin rather than a sweep. Record as still open, not resolved: what caused
the 2026-08-14 red render (not reproducible at HEAD, and the cited screenshot does not exist in
the worktree, the main checkout, or git history), and whether a pure-literal comparison target
should be a `WFTextTokenString` (no donor covers it; leave the working literals alone).

**6 — Add `BD-08`:** the `WFItems` row wrapper, settled by Donors 4 and 4.1. Record the two-kind
rule, the verbatim shape, the measured defect inventory per fork, the fix location, the guard,
and the unaudited boundary from A2. Add the bonus finding Donor 4.1 settled while it was open:
that a **numeric** conditional's right-hand operand uses `WFNumberValue` — serialized as a
**string**, not an integer — rather than `WFConditionalActionString`, that the two right-hand
slots are mutually exclusive, that an existence-family condition carries neither, and that the
left operand in that case carries the coercion aggrandizement. `if_block()` already implements
all three cases correctly; record it as confirmation, not as a change request.

**7 — Update the table of contents** at the top of `docs/CAPABILITY-DECISIONS.md` with both new
rows, matching the existing column format.
  </action>

  <verify>
    <automated>python3 - <<'PY'
import pathlib, re, sys
claude = pathlib.Path('.claude/CLAUDE.md').read_text()
body = claude.split('### Generator authoring rules')[1].split('### Verified iOS Shortcuts runtime semantics')[0]
items = re.findall(r'^(\d+)\. \*\*', body, flags=re.M)
assert items == [str(n) for n in range(1, 10)], f'axis list is not 1..9: {items}'
axis8 = body.split('\n8. ')[1].split('\n9. ')[0]
axis9 = body.split('\n9. ')[1]
axis7 = body.split('\n7. ')[1].split('\n8. ')[0]
for token in ('WFItemType', 'WFValue', 'Donor 4', 'verify_list_item_wrappers'):
    assert token in axis8, f'axis 8 does not name {token}'
assert 'container' in axis8.lower(), 'axis 8 does not state that this is a container defect'
for token in ('get_value', 'read_value', 'verify_compound_value_reads'):
    assert token in axis9, f'axis 9 does not name {token}'
assert 'pending_exit' in axis7, 'axis 7 was not extended with the container/leaf pattern'
stale = [n for n, line in enumerate(claude.splitlines(), 1) if 'seven parameter-defect axes' in line]
assert not stale, f'the old axis count survives at line(s) {stale}'
assert claude.count('nine parameter-defect axes') >= 2, 'the new count is not stated at both the heading and the /ponytail row'
caps = pathlib.Path('docs/CAPABILITY-DECISIONS.md').read_text()
for marker in ('BD-07', 'BD-08'):
    assert caps.count(marker) >= 2, f'{marker} is missing from the table of contents or the body'
bd7 = caps.split('BD-07')[-1]
for token in ('Donor 5', 'WFTextTokenString', 'WFTextTokenAttachment', 'REFUTED'):
    assert token in bd7, f'BD-07 does not record {token}'
bd8 = caps.split('BD-08')[-1]
for token in ('WFItemType', 'Donor 4.1', 'WFNumberValue', '660'):
    assert token in bd8, f'BD-08 does not record {token}'
print('axis list 1..9 with 7 extended, 8 and 9 added; BD-07 and BD-08 recorded')
PY</automated>
  </verify>

  <acceptance_criteria>
    - The numbered list under the generator-authoring-rules heading in `.claude/CLAUDE.md` contains exactly the items `1.` through `9.` in order.
    - Item 8 contains the strings `WFItemType`, `WFValue`, `Donor 4` and `verify_list_item_wrappers`, and the word `container`, and states that only `WFItemType` `0` is donor-observed.
    - Item 9 contains the strings `get_value`, `read_value` and `verify_compound_value_reads`, and names the four `COMPOUND_STATE_KEYS` members.
    - Item 7 contains the string `pending_exit` and states the seed-container / write-leaves / never-condition-100 rule.
    - The literal `seven parameter-defect axes` appears nowhere in `.claude/CLAUDE.md`, and the new count phrase appears at both the list heading and the `/ponytail` row.
    - `docs/CAPABILITY-DECISIONS.md` contains `BD-07` and `BD-08` at least twice each — once in the table of contents and once as a section heading.
    - The `BD-07` body contains `Donor 5`, `WFTextTokenString`, `WFTextTokenAttachment` and `REFUTED`, and records both open questions (the 2026-08-14 red render's cause, and the pure-literal comparison target) as unsettled.
    - The `BD-08` body contains `WFItemType`, `Donor 4.1`, `WFNumberValue` and the measured figure `660`.
    - No file under `tools/`, `src/`, `docs/*.py` or `artifacts/` was modified by this task.
  </acceptance_criteria>

  <done>The numbered axis list runs 1 through 9 with axis 7 extended and axes 8 and 9 added and the count consistent everywhere in the file, and both donor findings are recorded as first-class decision records with their unsettled boundaries stated rather than resolved.</done>
</task>

<task type="auto">
  <name>Task 2: Write the Phase 13 BUILD-NOTES record, then close the refuted counts as a class across ROADMAP, HANDOFF and the pending todo</name>

  <reversibility rating="reversible">Documentation and planning records only; the todo move is a `git mv` and every edit is a scoped replacement.</reversibility>

  <files>docs/BUILD-NOTES.md, .planning/ROADMAP.md, .planning/debug/HANDOFF.md, .planning/todos/pending/2026-08-15-fix-red-operator-and-list-wrapper-defects.md, .planning/todos/completed/2026-08-15-fix-red-operator-and-list-wrapper-defects.md</files>

  <read_first>
    - `docs/BUILD-NOTES.md` § 27 ("Phase 12 — the recording duty: assumptions, decisions and the ship gate") end to end — the immediately preceding section and the closest structural model for this one. Match its heading numbering, its subsection shape and its habit of quoting verbatim command output.
    - `docs/BUILD-NOTES.md` § 22 — the measured-evidence style for validator output, including index normalisation so a future run can diff against the record.
    - `13-01-SUMMARY.md` and `13-02-SUMMARY.md` — the verbatim `SystemExit` texts, the measured counts, the digests and the ordering-mask observation this section transcribes. Everything recorded must come from these, not be re-derived.
    - `13-RESEARCH.md` § "Evidence Recovered This Session", § "Measured Site Inventory at HEAD", § "The `if_block(\"Previous Respected\", 4, ...)` site — not what the ROADMAP thought", § "The screenshot does not exist", § "Assumptions Log" and § "Open Questions".
    - `.planning/ROADMAP.md` § "Phase 13" — the whole section, so the section edit is scoped to the Goal and Deliverables prose and leaves the `**Severity:**`, `**Requirements**:`, `**Depends on:**`, `**Plans:**` and `Plans:` checklist lines untouched.
    - `.planning/ROADMAP.md` § the milestone checklist near the top of the file — specifically the Phase 13 bullet, which sits roughly 550 lines above the phase section and is therefore **outside** any section-scoped edit. Measured: it asserts both refuted counts.
    - `.planning/debug/HANDOFF.md` — the whole file, but at minimum: the axis table row for operand type / coercion on conditionals; the donor-inventory row for Donor 5; the "SPUN OFF" block for this todo, which asserts both families' counts, cites the screenshot as new evidence, and names the false-lead site; and the closing todo-index entry. Measured: five sites carry a refuted or superseded claim.
    - `.planning/todos/pending/2026-08-15-fix-red-operator-and-list-wrapper-defects.md` — the file being closed, in full.
    - One file in `.planning/todos/completed/` end to end — the closure format to match.
  </read_first>

  <action>
**1 — Add a Phase 13 section to `docs/BUILD-NOTES.md`,** numbered as the next section after the
existing highest, following § 27's shape. It must record, in this order:

*The decrypts.* All three donors decrypted with the `.claude/CLAUDE.md` § 8 recipe, first attempt
each; Donor 5 analysed for the first time ever. State what each settled. Note the recording duty
this satisfies: a probe's result is recorded, not consumed.

*The measured inventory, per fork, as a table.* Family 1: total actions, mode-0 conditionals
carrying `WFConditionalActionString`, the variable-bearing subset and its condition-code split,
the raw-literal remainder, and the bare-placeholder count. Family 2: `is.workflow.actions.list`
actions, the correct one, the defective ones, bare rows, unwrapped rows, and the post-fix wrapped
count. Give the pre-fix and post-fix figures side by side.

*The refutation, stated plainly.* The ROADMAP's figures for both families, the measured figures,
and the verdict for each. Record that the site the ROADMAP named as a concrete starting point
passes a raw Python literal and is not a member of the family, and that this is a corrected
attribution rather than a discovered second defect. Record that the screenshot the ROADMAP and
the todo both cite is absent from the worktree, the main checkout and git history — verified three
ways, and its recorded filename contains a `U+2060` word joiner so it is not merely a quoting
problem — and that both defects were established independently and more precisely without it, so
nothing is lost. State that no task in this phase depended on reading it.

*The two guards.* Name each, its file, what it asserts, where it is registered, and on which forks
it is armed. Record that the Aware fork needed two new touch points for the new guard and none for
the extended one, and why.

*The sensitivity demonstrations, verbatim.* Transcribe every `SystemExit` text captured in plans
13-01 and 13-02, with the pre- and post-demonstration digests for both forks, using the two-row
evidence format Phase 12 established. Record the ordering mask inside
`verify_conditional_action_string()` as an observed property with the two distinct messages
quoted, and record that the chain order was not changed to work around it.

*Deviations and open assumptions.* A1 through A4 from `13-RESEARCH.md`, each written as still
open with its risk: the unprovable cause of the 2026-08-14 red render; `WFItemType` values beyond
`0` left unaudited by choice; the all-wrapped array being a configuration no donor exhibits; and
whether wrapping changes what `getitemfromlist` returns, which is device-only and belongs to
Phase 19 UAT. Add the installed-base note: a user who already imported the previous signed build
keeps the blank-row Mirror until they re-import, so Phase 19 must re-import rather than test a
stale install.

*The regression-protection statement.* CIRC-04 and ROOM-03 have no defect site in either family;
they are protected by `docs/phase5_self_check.py` and `docs/note_identity_check.py` staying green
through the rebuild, and no work was invented to make them look addressed.

Leave a clearly-labelled placeholder subsection for the gate B advisory read and the signed-artifact
provenance — plan 13-04 fills it. Do not guess its contents.

**2 — Correct the ROADMAP Phase 13 prose.** Rewrite the `**Goal:**` paragraph and the
`**Deliverables.**` paragraph of `.planning/ROADMAP.md` § "Phase 13" so they state the measured
figures and the corrected scope: family 1 is settled as already-correct with a pin as the
deliverable, family 2 is one emitter fix plus a recurrence guard across the measured number of
actions and rows per fork, and the doc pass carries the refutation. Add a dated correction note
attributing the change to the decrypted donors, so a reader sees the record was corrected rather
than always having said this.

Scope the section edit to those two paragraphs. Do **not** touch the `**Severity:**`,
`**Requirements**:`, `**Depends on:**` or `**Plans:**` lines, and do not touch the `Plans:`
checklist — those are owned by the planning and execution workflows, and a whole-file rewrite of
`ROADMAP.md` would destroy every phase entry outside this one. Use scoped replacements.

**3 — Close the refuted counts as a CLASS, not at the sites you happen to remember.** A
section-scoped edit reaches only the Phase 13 section. Two further vectors sit outside it and
were measured during plan review:

- **The ROADMAP milestone checklist bullet for Phase 13**, roughly 550 lines above the phase
  section, which restates both counts in one line. Rewrite that bullet so it describes the
  measured scope. It is a checklist line and cannot carry a paragraph, so the tombstone for it
  lives in the phase section the bullet points at — which step 2 has already written.
- **`.planning/debug/HANDOFF.md`**, which is the origin of the whole claim and the document a
  future cycle is most likely to read cold. Annotate every site that asserts a refuted or
  superseded figure — the `WFItems` instance count, the conditional site count, the axis table's
  operand/coercion tally, Donor 5's never-analysed status, and the false-lead attribution of the
  named starting site. **Preserve the original wording as history and attach a dated annotation
  beside it** carrying the marker `REFUTED` (or `SUPERSEDED` where the figure was simply
  overtaken), the measured replacement, and a pointer to the Phase 13 BUILD-NOTES section. Do not
  delete the original sentences: this file is a handoff record, and rewriting history in it
  destroys the audit trail that makes the refutation credible.

Then **sweep the whole tree** rather than trusting this list. Grep `.planning/` and `docs/` for
every refuted figure — both families' counts in every phrasing they appear in — and bring any
further hit into this same task under the same treatment. Two exclusions are deliberate and must
survive the sweep: `.planning/todos/completed/`, where the closed todo preserves its original text
under its own tombstone, and `.planning/phases/13-*/`, where the phase's own artifacts quote the
refuted figures in order to refute them. Every other occurrence must either be gone or sit beside
a `REFUTED` marker.

**Marker casing is load-bearing.** Every tombstone marker written into a non-exempt file — that
means `docs/BUILD-NOTES.md`, `docs/CAPABILITY-DECISIONS.md` and `.planning/debug/HANDOFF.md`, not
just HANDOFF — must be the **uppercase literal `REFUTED` or `SUPERSEDED`**, because the sweep's
±12-line tombstone window in this task's `<verify>` block matches case-sensitively while the
BUILD-NOTES section assertion accepts either case. Writing a lowercase `refuted` beside a quoted
original figure would satisfy the section assertion and then fail the sweep on the very file the
task just wrote. This applies with equal force to `BD-08` in `docs/CAPABILITY-DECISIONS.md`, which
is written one task earlier and carries no `REFUTED` requirement of its own. The failure is
self-diagnosing — the sweep names the offending `file:line` in its assertion message — so if it
fires, add the uppercase marker at the named line rather than relaxing the window.

**4 — Close the pending todo with a tombstone.** `git mv` the file from

`.planning/todos/pending/` to `.planning/todos/completed/`, matching the naming of the files
already there, and append a resolution block to the moved file recording: which of its numbered
solution steps were done as written, which were **refuted** and why (step 2's cross-check
inverted the conclusion; step 1's sweep was not performed and performing it would have replaced a
device-confirmed shape with a guess), that its family-2 instance figure under-counted the real
number of List actions by 33× and of rows by 330×, that its named concrete site was a false lead, that
the screenshot it cites does not exist, and that step 7's Sentient propagation happened through
the rebuild rather than as separate work. Add the closure date and the owning phase.

The block is a tombstone: it must be readable on its own by someone who never sees this phase's
planning directory, because that is the only thing that stops the refuted counts coming back.
  </action>

  <verify>
    <automated>python3 - <<'PY'
import pathlib, re, subprocess
notes = pathlib.Path('docs/BUILD-NOTES.md').read_text()
assert 'Phase 13' in notes, 'no Phase 13 section in BUILD-NOTES'
sections = re.findall(r'^## (\d+)\. (.+)$', notes, flags=re.M)
p13 = [s for s in sections if 'Phase 13' in s[1]]
assert p13, f'Phase 13 is not a numbered ## section: {sections[-3:]}'
body = notes.split('## %s. %s' % p13[0])[1]
for token in ('Donor 5', 'Donor 4.1', 'verify_list_item_wrappers', 'verify_conditional_action_string',
              'SystemExit', '660', 'CIRC-04', 'ROOM-03'):
    assert token in body, f'Phase 13 section does not record {token}'
assert re.search(r'\b(66|660)\b', body), 'Phase 13 section carries no measured figure'
assert 'REFUTED' in body or 'refuted' in body, 'Phase 13 section does not state the refutation'
road = pathlib.Path('.planning/ROADMAP.md').read_text()
p13road = road.split('### Phase 13')[1].split('### Phase 14')[0]
for token in ('66', '660', 'Donor 5'):
    assert token in p13road, f'ROADMAP Phase 13 does not carry the measured evidence token {token}'
assert '**Requirements**: CIRC-04, CIRC-07, ROOM-03, DIST-01, DIST-02' in p13road, 'the requirements line was disturbed'
assert '**Depends on:** Phase 12' in p13road, 'the dependency line was disturbed'
assert road.count('### Phase ') >= 20, 'ROADMAP lost phase entries - a scoped edit became a rewrite'
hand = pathlib.Path('.planning/debug/HANDOFF.md').read_text()
assert 'REFUTED' in hand or 'SUPERSEDED' in hand, 'HANDOFF.md carries no tombstone marker'
for token in ('660', '66'):
    assert token in hand, f'HANDOFF.md does not carry the measured figure {token}'
# whole-tree sweep: close the CLASS, not the two known sites.
REFUTED_LITERALS = [
    'the 14 red-operator sites and 2 blank-List sites',
    '2 confirmed instances',
    'sweep all 14 by class',
    'the 14 sites',
    '14 `WFConditionalActionString` sites',
    '20 Dumb, 25 Sentient',
]
EXEMPT = ('.planning/todos/completed/', '.planning/phases/13-')
survivors = []
for base in ('.planning', 'docs'):
    for path in pathlib.Path(base).rglob('*.md'):
        s = str(path)
        if any(x in s for x in EXEMPT):
            continue
        lines = path.read_text(errors='ignore').splitlines()
        for i, line in enumerate(lines):
            if any(lit in line for lit in REFUTED_LITERALS):
                window = '\n'.join(lines[max(0, i - 12):i + 13])
                if 'REFUTED' not in window and 'SUPERSEDED' not in window:
                    survivors.append(f'{s}:{i + 1}: {line.strip()[:90]}')
assert not survivors, 'refuted counts survive with no tombstone:\n' + '\n'.join(survivors)
old = pathlib.Path('.planning/todos/pending/2026-08-15-fix-red-operator-and-list-wrapper-defects.md')
new = pathlib.Path('.planning/todos/completed/2026-08-15-fix-red-operator-and-list-wrapper-defects.md')
assert not old.exists(), 'the todo is still in pending/'
assert new.exists(), 'the todo did not land in completed/'
closed = new.read_text()
for token in ('REFUTED', '660', 'Phase 13'):
    assert token in closed, f'the closed todo lacks its tombstone content: {token}'
tracked = subprocess.run(['git', 'status', '--porcelain'], capture_output=True, text=True).stdout
assert 'tools/' not in tracked and 'src/' not in tracked, f'this plan modified build inputs: {tracked}'
print('BUILD-NOTES Phase 13 section, corrected ROADMAP prose, tombstoned HANDOFF, closed todo')
print('whole-tree sweep clean: no refuted count survives outside the two deliberate exemptions')
PY</automated>
  </verify>

  <acceptance_criteria>
    - `docs/BUILD-NOTES.md` gains a numbered `## N. ... Phase 13 ...` section whose body contains `Donor 5`, `Donor 4.1`, `verify_list_item_wrappers`, `verify_conditional_action_string`, `SystemExit`, the measured figure `660`, and explicit `CIRC-04` / `ROOM-03` regression-protection wording.
    - That section transcribes the verbatim `SystemExit` texts from plans 13-01 and 13-02 and the pre- and post-demonstration digests for both forks, and records the ordering mask with both distinct messages quoted.
    - That section records A1 through A4 as open, records that the cited screenshot is absent from the repository and that no task depended on it, and records the stale-install note for Phase 19.
    - That section carries a clearly-labelled empty placeholder for the gate B advisory read and signed-artifact provenance, which plan 13-04 fills.
    - `.planning/ROADMAP.md` § "Phase 13" Goal and Deliverables prose contains the measured figures `66` and `660` and cites `Donor 5`.
    - The `**Requirements**: CIRC-04, CIRC-07, ROOM-03, DIST-01, DIST-02` and `**Depends on:** Phase 12` lines in that section are byte-identical to before, and `.planning/ROADMAP.md` still contains at least 20 `### Phase ` headings — proof the edit was scoped and not a rewrite.
    - `.planning/debug/HANDOFF.md` carries at least one `REFUTED` or `SUPERSEDED` marker and both measured figures `66` and `660`, with the original claim sentences preserved beside their annotations rather than deleted.
    - **The whole-tree sweep is clean.** For each of the literals `the 14 red-operator sites and 2 blank-List sites`, `2 confirmed instances`, `sweep all 14 by class`, `the 14 sites`, ``14 `WFConditionalActionString` sites`` and `20 Dumb, 25 Sentient`, every occurrence anywhere under `.planning/` or `docs/` is either absent, inside one of the two deliberate exemptions (`.planning/todos/completed/`, `.planning/phases/13-*/`), or within twelve lines of a `REFUTED` or `SUPERSEDED` marker. Any survivor fails the task.
    - The ROADMAP milestone checklist bullet for Phase 13 no longer carries either refuted count.
    - Every tombstone marker this task wrote into a non-exempt file uses the uppercase literal `REFUTED` or `SUPERSEDED` — verified by the sweep passing, since its window is case-sensitive while the BUILD-NOTES section assertion is not. No lowercase `refuted` sits as the only marker beside a quoted refuted figure in `docs/BUILD-NOTES.md`, `docs/CAPABILITY-DECISIONS.md` or `.planning/debug/HANDOFF.md`.
    - `.planning/todos/pending/2026-08-15-fix-red-operator-and-list-wrapper-defects.md` no longer exists and the same filename exists under `.planning/todos/completed/`, carrying a tombstone containing `REFUTED`, `660` and `Phase 13`, readable standalone.
    - `git status --porcelain` shows no modification under `tools/` or `src/` — this plan touched no build input.
  </acceptance_criteria>

  <done>The Phase 13 recording duty is discharged in one pass and the refuted counts are closed as a class: BUILD-NOTES carries the decrypts, measurements, refutation, verbatim guard evidence and open assumptions; the ROADMAP checklist bullet and phase prose both state the measured figures; HANDOFF.md carries a dated tombstone at every asserting site with its history preserved; the pending todo is closed with a standalone tombstone; and a whole-tree sweep proves no uncorrected source survives to re-assert either count.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning artifact → durable project record | Figures cross from `13-RESEARCH.md` and the two SUMMARYs into `.claude/CLAUDE.md` and `docs/`, where they become the authority every later phase reads. A transcription error here is laundered into ground truth. |
| stale record → future implementation pass | The ROADMAP, the pending todo and `HANDOFF.md` are the vector by which a refuted claim re-enters implementation. |
| scoped edit → whole-file record | `.planning/ROADMAP.md` holds every phase entry; an unscoped rewrite of one section destroys the rest. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-15 | Repudiation | the refuted counts in the ROADMAP checklist bullet, the ROADMAP Phase 13 section, `.planning/debug/HANDOFF.md` and the pending todo | high | mitigate | Every asserting record is corrected in place or annotated with a dated tombstone naming the donor evidence; the closed todo is readable standalone; `HANDOFF.md` keeps its original sentences beside the annotations so the audit trail survives. Deleting without a tombstone is the named anti-pattern that produced three cycles of re-litigation. |
| T-13-29 | Repudiation | a refuted-count assertion at a site nobody enumerated | high | mitigate | The class is closed by a whole-tree sweep of `.planning/` and `docs/` over six literal phrasings, not by a hand-listed set of sites; two exemptions are named explicitly and every other survivor must sit within twelve lines of a tombstone marker. A scoped section edit measurably left two sites uncorrected, which is exactly why enumeration is not the mitigation. |
| T-13-16 | Tampering | `.planning/ROADMAP.md` | high | mitigate | The edit is scoped to two paragraphs by explicit instruction; the verify block asserts the `**Requirements**:` and `**Depends on:**` lines are byte-identical and that at least 20 `### Phase ` headings survive, so a whole-file rewrite fails the task. |
| T-13-17 | Spoofing | measured figures transcribed into `docs/` | high | mitigate | Every figure must come from `13-01-SUMMARY.md`, `13-02-SUMMARY.md` or `13-RESEARCH.md`'s measured tables; the verify block asserts the specific measured tokens are present rather than accepting prose that merely sounds right. |
| T-13-18 | Repudiation | unsettled items written as settled | high | mitigate | `WFItemType` beyond `0`, the 2026-08-14 red render's cause, and the pure-literal comparison-target shape are each required to be written as explicitly unaudited, and the BD-07 acceptance criterion asserts both open questions are recorded. |
| T-13-19 | Elevation of privilege | ingested planning prose treated as instruction | medium | mitigate | The donor XML, the ROADMAP prose and the todo text are transcribed as data. No text lifted from them is executed, and no directive found inside a quoted record changes what this plan does. |
| T-13-20 | Information disclosure | recorded evidence | low | accept | Everything recorded is generator structure, action indices and hashes of locally-built artifacts. No secrets, no PII, no behavioural data — the project stores none and this phase adds none (ASVS V5 / V12). |
| T-13-SC | Tampering | npm / pip / cargo installs | low | accept | This plan runs no package-manager install and touches no dependency file. `13-RESEARCH.md` § "Package Legitimacy Audit" records the phase installs no external packages, so there is nothing to audit and no legitimacy checkpoint to gate. |
</threat_model>

<verification>
- The numbered axis list runs 1 through 9; axis 7 extended, axes 8 and 9 added; the count is consistent at every site in `.claude/CLAUDE.md`.
- `BD-07` and `BD-08` exist in both the table of contents and the body, each carrying its evidence tokens and its unsettled boundary.
- `docs/BUILD-NOTES.md` gains a numbered Phase 13 section carrying decrypts, measurements, refutation, verbatim guard messages, the mask, the assumptions and the regression-protection statement, plus an empty labelled placeholder for 13-04.
- The ROADMAP Phase 13 Goal and Deliverables prose carries the measured figures; the structural lines are byte-identical; the file still holds every other phase entry.
- The ROADMAP milestone checklist bullet for Phase 13 carries the measured scope.
- `.planning/debug/HANDOFF.md` carries a dated tombstone marker and both measured figures, with its original claim sentences preserved.
- The whole-tree sweep over six refuted-count phrasings across `.planning/` and `docs/` returns no untombstoned survivor outside the two deliberate exemptions.
- The pending todo is closed into `completed/` with a standalone tombstone.
- `git status --porcelain` shows no change under `tools/` or `src/`.
</verification>

<success_criteria>
- No project record still asserts a refuted count for either family, and that is proven by a sweep rather than by an enumeration of remembered sites.
- Both donor findings are recoverable from `docs/` alone, without the phase planning directory.
- Every item this phase did not settle is written down as unsettled, with its risk.
</success_criteria>

<output>
Create `.planning/phases/13-red-operator-conditionals-and-the-wfitems-list-wrapper/13-03-SUMMARY.md` when done.
</output>
