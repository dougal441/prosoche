---
phase: 01-capability-audit-config-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - docs/BUILD-NOTES.md
autonomous: true
requirements: [AUDIT-01, AUDIT-07]
user_setup: []

must_haves:
  truths:
    - "Every data, control-flow, interaction, and output action PROSOCHĒ depends on has an audit row carrying an exact identifier and a verdict."
    - "A reader can tell, for each of those actions, whether the parameter shape is evidenced or only the identifier is."
    - "Where an action the canonical strategy asks for does not exist, the row says so and a numbered deviation entry names the substitute."
    - "No identifier appears in the table that the executor did not find in a named ToolKit snapshot or reference doc."
  artifacts:
    - docs/BUILD-NOTES.md
  key_links:
    - "Each CAP row's Evidence cell -> the lookup recipe recorded in BUILD-NOTES section 3 by plan 01-01 (verdicts are re-derivable, not asserted)."
    - "Every non-VERIFIED verdict -> a DEV-NN entry in section 5 (a gap without a recorded fallback is invisible to Phase 2 and later)."
---

## Phase goal (verbatim from ROADMAP.md)

> Every iOS action PROSOCHĒ depends on is resolved to VERIFIED / UNVERIFIED / NOT AVAILABLE with its exact identifier and parameter shape before any behavioural logic is authored, and the tunable static config exists as a single editable block.

## Artifacts this phase produces

| Path | Produced by | Purpose |
|------|-------------|---------|
| `docs/BUILD-NOTES.md` | 01-01 (created), **01-02**, 01-04, 01-05 | Capability audit table, deviation log, user action items, coverage check |
| `docs/CAPABILITY-DECISIONS.md` | 01-01 (created), 01-04, 01-05 | The five blocker decisions BD-01..BD-05 |
| `src/CONFIG-BLOCK.md` | 01-01 (created), 01-03, 01-05 | The single editable tuning block |

This plan authors no plist XML, no package manifest, and no test harness.

## Decision legend (source: `01-CONTEXT.md`)

- **D-01** — Target iOS 26.x, native Shortcuts only.
- **D-07** — The "do not fabricate" protocol: safest fallback, record the deviation, keep the Shortcut runnable. UNVERIFIED is a correct outcome; an invented identifier is a defect.
- **D-13** — Claude's discretion over the exact shape of the build-notes table.

<objective>
Fill out the capability audit table in `docs/BUILD-NOTES.md` for every action PROSOCHĒ's data layer, control flow, interaction surface, and output surface depend on — the 25 rows that are not one of the five blockers.

This is horizontal expansion from the tracer slice plan 01-01 proved. The schema, verdict vocabulary, evidence-citation rule, and deviation-entry shape are already fixed; this plan only adds rows using them.

The evidence base is already substantially built: `.planning/research/STACK.md` section 3 carries a completed 16-item audit with exact identifiers and evidence paths. The job is to VERIFY and EXTEND that table, not to research from scratch — re-run each lookup so the row records what the ToolKit actually says today, then add the rows STACK.md does not cover.

Purpose: Phases 2, 3, 4, and 6 author plist XML against these identifiers. A wrong identifier here becomes an Unknown Action block or, worse, a validator false-pass that only breaks on a real iPhone (per D-07).

Output: `docs/BUILD-NOTES.md` sections 4 and 5, extended by 25 capability rows and their deviation entries.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-capability-audit-config-foundation/01-CONTEXT.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
@.planning/research/ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit rows for the data, state, and control-flow actions</name>

  <precondition>`docs/BUILD-NOTES.md` exists with sections 3 (Evidence protocol) and 4 (Capability audit table) populated by plan 01-01, and the ToolKit data directory `/Users/dougalhanson/.claude/plugins/cache/shortcuts-playground/shortcuts-playground/1.2.1/skills/shortcuts-playground/data/` is readable.</precondition>

  <files>docs/BUILD-NOTES.md</files>

  <read_first>
    - `docs/BUILD-NOTES.md` sections 2, 3, 4, 5 — the protocol, the lookup recipe, the table schema, the deviation shape
    - `.planning/research/STACK.md` section 3 rows 1–4 and 13–14, section 4 (control-flow primitives), section 5 (variable wiring)
    - `.planning/research/PITFALLS.md` A4 (dictionary type coercion), A5 (number/text coercion, no numeric-equals code), A6 (date arithmetic), A7 (fabricated identifiers, the named identifier gotchas)
    - `.planning/research/ARCHITECTURE.md` section 7 (Pressure→Circle bounded linear scan using Repeat and Get Item from List)
    - `PROSOCHE_Nine_Circles_Canonical_Strategy.md` section 31, the capability-audit list
    - Optionally load `Skill(skill="shortcuts-playground:shortcuts-playground")`
  </read_first>

  <action>
Append 14 rows to the section 4 table in `docs/BUILD-NOTES.md`, using the exact seven-column schema and four-value verdict vocabulary plan 01-01 established. Run the section 3 lookup recipe for every row. Do not copy a verdict from STACK.md without re-checking it; where your lookup diverges from STACK.md, record what you found and note the divergence in the Evidence cell.

For each row the candidate identifier below is a starting point for the lookup, NOT an answer. If a candidate is not present in any `ids` array, search the arrays for the plausible substrings instead and record the real identifier you find, or `NOT AVAILABLE` if there is none. Never write a candidate identifier into the table without having found it. Several candidates in this list are inferred rather than confirmed, and confirming or refuting them is exactly this task's job (per D-07).

Write these rows, in this order and with these IDs:

- `CAP-01` Get Current App — candidates `is.workflow.actions.getcurrentapp`, plus the inline magic-variable form `Type: "CurrentApp"` with `WFPropertyVariableAggrandizement` for Name and Bundle Identifier. In the Fallback cell record the automation-context caveat: Personal Automations pass the triggering app's identity, and if Get Current App proves unreliable inside an automation-invoked run the fallback is to trust the automation's own app filter rather than re-deriving the app.
- `CAP-02` Get File — candidates `is.workflow.actions.documentpicker.open`, `is.workflow.actions.file.select`.
- `CAP-03` Save File / overwrite — candidate `is.workflow.actions.documentpicker.save`. This row must state separately whether an overwrite / replace-existing parameter shape is evidenced. It must also record the file-existence finding: search the `ids` arrays and the prose docs for any existence-check action or filter predicate; the research base reports none exists. If confirmed, this is a deviation — write a DEV entry whose Substituted field records the bootstrap pattern the architecture already designs around: always attempt Get File, then treat a non-dictionary or empty result from the subsequent Detect Dictionary step as "state absent" and drive bootstrap off that, because Shortcuts has no try/catch and a missing file surfaces as an uncatchable runtime dialog.
- `CAP-04` Dictionary and JSON parsing — candidates `is.workflow.actions.dictionary` (with `WFItems` / `WFDictionaryFieldValueItems`) and `is.workflow.actions.detect.dictionary`. Record in the Fallback or Parameter shape cell that no distinct dictionary-to-JSON-string action is evidenced, so `state.json` writes build the JSON body as a Text action template.
- `CAP-05` Get Dictionary Value — candidates `is.workflow.actions.getvalueforkey` (param `WFDictionaryKey`, 1-based dot notation for nested access) and `is.workflow.actions.setvalueforkey` (requires `WFDictionary` wired explicitly). Record the coercion hazards from PITFALLS A4 in the Fallback cell: JSON booleans coerce to numeric 1 and 0 rather than the strings true and false; JSON null coerces to empty; a Dictionary Value compared directly inside an If can evaluate blank and must be routed through a Text action first; a nested read on a null parent breaks rather than returning empty.
- `CAP-06` Date arithmetic — candidates `is.workflow.actions.date`, `is.workflow.actions.adjustdate` (`WFDate`, non-empty `WFDuration` as a `WFQuantityFieldValue` of Magnitude and Unit, explicit `WFAdjustOperation`), `is.workflow.actions.format.date` (`WFDate`, `WFDateFormatStyle`, `WFDateFormatString`), `is.workflow.actions.gettimebetweendates` (`WFInput` plus exactly one of `WFDate` / `WFTimeUntilCustomDate` / `WFTimeUntilFromDate`, and `WFTimeUntilUnit`). Record the discipline from PITFALLS A6 in the Fallback cell: never put a CurrentDate magic token directly into a date operand — materialise it with a Date action set to Current Date and reference that action's output.
- `CAP-25` Base64 encoding — search the `ids` arrays for `base64`. Record the real identifier or `NOT AVAILABLE`. In the Fallback cell state plainly whether PROSOCHĒ needs it at all: the architecture stores `state.json` as plain JSON, so if Base64 is absent it is not on any critical path and no deviation is forced.
- `CAP-S01` Set Variable / Get Variable — candidates `is.workflow.actions.setvariable` (`WFVariableName`, `WFInput`), `is.workflow.actions.getvariable`.
- `CAP-S02` Text — candidate `is.workflow.actions.gettext`. Record explicitly that the Text action identifier is `gettext` and that a `.text` variant does not exist, since PITFALLS A7 names this as a documented gotcha.
- `CAP-S03` Number and Math — candidates `is.workflow.actions.number`, `is.workflow.actions.math`. Record that JSON-sourced numeric strings must pass through a Number action before Math or If use.
- `CAP-S04` If / Otherwise / End If — candidate `is.workflow.actions.conditional`, one identifier for all three parts distinguished by `WFControlFlowMode` integer 0, 1, 2 sharing one `GroupingIdentifier`. Record in the Parameter shape cell that every condition code requires an explicit `WFInput` wrapped as a Variable, and record the condition-code facts that matter to this build: 0 is "is less than" and there is no numeric-equals code, 3 is "is greater than or equal to", 4 is "string is", 99 is "contains", 100 is "has any value", 101 is "does not have any value". Record in the Fallback cell that `Otherwise If` with its own condition is macOS-27 only for this project's purposes and that nested If / Otherwise is used throughout instead (per D-01), and that nesting is evidenced working to depth 7.
- `CAP-S05` Repeat with a count — search the `ids` arrays for `repeat`. Record the real identifier for the count form and the two-endpoint mode 0 / mode 2 `GroupingIdentifier` pattern. Record that Repeat Index and Repeat Item must be referenced as a named `Type: Variable`, never as an `ActionOutput` pointing at the end action's UUID.
- `CAP-S06` Get Item from List — search the `ids` arrays for `getitemfromlist` and for `itemfromlist`. This row is load-bearing: the Pressure-to-Circle mapping is implemented as a bounded nine-iteration Repeat that indexes the active profile's threshold array. If no such action exists, that is a deviation and the Substituted field must name a working alternative.
- `CAP-S08` Set Name — candidate `is.workflow.actions.setitemname`, used to give a saved file a specific filename before Save File.

Append a DEV entry, numbered sequentially from the next unused number after `DEV-01`, for every row in this task whose Verdict is not `VERIFIED`, using the same five labelled fields plan 01-01 established: `Capability`, `Wanted`, `Verified`, `Substituted`, `Runnability`. A row with Verdict `VERIFIED (identifier only)` still needs a DEV entry, because the missing parameter shape is a real gap a later phase must resolve empirically; its Substituted field should name the Craig-Loop or on-device confirmation step that will resolve it.
  </action>

  <verify>
    <automated>for id in CAP-01 CAP-02 CAP-03 CAP-04 CAP-05 CAP-06 CAP-25 CAP-S01 CAP-S02 CAP-S03 CAP-S04 CAP-S05 CAP-S06 CAP-S08; do grep -qE "^\| $id \|.*\| (VERIFIED|UNVERIFIED|NOT AVAILABLE)" docs/BUILD-NOTES.md || { echo "MISSING OR UNJUDGED ROW $id"; exit 1; }; done; grep -q 'is.workflow.actions.gettext' docs/BUILD-NOTES.md && grep -q 'is.workflow.actions.conditional' docs/BUILD-NOTES.md && grep -q 'WFControlFlowMode' docs/BUILD-NOTES.md && grep -q 'is.workflow.actions.gettimebetweendates' docs/BUILD-NOTES.md && grep -q 'is.workflow.actions.detect.dictionary' docs/BUILD-NOTES.md && echo DATA-GROUP-OK</automated>
  </verify>

  <acceptance_criteria>
    - All 14 IDs `CAP-01`, `CAP-02`, `CAP-03`, `CAP-04`, `CAP-05`, `CAP-06`, `CAP-25`, `CAP-S01`, `CAP-S02`, `CAP-S03`, `CAP-S04`, `CAP-S05`, `CAP-S06`, `CAP-S08` appear as rows beginning `| CAP-` in section 4.
    - Every one of those rows has a Verdict cell holding exactly one of the four vocabulary values, and an Evidence cell naming at least one ToolKit JSON file, prose reference doc, or golden-shortcut XML filename.
    - The `CAP-S04` row records that condition code 0 means "is less than" and that no numeric-equals condition code exists.
    - The `CAP-S04` row records that nested If / Otherwise is used rather than `Otherwise If`, and cites the iOS 26 target.
    - The `CAP-05` row records the boolean-to-1/0 and null-to-empty coercion hazards and the route-through-Text rule for If comparisons.
    - The `CAP-03` row states whether a file-existence check exists, and if none exists a DEV entry records the Get File plus Detect Dictionary bootstrap substitute.
    - The `CAP-06` row names `is.workflow.actions.gettimebetweendates` and records the "materialise Current Date via a Date action first" rule.
    - Every row in this task whose Verdict is not `VERIFIED` has a matching DEV entry carrying all five labelled fields.
    - No identifier appears in any row that was not found by a lookup the executor ran; candidates that could not be found are recorded as `NOT AVAILABLE` rather than written in as though confirmed.
  </acceptance_criteria>

  <done>Every data, state, and control-flow action the OPEN/CLOSE handlers and the state engine depend on has an evidence-backed row and, where imperfect, a recorded fallback.</done>
</task>

<task type="auto">
  <name>Task 2: Audit rows for the interaction, output, and ancillary actions</name>

  <files>docs/BUILD-NOTES.md</files>

  <read_first>
    - `docs/BUILD-NOTES.md` sections 3, 4, 5 as extended by task 1
    - `.planning/research/STACK.md` section 3 rows 6, 7, 11, 12, 13, 14, 16
    - `.planning/research/PITFALLS.md` A2 (display vs data-flow parameter serialization), A3 (GroupingIdentifier in Menu blocks), C5 (Lock Screen from an automation context), C6 (actions that force a confirmation prompt)
    - `PROSOCHE_Nine_Circles_Canonical_Strategy.md` section 8.5 (Consult routing), section 11 Primitives F, H and I, section 31 capability-audit list
    - Optionally load `Skill(skill="shortcuts-playground:shortcuts-playground")`
  </read_first>

  <action>
Append 11 more rows to the section 4 table, same schema, same lookup discipline: candidates below are lookup starting points, never answers.

- `CAP-11` Ask for Input — candidate `is.workflow.actions.ask`, params `WFAskActionPrompt` and `WFInputType` with values Text, Number, URL, Date, Date and Time. This backs Confession's free-text intention and its custom time boundary.
- `CAP-12` Choose from Menu / Choose from List — candidates `is.workflow.actions.choosefrommenu` (control-flow modes 0, 1, 2, `WFMenuPrompt`, `WFMenuItems` array, one mode-1 case per item whose `WFMenuItemTitle` matches the `WFMenuItems` entry exactly and in identical order) and `is.workflow.actions.choosefromlist`. Record the order-and-title-exactness requirement in the Parameter shape cell — it is the top documented real-world failure mode and the six-exit menu and the Control Room manual menu both depend on it.
- `CAP-13` Open App — candidate `is.workflow.actions.openapp`, param `WFAppIdentifier`. Record that the OS27 `WFWindowingFormat` parameter must not be set at the iOS 26 target (per D-01).
- `CAP-14` Open URLs / web search — candidates `is.workflow.actions.openurl` (`WFInput`) and `is.workflow.actions.searchweb`. Record in the Fallback cell that if `searchweb`'s provider behaviour proves unsuitable on-device, the substitute is Open URL to a query-shaped search URL.
- `CAP-15` Maps search — candidates `is.workflow.actions.searchmaps`, `is.workflow.actions.getmapslink`, `is.workflow.actions.getdirections`.
- `CAP-21` Speak Text — candidate `is.workflow.actions.speaktext`. State in the Parameter shape cell whether the text-input parameter key is evidenced anywhere in the bundle; if it is not, the verdict is `VERIFIED (identifier only)` and the Fallback names the Craig-Loop confirmation step. Record the safety constraint that the Voice primitive never raises volume and never produces startling output.
- `CAP-22` Lock Screen — candidate `is.workflow.actions.lockscreen`, expected zero parameters. This row must explicitly distinguish it from two different actions it is easily confused with: `is.workflow.actions.lock.app` (Lock App, which locks a specific app via `WFLockAppOperation` and `WFApp`) and `com.apple.controlcenter.LockScreenIntent`. Record the platform provenance you find in the parameter-keys catalog. In the Fallback cell record the open question from PITFALLS C5: whether Lock Screen behaves correctly when invoked from an automatically-triggered Personal Automation rather than a manual run is not established by the bundle and must be confirmed on-device in Phase 5, and the named fallback per canonical strategy section 11 Primitive I is to route to Close, the Control Room, or another strongest safe exit.
- `CAP-23` Run Shortcut — candidates `is.workflow.actions.runworkflow`, `com.apple.shortcuts.RunShortcutIntent`. Record in the Fallback cell that PROSOCHĒ ships as a single monolithic action graph per fork, so this action is not on the critical path and is audited only because canonical section 31 lists it.
- `CAP-24` Wait — candidates `is.workflow.actions.delay`, `is.workflow.actions.waittoreturn`. State whether the wait-duration parameter key is evidenced; if not, the verdict is `VERIFIED (identifier only)` and the Fallback records that the general `WFQuantityFieldValue` pattern of Magnitude and Unit is the best-effort shape to confirm via the Craig Loop, and that the CLOSE handler's app-switch race brief-wait step depends on resolving it.
- `CAP-28` Get App & Website Data — candidate `com.apple.intelligenceplatform.IntelligencePlatform.IntelligencePlatformDataActionsAppIntentsExtension.CalculateAppUsageIntent`, display name "Get App & Website Activity", params `during`, `selectedDevice`, `activityType`, `startTime`, `endTime`. Record explicitly in the Fallback cell that this is out of scope for v1 and audited for completeness only, so nothing in v1 depends on its enum case values.
- `CAP-S07` Show Alert / Show Result / Show Notification — search the `ids` arrays for `alert`, `showresult`, and `notification` and record the real identifiers for all three surfaces the Knock, the Mirror, and the Control Room status use. Record in the Parameter shape cell the display-parameter rule that PITFALLS A2 names as runtime-verified: display-facing text parameters must use `WFTextTokenString` with an object-replacement placeholder and `attachmentsByRange`, even for a single bare variable, while data-flow parameters use `WFTextTokenAttachment` — using the attachment form on a display parameter validates and imports cleanly but renders empty at runtime. This is the highest-consequence authoring rule in the whole build and belongs in the audit record, not only in the research notes.

Append DEV entries, continuing the sequential numbering, for every row in this task whose Verdict is not `VERIFIED`.

Finally, add a short paragraph under the section 4 table headed `### Confirmation-prompt risk` recording the PITFALLS C6 finding as an open on-device question rather than a resolved verdict: certain actions can force an "Ask Before Running" confirmation the first time they run inside an automation even when the automation is configured to run without confirmation, which would break the silent-interception design. Name the actions on the OPEN fast path most likely to trigger it and state that Phase 2 must test the full OPEN handler from a real backgrounded automation trigger, not a manual foreground run. Do not assign a verdict to this — it is not a capability, it is a runtime behaviour the local toolchain cannot settle.
  </action>

  <verify>
    <automated>for id in CAP-11 CAP-12 CAP-13 CAP-14 CAP-15 CAP-21 CAP-22 CAP-23 CAP-24 CAP-28 CAP-S07; do grep -qE "^\| $id \|.*\| (VERIFIED|UNVERIFIED|NOT AVAILABLE)" docs/BUILD-NOTES.md || { echo "MISSING OR UNJUDGED ROW $id"; exit 1; }; done; grep -q 'is.workflow.actions.lock.app' docs/BUILD-NOTES.md && grep -q 'WFMenuItemTitle' docs/BUILD-NOTES.md && grep -q 'WFTextTokenString' docs/BUILD-NOTES.md && grep -q 'CalculateAppUsageIntent' docs/BUILD-NOTES.md && grep -q 'Confirmation-prompt risk' docs/BUILD-NOTES.md && ROWS=$(grep -cE '^\| CAP-[0-9S]+ \|.*\| (VERIFIED|UNVERIFIED|NOT AVAILABLE)' docs/BUILD-NOTES.md) && test "$ROWS" -ge 26 && echo "INTERACTION-GROUP-OK rows=$ROWS"</automated>
  </verify>

  <acceptance_criteria>
    - All 11 IDs `CAP-11`, `CAP-12`, `CAP-13`, `CAP-14`, `CAP-15`, `CAP-21`, `CAP-22`, `CAP-23`, `CAP-24`, `CAP-28`, `CAP-S07` appear as rows with a Verdict from the four-value vocabulary and a cited Evidence cell.
    - The section 4 table now holds at least 26 judged rows in total (the CAP-20 tracer row plus 25 added by this plan).
    - The `CAP-22` row names `is.workflow.actions.lock.app` and `com.apple.controlcenter.LockScreenIntent` as distinct actions it must not be confused with, and records the automation-context open question with the route-to-Close fallback.
    - The `CAP-12` row records that `WFMenuItemTitle` values must match the `WFMenuItems` entries exactly and in the same order.
    - The `CAP-S07` row records the `WFTextTokenString` display-parameter rule and names the failure mode as silent empty rendering at runtime.
    - The `CAP-28` row records that it is out of scope for v1.
    - A `### Confirmation-prompt risk` paragraph exists under the table, carries no verdict, and names Phase 2 as the owner of the on-device test.
    - Every row in this task whose Verdict is not `VERIFIED` has a matching DEV entry with all five labelled fields.
  </acceptance_criteria>

  <done>Every interaction, output, and ancillary action the Circles, the exits, and the Control Room depend on has an evidence-backed row, and the two authoring rules most likely to cause a silent runtime failure are recorded in the durable build record.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bundled ToolKit JSON snapshots → `docs/BUILD-NOTES.md` | Read-only local evidence transcribed into the document Phases 2–8 author plist XML against |
| `.planning/research/STACK.md` → `docs/BUILD-NOTES.md` | A prior research artifact copied forward; transcription drift here is invisible until an on-device run |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Spoofing | CAP row Identifier cell | critical | mitigate | Every candidate identifier in this plan is labelled a lookup starting point, not an answer; the acceptance criteria require that an unfound candidate is recorded `NOT AVAILABLE` rather than written in as confirmed |
| T-01-02 | Tampering | STACK.md → BUILD-NOTES.md transcription | high | mitigate | The action requires re-running each lookup and recording divergence from STACK.md in the Evidence cell rather than copying its verdicts |
| T-01-03 | Repudiation | unrecorded gaps | high | mitigate | Every non-`VERIFIED` verdict, including `VERIFIED (identifier only)`, requires a DEV entry naming the substitute and the step that will resolve it |
| T-01-07 | Denial of service | silent runtime failure from a validator false-pass | high | mitigate | The `WFTextTokenString` display-parameter rule and the `WFMenuItemTitle` ordering rule are recorded in the durable build record, not only in research notes, so later executors meet them at authoring time |
| T-01-04 | Information disclosure | this phase | low | accept | No network access, no user data, no secrets; local read-only files in, local documents out |
| T-01-SC | Tampering (supply chain) | package-manager installs | low | accept | No package manifest exists in this project and this plan runs no `npm`, `pip`, or `cargo` install |
</threat_model>

<verification>
- Section 4 of `docs/BUILD-NOTES.md` holds at least 26 judged rows.
- Every row added by this plan cites evidence traceable through the section 3 recipe.
- Every non-`VERIFIED` row has a numbered DEV entry.
- No plist XML file, package manifest, lockfile, or test-runner configuration was created.
</verification>

<success_criteria>
- A Phase 2 executor can author the router, bootstrap, and Control Room actions using only identifiers and parameter shapes recorded in this table.
- Every place the table is weaker than "identifier and parameter shape both evidenced" is visible as a DEV entry with a named resolution step, rather than being discovered on a real iPhone.
</success_criteria>

<output>
Create `.planning/phases/01-capability-audit-config-foundation/01-02-SUMMARY.md` when done.
</output>
