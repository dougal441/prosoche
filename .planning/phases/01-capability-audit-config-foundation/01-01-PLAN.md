---
phase: 01-capability-audit-config-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/BUILD-NOTES.md
  - docs/CAPABILITY-DECISIONS.md
  - src/CONFIG-BLOCK.md
autonomous: true
requirements: [AUDIT-01, AUDIT-02, AUDIT-07]
user_setup: []

must_haves:
  truths:
    - "A reader can open docs/BUILD-NOTES.md and see the exact table schema every later capability row must follow, plus at least one fully populated row."
    - "The Color Filters / grayscale question is answered with a verdict backed by a named evidence file, not an opinion."
    - "The Ash primitive has a recorded go/no-go decision with rationale in docs/CAPABILITY-DECISIONS.md."
    - "A deviation forced by an unverifiable action is recorded in a numbered deviation log that states what was wanted, what was verified, and what was substituted."
    - "src/CONFIG-BLOCK.md exists and carries the three nine-entry sequence orderings verbatim from canonical strategy 12."
  artifacts:
    - docs/BUILD-NOTES.md
    - docs/CAPABILITY-DECISIONS.md
    - src/CONFIG-BLOCK.md
  key_links:
    - "CAP-20 row Verdict cell -> DEV-01 deviation entry -> BD-01 decision -> the Ash entry in the CONFIG-BLOCK sequences table (one capability traced end to end through every artifact this phase produces)."
    - "Evidence protocol section -> every later CAP row's Evidence cell (later plans reuse this recipe rather than inventing their own)."
---

## Phase goal (verbatim from ROADMAP.md)

> Every iOS action PROSOCHĒ depends on is resolved to VERIFIED / UNVERIFIED / NOT AVAILABLE with its exact identifier and parameter shape before any behavioural logic is authored, and the tunable static config exists as a single editable block.

Note: this phase's ROADMAP Goal line is stated as an outcome, not in `As a / I want to / so that` user-story form. No user story has been invented for it. Run `/gsd mvp-phase 1` if a user-story framing is wanted.

## Artifacts this phase produces

| Path | Produced by | Purpose |
|------|-------------|---------|
| `docs/BUILD-NOTES.md` | 01-01 (created), 01-02, 01-04, 01-05 | Capability audit table, deviation log, user action items, coverage check |
| `docs/CAPABILITY-DECISIONS.md` | 01-01 (created), 01-04, 01-05 | The five blocker decisions BD-01..BD-05 |
| `src/CONFIG-BLOCK.md` | 01-01 (created), 01-03, 01-05 | The single editable tuning block, transcribable into a Shortcuts dictionary in Phase 2 |

No plist XML, no package manifest, no test harness, and no application scaffold is created in this phase.

## Decision legend (source: `.planning/phases/01-capability-audit-config-foundation/01-CONTEXT.md`)

D-NN below index the locked decisions in CONTEXT.md, in the order they appear there.

- **D-01** — Target iOS 26.x, native Shortcuts only. No companion app, no Screen Time blocking APIs, no private APIs.
- **D-07** — The "do not fabricate" protocol (canonical strategy 31): when an action cannot be verified, use the safest fallback, record the deviation in `docs/BUILD-NOTES.md` with what was wanted / what was verified / what was substituted, and keep the Shortcut runnable. UNVERIFIED is a correct research outcome; an invented action identifier is a defect.
- **D-08** — Blocker 1, Grayscale / Color Filters: decide the Ash primitive's fate among substituting a different visual-salience reduction, requiring user opt-in to a PROSOCHĒ-managed configuration, or degrading Ash to a non-environmental variant. Record the decision and its rationale.
- **D-13** — Claude's discretion: file layout, the exact shape of the build-notes table, and how the config block is expressed. Keep both human-readable and directly transcribable into plist.

<objective>
Establish the durable evidence spine of Phase 1 by driving ONE capability — Color Filters / grayscale (the Ash primitive, per D-08) — end to end through every artifact this phase produces: a real ToolKit lookup, a populated audit-table row, a numbered deviation-log entry, a recorded blocker decision, and the corresponding entry in the config block's sequence table.

This is the tracer slice. It is production-quality, not a draft: the table schema, verdict vocabulary, evidence-citation rule, deviation-entry shape, and decision-record shape it lays down are the exact ones plans 01-02 through 01-05 fill out. Nothing here gets rewritten later; it only gets extended.

Purpose: prove the whole documentation pipeline on the single hardest capability (the one confirmed absent) before 35 more rows are written against a schema nobody has exercised. Per D-07, recording NOT AVAILABLE is the success path here, not a failure.

Output: `docs/BUILD-NOTES.md`, `docs/CAPABILITY-DECISIONS.md`, `src/CONFIG-BLOCK.md`, each created with the tracer content described below.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-capability-audit-config-foundation/01-CONTEXT.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Ash / Color Filters end to end — audit row, deviation, decision, config entry</name>

  <precondition>The Shortcuts Playground ToolKit data directory exists at `/Users/dougalhanson/.claude/plugins/cache/shortcuts-playground/shortcuts-playground/1.2.1/skills/shortcuts-playground/data/` and contains `toolkit-v63-tool-ids.json`, `toolkit-v78-tool-ids.json`, `toolkit-v78-ios27-tool-ids.json`, `toolkit-v78-first-party-parameter-keys.json`, and `toolkit-v78-first-party-enum-cases.json`. Halt and report if absent — every verdict in this phase depends on these files.</precondition>

  <files>docs/BUILD-NOTES.md, docs/CAPABILITY-DECISIONS.md, src/CONFIG-BLOCK.md</files>

  <read_first>
    - `.planning/phases/01-capability-audit-config-foundation/01-CONTEXT.md` — the locked decisions, in full
    - `.planning/research/STACK.md` section 3 row 10 (Color Filters) and the section 1 note on the iOS-27-simulator snapshot gap
    - `.planning/research/PITFALLS.md` C3 (Color Filters has no verified action) and C4 (accessibility clobbering)
    - `PROSOCHE_Nine_Circles_Canonical_Strategy.md` section 11 Primitive B (Ash), section 12 (the three sequences), section 21 Grayscale / Color Filters, section 31 (capability audit + do-not-fabricate)
    - Optionally load `Skill(skill="shortcuts-playground:shortcuts-playground")` for authoritative identifier knowledge
  </read_first>

  <action>
Create the three artifact files, each carrying only the Ash slice plus the structure later plans extend.

**A. `docs/BUILD-NOTES.md`** — create with these sections in this order, using these exact heading texts so later plans can locate them: `# PROSOCHĒ — Build Notes`, `## 1. Purpose and scope`, `## 2. Do-not-fabricate protocol`, `## 3. Evidence protocol`, `## 4. Capability audit table`, `## 5. Deviation log`, `## 6. User action items`, `## 7. Coverage check`.

In this task populate sections 1, 4 and 5, and create sections 2, 3, 6 and 7 as headings with a one-line note naming their owner: sections 2 and 3 are completed by task 2 of this plan; section 6 (`User action items`) is appended to by plans 01-04 and 01-05 and uses entries numbered `UA-01`, `UA-02`, ... each with the labelled fields `What`, `Why only a human can do it`, `Exact steps`, `What to record on completion`, `Which phase is gated`; section 7 (`Coverage check`) is finalised by plan 01-05.

Section 1 states: this document is the durable capability record for the PROSOCHĒ build; every later phase treats it as ground truth; it is appended to, never rewritten; it covers iOS 26.x native Shortcuts only (per D-01).

Section 4 opens with the verdict vocabulary, which is closed — exactly four values, no others: `VERIFIED` (identifier and parameter shape both evidenced), `VERIFIED (identifier only)` (identifier evidenced, parameter shape not evidenced anywhere in the bundle), `UNVERIFIED` (identifier exists but no schema evidence, or only non-Playground external evidence), `NOT AVAILABLE` (no matching action found in any bundled snapshot). Then the table, with exactly these seven columns in this order: `ID`, `Capability`, `Identifier`, `Parameter shape`, `Verdict`, `Evidence`, `Fallback`. State the row-format rule: every row begins `| CAP-` at column 1; the Verdict cell contains exactly one of the four vocabulary values and nothing else; the Evidence cell names a real file path (and JSON key path or doc line reference) that a reader can re-check; the Fallback cell is `n/a` only when the Verdict is `VERIFIED`, and otherwise names a concrete substitute behaviour.

Then write the single row `CAP-20`, Capability `Color Filters / grayscale (Ash)`. Populate it from a real lookup you run now, not from memory. Query all three identifier snapshots (`toolkit-v63-tool-ids.json`, `toolkit-v78-tool-ids.json`, `toolkit-v78-ios27-tool-ids.json` — each a JSON object whose `ids` key holds a flat array of identifier strings) for case-insensitive substrings `colorfilter`, `grayscale`, `greyscale`, and `UniversalAccess`. Query `toolkit-v78-first-party-parameter-keys.json` (object with a `tools` key mapping identifier to a record carrying `displayName`, `parameters`, `platforms`) for the identifier `com.apple.UniversalAccess.UASettingsShortcuts.UAToggleColorFiltersIntent`. Record verbatim in the Evidence cell which snapshots contain it and which do not, and the `platforms` array value from the parameter-keys record. The research base reports it is present in the generic v63 and v78 snapshots, tagged `macOS 27` only in the parameter catalog, and absent from the iOS-27-simulator snapshot — confirm each of those three facts yourself and record whatever you actually find, including if it differs from the research base.

Set the Verdict to whatever the evidence supports. Per D-07, `NOT AVAILABLE` is the expected and correct outcome for iOS and is a success, not a failure. Do not soften the verdict to preserve a feature. Do not invent an identifier for a grayscale toggle. Note in the Fallback cell the decision reference `see BD-01`.

Section 5, the deviation log, uses numbered entries `DEV-01`, `DEV-02`, ... Each entry has exactly these five labelled fields: `Capability`, `Wanted`, `Verified`, `Substituted`, `Runnability`. Write `DEV-01` for CAP-20: Wanted = the Ash primitive as literally specified in canonical strategy section 11 Primitive B, a grayscale toggle iOS can apply and restore safely; Verified = whatever your lookup actually established; Substituted = the option selected in BD-01; Runnability = an explicit statement that the Shortcut remains runnable because the Ash sequence slot still resolves to a defined, safe behaviour and no OPEN path depends on a grayscale action existing (per D-07 point 4).

**B. `docs/CAPABILITY-DECISIONS.md`** — create with `# PROSOCHĒ — Capability Decisions` and an intro stating that decisions are numbered `BD-NN` (blocker decision), are binding on later phases, and that BD-01..BD-05 correspond to the five blockers named in `01-CONTEXT.md`. List all five BD headings up front as a table of contents with their owning plan, so later plans know where to write: BD-01 Ash / Color Filters (this plan), BD-02 Dimming / brightness read-back (01-04), BD-03 Silence / volume read-back (01-04), BD-04 Use Model On-Device literal (01-05), BD-05 Notes actions on the iOS target (01-04).

Write `## BD-01 — Ash / Color Filters` in full, with exactly these labelled fields: `Question`, `Evidence`, `Options considered`, `Decision`, `Rationale`, `Consequence for later phases`, `Requirement`. The `Options considered` field must list all three options D-08 names verbatim — substitute a different visual-salience reduction, require user opt-in to a PROSOCHĒ-managed configuration, degrade Ash to a non-environmental variant — and say why each was or was not chosen. The `Decision` field selects exactly one of those three; you have the discretion to choose, but you must state the rationale against canonical strategy section 21's absolute rule (if Shortcuts cannot detect and restore the original condition safely, either skip dynamic grayscale or require opt-in) and against PITFALLS C4 (a pre-existing accessibility configuration must never be blindly overridden). `Consequence for later phases` must name Phase 5 explicitly and state what CIRC-02 will be built to do. `Requirement` = `AUDIT-02`.

**C. `src/CONFIG-BLOCK.md`** — create with `# PROSOCHĒ — Config Block` and these section headings: `## How to use this file`, `## Config JSON literal`, `## Field reference`, `## Derived-value rules`, `## Change log`. In this task populate `## How to use this file` and put ONLY the `sequences` object into the `## Config JSON literal` section as a fenced JSON block (plan 01-03 fills in the remaining keys around it); leave `## Field reference`, `## Derived-value rules` and `## Change log` as headings with a one-line note naming plan 01-03 as their owner.

`## How to use this file` states: this literal is product configuration, not user data; it is never written to `state.json`; it is transcribed into a single Shortcuts `Text` action (`is.workflow.actions.gettext`) and parsed once per run via `Detect Dictionary` (`is.workflow.actions.detect.dictionary`) into a `Config` variable; it is authored once and copied verbatim into both the Dumb and Sentient forks, never hand-diverged.

The `sequences` object has exactly three keys, each a nine-entry array, transcribed verbatim from canonical strategy section 12 with no reordering and no omission: `Classic` = Knock, Ash, Silence, Confession, Dimming, Exile, Mirror, Voice, Ice. `BlackMirror` = Knock, Confession, Ash+Confession, Mirror, Silence+Mirror, Dimming+Mirror, Exile, Voice, Ice. `Ambient` = Ash, Silence, Dimming, Knock, Confession, Exile, Mirror, Voice, Ice.

Critical and binding: BD-01 redefines what the Ash primitive *does*, it does not delete Ash from the sequence orderings. All three arrays keep their nine entries and keep `Ash` and the combined `Ash+Confession` entry exactly where canonical section 12 puts them. Add a note immediately under the JSON block stating this explicitly and pointing to BD-01 for Ash's implementation. This keeps the sequence table the single source of truth for which primitives fire at each Circle and localises the grayscale deviation to one decision record.
  </action>

  <verify>
    <automated>test -f docs/BUILD-NOTES.md && test -f docs/CAPABILITY-DECISIONS.md && test -f src/CONFIG-BLOCK.md && grep -q '^| CAP-20 |' docs/BUILD-NOTES.md && grep -qE '^\| CAP-20 \|.*\| (VERIFIED|UNVERIFIED|NOT AVAILABLE)' docs/BUILD-NOTES.md && grep -q 'UAToggleColorFiltersIntent' docs/BUILD-NOTES.md && grep -q 'toolkit-v78-ios27-tool-ids.json' docs/BUILD-NOTES.md && grep -q 'DEV-01' docs/BUILD-NOTES.md && grep -q 'Runnability' docs/BUILD-NOTES.md && grep -q 'BD-01' docs/CAPABILITY-DECISIONS.md && grep -q 'AUDIT-02' docs/CAPABILITY-DECISIONS.md && grep -q 'BlackMirror' src/CONFIG-BLOCK.md && grep -q 'Ash+Confession' src/CONFIG-BLOCK.md && grep -q 'Detect Dictionary' src/CONFIG-BLOCK.md && echo TRACER-OK</automated>
  </verify>

  <acceptance_criteria>
    - `docs/BUILD-NOTES.md` exists and contains the seven headings `## 1. Purpose and scope`, `## 2. Do-not-fabricate protocol`, `## 3. Evidence protocol`, `## 4. Capability audit table`, `## 5. Deviation log`, `## 6. User action items`, `## 7. Coverage check`.
    - Section 4 declares exactly four verdict values and no others: `VERIFIED`, `VERIFIED (identifier only)`, `UNVERIFIED`, `NOT AVAILABLE`.
    - The audit table header row names exactly seven columns in order: `ID`, `Capability`, `Identifier`, `Parameter shape`, `Verdict`, `Evidence`, `Fallback`.
    - Exactly one data row exists and it begins `| CAP-20 |`; its Verdict cell holds one of the four vocabulary values; its Evidence cell names at least two of the five ToolKit JSON files by filename; its Fallback cell references `BD-01`.
    - `com.apple.UniversalAccess.UASettingsShortcuts.UAToggleColorFiltersIntent` appears in `docs/BUILD-NOTES.md` and no other grayscale or Color Filters identifier appears anywhere in the file.
    - `DEV-01` exists in section 5 with all five labelled fields `Capability`, `Wanted`, `Verified`, `Substituted`, `Runnability`.
    - `docs/CAPABILITY-DECISIONS.md` lists BD-01..BD-05 in its table of contents and contains a complete `## BD-01 — Ash / Color Filters` section with all seven labelled fields, three options considered, a single selected Decision, and `Requirement: AUDIT-02`.
    - `src/CONFIG-BLOCK.md` contains a `sequences` JSON object with exactly three keys `Classic`, `BlackMirror`, `Ambient`, each holding exactly nine entries, and `Ash` appears in all three orderings.
    - `src/CONFIG-BLOCK.md` contains the note that BD-01 redefines Ash's implementation rather than removing it from the sequence orderings.
  </acceptance_criteria>

  <reversibility rating="costly">BD-01 sets what CIRC-02 is built to do in Phase 5 and is referenced by the sequence table; reversing it after Phase 5 means rebuilding a primitive, but it is a document change and no shipped artifact depends on it yet.</reversibility>

  <done>One capability is traced end to end through all three Phase 1 artifacts, with a real evidence-backed verdict, a numbered deviation, a recorded decision, and its config-table entry.</done>
</task>

<task type="auto">
  <name>Task 2: Do-not-fabricate protocol and reproducible evidence protocol</name>

  <files>docs/BUILD-NOTES.md</files>

  <read_first>
    - `docs/BUILD-NOTES.md` as written by task 1
    - `.planning/research/PITFALLS.md` — the standalone "Do not fabricate protocol" block near the end of the file
    - `PROSOCHE_Nine_Circles_Canonical_Strategy.md` section 31, the capability-audit and do-not-fabricate paragraphs
    - `.planning/research/STACK.md` sections 1 and 2 — the bundled data-file inventory and the validator target-flag rationale
  </read_first>

  <action>
Fill in sections 2 and 3 of `docs/BUILD-NOTES.md`.

**Section 2, `## 2. Do-not-fabricate protocol`** — reproduce the protocol as a blockquote so it is quotable by every later phase, sourced from canonical strategy section 31 and the standalone block in `.planning/research/PITFALLS.md`, and stated as binding (per D-07). It must carry all four numbered points: do not invent the identifier or parameter shape; use the safest available fallback in the stated priority order (a verified weaker alternative, then skipping the behaviour entirely, and never a fallback that could strand the user in an unrecoverable state); record the deviation in this document; keep the Shortcut runnable. Add the one-line rule immediately after: a capability recorded as `UNVERIFIED` or `NOT AVAILABLE` is a correct research outcome and does not block a phase; an invented action identifier is a defect. State that the protocol applies with equal force to the Dumb and Sentient forks.

**Section 3, `## 3. Evidence protocol`** — document the exact reproducible lookup recipe every Evidence cell in section 4 must be traceable to, so a later reader can re-derive any verdict. Record:

The plugin root, verbatim: `/Users/dougalhanson/.claude/plugins/cache/shortcuts-playground/shortcuts-playground/1.2.1/`. The skill docs directory `skills/shortcuts-playground/` and the ToolKit data directory `skills/shortcuts-playground/data/`.

The five evidence sources and what each proves, with their real internal structure so a reader can query them: `toolkit-v63-tool-ids.json` — object with an `ids` key holding a flat array of 1794 identifier strings, the generic non-platform-segmented snapshot, treated as the operative iOS-availability signal for pre-existing actions. `toolkit-v78-tool-ids.json` — same shape, 2731 identifiers, macOS 27 additions. `toolkit-v78-ios27-tool-ids.json` — same shape, 1206 identifiers, from an iOS 27 Simulator ToolKit database; its *absence* of a row is inconclusive rather than disqualifying, because the simulator did not expose several long-standing first-party actions. `toolkit-v78-first-party-parameter-keys.json` — object with a `tools` key mapping identifier string to a record carrying `displayName`, `parameterCount`, a `parameters` array of `{key, typePythonName, name, sortOrder, platforms}`, and a top-level `platforms` array; OS27-scoped, so a `macOS 27`-only tag is a provenance fact, not proof of macOS exclusivity. `toolkit-v78-first-party-enum-cases.json` — object with a `types` key mapping a lowercased enum type name to its case list; this is where a picker's literal enum values live, and a `typePythonName` from the parameter catalog that has no matching key here means the literal cannot be recovered from the local toolchain.

The three secondary sources: the prose reference docs under `skills/shortcuts-playground/` (`ACTIONS.md`, `APPINTENTS.md`, `PARAMETER_TYPES.md`, `CONTROL_FLOW.md`, `VARIABLES.md`, `DATE_TIME.md`, `BEST_PRACTICES.md`, `EXAMPLES.md`), cited by filename and section; the 19 golden shortcut XMLs under `skills/shortcuts-playground/golden-shortcuts/xml/`, cited by filename, which are real-world ground truth for wiring and literal values; and clearly-labelled external corroboration, which may be recorded but may never on its own raise a verdict above `UNVERIFIED`.

Include a short runnable lookup snippet (a `python3` one-liner or heredoc reading the `ids` arrays and the `tools` / `types` objects and printing case-insensitive substring matches) so later plans and later phases re-run the identical query rather than improvising. Include a worked example showing the CAP-20 lookup from task 1 and the Evidence cell it produced.

State the evidence-citation rule as binding: an Evidence cell that does not name at least one of the five ToolKit JSON files, one of the prose reference docs, or one golden-shortcut XML filename is not acceptable, and a verdict resting only on external corroboration is recorded as `UNVERIFIED`.

Also record the project's validator and signer invocations for later phases, since this document is where the build facts live: validate with `validate-shortcut <file.xml> --target-macos 26 --target-platform ios`, and sign with `sign-shortcut <file.xml> --name "<name>"`. State why the target is 26 and not 27, and state that a validator pass is necessary but not sufficient because the validator cannot execute a Shortcut.
  </action>

  <verify>
    <automated>grep -q '## 2. Do-not-fabricate protocol' docs/BUILD-NOTES.md && grep -q '## 3. Evidence protocol' docs/BUILD-NOTES.md && for f in toolkit-v63-tool-ids.json toolkit-v78-tool-ids.json toolkit-v78-ios27-tool-ids.json toolkit-v78-first-party-parameter-keys.json toolkit-v78-first-party-enum-cases.json; do grep -q "$f" docs/BUILD-NOTES.md || { echo "MISSING EVIDENCE SOURCE $f"; exit 1; }; done && grep -q 'golden-shortcuts' docs/BUILD-NOTES.md && grep -q -- '--target-macos 26 --target-platform ios' docs/BUILD-NOTES.md && grep -q 'sign-shortcut' docs/BUILD-NOTES.md && echo EVIDENCE-PROTOCOL-OK</automated>
  </verify>

  <acceptance_criteria>
    - Section 2 contains a blockquote carrying all four numbered do-not-fabricate points and the explicit statement that `UNVERIFIED` and `NOT AVAILABLE` are correct outcomes while an invented identifier is a defect.
    - Section 2 states the protocol binds both the Dumb and Sentient forks.
    - Section 3 names all five ToolKit JSON filenames and describes the internal key (`ids`, `tools`, `types`) used to query each.
    - Section 3 records the plugin root path `/Users/dougalhanson/.claude/plugins/cache/shortcuts-playground/shortcuts-playground/1.2.1/`.
    - Section 3 states that absence from `toolkit-v78-ios27-tool-ids.json` is inconclusive rather than disqualifying.
    - Section 3 contains a runnable lookup snippet and a worked example reproducing the CAP-20 Evidence cell.
    - Section 3 states the binding citation rule and that external-only corroboration caps a verdict at `UNVERIFIED`.
    - Section 3 records the validator invocation string `--target-macos 26 --target-platform ios`, the `sign-shortcut` invocation, and the reason target 26 is used rather than 27.
  </acceptance_criteria>

  <done>Any later reader or executor can re-derive every verdict in this document from a named file and a stated query, and knows exactly what to do when a capability cannot be verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bundled ToolKit JSON snapshots → `docs/BUILD-NOTES.md` | Read-only local evidence is transcribed into a document every later phase treats as ground truth without re-verification |
| `docs/BUILD-NOTES.md` / `docs/CAPABILITY-DECISIONS.md` → Phase 2–8 executors | A wrong or unsourced fact here becomes a fabricated action identifier in signed, distributed `.shortcut` files |
| `src/CONFIG-BLOCK.md` → the Shortcuts action graph | Numeric values here later drive environmental changes (brightness, volume) on a real user's device |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Spoofing | CAP row Identifier cell | critical | mitigate | Closed four-value verdict vocabulary plus the binding citation rule in section 3: every Evidence cell names a real ToolKit JSON file, prose doc, or golden-shortcut XML; a plausible-sounding identifier with no citation is a defect, not a row |
| T-01-02 | Tampering | STACK.md → BUILD-NOTES.md transcription | high | mitigate | Task 1 re-runs the ToolKit lookup itself and records what it actually finds, including divergence from the research base, rather than copying STACK.md verdicts unchecked |
| T-01-03 | Repudiation | Ash deviation | high | mitigate | `DEV-01` carries all five labelled fields including `Runnability`, so Phase 5 can see exactly what was wanted, what was verified, and what was substituted |
| T-01-06 | Elevation of privilege | `sequences` config entries | medium | mitigate | BD-01 redefines Ash's implementation rather than deleting the sequence entry, so no Circle silently loses its intervention and no accessibility setting is changed without a recorded decision |
| T-01-04 | Information disclosure | this phase | low | accept | No network access, no user data, no secrets, no telemetry — the phase reads local read-only files and writes local documents |
| T-01-SC | Tampering (supply chain) | package-manager installs | low | accept | This project has no package manifest and this phase runs no `npm`, `pip`, or `cargo` install; the only external inputs are read-only JSON snapshots shipped inside an already-installed Claude Code plugin |
</threat_model>

<verification>
- `docs/BUILD-NOTES.md`, `docs/CAPABILITY-DECISIONS.md`, and `src/CONFIG-BLOCK.md` all exist at the repository root.
- The CAP-20 verdict is backed by a lookup the executor actually ran, and the Evidence cell names the files queried.
- The four-value verdict vocabulary, the seven-column table schema, the five-field deviation entry shape, and the seven-field decision shape are all declared and demonstrated once.
- No plist XML file, package manifest, lockfile, or test-runner configuration was created.
</verification>

<success_criteria>
- A capability confirmed absent from the platform is recorded as such, with evidence, without any invented identifier appearing anywhere in the repository.
- The Ash primitive has a single selected decision among the three options D-08 names, with rationale tied to canonical strategy section 21.
- Plans 01-02 through 01-05 can extend all three files without needing to invent any new structure.
</success_criteria>

<output>
Create `.planning/phases/01-capability-audit-config-foundation/01-01-SUMMARY.md` when done.
</output>
