---
phase: 04-close-pipeline-session-race
plan: 03
type: execute
wave: 2
depends_on: ["04-02"]
files_modified:
  - tools/build_state_engine.py
  - src/PROSOCHE-Dumb.xml
  - "artifacts/shortcuts/PROSOCHĒ — Nine Circles — Dumb.shortcut"
  - artifacts/shortcuts/MANIFEST.md
  - .planning/todos/pending/2026-08-15-ship-readiness-cleanup.md
autonomous: true
requirements: [SESS-02, SESS-03, SESS-04, SESS-05, SESS-06, SESS-07]
gap_closure: true
gap_ids: [G-04-4b]

must_haves:
  truths:
    - "A user can tell that OPEN completed, and which Circle/Pressure/Heat resulted, from an unconditional on-device Notification alone -- no breadcrumb inspection required."
    - "A user can tell that CLOSE completed, and the recorded session duration, from an unconditional on-device Notification alone -- independent of whether a contract was declared."
    - "The Leaving/Continue menu states which Circle is active, so it can no longer be mistaken for a CLOSE-path signal."
    - "The ship-readiness-cleanup todo records that a permanent replacement signal now exists before its breadcrumb-removal step is next executed."
  artifacts:
    - "tools/build_state_engine.py -- new notification() helper; STRING_ENVELOPE_PARAMS carries its two display parameters; open_pipeline(), close_pipeline(), and universal_leaving() are updated."
    - "src/PROSOCHE-Dumb.xml -- regenerated artifact carries both unconditional Notification calls and the improved menu prompt."
    - "artifacts/shortcuts/PROSOCHĒ — Nine Circles — Dumb.shortcut -- freshly signed, non-empty."
    - ".planning/todos/pending/2026-08-15-ship-readiness-cleanup.md -- Solution step 1 records the replacement signal as an already-delivered prerequisite."
  key_links:
    - "open_pipeline() breadcrumb J position -> notification(\"PROSOCHĒ\", Circle/Pressure/Heat) -> fires unconditionally, independent of OPEN_BISECT."
    - "close_pipeline() owns_if TRUE branch -> notification(\"PROSOCHĒ\", Session Duration) -> fires unconditionally, independent of whether Declared Duration > 0."
    - "universal_leaving() menu prompt -> text_token naming \"Circle Next\" -> disambiguates the OPEN-only dismissal menu from a CLOSE event."
---

<objective>
Give OPEN and CLOSE each a permanent, unconditional, non-blocking on-device confirmation
signal, and give the OPEN-only "Leaving / Continue" dismissal menu real explanatory copy --
so a user can confirm PROSOCHĒ actually fired, and tell OPEN apart from CLOSE, without
inspecting debug breadcrumbs.

Purpose: G-04-4b (.planning/debug/G-04-4b-no-open-close-indicator.md) found that once the
pending ship-readiness-cleanup todo strips OPEN_BISECT breadcrumbs as scoped, OPEN keeps a
confirmation signal for only 2 of 9 primitives (Knock, Mirror/Voice) and CLOSE has never had
any signal at all outside a rarely-true declared-contract-overrun alert -- and that
universal_leaving()'s bare "PROSOCHĒ" menu prompt carries no copy explaining what it is,
which is why the user testing UAT test 3 mistook an OPEN-only menu for a CLOSE-path signal.
This blocked UAT tests 4, 5, and 6 outright. This plan does not touch OPEN_BISECT or execute
any part of the ship-readiness-cleanup todo itself -- it only adds the permanent replacement
signal that todo's Solution section is missing, so that cleanup becomes safe to run
afterward, and records that fact in the todo file itself.

Output: A corrected `tools/build_state_engine.py`, a regenerated and re-signed
`PROSOCHĒ — Nine Circles — Dumb.shortcut` carrying both Notification calls and the improved
menu copy, and an updated ship-readiness-cleanup todo. On-device confirmation that the
Notifications and menu copy actually appear as intended happens in a later /gsd-verify-work
pass, not in this plan.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/debug/G-04-4b-no-open-close-indicator.md
@.planning/todos/pending/2026-08-15-ship-readiness-cleanup.md
@tools/build_state_engine.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add unconditional Notification confirmations for OPEN and CLOSE, and give the Leaving menu real copy</name>
  <files>tools/build_state_engine.py</files>
  <precondition>Plan 04-02's fixes are present in tools/build_state_engine.py (close_pipeline()'s ownership conditional already resolves to token("Captured Session ID")) -- this plan edits the same functions and assumes that wiring is already correct.</precondition>
  <action>
Add a new notification(title, body) helper immediately after the existing alert(title,
message) helper, following the exact same shape: it must call the
"is.workflow.actions.notification" action with WFNotificationActionTitle=title and
WFNotificationActionBody=body -- both parameters already VERIFIED usable on this project's
own iOS 26 target per docs/BUILD-NOTES.md's CAP-S07 entry. Do not set
WFNotificationActionSound or WFInput; leave them unset, matching alert()'s own minimal-params
style and this project's "no unsafe or startling" default posture. Add
"is.workflow.actions.notification": {"WFNotificationActionTitle", "WFNotificationActionBody"}
to STRING_ENVELOPE_PARAMS, matching the existing "is.workflow.actions.alert" entry, so any
bare attachment ever passed to these two display parameters gets the same automatic
WFTextTokenString safety net alert()'s parameters already have.

In open_pipeline(), immediately after the call to breadcrumb("J") (the last breadcrumb,
which currently sits right before save_state() and universal_leaving()), add one
unconditional call to the new notification() helper -- title "PROSOCHĒ", body built via
text_token() naming "Circle Next", "Pressure Next", and "Heat Final" in the identical
"Circle X · pressure Y · heat Z" phrasing knock() already uses (reuse that exact wording
for consistency). This call must NOT be gated by OPEN_BISECT or any other conditional --
it must fire on every genuine OPEN regardless of the breadcrumb toggle's state, since it is
the permanent replacement for breadcrumb J's de facto confirmation role, not another
debug-only signal.

In close_pipeline(), inside the owns_if TRUE branch (the only branch that ever completes a
CLOSE), add one unconditional call to the new notification() helper -- title "PROSOCHĒ", body
built via text_token() naming "Session Duration" in seconds (e.g. "Session closed ·
&lt;duration&gt; sec"). Place it after restore_managed_settings() and before the final
save_state("Reloaded State") call, so it fires whether or not a contract was declared --
unlike the existing conditional "Contract" alert, which only fires when Declared Duration is
greater than 0. Do not gate this call on Declared Duration or any other condition.

In universal_leaving(), change the menu's prompt from the bare string "PROSOCHĒ" to a
text_token() value naming "Circle Next" that states the Circle is open and that Leaving is
available (for example: "Circle &lt;N&gt; opened. Leave now, or continue?"). This directly
answers the two things the debug session found missing -- which Circle is active, and that
this menu belongs to the OPEN path -- so it can no longer be mistaken for a CLOSE-path signal.
Do not change the menu's items ("Leaving", "Continue") or its control flow.

Regenerate the artifact after these changes.
  </action>
  <verify>
    <automated>python3 tools/build_state_engine.py && python3 docs/state_engine_self_check.py && python3 -c "
import plistlib
actions = plistlib.loads(open('src/PROSOCHE-Dumb.xml','rb').read())['WFWorkflowActions']
notif = [a for a in actions if a.get('WFWorkflowActionIdentifier') == 'is.workflow.actions.notification']
assert len(notif) == 2, ('expected exactly 2 unconditional Notification calls (OPEN + CLOSE)', len(notif))
comments = [a.get('WFWorkflowActionParameters', {}).get('WFCommentActionText', '') for a in actions]
leaving = next(i for i, a in enumerate(actions) if a.get('WFWorkflowActionIdentifier') == 'is.workflow.actions.choosefrommenu' and a.get('WFWorkflowActionParameters', {}).get('WFControlFlowMode') == 0 and isinstance(a.get('WFWorkflowActionParameters', {}).get('WFMenuPrompt'), dict))
prompt = actions[leaving]['WFWorkflowActionParameters']['WFMenuPrompt']
assert 'Circle Next' in str(prompt), prompt
print('NOTIFICATIONS-AND-MENU-COPY-OK')
"
    </automated>
  </verify>
  <done>Exactly two unconditional is.workflow.actions.notification calls exist in the generated artifact (one reachable from every genuine OPEN, one reachable from every owning CLOSE); universal_leaving()'s menu prompt is no longer the bare string "PROSOCHĒ" and names the active Circle; python3 tools/build_state_engine.py and python3 docs/state_engine_self_check.py both exit 0.</done>
</task>

<task type="auto">
  <name>Task 2: Record the replacement signal in the ship-readiness-cleanup todo, then validate, sign, and re-verify</name>
  <files>.planning/todos/pending/2026-08-15-ship-readiness-cleanup.md, src/PROSOCHE-Dumb.xml, "artifacts/shortcuts/PROSOCHĒ — Nine Circles — Dumb.shortcut", artifacts/shortcuts/MANIFEST.md</files>
  <action>
Edit .planning/todos/pending/2026-08-15-ship-readiness-cleanup.md's "## Solution" section,
step 1 (the breadcrumb-strip step), adding a note that this precondition is now satisfied:
OPEN and CLOSE each carry a permanent, unconditional Notification confirmation
(tools/build_state_engine.py's notification() calls in open_pipeline() and close_pipeline(),
delivered by .planning/phases/04-close-pipeline-session-race/04-03-PLAN.md), so stripping
BUILD_STAMP/ROUTER_TRACE/OPEN_BISECT/the breadcrumb alerts no longer removes the only
on-device confirmation signal. Do not otherwise edit the todo's scope, do not strip
OPEN_BISECT or the breadcrumb alerts yourself, and do not execute any of the todo's other
four items -- this task only records that the missing prerequisite this todo's own Solution
section lacked has now been delivered.

Then run the project's operative validator invocation against the regenerated
src/PROSOCHE-Dumb.xml (--target-macos 26 --target-platform all, per docs/BUILD-NOTES.md's
recorded deviation), sign the validated XML under artifacts/shortcuts/ using this project's
established signer invocation and exact display name
"PROSOCHĒ — Nine Circles — Dumb", and confirm the signed output is non-empty. Update only the
three Dumb-fork rows of artifacts/shortcuts/MANIFEST.md with the freshly measured byte counts
and SHA-256 hashes; leave the Sentient rows and the broader MANIFEST staleness backfill
(already scoped to the ship-readiness-cleanup todo) untouched.

Do not perform on-device import or manual UAT here -- that happens in a later
/gsd-verify-work pass against .planning/phases/04-close-pipeline-session-race/04-UAT.md
tests 4, 5, and 6.
  </action>
  <verify>
    <automated>grep -q "04-03-PLAN.md" .planning/todos/pending/2026-08-15-ship-readiness-cleanup.md && validate-shortcut src/PROSOCHE-Dumb.xml --target-macos 26 --target-platform all && sign-shortcut src/PROSOCHE-Dumb.xml --name "PROSOCHĒ — Nine Circles — Dumb" --output-dir artifacts/shortcuts && test -s "artifacts/shortcuts/PROSOCHĒ — Nine Circles — Dumb.shortcut" && echo TODO-AND-SIGN-OK</automated>
  </verify>
  <done>The ship-readiness-cleanup todo records the delivered replacement signal as a satisfied prerequisite; validate-shortcut reports zero errors at --target-macos 26 --target-platform all; a freshly signed, non-empty "PROSOCHĒ — Nine Circles — Dumb.shortcut" exists; MANIFEST.md's Dumb rows reflect the new byte counts and hashes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Notification Center <-> bystander | A lock-screen or Notification Center banner is visible to anyone with physical access to the device, not only the account owner -- a different exposure surface than the in-app UI the rest of PROSOCHĒ uses. |
| Shortcuts runtime <-> state.json | Unchanged by this plan; notification() is read-only display, it never writes state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-5 | Information Disclosure | OPEN/CLOSE Notification bodies (Circle/Pressure/Heat, session duration) | low | accept | Bodies name only abstract internal metrics ("Circle 3 · pressure 8 · heat 5", "Session closed · 240 sec") -- never the tracked app's name, the user's stated intention, or any Note/contract content. A bystander glancing at a lock-screen banner cannot infer which app was used or why. Notification visibility on the lock screen is otherwise governed by the device's own system-wide Settings > Notifications privacy controls, which this project does not override. |
| T-04-6 | Tampering | universal_leaving() menu prompt | low | accept | The prompt now embeds a variable (Circle Next) for the first time via text_token(), the established display-parameter envelope this file already uses correctly everywhere else (knock(), mirror_and_voice(), the Contract alert) -- no new serialization pattern is introduced, so this carries the same low residual risk (a stale/incorrect Circle value display, not a state-mutation risk) as those existing sites. |
| T-04-SC | Tampering | package-manager installs | n/a | accept | No npm/pip/cargo installs occur in this plan -- only the existing generator's Python stdlib usage and this project's own bundled Shortcuts Playground validator/signer binaries. |

</threat_model>

<verification>
- python3 tools/build_state_engine.py exits 0 after Task 1's edits.
- python3 docs/state_engine_self_check.py exits 0.
- Exactly two is.workflow.actions.notification calls exist in the generated artifact, reachable unconditionally from open_pipeline() and close_pipeline()'s owning branch respectively.
- universal_leaving()'s menu prompt is no longer the bare string "PROSOCHĒ" and names the Circle.
- validate-shortcut src/PROSOCHE-Dumb.xml --target-macos 26 --target-platform all reports zero errors.
- A freshly signed, non-empty artifacts/shortcuts/PROSOCHĒ — Nine Circles — Dumb.shortcut exists.
- .planning/todos/pending/2026-08-15-ship-readiness-cleanup.md records the delivered replacement signal.
- No on-device UAT is performed in this plan; UAT tests 4, 5, and 6 are re-run in a later /gsd-verify-work pass against the rebuilt artifact.
</verification>

<success_criteria>
- OPEN and CLOSE each fire an unconditional, non-blocking Notification confirming completion, independent of any primitive, breadcrumb toggle, or declared contract.
- The Leaving/Continue menu states the active Circle, disambiguating it from a CLOSE event.
- The ship-readiness-cleanup todo no longer implicitly assumes breadcrumb removal leaves no replacement signal.
- The rebuilt artifact validates and signs cleanly.
</success_criteria>

<output>
Create `.planning/phases/04-close-pipeline-session-race/04-03-SUMMARY.md` when done
</output>
