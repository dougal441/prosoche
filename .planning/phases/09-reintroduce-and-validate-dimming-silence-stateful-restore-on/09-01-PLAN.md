---
phase: 09-reintroduce-and-validate-dimming-silence-stateful-restore-on
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/build_state_engine.py
  - docs/phase9_self_check.py
  - src/PROSOCHE-Dumb.xml
  - src/PROSOCHE-Sentient.xml
  - .planning/ROADMAP.md
autonomous: true
requirements: [RESTORE-01]
must_haves:
  truths:
    - "All 28 setbrightness/setvolume sites (not the stale 18) are covered by verify_numeric_operands() — the build guard no longer silently exempts them (ROADMAP criterion 1)"
    - "Of the 28, exactly 18 carry WFCoercionVariableAggrandizement/WFNumberContentItem (Restore Brightness x4 + Restore Volume x4 + Dim Target x10, all Text-sourced via read_value()/gettext) and exactly 10 correctly remain uncoerced (Silence Target, Number-sourced via number(), verified to have no stray Text-typed redefinition anywhere in either fork)"
    - "Removing the two new NUMERIC_OPERAND_FIELDS entries reproduces the pre-fix exemption bug (verify_numeric_operands() silently passes an uncoerced Text-sourced brightness write); restoring them makes the same synthetic defect fail the build — proving the guard is load-bearing, not accidentally passing"
    - "Both forks regenerate cleanly (exit 0) through the full static suite (generator self-checks, phase self-checks, Playground validator at iOS 26/all-platform target) with the fix applied"
    - "No repository documentation still cites '18' as the current deferred-site count for setbrightness/setvolume (ROADMAP.md Goal narrative corrected; docs/BUILD-NOTES.md and docs/CAPABILITY-DECISIONS.md confirmed to have no stragglers; HANDOFF.md's historical '18' table is left untouched because it already carries its own 2026-08-16 correction note)"
  artifacts:
    - tools/build_state_engine.py (NUMERIC_OPERAND_FIELDS table, two new entries)
    - docs/phase9_self_check.py (new — negative-control + site-audit self-check)
    - src/PROSOCHE-Dumb.xml (regenerated, 18/28 sites coerced)
    - src/PROSOCHE-Sentient.xml (regenerated, 18/28 sites coerced)
  key_links:
    - "NUMERIC_OPERAND_FIELDS -> _numeric_operand_sites() -> _numeric_operand_report() -> normalise_numeric_operands()/verify_numeric_operands() -> every setbrightness/setvolume site in both forks (dimming(), silence(), restore_managed_settings(), and the Test-a-Circle 1-9 unroll)"
    - "docs/phase9_self_check.py's monkeypatch of NUMERIC_OPERAND_FIELDS -> the same verify_numeric_operands() function the real build calls -> proves the guard change is genuinely load-bearing, not coincidentally passing"
---

<objective>
Close the setbrightness/setvolume numeric-coercion gap in the Shortcuts plist generator: add the
two missing `NUMERIC_OPERAND_FIELDS` table entries, prove the fix is load-bearing with an automated
negative control, and correct the stale "18 deferred sites" framing to the confirmed "28" wherever
it still appears uncorrected. This plan is entirely device-free and fully verifiable with static
checks — it does NOT prove the coercion shape is correct on a real iPhone (that is Plan B's job,
`09-02-PLAN.md`, which depends on this plan).

No SPEC.md exists for this phase and no requirement IDs existed before this planning pass (ROADMAP
Phase 9 read "Requirements: TBD"); per the specless-probe fallback, no automated edge/prohibition
probes are generated this run. `must_haves.truths` above are lifted directly from ROADMAP.md Phase
9's success criterion 1 (the only criterion this plan can satisfy without a device) plus the
Wave-0 gaps `09-RESEARCH.md`/`09-VALIDATION.md` both name. A new requirement family, RESTORE-01
through RESTORE-07 (one per ROADMAP success criterion), was proposed during this planning pass and
recorded in ROADMAP.md's Phase 9 `**Requirements:**` line; this plan owns RESTORE-01 only.

Purpose: RESTORE-01 is the single, fully-static prerequisite every device-proving criterion in
Plan B depends on — Plan B cannot meaningfully test capture/restore safety on hardware if the
generator itself is still shipping unaudited numeric operands.
Output: `tools/build_state_engine.py` with the two-entry fix, `docs/phase9_self_check.py` as a
reusable regression guard, both forks regenerated with the fix applied, and every stale "18"
reference in ROADMAP.md/docs corrected or confirmed already-corrected.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-reintroduce-and-validate-dimming-silence-stateful-restore-on/09-RESEARCH.md
@.planning/phases/09-reintroduce-and-validate-dimming-silence-stateful-restore-on/09-PATTERNS.md
@tools/build_state_engine.py
</context>

<tasks>

<task type="tracer" tdd="false">
  <name>Task 1: Add the two NUMERIC_OPERAND_FIELDS entries and regenerate both forks end-to-end</name>
  <files>tools/build_state_engine.py, src/PROSOCHE-Dumb.xml, src/PROSOCHE-Sentient.xml</files>
  <read_first>
    tools/build_state_engine.py lines 2342-2365 (the `NUMERIC_OPERAND_FIELDS` table and its cycle-14
    docstring comment, showing the exact sibling-entry syntax to mirror); lines 2368-2528
    (`_numeric_operand_sites()`, `_operand_descriptor()`, `_numeric_operand_report()`,
    `normalise_numeric_operands()`, `verify_numeric_operands()` — confirms no other function needs
    editing); lines 433-440 (`set_brightness()`/`set_media_volume()`, the two producer functions the
    new entries cover); lines 2870-2886 (the unconditional `normalise_numeric_operands(actions)` /
    `verify_numeric_operands(actions)` call sites inside `main()`); `tools/build_sentient.py` lines
    1-30 (confirms it imports `normalise_numeric_operands`/`verify_numeric_operands` from
    `build_state_engine` and reads `src/PROSOCHE-Dumb.xml` as its own `SOURCE` — so Sentient must be
    regenerated in the same pass, not assumed to inherit the fix automatically); 09-RESEARCH.md
    section "The fix mechanism — table-driven, already proven for 4 sibling axes" and "Which of the
    28 sites actually need the coercion (not all, mechanically)"; 09-PATTERNS.md "The two-line
    generator fix" and "The two producer call sites the new table entries cover".
  </read_first>
  <action>
    In `tools/build_state_engine.py`, add exactly two entries to the `NUMERIC_OPERAND_FIELDS` dict
    (currently at line ~2362-2365, immediately after the existing
    `"is.workflow.actions.getitemfromlist": ("WFItemIndex",),` entry): `"is.workflow.actions.setbrightness": ("WFBrightness",)`
    and `"is.workflow.actions.setvolume": ("WFVolume",)`, matching the exact single-field tuple
    syntax the `getitemfromlist` entry already uses. Do not touch `_numeric_operand_sites()`,
    `_operand_descriptor()`, `_numeric_operand_report()`, `normalise_numeric_operands()`,
    `verify_numeric_operands()`, `set_brightness()`, or `set_media_volume()` — per D-none (this is a
    mechanical RESTORE-01 fix with no locked user decision to trace), the fix is purely additive at
    the table per 09-RESEARCH.md and 09-PATTERNS.md's independent confirmation that every downstream
    function reads the table generically. Regenerate the Dumb fork with `python3
    tools/build_state_engine.py`, then regenerate the Sentient fork with `python3
    tools/build_sentient.py` (required — Sentient is not regenerated automatically by the Dumb build;
    it reads Dumb's freshly-written XML as its own source). Note: the on-disk
    `src/PROSOCHE-Sentient.xml` may already differ from a fresh `build_sentient.py` run for reasons
    unrelated to this fix (pre-existing generator drift confirmed during planning — schema_version
    and `pending_exit` seeding were applied to Dumb's generator path but the checked-in Sentient.xml
    predates a `build_sentient.py` re-run); a large diff on Sentient.xml beyond the two new
    coercion aggrandizements is expected and not a sign this task did something wrong — verify via
    the acceptance criteria below, not via diff size.
  </action>
  <verify>
    <automated>python3 tools/build_state_engine.py && python3 tools/build_sentient.py && python3 docs/state_engine_self_check.py && python3 docs/phase5_self_check.py && python3 /Users/dougalhanson/.claude/plugins/cache/shortcuts-playground/shortcuts-playground/1.2.1/skills/shortcuts-playground/scripts/validate_shortcut.py src/PROSOCHE-Dumb.xml --target-macos 26 --target-platform all && python3 /Users/dougalhanson/.claude/plugins/cache/shortcuts-playground/shortcuts-playground/1.2.1/skills/shortcuts-playground/scripts/validate_shortcut.py src/PROSOCHE-Sentient.xml --target-macos 26 --target-platform all</automated>
  </verify>
  <acceptance_criteria>
    All five commands in `<verify>` exit 0 in sequence (a non-zero exit anywhere, especially from
    `verify_numeric_operands()` inside the generator run, means the table entries are malformed or a
    genuinely uncoerced numeric site was found — do not silence or skip past a `SystemExit`). `grep -n
    'is.workflow.actions.setbrightness\|is.workflow.actions.setvolume' tools/build_state_engine.py |
    grep NUMERIC_OPERAND_FIELDS -A2` (or a direct read of the table) confirms exactly two new lines,
    unchanged tuple syntax. `git diff --stat tools/build_state_engine.py` shows only the
    `NUMERIC_OPERAND_FIELDS` dict changed (2 insertions) — no other lines in this file differ.
  </acceptance_criteria>
  <done>
    Both forks regenerate and pass the full static suite with `verify_numeric_operands()` now
    covering all 28 `setbrightness`/`setvolume` sites; `set_brightness()`/`set_media_volume()` and
    every other numeric-operand code path are byte-identical to before except for the two table
    entries.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add the negative-control + site-audit self-check (Wave 0 gap)</name>
  <files>docs/phase9_self_check.py</files>
  <read_first>
    09-RESEARCH.md "Site-audit script used to establish the 28-site count (reusable for the plan's
    own verification)" (the direct plist-inspection script to adapt, not re-derive); 09-VALIDATION.md
    "Wave 0 Requirements" item 2 ("A negative-control check that verify_numeric_operands() actually
    fails when the two new NUMERIC_OPERAND_FIELDS entries are removed"); `docs/state_engine_self_check.py`
    in full (96 lines — the exact standalone-script, `assert`-based, `if __name__ == "__main__":
    main()` pattern this new file must follow, including its `structural_check()`'s plistlib-based
    direct XML inspection style); `tools/build_state_engine.py` lines 107-137 (`action()`, `output()`,
    `variable()`) and line 224 (`set_var()`) — the exact helper functions to import and reuse for
    building a synthetic action list, rather than hand-writing raw plist dicts; lines 2496-2527
    (`verify_numeric_operands()`, confirming it raises `SystemExit` on an offending site and is safe
    to call directly against a hand-built `actions` list, not only the full generated graph).
  </read_first>
  <action>
    Create `docs/phase9_self_check.py` as a standalone script (module docstring, no third-party
    imports, `if __name__ == "__main__": main()`) that adds `tools/` to `sys.path` and imports
    `build_state_engine` (module alias `bse`), then implements two functions called from `main()`:
    (1) `negative_control()` — build a synthetic 3-action list using `bse.action()`/`bse.set_var()`/
    `bse.output()`/`bse.variable()` that reproduces a Text-sourced (via a bare
    `is.workflow.actions.gettext` action carrying a `UUID` parameter, mirroring how `read_value()`
    sources `Dim Target`/`Restore Brightness`/`Restore Volume`) uncoerced `WFBrightness` write on
    `is.workflow.actions.setbrightness`; temporarily pop
    `"is.workflow.actions.setbrightness"`/`"is.workflow.actions.setvolume"` from
    `bse.NUMERIC_OPERAND_FIELDS` (save and restore via try/finally, never leave the module-level dict
    mutated on exit) and assert `bse.verify_numeric_operands()` does NOT raise `SystemExit` in that
    state (reproducing the pre-fix exemption bug on the synthetic fixture); with the table entries
    restored, assert the SAME synthetic list DOES raise `SystemExit`; then call
    `bse.normalise_numeric_operands()` on the synthetic list and assert `verify_numeric_operands()` no
    longer raises. (2) `site_audit()` — adapt 09-RESEARCH.md's reusable plistlib script to load both
    `src/PROSOCHE-Dumb.xml` and `src/PROSOCHE-Sentient.xml`, count `setbrightness`/`setvolume` sites
    and how many carry a `WFCoercionVariableAggrandizement`/`WFNumberContentItem` aggrandizement on
    their operand descriptor, and assert each fork shows exactly 14 `setbrightness` sites (all 14
    coerced: `Restore Brightness` x4 + `Dim Target` x10) and exactly 14 `setvolume` sites (only 4
    coerced — `Restore Volume`; `Silence Target`'s 10 sites are correctly left uncoerced because they
    are already Number-sourced via `number()`, per 09-RESEARCH.md's site-classification table). `grep
    -n '"Silence Target"' tools/build_state_engine.py` once as part of this task (not inside the
    script) to independently confirm there is still exactly one `Set Variable` definition of that
    name (via `number(0.10, "Silence Target")`) and no stray Text-typed redefinition — record the
    grep's line-count result in this task's commit message or summary.
  </action>
  <verify>
    <automated>python3 docs/phase9_self_check.py</automated>
  </verify>
  <acceptance_criteria>
    `python3 docs/phase9_self_check.py` exits 0 and prints its own success line (an `AssertionError`
    anywhere means either the negative-control fixture doesn't reproduce the intended pre/post-fix
    behavior, or the live forks don't match the expected 18-coerced/10-uncoerced split — do not weaken
    an assertion to make it pass). `grep -c '"Silence Target"' tools/build_state_engine.py` returns
    exactly 3 (one `number()` definition site, two consuming reads at the `if_block`/`set_media_volume`
    call sites already present in `silence()`) confirming no additional (and therefore no stray
    Text-typed) definition exists.
  </acceptance_criteria>
  <done>
    `docs/phase9_self_check.py` exists, passes, and independently proves both that the new table
    entries are load-bearing (not accidentally passing) and that the live-generated forks carry the
    exact expected 18/10 coercion split across all 28 sites.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Correct the stale "18 sites" framing wherever it is not already corrected</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    09-RESEARCH.md "Site count correction — 28, not 18" and "Common Pitfalls — Pitfall 2"; the
    already-applied correction note in `.planning/debug/HANDOFF.md` lines 429-434 (confirms HANDOFF.md
    is already corrected and must NOT be re-edited — its historical "18" table is deliberately
    preserved as historical context, with an explicit "Do not cite the '18' figure below as current"
    note directly above it); `.planning/ROADMAP.md` lines 231-241 (Phase 9's Goal paragraph, which
    still reads "fix the 18 uncoerced `setbrightness`/`setvolume` sites" even though criterion 1 two
    lines below it already reads "28 deferred... sites"); `.planning/todos/pending/2026-08-16-reintroduce-and-validate-dimming-and-silence-stateful-restor.md`
    Addendum 2 (already corrects the todo's own Problem-section "18" — confirms the todo itself needs
    no further edit, only a grep-based confirmation).
  </read_first>
  <action>
    In `.planning/ROADMAP.md`, edit only the Phase 9 Goal paragraph (currently: "fix the 18 uncoerced
    `setbrightness`/`setvolume` sites left over from the cycle-14 type audit") to read "fix the 28
    uncoerced `setbrightness`/`setvolume` sites left over from the cycle-14 type audit and the later
    Test-a-Circle menu unroll (corrected 2026-08-16 from the stale 18 figure — see criterion 1 and
    `09-RESEARCH.md`)" — a scoped `Edit`, not a `Write`, touching only this sentence; do not alter any
    other line in ROADMAP.md as part of this task (the Requirements line and Plans list are the
    planner's own responsibility, not this task's). Then run `grep -rn "18 uncoerced\|18 deferred\|18
    brightness" docs/ .planning/ROADMAP.md` and confirm every remaining hit is either (a) inside
    `.planning/debug/HANDOFF.md`'s already-corrected historical table (leave untouched — it carries
    its own correction note) or (b) inside a todo file's Problem section that already has a
    same-day-dated Addendum correcting it (leave untouched — todos are historical records, corrected
    via addendum, not rewritten). If any hit is found that is NOT already covered by an existing
    correction note, add a one-line correction at that hit using the same pattern as ROADMAP.md's own
    fix, and record which file(s) required a new correction versus which were already covered.
  </action>
  <verify>
    <automated>grep -c "fix the 28 uncoerced" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    The automated grep returns exactly `1` (the corrected Goal sentence exists exactly once).
    `grep -rn "18 uncoerced\|18 deferred\|18 brightness" docs/ .planning/ROADMAP.md` returns zero
    hits outside `.planning/debug/HANDOFF.md` and already-addended todo files (a hit inside either of
    those two categories is expected and correct — it is a preserved historical record, not a defect).
  </acceptance_criteria>
  <done>
    ROADMAP.md's Phase 9 Goal paragraph states 28, matching its own criterion 1; no repository
    documentation outside the two explicitly-exempted historical-record categories still cites 18 as
    the current count.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Generator (build-time Python) → generated plist actions | The `NUMERIC_OPERAND_FIELDS` table and its consumers are the only place this project asserts an operand's runtime type before it reaches a safety-critical Set-action; a gap here is invisible to every other check in the toolchain |
| Generated plist → iOS Shortcuts runtime | The coercion aggrandizement this table drives is validator-invisible, catalog-invisible, and signed-artifact-invisible (per `.claude/CLAUDE.md`'s own conventions) — only an on-device UI check (Plan B) closes this boundary, which this plan cannot cross |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-01 | Tampering | `NUMERIC_OPERAND_FIELDS` / `WFBrightness`/`WFVolume` coercion shape | high | mitigate | Applied by analogy to the already-device-confirmed Donor-4.1 pattern (Task 1); the shape itself remains `[ASSUMED]` until Plan B's on-device visual chip check confirms it — this plan does not claim device confirmation, only that the audit mechanism now covers the site |
| T-09-02 | Tampering | `docs/phase9_self_check.py`'s negative-control fixture vs. the real `verify_numeric_operands()` guard | medium | mitigate | The self-check calls the actual production function (`bse.verify_numeric_operands`), not a reimplementation, so it cannot silently drift from the guard it is proving is load-bearing (Task 2) |
| T-09-03 | Repudiation | Stale "18 sites" documentation causing a future auditor to under-count coverage | low | mitigate | Task 3's grep-based sweep plus the negative-control's own site-count assertions (14+14=28) make "28" independently verifiable rather than trusted prose |
| T-09-04 | Denial of Service (device usability) | `set_brightness()`/`set_media_volume()` sites this fix touches | high | accept (transferred to Plan B) | This plan makes no device-side change and cannot itself strand a device; the DoS-of-usability risk only materializes once the fix ships on hardware, which is explicitly Plan B's scope (T-09-B-01 in `09-02-PLAN.md`) |
</threat_model>

<verification>
Full static suite (Task 1's `<verify>` command chain) must pass with exit 0 across all five
commands, and `docs/phase9_self_check.py` (Task 2) must independently confirm both the load-bearing
negative control and the exact 18/10 coercion split across 28 sites in both forks. This plan's
verification is exhaustive for RESTORE-01's static half; it is explicitly silent on whether the
coercion shape is correct on real hardware — that question belongs entirely to `09-02-PLAN.md`.
</verification>

<artifacts_produced>
## Artifacts this phase produces (this plan's share)

- `tools/build_state_engine.py` — two new dict entries in `NUMERIC_OPERAND_FIELDS`:
  `"is.workflow.actions.setbrightness": ("WFBrightness",)` and
  `"is.workflow.actions.setvolume": ("WFVolume",)`. No new functions; no renamed symbols.
- `docs/phase9_self_check.py` (new file) — functions `negative_control()`, `site_audit()`, `main()`;
  imports `tools/build_state_engine.py` as `bse` via a `sys.path` insert, reusing `bse.action()`,
  `bse.output()`, `bse.variable()`, `bse.set_var()`, `bse.NUMERIC_OPERAND_FIELDS`,
  `bse.normalise_numeric_operands()`, `bse.verify_numeric_operands()` — no new production symbols in
  `build_state_engine.py` itself.
- `src/PROSOCHE-Dumb.xml`, `src/PROSOCHE-Sentient.xml` — regenerated in place (not new files;
  existing generated artifacts, now carrying the coercion fix at all 18 sites that need it).
- `.planning/ROADMAP.md` — Phase 9's Goal paragraph corrected from "18" to "28" (Task 3); no other
  line in ROADMAP.md is touched by this plan's own tasks (the Requirements/Plans placeholder fills
  were made separately by the planner during this same planning pass, not by an executor task).
</artifacts_produced>

<success_criteria>
- `python3 tools/build_state_engine.py && python3 tools/build_sentient.py` both exit 0 with the two
  new table entries in place
- `docs/phase9_self_check.py` exits 0, proving the guard is load-bearing and both forks show 14
  `setbrightness` sites (14 coerced) / 14 `setvolume` sites (4 coerced, 10 correctly not)
- Full static suite (generator + both self-checks + Playground validator on both forks) is green
- No stale "18 sites" reference remains outside `.planning/debug/HANDOFF.md`'s preserved historical
  table and already-addended todo files
</success_criteria>

<output>
Create `.planning/phases/09-reintroduce-and-validate-dimming-silence-stateful-restore-on/09-01-SUMMARY.md` when done
</output>
