---
phase: 16-dimming-and-silence-as-distinct-device-proven-circles
plan: 06
type: execute
wave: 5
depends_on: ["16-01", "16-02", "16-03", "16-04", "16-05"]
files_modified:
  - src/PROSOCHE-Dumb.xml
  - src/PROSOCHE-Sentient.xml
  - artifacts/shortcuts/
  - artifacts/shortcuts/MANIFEST.md
  - .planning/phases/16-dimming-and-silence-as-distinct-device-proven-circles/16-UAT.md
  - .planning/phases/16-dimming-and-silence-as-distinct-device-proven-circles/COVERAGE.md
  - .planning/phases/09-reintroduce-and-validate-dimming-silence-stateful-restore-on/09-UAT.md
autonomous: false
requirements: [CIRC-03, CIRC-05, SAFE-01, SAFE-02, SAFE-03, SAFE-05, DIST-03]
must_haves:
  truths:
    - "Both forks are rebuilt from the post-16-05 generator, re-signed under their two EXACT existing display names, and the six manifest rows are refreshed and proven against disk."
    - "The full static suite is green at the end of the phase for both forks, including the manifest check, which was deliberately red from 16-01's first rebuild until this plan closed it."
    - "16-UAT.md exists, is cold-runnable by a tester who was not present for this phase, and is pinned to the build identity it was written against by commit, display name, byte count and SHA-256 per fork."
    - "16-UAT.md covers the closed-loop proof first - the device-details read returns a real correctly-typed value, the has-any-value guard correctly SKIPS the change when the read returns nothing, and CLOSE restores the original exactly - and only then the five failure modes."
    - "The compound overlap-plus-force-quit-the-winner trial is a NAMED test in 16-UAT.md, not an optional extra, because that is the state Emergency Restore exists for."
    - "The screen-locked case is handed to Phase 18 by reference rather than duplicated, and 16-UAT.md says so, so the same device behaviour is not investigated twice from two angles."
    - "16-UAT.md carries an explicit batching note naming the other outstanding device work this session should discharge, so a future reader does not schedule three sessions for overlapping ground."
    - "16-UAT.md carries a safety preamble the tester reads before anything else: brightness and volume will actually change, iOS Settings is the only recovery if the phone is left dim or quiet, and any device already running a post-coercion-fix build may be dim right now with no capture on disk."
    - "09-UAT.md is marked superseded rather than edited, and its single recorded pass is explicitly NOT carried forward, because it has no build-identity header and the artifact it described has since been renamed, gained an eleventh dispatch rendering, and been re-signed."
    - "SAFE-05: Emergency Restore has a named test for recovery after EVERY failure mode the instrument trials, and the instrument records that it has never been tapped on a device."
    - "DIST-03 ordering: the device state is RE-MEASURED at execution time and the real measured output is recorded verbatim; no device result is passed, inferred, or substituted from a simulator or a decrypted artifact."
    - "COVERAGE.md records the reasoned no-external-API declaration for this phase rather than a fabricated coverage matrix."
    - "16-UAT.md's build-identity pin is asserted as TWO DISTINCT full 64-character SHA-256 digests, both present in the refreshed manifest - one per fork. A looser hex pattern also matches the pinned commit SHA the same instrument carries, so a 16-or-more-character check can be satisfied without either fork digest being present at all."
    - "docs/retired_clause_check.py, created by plan 16-05, runs in this plan's verify chain, so the retired-clause class is gated at phase close as well as at the moment it was swept."
    - { statement: "DIST-03 (unclassified edge, surfaced not dismissed): both forks import onto a real iPhone and complete a first manual run. Device-gated by definition - Personal Automations are user-created on device and cannot be exercised anywhere else.", verification: backstop }
    - { statement: "CIRC-03, CIRC-05, SAFE-03, SAFE-05: capture, apply and restore close on real hardware across all five failure modes, and Emergency Restore recovers from each. No automated or simulator substitute exists.", verification: backstop }
  artifacts:
    - artifacts/shortcuts/MANIFEST.md
    - .planning/phases/16-dimming-and-silence-as-distinct-device-proven-circles/16-UAT.md
    - .planning/phases/16-dimming-and-silence-as-distinct-device-proven-circles/COVERAGE.md
  key_links:
    - "post-16-05 generator -> rebuilt XML -> re-signed .shortcut -> refreshed MANIFEST row -> 16-UAT.md's pinned SHA-256: the instrument is worthless if it is not pinned to the artifact it describes, and the pin is only real if the check demands a full 64-character digest rather than any long hex run"
    - "docs/retired_clause_check.py (added by plan 16-05) -> this plan's verify chain: a gate that runs once in the plan that created it is a sweep, not a gate"
    - "exact existing display names -> the user's two Personal Automations: a rename silently breaks them, and the filename is the sole carrier of the name"
    - "the measured device state -> the checkpoint branch taken -> what 16-UAT.md's outcome fields may say"
  prohibitions:
    - "MUST NOT record any device-gated test as passed, inferred, or satisfied by a simulator, a static check, or a decrypted artifact."
    - "MUST NOT re-sign either fork under any display name other than the two exact existing ones; a rename breaks the user's Personal Automations silently."
    - "MUST NOT carry forward 09-UAT.md's single recorded pass as still valid; it was measured against a superseded build with no build-identity header."
    - "MUST NOT edit a manifest row to match disk without re-signing the artifact that row describes."
    - "MUST NOT fabricate an API coverage matrix; this phase integrates no external API and the declaration says so with its reason."
---

<objective>
Close the phase: rebuild and re-sign both forks from the post-16-05 generator, refresh the six
manifest rows, take the static suite fully green, author the device instrument, and hand the
device-gated remainder over honestly.

Pitfall 1 governs the ordering of this whole phase and this plan is where it pays off: opening with
"run the eleven outstanding tests" would have dimmed a tester's phone, found that CLOSE does not
restore, found that Emergency Restore does not restore, and ended with a known result and a dim
phone. The persistence defect is fixed first, the artifacts are re-signed, and the instrument is
pinned to the new build — so the device session, when it happens, tests something new.

**This phase will end at `human_needed`, and that is the correct honest outcome.** Plan for it
rather than around it. Precedent: Phase 10's DIST-03 resolution and Phase 12's and Phase 13's
`verification_deferred_human`.

Purpose: leave behind an instrument good enough that one device session discharges several debts,
and a record in which nothing untested is recorded as tested.

Output: two re-signed forks, a green suite, `16-UAT.md`, `COVERAGE.md`, a superseded `09-UAT.md`,
and a checkpoint resolved on the measured truth.

**Constraint D-MANIFEST closes here.** `docs/manifest_check.py` has been deliberately red since plan
16-01's first rebuild. Task 1 is what makes it green, by re-signing and then refreshing the rows —
never by editing rows to match a stale artifact.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-dimming-and-silence-as-distinct-device-proven-circles/16-RESEARCH.md
@.planning/phases/16-dimming-and-silence-as-distinct-device-proven-circles/16-PATTERNS.md
@.planning/phases/16-dimming-and-silence-as-distinct-device-proven-circles/16-VALIDATION.md
@.planning/phases/16-dimming-and-silence-as-distinct-device-proven-circles/16-01-SUMMARY.md
@.planning/phases/16-dimming-and-silence-as-distinct-device-proven-circles/16-02-SUMMARY.md
@.planning/phases/16-dimming-and-silence-as-distinct-device-proven-circles/16-03-SUMMARY.md
@.planning/phases/16-dimming-and-silence-as-distinct-device-proven-circles/16-04-SUMMARY.md
@.planning/phases/16-dimming-and-silence-as-distinct-device-proven-circles/16-05-SUMMARY.md
@.claude/CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rebuild, re-sign under the exact existing names, refresh the manifest, take the suite green</name>
  <precondition>`git merge-base --is-ancestor 7ca8ebbfe467da38e594bdd41687c094a1f0c678 HEAD` exits 0 — the build-provenance guard that gates every generator run. If it fails, halt.</precondition>
  <files>src/PROSOCHE-Dumb.xml, src/PROSOCHE-Sentient.xml, artifacts/shortcuts/, artifacts/shortcuts/MANIFEST.md</files>
  <read_first>
    - `artifacts/shortcuts/MANIFEST.md` — the six rows and the layered narrative paragraphs, so the new paragraph is appended in the file's established form rather than replacing history; note that earlier paragraphs are deliberately retained so a reader can identify a build already on a device.
    - `docs/manifest_check.py` — what it actually proves: that every declared row's byte count and SHA-256 match disk, and that each fork has a source, a dated archive and a signed row.
    - `tools/build_sentient.py` — the two canonical display-name constants and the comment explaining why the source FILENAMES still carry the old fork words while the PRODUCTS carry the new names.
    - `.claude/CLAUDE.md` section 8 — the signing invocation, the filename-equals-display-name discipline, and the two known signer quirks that are auto-retried.
    - `.planning/phases/16-dimming-and-silence-as-distinct-device-proven-circles/16-PATTERNS.md` — the Shared Pattern "Rebuild ripple", which enumerates every step this task owes.
  </read_first>
  <action>
Rebuild both forks in one pass — the Sentient build forks the built Dumb source, so ordering matters
and Dumb must build first. Then sign each fork under its **exact existing display name**, with the
signed filename equal to that display name and no suffix appended. The two canonical names are held
as constants in the Sentient builder; read them from there rather than typing them, because a signed
artifact carries no display name internally — the filename is the sole carrier, so signed-name
discipline is load-bearing rather than cosmetic, and a rename silently breaks the user's two
Personal Automations.

Refresh all six manifest rows — source, dated archive and signed, for each fork — with the byte
counts and SHA-256 digests measured from disk after signing. Append a new narrative paragraph in the
file's established form recording that this rebuild carries the Phase 16 capture-persistence fix,
the zero brightness floor and target, and the snapshot leaf removal, and naming the superseded
previous rows so a reader can identify a build already on a device. Do not delete earlier
paragraphs.

Then run the full static suite for both forks and take it green, including the manifest check, which
closes here. If any checker is red at this point, it is a genuine regression from one of the four
earlier plans — investigate it, do not adjust the checker to match.

Record gate B's output separately for both forks as advisory. Expect exit 1 with exactly one waived
line per fork. Anything gate B reports outside that waiver is a real finding and must be
investigated before the artifact ships. Never chain gate B into a success condition.

Finally, decrypt-verify what actually shipped rather than trusting the unsigned source plus a file
timestamp: round-trip each signed artifact back to its plist and confirm the recovered action list
matches the built source. This project's own convention treats the signed artifact as recoverable
and treats decrypt-verification as the check on what shipped.
  </action>
  <verify>
    <automated>bash -c 'set -e; git merge-base --is-ancestor 7ca8ebbfe467da38e594bdd41687c094a1f0c678 HEAD; python3 tools/build_state_engine.py; python3 tools/build_sentient.py; python3 docs/state_engine_self_check.py; python3 docs/phase5_self_check.py; python3 docs/phase6_self_check.py; python3 docs/phase7_self_check.py; python3 docs/phase9_self_check.py; python3 docs/environmental_restore_check.py; python3 docs/sequence_dispatch_check.py; python3 docs/note_identity_check.py; python3 docs/sentient_core_check.py; python3 docs/sentient_audit_check.py; python3 docs/retired_clause_check.py; python3 docs/manifest_check.py; validate-shortcut src/PROSOCHE-Dumb.xml --target-macos 26 --target-platform all; validate-shortcut src/PROSOCHE-Sentient.xml --target-macos 26 --target-platform all; echo "full static suite green, both forks, gate A clean"'</automated>
  </verify>
  <acceptance_criteria>
    - Every `docs/*.py` checker exits 0, INCLUDING `docs/manifest_check.py`, which prints its passed line with the six rows verified against disk, and INCLUDING `docs/retired_clause_check.py`, the gate plan 16-05 created. It runs here so the retired-clause class is gated at phase close as well as in the plan that swept it — a gate that only ever runs once is a sweep.
    - Gate A reports `Validation passed.` and exits 0 for both `src/PROSOCHE-Dumb.xml` and `src/PROSOCHE-Sentient.xml`.
    - Gate B output for both forks is recorded verbatim in the SUMMARY: exit 1 with exactly one waived line each. Any additional line is recorded as a real finding and investigated before proceeding.
    - Both signed artifacts exist, are non-zero bytes, carry the AEA1 magic, and their filenames equal the two canonical display names exactly — assert the names by reading the constants from `tools/build_sentient.py`, not by typing them.
    - Each signed artifact is decrypt-verified: the recovered plist's action count and its first five actions match the built source. Record both action counts in the SUMMARY.
    - The manifest gains a new narrative paragraph naming this rebuild's three changes and the superseded rows; no earlier paragraph is deleted.
  </acceptance_criteria>
  <done>Both forks are rebuilt, re-signed under their exact existing names, decrypt-verified, and the entire static suite including the manifest check is green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Author 16-UAT.md as a cold-runnable pinned instrument, supersede 09-UAT.md, and write COVERAGE.md</name>
  <files>.planning/phases/16-dimming-and-silence-as-distinct-device-proven-circles/16-UAT.md, .planning/phases/16-dimming-and-silence-as-distinct-device-proven-circles/COVERAGE.md, .planning/phases/09-reintroduce-and-validate-dimming-silence-stateful-restore-on/09-UAT.md</files>
  <read_first>
    - `.planning/phases/13-red-operator-conditionals-and-the-wfitems-list-wrapper/13-UAT.md` in full — this is the shape analog: the most recent and the best-shaped instrument this project has. Copy its frontmatter, its build-identity header table, its re-import precondition block, its "why device-only" section and its per-test Setup / Sequence / Expected observation / Failure evidence / blank outcome structure.
    - `.planning/phases/09-reintroduce-and-validate-dimming-silence-stateful-restore-on/09-UAT.md` — the file being superseded, read for its test *content* only; it is explicitly NOT the shape model.
    - `.planning/phases/16-dimming-and-silence-as-distinct-device-proven-circles/16-RESEARCH.md` — "The device instrument" section, which enumerates the seven elements the instrument owes, plus Pitfalls 2 and 4 through 7.
    - `.planning/phases/16-dimming-and-silence-as-distinct-device-proven-circles/16-VALIDATION.md` — the Manual-Only Verifications table, which is the authoritative list of what may only be settled on hardware.
    - `artifacts/shortcuts/MANIFEST.md` as refreshed by task 1 — the source of the SHA-256 values the instrument pins to.
    - The five preceding SUMMARYs in this phase — the instrument must describe what actually shipped, not what was planned.
  </read_first>
  <action>
Write `.planning/phases/16-dimming-and-silence-as-distinct-device-proven-circles/16-UAT.md`.

Frontmatter carries `status`, the phase slug, a `source` list naming this phase's five SUMMARYs and
this plan, `blocked_on` if the checkpoint resolves blocked, and started/updated dates.

**Build-identity header.** A table pinning: the commit the artifacts were signed at, both fork
display names, the byte count and SHA-256 for each signed artifact, the manifest rows they come
from, and the one-line re-verification recipe. `09-UAT.md`'s lack of this is the single reason its
recorded pass cannot be carried forward, so this header is the instrument's most load-bearing
section, not boilerplate.

**Safety preamble, before anything else.** Brightness and volume will actually change. If the phone
is left dim or quiet, iOS Settings is the only recovery. Do not begin if the phone is needed for
anything else in the next N minutes. And state the live hazard plainly: any device that has already
run a post-coercion-fix build and reached Dimming or Silence is dim and quiet **right now** with no
capture on disk, and Emergency Restore cannot help it — that device must be restored by hand before
testing begins, and that fact is itself the clearest possible statement of the defect this phase
fixed.

**Re-import precondition block.** A device still holding a pre-fix build has an unpersisted capture,
so a stale install produces a false negative on nearly every test. Say how to confirm which build is
installed and how to replace it.

**Why device-only, per test** — naming the rung each test needs and why nothing cheaper settles it,
citing the rung-2 ceiling. New this phase and important: name the tests that were **already settled
at rung 1 or 2** by plans 16-01 and 16-02 so they are not re-run on hardware. The coercion chip gate
in particular may have been re-established at rung 2 by plan 16-02; if so, say what remains and what
does not.

**Setup** — fresh-install both forks from the pinned artifacts, delete the state file, re-point both
Personal Automations by exact display name, and record the pre-session brightness and volume **by
hand, in writing**. The entire proof is a comparison against those two numbers.

**Tests** — **twelve** of them, in this order, each with Setup / Sequence / Expected observation /
Failure evidence to capture / a blank outcome field.

Use `13-UAT.md`'s heading shape exactly: a level-three heading, the test number, a period, then the
title — `### 7. Device restart mid-session`. Number them 1 through 12 contiguously. The verify
parses these headings, so a test written as a bullet or a bold paragraph is invisible to it and
fails the check. Test 10's heading must itself name **both** the overlap and the force-quit; a
heading like "The compound trial" that defers the substance to the body does not satisfy the gate,
and the gate is spelled that way on purpose — Pitfall 4 is about compound modes being demoted, and
a heading that hides which modes compound is the first step of that demotion.

1. The coercion chip gate re-established against THIS build (or marked settled at rung 2 with a
   pointer, if plan 16-02 closed it).
2. A real capture that is **visible in the state file** — the direct test for the persistence defect
   and the one that would have caught it. This is the single most valuable test in the instrument.
3. The has-any-value skip: the change is correctly SKIPPED when the read returns nothing.
4. The full capture, apply and restore round trip, compared against the hand-recorded originals.
5. A zero-brightness observation, as the device input to the brightness-floor decision that D-01
   took on evidence this phase could not itself produce.
6. App force-quit mid-session.
7. Device restart mid-session.
8. CLOSE never fires.
9. Two overlapping sessions.
10. **The compound trial: overlap plus force-quit-the-winner.** A named test, not an optional extra —
    this is the state Emergency Restore exists for and testing the four modes separately never
    reaches it.
11. Emergency Restore after each failure mode above, recording that it has never been tapped on a
    device before this session.
12. A cross-check that the removed snapshot leaves are absent from a freshly bootstrapped state file
    and that nothing broke by their absence.

**Hand the screen-locked case to Phase 18** by reference rather than duplicating it. Phase 18 owns
it, its spikes already validated that screen lock fires CLOSE, and its ROADMAP text says explicitly
that the two should be investigated together rather than twice.

**Batching note in the header.** This session should also discharge the outstanding Phase 12 test 3
(the brightness and volume restore test), and should be scheduled with Phase 19's nine-Circle sweep
and Phase 18's lock investigation. Say so where a scheduler will see it.

**Known-artifact list.** Phase 13 is merged, so its defects are no longer expected — but record the
known outstanding artifacts anyway (for example a Circle that dispatches nothing) so a blank alert
during the trials is attributed correctly rather than blamed on Dimming.

**A Verdict placeholder** with a stated shape: demonstrated-safe or retired, citing test numbers per
claim.

Then mark `09-UAT.md` superseded. Add a superseded banner at the top naming this file as its
replacement and stating why — no build-identity header, pre-rename fork names, and a test list that
predates the persistence finding — and state explicitly that its single recorded pass does NOT carry
forward. Do not delete its content and do not rewrite its tests; supersede, do not edit.

Finally write `COVERAGE.md` in the phase directory with the reasoned declaration, since the API
detector fires on trigger vocabulary but this phase integrates nothing external: no external API
integration; a local Shortcuts plist generator and static checkers; no network surface. Give the
reason, not just the verdict.
  </action>
  <verify>
    <automated>bash -c 'set -e; python3 - <<PY
import pathlib, re
d = pathlib.Path(".planning/phases/16-dimming-and-silence-as-distinct-device-proven-circles")
u = (d / "16-UAT.md").read_text()
assert u.startswith("---"), "16-UAT.md has no YAML frontmatter"
# A SHA-256 is EXACTLY 64 hex characters. The pre-revision pattern accepted any hex run of
# 16 or more, which also matches the pinned commit SHA this same instrument carries by
# design - so the pin could be satisfied with zero fork digests present. Demand full-length
# digests, demand TWO DISTINCT ones (one per fork), and demand each match a manifest row in
# full rather than on a 16-character prefix.
digests = set(re.findall(r"(?<![0-9a-f])[0-9a-f]{64}(?![0-9a-f])", u))
assert len(digests) >= 2, f"16-UAT.md pins {len(digests)} full 64-character SHA-256 digest(s); it owes one per fork, and a commit SHA is not a digest"
man = pathlib.Path("artifacts/shortcuts/MANIFEST.md").read_text()
hits = sorted(g for g in digests if g in man)
assert len(hits) >= 2, f"16-UAT.md carries {len(digests)} digests but only {len(hits)} appear verbatim in the refreshed MANIFEST - the instrument is not pinned to the artifacts that shipped"
for needle in ("outcome", "Emergency Restore", "Setup"):
    assert needle in u, f"16-UAT.md missing required element: {needle}"
# The instrument owes TWELVE numbered tests. The pre-revision threshold of 10 under-enforced
# both the acceptance criteria and the task body, either of which would have caught it.
assert u.count("outcome") >= 12, "16-UAT.md has fewer than 12 outcome fields; the instrument owes 12 numbered tests"
headings = re.findall("(?m)^### +([0-9]+)[.] +(.*)", u)
assert len(headings) >= 12, f"16-UAT.md carries {len(headings)} numbered test headings, expected at least 12"
numbers = [int(n) for n, _ in headings]
assert numbers[:12] == list(range(1, 13)), f"16-UAT.md test numbering is not a contiguous 1..12 run: {numbers}"
# RESEARCH Pitfall 4: compound failure modes get demoted to a note under one of the simple
# tests, so the state Emergency Restore actually exists for never gets exercised. Require the
# compound trial to hold its own number, and to say so in its heading.
compound_ids = [n for n, t in headings if "overlap" in t.lower() and "force" in t.lower()]
assert compound_ids, "the compound overlap-plus-force-quit trial is not its own numbered test - its heading must name both the overlap and the force-quit"
c = (d / "COVERAGE.md").read_text()
assert "No external API integration" in c, "COVERAGE.md missing the reasoned declaration"
o = pathlib.Path(".planning/phases/09-reintroduce-and-validate-dimming-silence-stateful-restore-on/09-UAT.md").read_text()
assert "supersed" in o.lower(), "09-UAT.md not marked superseded"
assert "16-UAT.md" in o, "09-UAT.md does not name its replacement"
print(f"16-UAT.md pinned to MANIFEST; {len(headings)} numbered tests, 1..12 contiguous; compound overlap-plus-force-quit trial is test {compound_ids}; COVERAGE.md declared; 09-UAT.md superseded")
PY
python3 docs/manifest_check.py'</automated>
  </verify>
  <acceptance_criteria>
    - `16-UAT.md` exists with YAML frontmatter and a build-identity header pinning commit, both display names, byte counts and SHA-256 per fork.
    - **The digest pin is asserted as two DISTINCT full 64-character SHA-256 values, each matching a refreshed `artifacts/shortcuts/MANIFEST.md` row verbatim.** A looser hex pattern also matches the pinned commit SHA the instrument carries by design, so a length-tolerant check can pass with neither fork digest present. Full length, distinct, and matched in full — not on a prefix.
    - The instrument contains **at least 12 numbered tests**, numbered 1..12 contiguously under `### N.` headings, each with a blank `outcome:` field — asserted by the verify, which counts headings and outcome fields separately so a shortfall in either fails.
    - The compound overlap-plus-force-quit trial is present as its own numbered test rather than a note, and its **heading** names both the overlap and the force-quit — asserted by the verify.
    - A safety preamble appears before the first test and names iOS Settings as the recovery path and the live already-dim-device hazard.
    - A re-import precondition block explains why a stale install produces a false negative.
    - The screen-locked case is referred to Phase 18 by name and is NOT duplicated as a test here.
    - A batching note naming the other outstanding device work appears in the header.
    - `09-UAT.md` carries a superseded banner naming `16-UAT.md`, states that its single recorded pass does not carry forward, and its original test content is otherwise unedited.
    - `COVERAGE.md` contains the reasoned declaration with its stated reason.
    - `python3 docs/manifest_check.py` still exits 0 — authoring documents must not have disturbed the artifacts.
  </acceptance_criteria>
  <done>A cold-runnable, build-pinned device instrument exists; `09-UAT.md` is superseded rather than edited; the coverage declaration is reasoned rather than fabricated.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Resolve the device session on the MEASURED device state — never on an assumed one</name>
  <what-built>
    Plans 16-01 through 16-05 fixed the capture-persistence defect and guarded it, closed the
    coercion question as far as a free rung reaches, moved the brightness floor and target to zero
    per D-01 (16-03, the code half), removed the two dead snapshot leaves per D-02 (16-04), and
    corrected the live record with a repo-scoped gate behind it (16-05, the record half). Task 1 of
    this plan rebuilt and re-signed both forks and took the full static suite green. Task 2 authored
    `16-UAT.md`, pinned to those artifacts.

    Everything this phase shipped is **structurally proven and behaviourally unproven**. The
    capture-and-restore loop has still never executed on hardware.
  </what-built>
  <how-to-verify>
    **Step 1 — re-measure the device state and record the real output. Do not assume it.**

    At planning time on 2026-08-17 this was measured and it did NOT match what `16-CONTEXT.md` and
    `16-RESEARCH.md` recorded. Both stated that `xcrun devicectl list devices` returns
    `No devices found.` `16-CONTEXT.md`'s stale block was marked **superseded in place** during the
    2026-08-18 plan revision — read it as superseded, not as authority — but `16-RESEARCH.md` still
    carries the old claim. The actual measured result was a **paired but disconnected** iPhone:

    ```
    xcrun devicectl list devices
    # dougal | 8E45671C-9E4D-54C9-AC19-2EB65747337E | available (paired) | iPhone16,1
    xcrun devicectl list devices --json-output <path>
    # tunnelState: disconnected | pairingState: paired | transport: wired | osVersion: 26.6
    ```

    So the blocker is real but its **reason has changed**: it is not that no device is known, it is
    that the known device has no live tunnel. Recording the old reason would be recording something
    false, which this project forbids as firmly as it forbids recording a false pass.

    Re-run both commands now and read `tunnelState` from the JSON, not the State column from the
    table. The decision rule:

    - `tunnelState: connected` → a device IS available. Take the PROCEED branch.
    - anything else, or no devices at all → take the BLOCKED branch, recording the actual measured
      `tunnelState` and the actual table output verbatim.

    **Step 2A — PROCEED branch (a live tunnel exists).**

    Run `16-UAT.md` from its Setup section, in order. Read the safety preamble first and restore the
    phone by hand if it is already dim or quiet from an earlier build. Record every test's `outcome:`
    from observation only. Where iPhone Mirroring is live, drive the device directly and exhaust
    what can be observed in the one session rather than handing back a list of taps. Discharge the
    batched items named in the instrument's header while the session is open.

    A test you did not run is BLOCKED, not passed. A test that was settled at rung 2 is recorded as
    settled at rung 2, with its channel named — never promoted to a device result.

    **Step 2B — BLOCKED branch (no live tunnel).**

    Record every device test in `16-UAT.md` with a blank or BLOCKED outcome and set the frontmatter
    `status: blocked` with `blocked_on: DIST-03`. In the blocked reason, write the **measured**
    output verbatim — the paired-but-disconnected state if that is what you measured — rather than
    copying the older `No devices found.` wording from `16-CONTEXT.md`. Note in `16-UAT.md` that the
    device is an iPhone16,1 on iOS 26.6 if that is still what is reported, since that is
    Apple-Intelligence-capable hardware and therefore able to exercise the Aware fork when a session
    is arranged.

    Then update `.planning/STATE.md`'s standing device backlog and blockers to carry the corrected
    reason, and correct the stale `No devices found.` claim in `16-RESEARCH.md`, marking it as
    re-measured on the execution date rather than deleting the original assertion. `16-CONTEXT.md`
    was already marked superseded in place on 2026-08-18; re-verify that marking rather than
    re-applying it, and record the re-verification.

    Either way, this phase is expected to end at `human_needed`. That is the correct outcome and not
    a failure of the phase.
  </how-to-verify>
  <resume-signal>Type "proceed" if `tunnelState` is connected and you have run the instrument; type "blocked" if it is not, and the plan will record the measured reason. Or describe what you observed.</resume-signal>
</task>

</tasks>

<assumption_delta_decision>
**Decision: `no-change`.**

The assumption-delta detector fired on a pluralization signal — the term "second" in the phrase
"Two things are true and the second is the risk," carried from the ROADMAP goal into `16-CONTEXT.md`.
Assessed honestly, this is a **false positive**. The phrase is rhetorical prose about two true
statements, not the introduction of a second platform, a second authentication method, a second
tenant, or a second source of truth. Nothing in this phase changes the identity model: PROSOCHĒ is
single-user, single-device and network-free, with no accounts and no auth surface. No
identity-model change is manufactured to satisfy the detector.
</assumption_delta_decision>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| signed artifact → the user's device | Once imported, the artifact changes the user's display and volume with no runtime supervision. |
| manifest row → a reader identifying a build already on a device | A stale or hand-edited row makes it impossible to tell which build a device is running. |
| measured device state → the recorded verdict | The gap between what was measured and what gets written down is where a fabricated result would enter. |
| display name → the user's Personal Automations | The automations bind by exact display name and the filename is the sole carrier of that name. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-27 | Spoofing | a device-gated test recorded as passed on non-device evidence | critical | mitigate | Authored as the first prohibition. The checkpoint's decision rule is mechanical and reads `tunnelState` from JSON rather than eyeballing a table; a test not run is BLOCKED, and a rung-2 result keeps its channel label. Precedent for the honest outcome: Phase 10, 12 and 13. |
| T-16-28 | Repudiation | a blocked reason copied from a stale document rather than measured | high | mitigate | The measured device state at planning time contradicts what CONTEXT and RESEARCH assert. The checkpoint requires the reason to be re-measured and recorded verbatim, and requires the stale assertions to be corrected as re-measured rather than deleted. |
| T-16-29 | Tampering | a fork re-signed under a changed display name | high | mitigate | The names are read from the generator's own constants rather than typed; the acceptance criteria assert filename equality against those constants. A signed artifact carries no internal display name, so the filename is the only carrier. |
| T-16-30 | Tampering | a manifest row edited to match disk without re-signing | high | mitigate | Authored as a prohibition; `docs/manifest_check.py` proves every row against disk and is in this plan's verify chain, and the decrypt-verify step proves the signed artifact matches the built source. |
| T-16-31 | Denial of Service | a tester's phone left dim or quiet by the session | high | mitigate | The instrument's safety preamble names iOS Settings as the recovery, warns about the already-dim-device hazard before any trial, and the restore trials are ordered so Emergency Restore is exercised after each failure mode rather than only at the end. |
| T-16-32 | Information Disclosure | none | low | accept | The product has no network surface and no secrets; nothing leaves the device. Recorded for completeness rather than as an open risk. |
| T-16-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no external packages. The signing toolchain is the vetted Shortcuts Playground plugin plus the macOS `shortcuts` CLI; `16-RESEARCH.md` § Package Legitimacy Audit records the phase as installing nothing, verified by direct inspection. |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces (this plan's share, and the phase-level roll-up)

**New files, this plan**
- `.planning/phases/16-.../16-UAT.md` — the pinned, cold-runnable device instrument
- `.planning/phases/16-.../COVERAGE.md` — the reasoned no-external-API declaration

**Modified, this plan**
- `artifacts/shortcuts/MANIFEST.md` — six refreshed rows plus one appended narrative paragraph
- `artifacts/shortcuts/` — both signed `.shortcut` artifacts re-signed under their exact existing names
- `src/PROSOCHE-Dumb.xml`, `src/PROSOCHE-Sentient.xml` — final rebuild
- `.planning/phases/09-.../09-UAT.md` — superseded banner only

**Phase-level roll-up — every new symbol, file and config key this phase creates**

| Kind | Name | Owning plan |
|---|---|---|
| build guard | `verify_capture_persistence` | 16-01 |
| helper | `_save_source_dictionary` | 16-01 |
| checker function | `capture_persistence_negative_control` | 16-01 |
| build guard | `verify_no_removed_snapshot_leaf_reads` | 16-04 |
| constant | the retired-leaf-names constant naming D-02 | 16-04 |
| file | `.planning/spikes/010-coercion-at-a-direct-set-parameter/` (README, drafts, signed probe, screenshots) | 16-02 |
| checker (new `docs/*.py`) | `docs/retired_clause_check.py` — repo-scoped retired-clause gate plus the record-agrees-with-build cross-check | 16-05 |
| file | `.planning/phases/16-.../16-UAT.md` | 16-06 |
| file | `.planning/phases/16-.../COVERAGE.md` | 16-06 |
| config key changed | `safety.brightness_floor` `0.10` → `0` | 16-03 |
| config key changed | `safety.dim_target` `0.12` → `0` | 16-03 |
| state keys removed | the four `settings_snapshot.*.changed_at` / `.changed_by_session_id` leaves | 16-04 |

No new config keys are created; two are changed and four state-shape keys are removed.
</artifacts_this_phase_produces>

<verification>
- Every `docs/*.py` checker exits 0, including `docs/manifest_check.py` (constraint D-MANIFEST closes here) and `docs/retired_clause_check.py` (created by plan 16-05, run here so it gates rather than sweeps)
- Gate A reports `Validation passed.` on both forks
- Gate B recorded separately as advisory for both forks: exit 1 with exactly the one waived line each, never chained
- Both signed artifacts exist under their exact canonical display names, are AEA1, and decrypt-verify against the built sources
- `16-UAT.md` pins SHA-256 digests that match refreshed manifest rows, carries at least 12 tests with blank outcome fields, and names Phase 18 as the owner of the locked-screen case
- `09-UAT.md` carries a superseded banner naming its replacement
- `COVERAGE.md` carries the reasoned declaration
- The device checkpoint is resolved on a re-measured `tunnelState`, with the real output recorded verbatim
</verification>

<success_criteria>
Both forks ship rebuilt, re-signed and decrypt-verified under unchanged display names; the full
static suite is green; a cold-runnable instrument pinned to those exact artifacts exists; and every
device-gated claim is recorded either as an observation actually made or as BLOCKED with its real,
re-measured reason. The phase ending at `human_needed` is the expected and correct outcome.
</success_criteria>

<output>
Create `.planning/phases/16-dimming-and-silence-as-distinct-device-proven-circles/16-06-SUMMARY.md` when done.
Record: the two signed byte counts and SHA-256 digests, both decrypt-verified action counts, gate B's
verbatim output per fork, the re-measured device state verbatim including `tunnelState`, which
checkpoint branch was taken and why, and the list of device tests left BLOCKED with their real reason.
</output>
