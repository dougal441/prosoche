---
phase: 16-dimming-and-silence-as-distinct-device-proven-circles
plan: 03
type: execute
wave: 2
depends_on: ["16-01", "16-02"]
files_modified:
  - tools/build_state_engine.py
  - src/PROSOCHE-Dumb.xml
  - src/PROSOCHE-Sentient.xml
  - docs/environmental_restore_check.py
  - docs/phase5_self_check.py
  - .planning/REQUIREMENTS.md
  - docs/CAPABILITY-DECISIONS.md
  - .claude/CLAUDE.md
autonomous: true
requirements: [SAFE-01, SAFE-02, CIRC-05]
must_haves:
  truths:
    - "safety.brightness_floor is 0 and safety.dim_target is 0 in the Config literal of both built forks, per locked decision D-01."
    - "CIRC-05 boundary: at the threshold the floor and the target are equal, so the floor binds exactly rather than never - the assertion that the target is at or above the floor still holds at equality, and one step below the floor remains impossible because the target is the floor."
    - "The retired never-zero clause is removed from ALL NINE places it is encoded, in one commit, as one defect class. NINE is a MEASURED count taken during plan revision, not a transcription - an earlier six-site list in 16-CONTEXT.md was incomplete and a later eight-site list from review was still one short. The counting unit is a distinct assertion of the retired clause that must be independently corrected; an assertion's own failure message and adjacent narrative comment belong to that assertion, but a separate summary of what an assertion asserts counts on its own, because relaxing the assertion makes the summary false."
    - "The nine measured sites, with line anchors recorded as of 2026-08-17 and to be located by content because every anchor in this phase shifts: (1) docs/environmental_restore_check.py the dim-target strictness assertion plus its failure message, measured :257-258, with the narrative comment block above it at :246-251; (2) docs/environmental_restore_check.py the module docstring's summary of what the checker asserts, measured :17, which names a strictly-positive dim target and becomes false the moment site 1 is relaxed; (3) docs/phase5_self_check.py the per-action brightness-parameter assertion, measured :117; (4) .planning/REQUIREMENTS.md SAFE-01's statement, measured :123; (5) .planning/REQUIREMENTS.md CIRC-05's statement clause, measured :66; (6) .planning/REQUIREMENTS.md CIRC-05's parenthetical describing what the environmental checker guards, on the same line :66 but a separate assertion that site 5's amendment does not reach; (7) docs/CAPABILITY-DECISIONS.md BD-02's historical band-and-floor text, measured :44, :54 and :58, plus its Phase 9 addendum's provisional framing at :60; (8) .claude/CLAUDE.md's Constraints safety bullet, measured :23; (9) tools/build_state_engine.py dimming()'s emitted Shortcuts comment action, measured :597-600, which has no Python docstring and therefore is NOT reached by any docstring instruction."
    - "Site 9 is an EMITTED ARTIFACT STRING, not source commentary: dimming()'s comment() renders 11 times per fork through primitive_dispatch(), so before this plan both signed forks ship 11 user-visible comment actions each asserting a floor the same build sets to zero. Measured 2026-08-17: 11 occurrences in src/PROSOCHE-Dumb.xml and 11 in src/PROSOCHE-Sentient.xml. After the rebuild the count is zero in both."
    - "silence()'s parallel comment block is NOT part of this class and is not edited. Its volume clause is SAFE-02 - Media scoping and never increase - which remains true under D-01. Measured and confirmed during plan revision."
    - "SAFE-01 and CIRC-05 are restated to the real safety property - brightness is only ever changed when its original has been captured AND durably persisted before the change, and is always restored - and both restatements are recorded as requirement amendments traceable to D-01, not as silent edits."
    - "The assertion that the dim target is at or above the brightness floor is byte-identical to its pre-plan form; no other assertion in the environmental checker is weakened."
    - "SAFE-02 is untouched by D-01: volume is never increased, every volume write remains Media-scoped, and the allow-volume-increase flag remains false."
    - "SAFE-02 adjacency: when the captured volume already equals the silence target, the already-quiet arm fires and no volume write occurs, so no path can raise volume by writing a target above the captured original."
    - "SAFE-02 empty: when the device-details volume read returns nothing, the gates skip the write entirely rather than writing a default."
    - "SAFE-02 ordering: every restore writes back only a captured original, and the Media scoping is applied at every write site rather than at a single chokepoint."
    - "SAFE-05 adjacency: Emergency Restore invoked when the captured original equals the current setting writes the same value back and clears the snapshot, leaving the user no worse off."
    - "SAFE-05 empty: Emergency Restore invoked with a cleared-sentinel snapshot fails the numeric gate and skips the write, which is the fail-safe direction, rather than writing an empty or zero value."
    - "SAFE-05 ordering: Emergency Restore restores the managed settings before it clears the cooldown and the active session, and saves on the branch that owns the dictionary it wrote."
    - { statement: "SAFE-01 (unclassified edge, surfaced not dismissed): the practical minimum brightness on real hardware is dim rather than a black or unusable screen, so a target of 0 is safe to ship. This rests on one unrepeated user report and is a device observation this plan cannot make.", verification: backstop }
  artifacts:
    - tools/build_state_engine.py
    - src/PROSOCHE-Dumb.xml
    - src/PROSOCHE-Sentient.xml
    - docs/environmental_restore_check.py
    - docs/phase5_self_check.py
    - .planning/REQUIREMENTS.md
    - docs/CAPABILITY-DECISIONS.md
    - .claude/CLAUDE.md
  key_links:
    - "src/PROSOCHE-Dumb.xml Config literal -> the generator's in-place rewrite -> both built forks: the literal lives ONLY in the XML, never in a Python constant"
    - "the environmental checker's target assertion -> the target-at-or-above-floor assertion: relaxing the first must not disturb the second"
    - "the environmental checker's dim-target assertion -> that checker's own module docstring: relaxing the assertion falsifies the docstring's summary of it, which is the same failure shape as CIRC-05's parenthetical"
    - "tools/build_state_engine.py dimming()'s comment() -> primitive_dispatch()'s 11 renderings -> both shipped forks: the ONLY way to change 22 user-visible comment actions is to change the one generator string and rebuild"
    - "D-01 -> REQUIREMENTS SAFE-01 and CIRC-05 -> BD-02 -> CLAUDE.md safety constraint -> the generator's emitted comment: one decision, five carriers, one commit"
  prohibitions:
    - "MUST NOT weaken any assertion in docs/environmental_restore_check.py other than the single dim-target strictness assertion that D-01 names."
    - "MUST NOT relax any volume floor, Media scoping, or the allow-volume-increase flag; D-01 is about brightness only and the volume floor is explicitly unchanged."
    - "MUST NOT edit silence()'s comment block in tools/build_state_engine.py. It states SAFE-02, which D-01 does not touch, and it was measured correct during plan revision. Editing it is scope creep, not class-fixing."
    - "MUST NOT edit REQUIREMENTS.md silently; SAFE-01 and CIRC-05 must carry a visible amendment trace back to D-01."
    - "MUST NOT re-open or re-litigate D-01; it is a LOCKED user decision and this plan implements it."
---

<objective>
Implement locked decision **D-01**: `safety.brightness_floor` moves `0.10 -> 0` and
`safety.dim_target` moves `0.12 -> 0`. Dimming reaches the device's true minimum.

The correction has to be observable rather than inert. A floor of 0 under an unchanged target of
0.12 would never bind, so the target follows the floor down - that is why D-01 moves both.

The real work is that the retired clause is encoded in **nine measured places**, and this is one
defect class. The project rule is fix whole classes, never site-by-site.

**Nine is a measurement, not a transcription.** 16-CONTEXT.md enumerated six; a review pass found
two more; a re-measurement during plan revision found a ninth. Three of the nine are statements
that are now literally false and get amended with a visible trace to D-01, not quietly rewritten.
Two more are *descriptions of assertions* — REQUIREMENTS CIRC-05's parenthetical and the
environmental checker's own module docstring — which are true today and become false the instant
task 1 relaxes the assertion they describe. That is the shape this class keeps hiding in, so the
full enumeration is carried in `must_haves.truths` with its measured line anchors.

**The ninth site is not documentation — it ships.** `dimming()` emits a Shortcuts `comment()`
action carrying the retired clause, and `primitive_dispatch()` renders it eleven times per fork.
Measured 2026-08-17: both signed forks currently ship **11 user-visible comment actions each**
asserting a floor the same build sets to zero. It has no Python docstring, so no docstring-level
instruction reaches it.

The safety property is unchanged and is the whole point of the phase: **capture-and-restore
reliability**, not floor avoidance. Plan 16-01 made that property real for the first time by
persisting the capture; this plan is only safe to land after it, which is why it is wave 2.

Purpose: retire a clause the evidence retired, on the main line, without weakening anything that is
still load-bearing.

Output: both forks rebuilt with the new Config literal and with the generator's emitted comment
corrected; two checkers reconciled; four records amended.

**Constraint D-MANIFEST:** `python3 docs/manifest_check.py` remains deliberately red from plan
16-01's first rebuild until plan 16-05 re-signs and refreshes it. Do NOT edit MANIFEST rows.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/16-dimming-and-silence-as-distinct-device-proven-circles/16-CONTEXT.md
@.planning/phases/16-dimming-and-silence-as-distinct-device-proven-circles/16-PATTERNS.md
@.planning/phases/16-dimming-and-silence-as-distinct-device-proven-circles/16-01-SUMMARY.md
@.claude/CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Move the floor and the target to zero, correct the generator's emitted comment, and reconcile the two checkers that encode the retired clause</name>
  <files>tools/build_state_engine.py, src/PROSOCHE-Dumb.xml, src/PROSOCHE-Sentient.xml, docs/environmental_restore_check.py, docs/phase5_self_check.py</files>
  <read_first>
    - `src/PROSOCHE-Dumb.xml` - the Config literal's safety block (measured at lines 162-166 before this phase moved anything; locate it by content, never by line number).
    - `.planning/phases/16-dimming-and-silence-as-distinct-device-proven-circles/16-PATTERNS.md` - the "Reading this file" preamble, which states that the generator edits the XML in place with one read and one write, so the Config literal lives ONLY in the XML and there is no Python constant to change; and the section "docs/environmental_restore_check.py - the floor relaxation (work item 4)".
    - `tools/build_state_engine.py` - `dimming()` in full, and in particular the three-bullet `comment(...)` block that is its FIRST emitted action (measured :597-600). Read `silence()`'s parallel three-bullet `comment(...)` block immediately below it (measured :618-621) as well, so you can see for yourself that the two differ and that only one is in scope. Read `primitive_dispatch()` to confirm the rendering count.
    - `docs/environmental_restore_check.py` - **the whole file, top to bottom, not only the assertion block.** Read the MODULE DOCSTRING first (measured :17), which enumerates what this checker asserts and includes a summary of the dim-target assertion's strictness; then the assertion block around the Config literal read (measured :255-260); then the narrative comment above it that already explains the BD-02 addendum (measured :246-251); then the deliberately-not-asserted note about coercion counts. The docstring and the assertion are two separate sites in the same file - this is exactly the trap this plan's site list exists to close.
    - `docs/phase5_self_check.py` - the per-action loop asserting over the brightness parameter (measured :116-117), and the `require()` helper.
    - `.planning/phases/16-dimming-and-silence-as-distinct-device-proven-circles/16-CONTEXT.md` - the D-01 decision text in full, including its site list and the explicit instruction about which assertion changes and which stays.
    - This plan's own `must_haves.truths` - the nine-site enumeration with its measured anchors. It is the work list for this task and task 2 together; do not work from 16-CONTEXT.md's list alone.
  </read_first>
  <action>
Edit the Config literal in `src/PROSOCHE-Dumb.xml` so the safety block's brightness floor and dim
target are both `0`. Locate the block by content. Leave the other two safety keys - the
allow-volume-increase flag and the managed-color-filters flag - exactly as they are; D-01 is about
brightness only.

**Then correct the generator's emitted comment — site 9, and the only one of the nine that ships.**
In `tools/build_state_engine.py`, `dimming()` opens with a three-bullet `comment(...)` action. Its
middle bullet couples two separate claims: one about not brightening an already-dim screen, which
is still true and describes the already-dim arm, and one forbidding a zero brightness write, which
D-01 retires. Rewrite that bullet so it states the rule the build actually ships: the safety
property is capture-and-restore reliability — the original is captured and durably persisted before
any change and is always restored — and the dim target may reach the device's true minimum. Do not
replace one floor claim with a softer floor claim; state the property, not a bound.

This is a Shortcuts `comment()` **action**, not a Python docstring — `dimming()` has no docstring at
all, so an instruction phrased as "update the function docstrings" does not reach it. It renders
through `primitive_dispatch()` eleven times per fork, so this one string edit removes 11 shipped
comment actions per fork, 22 across both. Measure the before and after counts rather than assuming
them; the verify below asserts the after count is zero in both forks.

**`silence()`'s parallel comment block is correct as-is and MUST NOT be edited.** Its volume bullet
states Media-only scoping and never-increase, which is SAFE-02 — untouched by D-01 and still true.
This was measured during plan revision. Do not invent a symmetrical change there; the symmetry of
the two functions is not a reason to edit both, and doing so would weaken a live safety statement.

Then relax the environmental checker's dim-target assertion from a strict greater-than-zero test to
an at-or-above-zero test, and update its failure message so the message and the assertion agree.
Update the narrative comment block immediately above it, which already tells the story of the BD-02
addendum, to record that the addendum is now settled on main by D-01 rather than provisional and
fork-scoped. Per D-01, **the adjacent assertion that the dim target is at or above the brightness
floor stays exactly as it is** - and note in the SUMMARY that at equality (both zero) it still
holds, which is what makes the floor bind rather than never bind.

**Then correct that same file's module docstring — site 2.** Near the top it enumerates what this
checker asserts, and its dim-target clause describes the assertion's strictness. The moment you
relax the assertion, that clause is false. Rewrite it to describe what the checker asserts after
this task: a dim target at or above the configured brightness floor. This is the identical failure
shape as CIRC-05's parenthetical in task 2 — a description of an assertion, invalidated by
relaxing the assertion — and it is the site the six-site and eight-site enumerations both missed.
A checker whose own docstring misdescribes it is exactly the self-invalidating text this project's
grep-gate hygiene rule exists to prevent.

Do not weaken any other assertion in that file. In particular leave the media-scoping assertion,
the allow-volume-increase assertion, the required-symbols tuple, the site counts and the bootstrap
seed assertions untouched.

Then handle the second checker, which encodes the same retired clause from a different angle: the
phase5 self-check's per-action loop asserts over the brightness action's parameter that it is not
zero. It does not fire today, because every shipped brightness parameter is a variable descriptor
dictionary rather than a scalar - so it is currently decoration. Review it in this same commit so
the two checkers cannot disagree about what D-01 decided. Either remove it, or convert it to an
assertion that is actually true and load-bearing under D-01 - the honest version of what it was
trying to protect is that a brightness write is never fed an unguarded literal, which the existing
restore-gate build guard already enforces from the other direction. Whichever you choose, record
the reasoning inline so a future reader knows the clause was retired deliberately and not lost.

Rebuild both forks. The generator reads and rewrites the Dumb XML in place and the Sentient build
forks the built Dumb source, so the Config change propagates to both from this one edit.
  </action>
  <verify>
    <automated>bash -c 'set -e; python3 tools/build_state_engine.py; python3 tools/build_sentient.py; python3 docs/environmental_restore_check.py; python3 docs/phase5_self_check.py; python3 docs/phase9_self_check.py; python3 docs/state_engine_self_check.py; python3 - <<PY
import sys, pathlib, plistlib
# Reuse the project-canonical locate-by-content reader rather than inventing a fourth idiom
# for the same literal - the same reason docs/phase5_self_check.py imports it. It finds the
# gettext action whose WFTextActionText is the Config JSON and returns it PARSED, so there is
# no regex here at all. An earlier draft of this verify hand-rolled a nested-dict walk that
# matched nothing and a backslash regex that could not survive this heredoc; both were
# structurally incapable of passing. Do not reintroduce either.
# No single quotes and no backslashes anywhere below: this runs in an UNQUOTED heredoc nested
# inside a single-quoted bash -c, where a single quote closes the bash argument and a
# backslash reaches Python literally.
sys.path.insert(0, "docs")
from sequence_dispatch_check import config_literal
for fork in ("Dumb", "Sentient"):
    path = pathlib.Path(f"src/PROSOCHE-{fork}.xml")
    # Site 9, asserted against the REBUILT artifact: the retired clause must no longer ship.
    # Measured before this plan: 11 occurrences per fork, one per primitive_dispatch rendering.
    shipped = path.read_text().count("never set zero")
    assert shipped == 0, f"{fork}: the retired zero-floor clause still ships in {shipped} emitted comment actions - the generator string was not corrected, or the fork was not rebuilt"
    safety = config_literal(plistlib.loads(path.read_bytes())["WFWorkflowActions"])["safety"]
    floor, target = safety["brightness_floor"], safety["dim_target"]
    assert floor == 0, f"{fork}: brightness_floor is {floor}, expected 0"
    assert target == 0, f"{fork}: dim_target is {target}, expected 0"
    assert target >= floor, f"{fork}: target below floor"
    # D-01 is brightness-only: the other two safety keys must be untouched.
    assert safety["allow_volume_increase"] is False, f"{fork}: allow_volume_increase moved; D-01 does not touch it"
    print(f"{fork}: brightness_floor={floor} dim_target={target}; target>=floor holds at equality; 0 shipped comment actions carry the retired clause")
# The SAFE-02 half of the same comment pair must survive untouched: silence() still states
# Media-only scoping and never-increase in BOTH forks. A zero here means silence() was edited.
for fork in ("Dumb", "Sentient"):
    kept = pathlib.Path(f"src/PROSOCHE-{fork}.xml").read_text().count("never increase it")
    assert kept > 0, f"{fork}: silence() SAFE-02 comment was edited or removed - it is explicitly out of D-01 scope"
    print(f"{fork}: silence() SAFE-02 comment intact in {kept} renderings")
PY
validate-shortcut src/PROSOCHE-Dumb.xml --target-macos 26 --target-platform all
validate-shortcut src/PROSOCHE-Sentient.xml --target-macos 26 --target-platform all'</automated>
  </verify>
  <acceptance_criteria>
    - The Config literal in BOTH built forks reports a brightness floor of 0 and a dim target of 0, located by content and asserted by the verify script above.
    - **Site 9, rebuilt-artifact assertion:** after the rebuild, the retired zero-floor clause occurs **zero** times in `src/PROSOCHE-Dumb.xml` and zero times in `src/PROSOCHE-Sentient.xml` - asserted by the verify script, which runs it only after both generators have run. Record the measured before count (11 per fork at plan-revision time) and the after count in the SUMMARY.
    - `silence()`'s comment block is **unedited**. Prove it two ways: `git diff tools/build_state_engine.py` shows no change inside `silence()`, and the verify script's SAFE-02 positive check finds its volume clause still present in both rebuilt forks.
    - The at-or-above-floor relationship still holds at equality, and the assertion enforcing it is byte-identical to its pre-plan form - prove this with `git diff` on that specific line showing no change.
    - The environmental checker's **module docstring** no longer describes the dim-target assertion as a strictness test and instead describes what the checker asserts after this task. A `git diff docs/environmental_restore_check.py` that touches the assertion but not the docstring fails this task - that is precisely the miss this plan was revised to close.
    - `python3 docs/environmental_restore_check.py` exits 0 and prints its passed line.
    - `python3 docs/phase5_self_check.py` exits 0.
    - `python3 docs/phase9_self_check.py` and `python3 docs/state_engine_self_check.py` both exit 0 - this task changes a literal and a comment string, not a site count, so neither may move. `phase9_self_check.py` counts `setbrightness`/`setvolume` actions and `state_engine_self_check.py` inspects the CLOSE owner arm; a `comment()` text edit adds and removes no action, so a movement in either is a regression to investigate, not a number to update.
    - Gate A reports `Validation passed.` on both forks.
    - `git diff docs/environmental_restore_check.py` shows exactly one changed assertion, plus its message, its narrative comment and the module docstring; any second weakened assertion in that file fails this task.
    - The phase5 checker's retired brightness-parameter clause is either removed or replaced, with inline reasoning recorded either way.
  </acceptance_criteria>
  <done>Both forks ship a zero floor and a zero dim target and no longer ship the retired clause in any emitted comment action; the two checkers and the one checker docstring agree about what D-01 decided; `silence()` is untouched; and no other assertion anywhere was weakened.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Amend SAFE-01 and CIRC-05 as traceable requirement amendments, and correct BD-02 and the project safety constraint</name>
  <files>.planning/REQUIREMENTS.md, docs/CAPABILITY-DECISIONS.md, .claude/CLAUDE.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` - the SAFE-01 and CIRC-05 entries, and the surrounding entries' formatting so the amendment matches how this file already records phase-driven revisions (CIRC-03, CIRC-05 and SAFE-05 all already carry parenthetical Phase-10 amendment notes; copy that form).
    - `docs/CAPABILITY-DECISIONS.md` - the BD-02 entry in full plus its Phase 9 addendum, and the BD-01/BD-02/BD-03 consistency note, which is binding on all three and must stay internally consistent after this edit.
    - `.claude/CLAUDE.md` - the `## Constraints` safety bullet, which still describes the relaxed floor as provisional and scoped to an experimental fork.
    - `.planning/phases/16-dimming-and-silence-as-distinct-device-proven-circles/16-CONTEXT.md` - the D-01 text, which is the authority being cited.
    - `.planning/phases/16-dimming-and-silence-as-distinct-device-proven-circles/16-01-SUMMARY.md` - what plan 16-01 actually landed, since the restated requirement asserts durable persistence and must describe what now exists.
  </read_first>
  <action>
Restate **SAFE-01** in `.planning/REQUIREMENTS.md`. Its current text asserts that brightness is
never set to zero, which is literally false under D-01. Replace it with the real safety property:
brightness is only ever changed when its original value has been captured **and durably persisted**
before the change, and is always restored. Record it as a **requirement amendment traceable to
D-01**, in the same parenthetical form the file already uses for its Phase-10 amendments - naming
the decision, the date, and the reason the previous wording was retired (avoiding the floor was
never itself the safety property; capture-and-restore reliability is).

Restate **CIRC-05**, which needs **two** distinct corrections on the same line - sites 5 and 6 of
the nine. Do not treat "amend the clause and keep the rest" as sufficient here; the rest contains a
second false assertion.

*Site 5 - the statement clause.* Its opening sentence carries the same never-to-zero clause. Amend
it to: dimming reduces brightness only when reversible - meaning only when the original has been
captured and persisted - otherwise degrading safely.

*Site 6 - the trailing parenthetical.* The Phase 10 parenthetical closes by describing what
`docs/environmental_restore_check.py` guards, and its description of that guard's dim-target
assertion asserts a strictness that task 1 has just removed. Read the checker as task 1 leaves it
and rewrite the parenthetical to describe the assertion that now exists: a dim target at or above
the configured brightness floor. Keep the rest of the parenthetical - the Phase 10 note about
"degrades safely" being the per-run fallback and the cancelled cut is unrelated to D-01 and stays.

This site is why the task-2 verify below greps the CIRC-05 line for the retired strictness wording
rather than only checking that `D-01` appears: a citation can be added while a false clause
survives three sentences later on the same line, which is exactly what would have shipped.

Note in both amendments that the durable-persistence half of the restated property is what plan
16-01 made true; before it, the property could not have been satisfied by any build. That is the
whole reason this restatement is honest rather than a weakening.

**Write the amendment traces so they name the retired clause without restating it.** Say that the
prior zero-floor wording was superseded by D-01 and why; do not quote the old sentence back into
the entry. A retired clause quoted inside its own amendment is still a false assertion sitting in
the requirements file, and it is the same self-invalidating-text failure this plan exists to close.
The verify below greps the two entries for the retired wording, so a verbatim quotation fails.

Leave SAFE-02, SAFE-03 and SAFE-05 alone. SAFE-02's volume floor is explicitly unchanged by the
addendum - never increase, no startling output - and SAFE-03 and SAFE-05 are already stated as the
capture-and-restore property this amendment is aligning the other two to.

Then update `docs/CAPABILITY-DECISIONS.md` BD-02 to record D-01 as decided on the main line,
superseding the historical band-and-never-zero text and promoting the Phase 9 addendum from
provisional to settled. Append rather than rewrite history, matching how this file already records
superseding decisions. Check the BD-01/BD-02/BD-03 consistency note afterwards and confirm it still
reads true - all three are the same canonical rule applied to three evidentiary outcomes, and that
framing survives D-01 because D-01 changes a floor and not the rule.

Finally correct the `## Constraints` safety bullet in `.claude/CLAUDE.md`. It currently frames the
relaxed floor as unconfirmed and provisional, and scopes it to an experimental fork, with the
relaxation contingent on a Phase 9 device proof that never happened. That is now stale in two ways:
the decision has been taken on main by D-01, and the contingency it names was about a capture and
restore loop that plan 16-01 has now made structurally sound but which remains device-unproven.
Write it so both facts survive - the floor is decided, and the device proof is still outstanding
and is tracked by this phase's UAT instrument. Do not overstate: nothing in this phase makes the
loop device-proven.
  </action>
  <verify>
    <automated>bash -c 'set -e; python3 - <<PY
import pathlib
# No backslash escapes in this script: it runs inside an UNQUOTED heredoc nested in a
# single-quoted bash -c, where a backslash would survive to Python literally. See task 1.
req = pathlib.Path(".planning/REQUIREMENTS.md").read_text()
for rid in ("SAFE-01", "CIRC-05"):
    lines = [l for l in req.splitlines() if f"**{rid}**" in l]
    assert len(lines) == 1, f"expected exactly one {rid} requirement entry, found {len(lines)}"
    body = lines[0]
    low = body.lower()
    assert "D-01" in body, f"{rid} amendment does not cite D-01"
    assert "persist" in low, f"{rid} restatement does not name durable persistence"
    # Site 5: the statement clause. A citation added while the clause survives is not an amendment.
    assert "never to zero" not in low, f"{rid} still carries the retired never-to-zero clause"
    assert "never set to zero" not in low, f"{rid} still carries the retired never-to-zero clause"
    # Site 6: the parenthetical describing the environmental checker guard. This is the one that
    # task 1 falsifies and that an amendment scoped to the clause alone leaves standing.
    assert "strictly positive" not in low, f"{rid} still describes the dim-target guard as a strictness test - site 6 was not corrected"
cap = pathlib.Path("docs/CAPABILITY-DECISIONS.md").read_text()
assert "D-01" in cap, "BD-02 does not record D-01"
cl = pathlib.Path(".claude/CLAUDE.md").read_text()
bullets = [l for l in cl.splitlines() if l.startswith("- **Safety**")]
assert len(bullets) == 1, f"expected exactly one Constraints Safety bullet, found {len(bullets)}"
bullet = bullets[0]
assert "D-01" in bullet, "the CLAUDE.md Safety bullet does not cite D-01"
assert "provisional, not confirmed" not in bullet, "the Safety bullet still frames the floor as unsettled"
assert "experimental fork only" not in bullet, "the Safety bullet still scopes the correction to the experimental fork"
print("D-01 recorded in REQUIREMENTS (SAFE-01 clause, CIRC-05 clause AND parenthetical), CAPABILITY-DECISIONS and the CLAUDE.md Safety bullet")
PY
python3 docs/environmental_restore_check.py; python3 docs/phase5_self_check.py'</automated>
  </verify>
  <acceptance_criteria>
    - The SAFE-01 entry in `.planning/REQUIREMENTS.md` no longer asserts that brightness is never set to zero, states the capture-and-durably-persist-then-restore property instead, and cites D-01 with its date.
    - The CIRC-05 entry likewise cites D-01 and no longer carries the never-to-zero clause, while keeping the rest of its statement.
    - **CIRC-05's trailing parenthetical (site 6) is corrected too.** Its description of what `docs/environmental_restore_check.py` guards must match the assertion task 1 leaves behind - a dim target at or above the configured floor - and the verify script asserts the retired strictness wording is gone from that line. An amendment that cites D-01 while leaving this parenthetical intact fails this task; that combination is what the pre-revision plan would have shipped.
    - Neither amendment restates the retired clause verbatim in order to cite it; the trace names the superseded wording indirectly. The verify greps for it, so a quotation fails.
    - Both amendments are visibly marked as amendments rather than presented as the original text - a reader diffing the file must be able to see that a requirement was revised and why.
    - `docs/CAPABILITY-DECISIONS.md` BD-02 records D-01 as decided on main, supersedes the historical band text, and promotes the Phase 9 addendum from provisional to settled, appending rather than deleting the prior record.
    - The BD-01/BD-02/BD-03 consistency note still reads true after the edit; state in the SUMMARY that it was re-read and why it survives.
    - The `## Constraints` safety bullet in `.claude/CLAUDE.md` no longer calls the relaxed floor provisional or scopes it to an experimental fork, and it still states plainly that the capture-and-restore loop is device-unproven.
    - SAFE-02, SAFE-03 and SAFE-05 are unchanged - assert with `git diff .planning/REQUIREMENTS.md` showing no edit to those three entries.
    - `python3 docs/environmental_restore_check.py` and `python3 docs/phase5_self_check.py` both still exit 0.
  </acceptance_criteria>
  <done>D-01 is recorded as decided in all four documentary records, SAFE-01 carries a traceable amendment and CIRC-05 carries both of its two corrections (statement clause and guard parenthetical), and no requirement outside D-01's scope moved.</done>
</task>

</tasks>

<reversibility rating="costly">
D-01 changes shipped, user-visible safety behaviour: a build carrying a zero floor reaches the
device's true minimum brightness. Reverting requires a Config edit, a rebuild, a re-sign and a
re-import by anyone who already installed - so it is not a cheap undo, but it is a bounded and
fully mechanical one with no data migration and no irreversible side effect. It is **not** one-way,
so no decision checkpoint is inserted; D-01 is in any case a LOCKED user decision and a checkpoint
here would re-litigate it.
</reversibility>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Config literal (build time) → shipped runtime behaviour | A number in the Config literal becomes a physical change to the user's display with no runtime validation between the two. |
| requirement text → future implementer | A requirement that states a false property licenses a future change that a true one would have blocked. |
| iOS device state → shortcut run | Unchanged from 16-01: `Get Device Details` remains the untrusted external reading. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-09 | Denial of Service (of the device's own usability) | a zero brightness target on real hardware | high | mitigate | The mitigation is not floor avoidance, which the evidence retired - it is that the original is captured and durably persisted before the change (plan 16-01) and restored by four independent triggers. The residual claim, that the practical minimum is dim rather than black, is authored as a **backstop** truth and is device-gated; this phase does not assert it. |
| T-16-10 | Tampering | `docs/environmental_restore_check.py` | high | mitigate | Exactly one assertion may be relaxed. The acceptance criteria require a `git diff` showing a single changed assertion, and require the at-or-above-floor assertion to be byte-identical. |
| T-16-11 | Repudiation | `.planning/REQUIREMENTS.md` | medium | mitigate | SAFE-01 and CIRC-05 must carry a visible amendment trace citing D-01 and its date; a silent rewrite fails the acceptance criteria. |
| T-16-12 | Denial of Service / startling output | volume floor and Media scoping | high | mitigate | Explicitly out of D-01's scope and authored as a prohibition. SAFE-02's structural guards - Media scoping at every write site and the allow-volume-increase flag - are asserted unchanged by the environmental checker, which is run in this plan's verify chain. `silence()`'s SAFE-02 comment is additionally protected by a positive assertion in task 1's verify: it must still be present in both rebuilt forks. |
| T-16-13 | Tampering | the two checkers, the checker docstring and the requirement parenthetical disagreeing about the retired clause | medium | mitigate | All are reconciled in the same commit by tasks 1 and 2, so a future reader cannot find one artifact still asserting what another retired. The three descriptions-of-assertions (the env checker module docstring, CIRC-05's parenthetical, and the assertion's own failure message) are each gated by a negative grep, because a description that outlives its assertion is the exact failure this plan was revised to close. |
| T-16-14 | Spoofing (of a safety guarantee, to the user) | `dimming()`'s emitted Shortcuts `comment()` action, rendered 11x per fork | high | mitigate | Before this plan, both signed forks shipped 11 user-visible comment actions each asserting a floor the same build sets to zero - a false safety claim inside the product, not merely in its documentation. Task 1 corrects the single generator string; task 1's verify asserts a count of zero against the **rebuilt** artifact, so the fix cannot be recorded without the rebuild that carries it. |
| T-16-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no external packages; it edits one JSON literal inside an XML file, one generator comment string, two Python checkers and three markdown records. |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces (this plan's share)

**Changed config keys** (the only config keys this phase changes)
- `safety.brightness_floor` — `0.10` → `0`
- `safety.dim_target` — `0.12` → `0`

Both live **only** in `src/PROSOCHE-Dumb.xml`'s Config literal; there is no Python constant.

**Modified symbols / records** (the nine measured sites, mapped to their carriers)
- `tools/build_state_engine.py::dimming` — the emitted `comment()` string corrected (site 9); `silence()` deliberately untouched
- `docs/environmental_restore_check.py` — one relaxed assertion plus its message and narrative comment (site 1), and the module docstring's summary of that assertion (site 2)
- `docs/phase5_self_check.py` — the retired brightness-parameter clause removed or replaced (site 3)
- `.planning/REQUIREMENTS.md` — SAFE-01 amended (site 4); CIRC-05's statement clause (site 5) and its guard parenthetical (site 6) both amended, traceable to D-01
- `docs/CAPABILITY-DECISIONS.md` — BD-02 records D-01 as decided on main (site 7)
- `.claude/CLAUDE.md` — `## Constraints` safety bullet corrected (site 8)
- `src/PROSOCHE-Dumb.xml`, `src/PROSOCHE-Sentient.xml` — rebuilt: the Config literal moves, and 11 emitted comment actions per fork lose the retired clause

**No new files. No new symbols.**
</artifacts_this_phase_produces>

<verification>
- `python3 tools/build_state_engine.py && python3 tools/build_sentient.py` exits 0
- `python3 docs/state_engine_self_check.py && python3 docs/phase5_self_check.py && python3 docs/phase9_self_check.py && python3 docs/environmental_restore_check.py` exits 0
- The Config literal in both built forks reports floor 0 and target 0, asserted by content
- The retired clause occurs **zero** times in each rebuilt fork XML, and `silence()`'s SAFE-02 clause still occurs in both — both asserted after the rebuild in task 1's verify
- Gate A reports `Validation passed.` on both forks
- Gate B run separately and recorded as advisory: expect exit 1 with exactly the one waived line per fork
- `python3 docs/manifest_check.py` expected RED (constraint D-MANIFEST), excluded from the chain
</verification>

<success_criteria>
The floor and the target are both zero in both shipped forks; the retired clause is gone from all
**nine** measured places it was encoded, in one commit, including the eleven-per-fork emitted comment
actions that shipped it to users and the two descriptions-of-assertions that would otherwise have
been falsified by the relaxation; SAFE-01 and CIRC-05 carry visible amendments citing D-01, with
CIRC-05 corrected in both of its two places; and nothing outside D-01's stated scope was weakened —
`silence()`'s SAFE-02 comment in particular is proven untouched.
</success_criteria>

<output>
Create `.planning/phases/16-dimming-and-silence-as-distinct-device-proven-circles/16-03-SUMMARY.md` when done.
Record: the **nine** sites and how each was handled, the measured before/after counts of the retired
clause in both fork XMLs (expected 11 → 0 per fork), the `git diff` evidence that exactly one
assertion was relaxed, the `git diff` evidence that `silence()` is unchanged, confirmation that the
at-or-above-floor assertion is unchanged and still holds at equality, and confirmation that the
BD-01/BD-02/BD-03 consistency note was re-read and survives.
</output>
