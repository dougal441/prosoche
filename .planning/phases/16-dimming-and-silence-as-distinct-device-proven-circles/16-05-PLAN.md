---
phase: 16-dimming-and-silence-as-distinct-device-proven-circles
plan: 05
type: execute
wave: 4
depends_on: ["16-03", "16-04"]
files_modified:
  - src/CONFIG-BLOCK.md
  - docs/CAPABILITY-DECISIONS.md
  - docs/BUILD-NOTES.md
  - docs/retired_clause_check.py
  - .planning/REQUIREMENTS.md
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/todos/pending/2026-08-16-dimming-and-silence-as-distinct-circles.md
  - .claude/CLAUDE.md
autonomous: true
requirements: [SAFE-01, CIRC-05]
must_haves:
  truths:
    - "The RECORD half of locked decision D-01 lands here, split out of plan 16-03 by user decision on 2026-08-18 so that the safety-critical code fix is not held hostage to a documentation sweep that had already failed four times."
    - "The canonical strategy is FROZEN and OUT OF SCOPE. PROSOCHE_Nine_Circles_Canonical_Strategy.md is a historical design input, not a living spec, and no task may modify it. The same freeze applies to every closed prior-phase directory under .planning/phases/, to .planning/debug/resolved/, .planning/todos/completed/, .planning/research/, and to artifacts/ - they are records of what was true then, and rewriting them destroys the audit trail this project depends on."
    - "docs/CAPABILITY-DECISIONS.md BD-02 records the supersession EXPLICITLY: that canonical strategy section 21's floor clause is superseded on the main line by D-01, that the canon is retained unmodified as the original design input, and that BD-02 is the authority where the two disagree. Three of the amended sites cite section 21 as their authority, so this note is what keeps the amended record coherent."
    - "SAFE-01 and CIRC-05 are restated to the real safety property - brightness is only ever changed when its original has been captured AND durably persisted before the change, and is always restored - and both restatements are recorded as requirement amendments traceable to D-01, not as silent edits."
    - "CIRC-05 carries TWO distinct corrections on one line: its statement clause, and its trailing parenthetical describing what the environmental checker guards. An amendment scoped to the clause alone leaves the parenthetical standing and false."
    - "The site list in this plan is MEASURED (2026-08-18, repo-wide, historical paths excluded) and is explicitly NOT PROVEN EXHAUSTIVE. Four prior enumerations of this class each claimed completeness and each was wrong; this one makes no such claim. The repo-scoped gate in task 3 is what makes a fifth undercount VISIBLE rather than silent."
    - "The measured live site list, anchors as of 2026-08-18 and to be located BY CONTENT because anchors shift: R1 src/CONFIG-BLOCK.md the fenced Config JSON safety block :81-82; R2 the brightness-floor table row :136 (four assertions in one row - value, purpose, cited authority, constraint cell); R3 the dim-target table row :137 (value plus constraint cell); R4 docs/CAPABILITY-DECISIONS.md BD-02 Question's quotation of canonical section 11 :44; R5 BD-02 Decision paragraph :54; R6 BD-02 Consequence-for-later-phases :58; R7 BD-02 Phase 9 addendum's provisional and fork-scoped framing :60; R8 docs/BUILD-NOTES.md CAP-16 Fallback cell, self-declared binding on Phase 5's CIRC-05, :204; R9 .planning/REQUIREMENTS.md SAFE-01 :123; R10 REQUIREMENTS CIRC-05 statement clause :66; R11 REQUIREMENTS CIRC-05 trailing parenthetical, same line, separate assertion; R12 .planning/PROJECT.md success criterion :38; R13 .planning/PROJECT.md Constraints Safety bullet :81; R14 .planning/ROADMAP.md phase acceptance criterion :144; R15 ROADMAP Phase 9 criterion 4 :287; R16 ROADMAP Phase 16 goal block :789; R17 ROADMAP plan-list tracking line :815; R18 .claude/CLAUDE.md Constraints Safety bullet :23; R19 .claude/CLAUDE.md capability-audit row 8 :170; R20 .planning/STATE.md Phase 9 decision-log entry :146; R21 .planning/todos/pending/2026-08-16-dimming-and-silence-as-distinct-circles.md :90."
    - "FOUR of those twenty-one sites - R13, R15, R16 and R20 - were found during this revision by re-measuring the repo, and appear in NO prior enumeration of this class. That is the fifth undercount, caught before execution rather than after. R13 in particular is a SECOND site in .planning/PROJECT.md, a file whose first site was already on the list: opening a file is not the same as finishing it."
    - "R17 was discharged at PLAN time on 2026-08-18 when the ROADMAP plan list was rewritten for the six-plan split; the executor re-verifies it rather than re-fixing it."
    - "Every supersession note this plan writes references the retired clause by CITATION - naming where it lived - and never by QUOTATION. A note that quotes what it supersedes is itself a surviving occurrence and the gate reports it. This rule is what makes the gate implementable without an ever-growing allowlist."
    - "A repo-scoped gate exists as a standalone docs/*.py checker: it greps LIVE files for the retired vocabulary and fails listing every surviving occurrence with its file and line, it carries its exclusions as an explicit commented allowlist so a reader can see what was deliberately spared and why, and it carries a comment stating plainly that it CANNOT catch non-lexical encodings."
    - "The gate names docs/phase5_self_check.py:117 as the known non-lexical instance, with the measurement that proved it: a case-insensitive grep of the retired vocabulary over that whole file returns ZERO matches while the site is live, because it encodes the retired rule as a value check rather than a sentence. Nobody may later mistake a green gate for proof the class is empty."
    - "The gate carries a SECOND invariant that fails with the first: src/CONFIG-BLOCK.md's fenced Config JSON and the built forks' Config literal agree on BOTH brightness_floor and dim_target. That is the mechanical assertion that would have caught the CONFIG-BLOCK miss, and it belongs in the same file because the two failures have the same cause - a record drifting from the build it describes."
    - "The gate is registered in 16-VALIDATION.md's full-suite command and in plan 16-06's verify chain, so it runs on every future suite rather than once."
    - "CIRC-05 boundary: the amended CIRC-05 text describes the assertion that actually exists after plan 16-03 - a dim target at or above the configured brightness floor, which holds at equality when both are zero."
    - { statement: "SAFE-01 (unclassified edge, surfaced not dismissed): the practical minimum brightness on real hardware is dim rather than a black or unusable screen, so the amended requirement is safe to ship. This rests on one unrepeated user report and is a device observation this plan cannot make; the amended text must therefore describe the property, not vouch for the hardware.", verification: backstop }
  artifacts:
    - docs/retired_clause_check.py
    - src/CONFIG-BLOCK.md
    - docs/CAPABILITY-DECISIONS.md
    - docs/BUILD-NOTES.md
    - .planning/REQUIREMENTS.md
    - .planning/PROJECT.md
    - .planning/ROADMAP.md
    - .planning/STATE.md
    - .claude/CLAUDE.md
  key_links:
    - "D-01 -> BD-02's explicit section-21 supersession note -> R2, R8 and R19, the three amended sites that cite section 21 as their authority: without the note the amended record is incoherent"
    - "plan 16-03's Config literal change -> src/CONFIG-BLOCK.md's fenced JSON -> the gate's second invariant: the transcription source and the shipped literal must agree, and the gate is what makes that mechanical"
    - "the gate -> 16-VALIDATION.md's full-suite command -> plan 16-06's verify chain: a gate that runs once is a sweep, not a gate"
    - "the gate's allowlist -> the frozen historical record: the exclusions ARE the freeze, expressed in code, so the freeze survives the memory of the person who decided it"
    - "docs/phase5_self_check.py:117 -> the gate's blind-spot comment: the gate's own limits must be written where a reader of the gate will find them"
  prohibitions:
    - "MUST NOT modify PROSOCHE_Nine_Circles_Canonical_Strategy.md. It is frozen by user decision 2026-08-18 and is OUT OF SCOPE for every task in this plan."
    - "MUST NOT modify any closed prior-phase directory under .planning/phases/, nor .planning/debug/resolved/, .planning/todos/completed/, .planning/research/, nor anything under artifacts/. These are historical records; rewriting them destroys the audit trail."
    - "MUST NOT quote the retired clause inside any amendment or supersession note. Cite where it lived instead."
    - "MUST NOT claim this site list is complete. It is measured; it is not proven exhaustive; and the plan says so in its own text."
    - "MUST NOT edit any file under tools/, src/*.xml, or any docs/*.py checker other than the new gate this plan creates. Those belong to plans 16-01, 16-03 and 16-04, and editing one here corrupts a completed plan's evidence."
    - "MUST NOT add an allowlist entry to the gate without a written reason in the same line; an unexplained exclusion is indistinguishable from a defect."
    - "MUST NOT re-open or re-litigate D-01; it is a LOCKED user decision and this plan implements its record half."
---

<objective>
Carry the **RECORD half** of locked decision **D-01**: every documentation, requirement, roadmap
and decision carrier that still asserts the retired brightness-floor clause, plus the repo-scoped
gate that makes the next undercount visible instead of silent.

**Why this plan exists as its own plan.** The D-01 site class was undercounted four times — six,
then eight, then nine, then thirteen-or-more. On 2026-08-18 the user stopped the
enumerate-and-recheck loop and split the work: plan 16-03 keeps the safety-critical CODE fix and
lands on schedule; this plan carries the documentation sweep with the bad track record. The code
fix must not be held hostage to it.

**Why enumeration alone kept failing, and what ends it.** The class is not purely lexical. Measured
2026-08-18: a case-insensitive grep of every retired phrase over `docs/phase5_self_check.py`
returns **zero matches**, yet `:117` is a live site — it encodes the retired rule as a *value
check* with none of the vocabulary. Plan 16-03 fixes that one by hand because only a read reaches
it. So **no grep gate can be the complete answer, and no single enumeration pass has proven
trustworthy.** This plan therefore does both and presents neither alone as complete: a mechanical
gate for the lexical majority, which is self-correcting and re-runs forever, and an explicit
human-reasoned list for the residue.

**This plan's site list is measured, not proven exhaustive — and it says so in its own text.**
Re-measuring the repo during this revision found **four sites that appear in no prior
enumeration**: a *second* site in `.planning/PROJECT.md` (a file whose first site was already on
the list), two more in `.planning/ROADMAP.md`, and one in `.planning/STATE.md`. That is the fifth
undercount, caught before execution rather than after.

**The canonical strategy is frozen.** `PROSOCHE_Nine_Circles_Canonical_Strategy.md` is a historical
design input, not a living spec, and no task may touch it. Instead BD-02 records the supersession
explicitly. That note is load-bearing: three of the amended sites cite canonical §21 as their
authority, so without it the amended record does not cohere.

Purpose: make the live record say what the build actually does, and leave behind the mechanism that
reports the next drift.

Output: twenty-one measured sites corrected across nine files; an explicit §21 supersession note in
BD-02; a new standalone checker carrying two invariants and a commented allowlist; and that checker
wired into the full-suite command and the final plan's verify chain.

**Constraint D-MANIFEST:** `python3 docs/manifest_check.py` remains deliberately red until plan
16-06 re-signs and refreshes it. This plan touches no artifact and must not edit MANIFEST rows.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/16-dimming-and-silence-as-distinct-device-proven-circles/16-CONTEXT.md
@.planning/phases/16-dimming-and-silence-as-distinct-device-proven-circles/16-PATTERNS.md
@.planning/phases/16-dimming-and-silence-as-distinct-device-proven-circles/16-03-SUMMARY.md
@.claude/CLAUDE.md
</context>

<retired_vocabulary_inventory>
## The retired vocabulary — the gate's pattern list, and this plan's work list

Held here rather than inside a task body on purpose: several `<verify>` blocks in this plan
negative-grep these strings, and a plan that echoes a negative-grep literal into an instruction
teaches the executor to write it back into the file being gated.

**Family A — absolute-prohibition phrasings.** Case-insensitive substrings:

```
never zero
never set zero
never to zero
never set to zero
never reaches zero
never be lowered to zero
zero brightness
never `0`
```

**Family B — the retired percentage band.** Case-insensitive substrings, both dash forms:

```
10-15
10–15
```

**Family C — the retired strictness description, SCOPED.** The substring `strictly positive`
counts **only** when `dim_target` or `dim target` appears within a window of **±6 lines**,
case-insensitively.

The window size is measured, not guessed. Swept 2026-08-18 across ±2, ±4, ±6, ±8 and ±10 over
the two files that contain the phrase:

| window | `build_state_engine.py:2084` (epoch — must be excluded) | `environmental_restore_check.py` `:17` / `:250` / `:258` (all real) |
|---|---|---|
| ±2, ±4 | excluded ✓ | `:250` **missed** — its nearest dim-target mention is 6 lines away |
| **±6** | **excluded ✓** | **all three flagged ✓** |
| ±8, ±10 | excluded ✓ | all three flagged ✓ |

±6 is the smallest window that catches every real site, and the false positive stays excluded all
the way out to ±10, so the choice is not delicately balanced. Cite the epoch line by name in a
comment beside the scoping, so the scoping reads as deliberate rather than sloppy. This is the
difference between a gate people run and a gate people disable.

**The known non-lexical residue, which Family A/B/C cannot reach.** `docs/phase5_self_check.py:117`
asserts the brightness parameter is not zero — the retired rule, expressed as a value comparison,
carrying none of the vocabulary above. Measured 2026-08-18: `grep -niE` for every pattern above
over that whole file returns **zero** matches. Plan 16-03 fixes it by hand. The gate must say so
about itself.
</retired_vocabulary_inventory>

<measured_site_list>
## Measured 2026-08-18, repo-wide, historical paths excluded — NOT proven exhaustive

| # | File | Anchor | What is asserted there |
|---|---|---|---|
| R1 | `src/CONFIG-BLOCK.md` | :81-82 | the fenced Config JSON's two safety values; the file's own header calls this block "the transcription source, not a description of one", so after 16-03 it contradicts the shipped artifact |
| R2 | `src/CONFIG-BLOCK.md` | :136 | the brightness-floor table row — **four assertions in one row**: the default value, the purpose text, the cited §21 authority, and the constraint cell's imperative prohibition |
| R3 | `src/CONFIG-BLOCK.md` | :137 | the dim-target table row — the default value and the constraint cell's band requirement |
| R4 | `docs/CAPABILITY-DECISIONS.md` | :44 | BD-02's Question, quoting canonical §11 |
| R5 | `docs/CAPABILITY-DECISIONS.md` | :54 | BD-02's Decision paragraph |
| R6 | `docs/CAPABILITY-DECISIONS.md` | :58 | BD-02's Consequence-for-later-phases |
| R7 | `docs/CAPABILITY-DECISIONS.md` | :60 | BD-02's Phase 9 addendum — provisional framing, fork-scoped, and "main line unaffected" |
| R8 | `docs/BUILD-NOTES.md` | :204 | CAP-16's Fallback cell, which **declares itself binding on Phase 5's CIRC-05** |
| R9 | `.planning/REQUIREMENTS.md` | :123 | SAFE-01's statement |
| R10 | `.planning/REQUIREMENTS.md` | :66 | CIRC-05's statement clause |
| R11 | `.planning/REQUIREMENTS.md` | :66 | CIRC-05's **trailing parenthetical** describing what the environmental checker guards — same line as R10, separate assertion |
| R12 | `.planning/PROJECT.md` | :38 | the project success criterion |
| **R13** | `.planning/PROJECT.md` | :81 | **NEW 2026-08-18** — the `## Constraints` Safety bullet. A **second** site in a file whose first site was already on the list |
| R14 | `.planning/ROADMAP.md` | :144 | a phase acceptance criterion, asserting the clause twice in one sentence |
| **R15** | `.planning/ROADMAP.md` | :287 | **NEW 2026-08-18** — Phase 9's criterion 4, which carries the band vocabulary in the *corrected* framing; substantively already aligned with D-01 but silent about the main-line decision |
| **R16** | `.planning/ROADMAP.md` | :789 | **NEW 2026-08-18** — the Phase 16 goal block, which still says the correction "needs a decision on main". The decision has been taken |
| R17 | `.planning/ROADMAP.md` | :815 | the plan-list tracking line — **discharged at plan time 2026-08-18** during the six-plan rewrite; re-verify, do not re-fix |
| R18 | `.claude/CLAUDE.md` | :23 | the `## Constraints` Safety bullet — "provisional, not confirmed", "experimental fork only" |
| R19 | `.claude/CLAUDE.md` | :170 | capability-audit row 8's guidance cell — a **second** site in a file already opened for R18 |
| **R20** | `.planning/STATE.md` | :146 | **NEW 2026-08-18** — the Phase 9 decision-log entry: "scoped to this experimental fork only", "main line's floor is untouched", "provisional". False on this branch once D-01 lands |
| R21 | `.planning/todos/pending/2026-08-16-dimming-and-silence-as-distinct-circles.md` | :90 | a **pending** todo, therefore live |

**Anchors are measurements and will shift — locate every site by content.** Four of these
(R13, R15, R16, R20) appear in no prior enumeration of this class. The list is measured; it is not
proven exhaustive; task 3's gate is what makes a sixth undercount visible.
</measured_site_list>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Correct the binding authority records — CONFIG-BLOCK, BD-02 with its supersession note, CAP-16, and the two requirements</name>
  <files>src/CONFIG-BLOCK.md, docs/CAPABILITY-DECISIONS.md, docs/BUILD-NOTES.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - This plan's `<retired_vocabulary_inventory>` and `<measured_site_list>` sections — they are the work list for this task and task 2 together. Sites R1 through R11 belong here.
    - `.planning/phases/16-dimming-and-silence-as-distinct-device-proven-circles/16-CONTEXT.md` — the D-01 text in full, including the 2026-08-18 revision block recording the frozen canon, the split, the gate, and the measured-not-exhaustive status.
    - `.planning/phases/16-dimming-and-silence-as-distinct-device-proven-circles/16-03-SUMMARY.md` — what 16-03 actually landed. The amended CIRC-05 parenthetical must describe the assertion that now exists in `docs/environmental_restore_check.py`, so read what that assertion became rather than what this plan predicted.
    - `src/CONFIG-BLOCK.md` — the "How to use this file" header (its own claim that the fenced block is the transcription source), the fenced Config JSON, and the key-by-key table. Note that the two rows in scope each pack several separate assertions into one table row.
    - `docs/CAPABILITY-DECISIONS.md` — the BD-02 entry in full plus its Phase 9 addendum, and the BD-01/BD-02/BD-03 consistency note, which is binding on all three and must still read true afterwards.
    - `docs/BUILD-NOTES.md` — CAP-16's row and §2's do-not-fabricate protocol; and how this file already records superseding entries, so the correction is appended in its established form.
    - `.planning/REQUIREMENTS.md` — the SAFE-01 and CIRC-05 entries, and the surrounding entries' formatting: CIRC-03, CIRC-05 and SAFE-05 already carry parenthetical Phase-10 amendment notes. Copy that form.
  </read_first>
  <action>
Correct sites R1 through R11. Locate every one by content, never by line number.

**R1 — the fenced Config JSON in `src/CONFIG-BLOCK.md`.** Bring both safety values into agreement
with what plan 16-03 shipped. This file's own header claims the fenced block is the transcription
source rather than a description of one, so leaving it stale makes the file assert something about
the build that is not true. Task 3 adds the mechanical cross-check that would have caught this;
here, fix it.

**R2 and R3 — the two table rows.** Each row packs several independent assertions into one line:
the default value, the purpose text, the cited authority, and the constraint cell. Correct **all**
of them, not just the value. The constraint cells currently carry imperative prohibitions that D-01
retires; replace them with the constraint that actually holds — the dim target stays at or above
the configured brightness floor, which is exactly what `docs/environmental_restore_check.py`
asserts after 16-03. The cited-authority cells point at canonical §21; leave the citation and add
the supersession pointer to BD-02, because the canon is frozen and BD-02 is now the authority.

**R4 through R7 — BD-02, and the supersession note the whole amended record depends on.** Append
rather than rewrite, matching how this file already records superseding decisions, and record:

1. D-01 as **decided on the main line**, with its date, superseding BD-02's own historical
   band-and-floor text and promoting the Phase 9 addendum from provisional and fork-scoped to
   settled.
2. **The explicit canonical-strategy supersession**, which is a named deliverable of this plan and
   not a nicety. State that canonical strategy §21's floor clause is superseded on the main line by
   D-01; that the canonical strategy is **retained unmodified** as the original design input and is
   deliberately not edited; and that **BD-02 is the authority where the two disagree.** Three of
   the sites this plan amends (R2, R8, R19) cite §21 as their authority, so this note is what keeps
   the amended record coherent rather than merely consistent-looking.
3. That the Question paragraph's quotation of canonical §11 is a quotation of a frozen source and
   is retained as such, now read through the supersession note above.

Then re-read the BD-01/BD-02/BD-03 consistency note and confirm it still reads true — all three are
the same canonical rule applied to three evidentiary outcomes, and that framing should survive
D-01, because D-01 changes a bound and not the rule. Record in the SUMMARY that it was re-read and
why it survives. If it does **not** survive, say so and stop rather than quietly patching it.

**R8 — CAP-16's Fallback cell in `docs/BUILD-NOTES.md`.** This cell declares itself binding on
Phase 5's CIRC-05, so a stale cell is not a note, it is an instruction. Correct it to the property
and point it at BD-02's supersession note. Append the correction in the file's established
superseding form; do not delete the historical text.

**R9 — SAFE-01 in `.planning/REQUIREMENTS.md`.** Its current text asserts a bound that is literally
false under D-01. Replace it with the real safety property: brightness is only ever changed when
its original value has been captured **and durably persisted** before the change, and is always
restored. Record it as a **requirement amendment traceable to D-01**, in the parenthetical form the
file already uses for its Phase-10 amendments — naming the decision, the date, and the reason the
previous wording was retired: avoiding a particular value was never itself the safety property;
capture-and-restore reliability is.

**R10 and R11 — CIRC-05, which needs TWO corrections on one line.** Do not treat "amend the clause
and keep the rest" as sufficient; the rest contains a second false assertion.

*R10, the statement clause.* Amend it to: dimming reduces brightness only when reversible —
meaning only when the original has been captured and persisted — otherwise degrading safely.

*R11, the trailing parenthetical.* The Phase 10 parenthetical closes by describing what
`docs/environmental_restore_check.py` guards, and its description asserts a strictness that plan
16-03 removed. Read the checker as 16-03 left it and rewrite the parenthetical to describe the
assertion that now exists. Keep the rest of the parenthetical — the Phase 10 note about degrading
safely being the per-run fallback, and the cancelled cut, are unrelated to D-01 and stay.

Note in both amendments that the durable-persistence half of the restated property is what plan
16-01 made true; before it, the property could not have been satisfied by any build. That is what
makes this restatement honest rather than a weakening.

**Write every amendment and supersession note by citation, never by quotation.** Name where the
retired clause lived — BD-02's original Decision paragraph, canonical strategy §21 — and say it is
superseded by D-01. Do not reproduce the retired sentence inside the note that retires it. A
retired clause quoted inside its own amendment is still a live false assertion sitting in the file,
and task 3's gate reports it as one. The verify below greps for it.

Leave SAFE-02, SAFE-03 and SAFE-05 alone. SAFE-02's volume constraint is explicitly unchanged by
D-01, and SAFE-03 and SAFE-05 already state the capture-and-restore property this amendment aligns
the other two to.

**Do not touch `PROSOCHE_Nine_Circles_Canonical_Strategy.md`.** It is frozen. That is the whole
reason BD-02 carries the supersession note.
  </action>
  <verify>
    <automated>bash -c 'set -e; python3 - &lt;&lt;"PY"
import pathlib, re, subprocess
NL = chr(10)
DASH = chr(8211)
BANNED = ("never zero", "never set zero", "never to zero", "never set to zero",
          "never reaches zero", "never be lowered to zero", "zero brightness",
          "never `0`", "10-15", "10" + DASH + "15")

def scan(path, needle_desc):
    text = pathlib.Path(path).read_text()
    return [(i + 1, l) for i, l in enumerate(text.splitlines())
            for b in BANNED if b in l.lower()]

# R1-R3: the transcription source must agree with the build and must stop asserting the bound.
cb = pathlib.Path("src/CONFIG-BLOCK.md").read_text()
block = re.search("```json(.+?)```", cb, re.S)
assert block, "could not locate the fenced Config JSON in src/CONFIG-BLOCK.md"
import json
cfg = json.loads(block.group(1))["safety"]
assert cfg["brightness_floor"] == 0, "CONFIG-BLOCK fenced JSON brightness_floor is " + repr(cfg["brightness_floor"]) + ", expected 0 (R1)"
assert cfg["dim_target"] == 0, "CONFIG-BLOCK fenced JSON dim_target is " + repr(cfg["dim_target"]) + ", expected 0 (R1)"
assert cfg["allow_volume_increase"] is False, "CONFIG-BLOCK allow_volume_increase moved; D-01 does not touch it"

# R1-R11: no banned phrasing survives in any of the four files this task owns, and each cites D-01.
for path, sites in (("src/CONFIG-BLOCK.md", "R1-R3"),
                    ("docs/CAPABILITY-DECISIONS.md", "R4-R7"),
                    ("docs/BUILD-NOTES.md", "R8"),
                    (".planning/REQUIREMENTS.md", "R9-R11")):
    hits = scan(path, sites)
    assert not hits, path + " (" + sites + ") still carries the retired vocabulary at lines " + str([n for n, _ in hits]) + " - an amendment that quotes what it supersedes is itself a surviving occurrence"
    assert "D-01" in pathlib.Path(path).read_text(), path + " does not cite D-01"

# R4-R7: the section-21 supersession note is a NAMED deliverable, so it is asserted specifically.
cap = pathlib.Path("docs/CAPABILITY-DECISIONS.md").read_text()
low = cap.lower()
assert "supersede" in low, "BD-02 does not record a supersession at all"
assert "21" in cap, "BD-02 does not name canonical strategy section 21 as the superseded authority"
assert "unmodified" in low or "not edited" in low or "retained" in low, "BD-02 does not record that the canonical strategy is retained unmodified as the original design input"

# R9-R11: two entries, three corrections, all traceable.
req = pathlib.Path(".planning/REQUIREMENTS.md").read_text()
for rid in ("SAFE-01", "CIRC-05"):
    lines = [l for l in req.splitlines() if "**" + rid + "**" in l]
    assert len(lines) == 1, "expected exactly one " + rid + " entry, found " + str(len(lines))
    body = lines[0].lower()
    assert "d-01" in body, rid + " amendment does not cite D-01"
    assert "persist" in body, rid + " restatement does not name durable persistence"
    # R11 specifically: the parenthetical describing the environmental checker guard.
    assert "strictly positive" not in body, rid + " still describes the dim-target guard as a strictness test - R11 was not corrected"

# This task owns four files and no code. Anchor on one of its own, then forbid the rest.
files = [p for p in subprocess.run(["git", "show", "--name-only", "--format=", "HEAD"],
                                   capture_output=True, text=True, check=True).stdout.splitlines() if p]
assert files, "HEAD carries no file list - commit this task before running its verify"
OWNED = ("src/CONFIG-BLOCK.md", "docs/CAPABILITY-DECISIONS.md", "docs/BUILD-NOTES.md", ".planning/REQUIREMENTS.md")
assert [p for p in files if p in OWNED], "HEAD is not this task commit - it carries none of " + str(OWNED)
frozen = [p for p in files if p.startswith("artifacts/") or p.startswith(".planning/research/")
          or p.startswith(".planning/debug/resolved/") or p.startswith(".planning/todos/completed/")
          or "Canonical_Strategy" in p or p.startswith("tools/") or p.startswith("src/PROSOCHE-")]
assert not frozen, "this task commit touches frozen or out-of-scope paths: " + str(frozen)
print("R1-R11 corrected; BD-02 records the section-21 supersession and the canon retained unmodified; no frozen path touched")
PY'</automated>
  </verify>
  <acceptance_criteria>
    - `src/CONFIG-BLOCK.md`'s fenced Config JSON reports a brightness floor of 0 and a dim target of 0, parsed as JSON by the verify rather than pattern-matched, and its other two safety keys are unchanged.
    - Both `src/CONFIG-BLOCK.md` table rows are corrected in **every** cell that carried an assertion — value, purpose, authority pointer and constraint — not only the value cell. R2 packs four assertions into one row and a value-only edit leaves three standing.
    - `docs/CAPABILITY-DECISIONS.md` BD-02 records D-01 as decided on main, supersedes its own historical band-and-floor text, and promotes the Phase 9 addendum from provisional to settled, appending rather than deleting the prior record.
    - **BD-02 carries the explicit canonical-strategy supersession note** — that §21's floor clause is superseded on the main line by D-01, that the canonical strategy is retained unmodified as the original design input, and that BD-02 is the authority where the two disagree. Asserted specifically by the verify, because three amended sites cite §21 and without this note the amended record does not cohere.
    - The BD-01/BD-02/BD-03 consistency note still reads true; the SUMMARY records that it was re-read and why it survives. If it does not survive, the task halts and reports rather than patching it.
    - `docs/BUILD-NOTES.md` CAP-16's Fallback cell no longer instructs a bound and points at BD-02's supersession note; the historical text is appended to, not deleted.
    - The SAFE-01 entry states the capture-and-durably-persist-then-restore property and cites D-01 with its date.
    - **CIRC-05 carries both of its two corrections** — the statement clause and the trailing parenthetical. The parenthetical's description of what `docs/environmental_restore_check.py` guards matches the assertion 16-03 actually left behind. An amendment that cites D-01 while leaving the parenthetical intact fails this task.
    - No amendment or supersession note reproduces the retired clause verbatim; the verify greps all four files for the retired vocabulary and a quotation fails.
    - Both requirement amendments are visibly marked as amendments — a reader diffing the file can see that a requirement was revised and why.
    - SAFE-02, SAFE-03 and SAFE-05 are unchanged — assert with `git diff .planning/REQUIREMENTS.md` showing no edit to those three entries.
    - The task commit touches no frozen path and no generator, artifact or checker file — asserted by the verify against `git show --name-only HEAD`, anchored first on one of this task's own declared files so it cannot pass against an earlier commit.
  </acceptance_criteria>
  <done>The four binding authority records state the property rather than the retired bound, BD-02 carries the explicit §21 supersession note the rest of the amended record depends on, and CIRC-05 carries both of its corrections.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Correct the project-level and tracking records, including the four sites no prior enumeration found</name>
  <files>.planning/PROJECT.md, .planning/ROADMAP.md, .planning/STATE.md, .claude/CLAUDE.md, .planning/todos/pending/2026-08-16-dimming-and-silence-as-distinct-circles.md</files>
  <read_first>
    - This plan's `<measured_site_list>` — sites R12 through R21 belong to this task, and four of them (R13, R15, R16, R20) appear in no prior enumeration.
    - `.planning/PROJECT.md` — **both** the success-criteria list and the `## Constraints` block. This file carries the class **twice**, and only one of the two was ever on a list before this revision. Read both before editing either.
    - `.planning/ROADMAP.md` — **three** live occurrences: a phase acceptance criterion, Phase 9's criterion 4, and the Phase 16 goal block. Read each in its surrounding context, because they need different treatments: one is false, one is substantively already aligned but silent about the main-line decision, and one says a decision is still outstanding that has since been taken.
    - `.planning/STATE.md` — the Accumulated Context decision-log entry from Phase 9. Read how this file records superseding decisions elsewhere before choosing how to mark it.
    - `.claude/CLAUDE.md` — **both** the `## Constraints` Safety bullet and capability-audit row 8's guidance cell. A second site in a file already opened for the first is the exact shape that produced two of the four prior undercounts.
    - `.planning/todos/pending/2026-08-16-dimming-and-silence-as-distinct-circles.md` — a pending todo, therefore live rather than historical.
    - `.planning/phases/16-dimming-and-silence-as-distinct-device-proven-circles/16-03-SUMMARY.md` — so the corrected text describes what actually shipped.
  </read_first>
  <action>
Correct sites R12 through R21. Locate every one by content.

**R12 and R13 — `.planning/PROJECT.md`, twice.** The success criterion and the `## Constraints`
Safety bullet each assert the retired bound independently. R13 was found by re-measuring during
this revision and appears in no prior enumeration of this class. Correct both to the property:
every environmental change is captured and durably persisted before it is applied and reliably
restored, and any setting whose original cannot be captured is left unchanged. Do not replace one
bound with a gentler bound.

**R14 — the ROADMAP phase acceptance criterion.** It asserts the clause twice in a single sentence,
once about Dimming specifically and once in the across-all-primitives summary. Correct both. Keep
everything about volume and accessibility exactly as it is; D-01 is brightness-only.

**R15 — ROADMAP Phase 9 criterion 4.** This one is substantively *already* aligned with D-01: it
already says brightness may target the device's true minimum. What it lacks is the main-line
settlement — it reads as a fork-scoped correction dated to Phase 9. Add the pointer that D-01
settled it on main, and remove the retired band vocabulary while keeping the criterion's meaning
and its date intact. This is a closed phase's criterion inside a living document: amend it visibly,
do not rewrite its history.

**R16 — the ROADMAP Phase 16 goal block.** It still says the correction "needs a decision on main".
That decision has been taken. Record D-01 as taken, with its date, and reference the plan that
implemented each half.

**R17 — the ROADMAP plan-list tracking line.** Discharged at plan time on 2026-08-18 when the plan
list was rewritten for the six-plan split. **Re-verify it rather than re-fixing it**, and record in
the SUMMARY that it was already correct on arrival. If it is not, that is a finding worth stating.

**R18 and R19 — `.claude/CLAUDE.md`, twice.**

*R18, the `## Constraints` Safety bullet.* It frames the relaxed floor as unconfirmed and
provisional, scopes it to an experimental fork, and makes it contingent on a Phase 9 device proof
that never happened. Two things are now stale: the decision has been taken on main by D-01, and the
contingency it names concerned a capture-and-restore loop that plan 16-01 made structurally sound
but which remains device-unproven. Write it so **both** facts survive — the decision is settled,
and the device proof is still outstanding and tracked by this phase's UAT instrument. Do not
overstate: nothing in this phase makes the loop device-proven.

*R19, capability-audit row 8's guidance cell.* Its guidance for the Dimming primitive still names a
bound and a prototype band as the value to use. Correct it to the property and point at BD-02.
Note explicitly in the SUMMARY that this was a **second** site in a file already opened for R18 —
that shape produced two of the four prior undercounts, and naming it is how the next reader avoids
repeating it.

**R20 — the `.planning/STATE.md` Phase 9 decision-log entry.** It records the correction as scoped
to the experimental fork, leaves the main line untouched, and calls it provisional. All three are
false on this branch once D-01 lands. This is a decision *log*, so mark it **superseded in place**
in whatever form this file already uses for superseding decisions, and add the D-01 entry as the
current record. Do not delete the Phase 9 entry — the log's value is that it shows what was
believed when.

**R21 — the pending todo.** It is pending, therefore live. Record that D-01 settled the item, with
its date and the plans that implemented it, rather than deleting the paragraph.

**Everything by citation, never by quotation** — the same rule as task 1, for the same reason: task
3's gate reports a quoted clause as a surviving occurrence, and the verify below greps these files.

**Frozen and out of scope, in this task as in every other:** the canonical strategy, all closed
prior-phase directories, `.planning/debug/resolved/`, `.planning/todos/completed/`,
`.planning/research/`, and `artifacts/`.
  </action>
  <verify>
    <automated>bash -c 'set -e; python3 - &lt;&lt;"PY"
import pathlib, subprocess
DASH = chr(8211)
BANNED = ("never zero", "never set zero", "never to zero", "never set to zero",
          "never reaches zero", "never be lowered to zero", "zero brightness",
          "never `0`", "10-15", "10" + DASH + "15")
TODO = ".planning/todos/pending/2026-08-16-dimming-and-silence-as-distinct-circles.md"

for path, sites in ((".planning/PROJECT.md", "R12-R13"),
                    (".planning/ROADMAP.md", "R14-R17"),
                    (".planning/STATE.md", "R20"),
                    (".claude/CLAUDE.md", "R18-R19"),
                    (TODO, "R21")):
    text = pathlib.Path(path).read_text()
    hits = [(i + 1, b) for i, l in enumerate(text.splitlines())
            for b in BANNED if b in l.lower()]
    assert not hits, path + " (" + sites + ") still carries the retired vocabulary at " + str(hits) + " - locate by content, and cite rather than quote"
    assert "D-01" in text, path + " does not cite D-01"

# R13 and R19 are SECOND sites in files whose first site was already known. Assert each file
# was corrected in BOTH of its regions rather than in the one that was on the older list.
proj = pathlib.Path(".planning/PROJECT.md").read_text().lower()
assert "captur" in proj and "restor" in proj, "PROJECT.md does not state the capture-and-restore property"
cl_lines = pathlib.Path(".claude/CLAUDE.md").read_text().splitlines()
safety = [l for l in cl_lines if l.startswith("- **Safety**")]
assert len(safety) == 1, "expected exactly one Constraints Safety bullet, found " + str(len(safety))
assert "provisional, not confirmed" not in safety[0], "R18: the Safety bullet still frames the floor as unsettled"
assert "experimental fork only" not in safety[0], "R18: the Safety bullet still scopes the correction to the experimental fork"
assert "D-01" in safety[0], "R18: the Safety bullet does not cite D-01"
row8 = [l for l in cl_lines if l.lstrip().startswith("| 8 |")]
assert len(row8) == 1, "expected exactly one capability-audit row 8, found " + str(len(row8))
assert "D-01" in row8[0] or "BD-02" in row8[0], "R19: capability-audit row 8 was not reconciled - it is the SECOND site in a file already opened for R18, and that shape caused two of the four prior undercounts"

# R17 was discharged at plan time; assert it stayed discharged.
road = pathlib.Path(".planning/ROADMAP.md").read_text()
assert "all six sites" not in road, "R17: the stale six-site tracking line reappeared in the ROADMAP"

files = [p for p in subprocess.run(["git", "show", "--name-only", "--format=", "HEAD"],
                                   capture_output=True, text=True, check=True).stdout.splitlines() if p]
assert files, "HEAD carries no file list - commit this task before running its verify"
OWNED = (".planning/PROJECT.md", ".planning/ROADMAP.md", ".planning/STATE.md", ".claude/CLAUDE.md", TODO)
assert [p for p in files if p in OWNED], "HEAD is not this task commit - it carries none of " + str(OWNED)
frozen = [p for p in files if p.startswith("artifacts/") or p.startswith(".planning/research/")
          or p.startswith(".planning/debug/resolved/") or p.startswith(".planning/todos/completed/")
          or "Canonical_Strategy" in p or p.startswith("tools/") or p.startswith("src/PROSOCHE-")]
assert not frozen, "this task commit touches frozen or out-of-scope paths: " + str(frozen)
print("R12-R21 corrected across five files, including the four sites no prior enumeration found; no frozen path touched")
PY'</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/PROJECT.md` is corrected in **both** of its regions — the success criterion and the `## Constraints` Safety bullet. R13 is the second of the two and appears in no prior enumeration; a diff touching only the first fails this task.
    - `.planning/ROADMAP.md` is corrected at **all three** live sites: the phase acceptance criterion (which asserts the clause twice in one sentence, both corrected), Phase 9's criterion 4 (amended visibly with the main-line settlement, its date and meaning intact), and the Phase 16 goal block (D-01 recorded as taken rather than outstanding).
    - The ROADMAP plan-list tracking line is re-verified as already discharged at plan time, and the SUMMARY says so. If it was not already correct, that is recorded as a finding.
    - `.claude/CLAUDE.md` is corrected in **both** of its regions — the Safety bullet and capability-audit row 8 — and the verify asserts each independently. The SUMMARY names explicitly that row 8 was a second site in an already-opened file, since that shape produced two of the four prior undercounts.
    - The Safety bullet states **both** that the decision is settled on main **and** that the capture-and-restore loop remains device-unproven, tracked by this phase's UAT instrument. Nothing in this phase makes it device-proven and the bullet must not imply otherwise.
    - `.planning/STATE.md`'s Phase 9 decision-log entry is marked **superseded in place** with the D-01 entry added as the current record; the Phase 9 entry is not deleted.
    - The pending todo records D-01 as settling the item, with the date and the implementing plans, rather than being deleted.
    - No corrected text reproduces the retired clause verbatim; the verify greps all five files.
    - The task commit touches no frozen path and no generator, artifact or checker file — asserted against `git show --name-only HEAD`, anchored first on one of this task's own declared files.
  </acceptance_criteria>
  <done>All ten project-level and tracking sites state the property, the two double-sited files are corrected in both regions, and the decision log is superseded in place rather than rewritten.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Build the repo-scoped gate with its commented allowlist, its stated blind spot, and the record-agrees-with-build cross-check</name>
  <files>docs/retired_clause_check.py, .planning/phases/16-dimming-and-silence-as-distinct-device-proven-circles/16-VALIDATION.md</files>
  <read_first>
    - This plan's `<retired_vocabulary_inventory>` — the exact pattern families, the scoping rule for family C, and the measured non-lexical residue. It is the gate's specification.
    - `docs/note_identity_check.py` in full — the shape analog and the closest precedent in this repo. Note its "TWO INVARIANTS, ONE FILE, because they fail together" framing, its module docstring stating the defect and its consequence, its `ROOT = Path(__file__).resolve().parents[1]` idiom, its named constants with reasons, its read-only no-subprocess discipline, and its terminal passed line.
    - `docs/sequence_dispatch_check.py` — `config_literal()`, the project-canonical locate-and-parse reader for the built Config literal. Reuse it; do not write a fourth reader for the same literal.
    - `.planning/phases/16-dimming-and-silence-as-distinct-device-proven-circles/16-PATTERNS.md` — the standalone-checker conventions and the "Comments that own a number" shared pattern.
    - `.planning/phases/16-dimming-and-silence-as-distinct-device-proven-circles/16-VALIDATION.md` — the full-suite command block and the note about gate B never being chained.
    - `docs/phase5_self_check.py` around its per-action brightness assertion, as plan 16-03 left it — the gate's blind-spot comment cites this file, so the citation must point somewhere real.
  </read_first>
  <action>
Create `docs/retired_clause_check.py` as a standalone checker in this project's established style:
a module docstring that states the defect and its consequence, module-level constants with reasons
attached, read-only, no subprocess, no rebuild, and a terminal passed line.

Carry **two invariants in one file, because they fail together** — the same framing
`docs/note_identity_check.py` already establishes here. Both failures have one cause: a record
drifting from the build it describes.

**Invariant 1 — no live file still asserts the retired clause.** Walk the repository, read text
files, and report **every** surviving occurrence of the retired vocabulary with its file and line.
Take the pattern families and the family-C scoping rule verbatim from this plan's
`<retired_vocabulary_inventory>`; that section exists so the list is specified in one place. Family
C must be scoped to a nearby dim-target mention, because unscoped it produces a known false
positive in the generator that describes a strictly positive epoch — cite that measured false
positive in a comment beside the scoping, so the scoping reads as deliberate rather than sloppy.

Fail with a single report listing every occurrence, not the first one. This class has been
under-fixed four times by fixing what was visible and re-measuring afterwards; a gate that reports
one site per run reproduces exactly that loop.

**The allowlist is explicit, commented, and part of the deliverable.** Do not bury the exclusions in
a walk condition. Hold them as a named constant where each entry carries its reason inline, so a
reader can see what was deliberately spared and why rather than inferring it. Two tiers:

*Tier 1, path prefixes — the frozen historical record.* The canonical strategy, which is a
historical design input and not a living spec, frozen by user decision 2026-08-18. Everything under
`.planning/phases/`, which are dated per-phase planning records — this subsumes both the closed
prior-phase directories and this phase's own artifacts, which quote the retired clause precisely in
order to describe its retirement. `.planning/debug/resolved/`, `.planning/todos/completed/` and
`.planning/research/`, all closed records. Everything under `artifacts/`, which is archived and
signed and whose bytes are pinned by manifest digests. The version-control directory. And the gate's
own source file, which necessarily holds the pattern list. State the tradeoff of the
`.planning/phases/` exclusion honestly in its comment: it is broad, and it is chosen because those
files are dated records of intent rather than live authority.

*Tier 2, anchored per-site entries.* A file path plus a short distinguishing substring plus a
reason, for any live occurrence deliberately spared. Anchor on content, never on a line number,
because every anchor in this phase has shifted. Require a reason on every entry; an unexplained
exclusion is indistinguishable from a defect. Seed this tier empty if tasks 1 and 2 left nothing
live, and record in the SUMMARY every entry that was added and why.

**State the gate's own limit where a reader of the gate will find it.** Add a comment saying plainly
that this check **cannot catch non-lexical encodings**, citing `docs/phase5_self_check.py` and the
measurement that proves it: a case-insensitive grep of every pattern here over that whole file
returns zero matches, while the site it carried was live and was fixed by hand in plan 16-03.
Nobody may later read a green gate as proof the class is empty.

**Invariant 2 — the record agrees with the build.** Parse the fenced Config JSON from
`src/CONFIG-BLOCK.md` and the Config literal from **both** built forks using
`sequence_dispatch_check.config_literal`, and assert all three agree on the brightness floor and the
dim target. This is the assertion that would have caught the CONFIG-BLOCK drift mechanically instead
of by a fifth enumeration pass. Do not pin the values themselves — pin the **agreement**, so the
check keeps working the next time a tuning value legitimately changes.

Finally, register the gate. Add it to the full-suite command in `16-VALIDATION.md`, positioned with
the other `docs/*.py` checkers, and add a line noting that it lands in plan 16-05 and is therefore
expected absent before wave 4 — the same shape as the existing D-MANIFEST deliberate-red note, so a
reader running the suite mid-phase is not misled. Plan 16-06 carries it in its own verify chain.
  </action>
  <verify>
    <automated>bash -c 'set -e; python3 docs/retired_clause_check.py; python3 - &lt;&lt;"PY"
import pathlib, subprocess, sys
src = pathlib.Path("docs/retired_clause_check.py").read_text()
low = src.lower()

# The allowlist is part of the deliverable, not an implementation detail.
for token in ("canonical", "artifacts/", ".planning/phases/", "debug/resolved", "todos/completed", "research/"):
    assert token in src, "the gate allowlist does not name " + token + " - the freeze must be visible in the gate, with its reason"
# The stated blind spot: a green gate must never be readable as proof the class is empty.
assert "phase5_self_check" in src, "the gate does not name the known non-lexical instance"
assert "non-lexical" in low or "not lexical" in low, "the gate does not state that it cannot catch non-lexical encodings"
# Family C scoping, and the measured false positive that motivates it.
assert "strictly positive" in low, "the gate does not carry the retired strictness pattern"
assert "epoch" in low, "the gate does not cite the measured false positive that motivates scoping the strictness pattern"
# Invariant 2 must reuse the canonical reader rather than a fourth hand-rolled one.
assert "config_literal" in src, "the gate does not reuse sequence_dispatch_check.config_literal for the built Config literal"
assert "CONFIG-BLOCK" in src, "the gate does not cross-check the transcription source against the build"

# The gate must have TEETH: injecting a retired phrase into a live file makes it fail.
probe = pathlib.Path("docs/_retired_clause_probe.md")
assert not probe.exists(), "probe path already exists - choose another"
probe.write_text("Synthetic control for docs/retired_clause_check.py: never set to zero." + chr(10))
try:
    r = subprocess.run([sys.executable, "docs/retired_clause_check.py"], capture_output=True, text=True)
    assert r.returncode != 0, "the gate PASSED against an injected live occurrence - it is decoration, not a gate"
    assert "_retired_clause_probe" in (r.stdout + r.stderr), "the gate failed but did not name the offending file and line"
finally:
    probe.unlink()
r = subprocess.run([sys.executable, "docs/retired_clause_check.py"], capture_output=True, text=True)
assert r.returncode == 0, "the gate did not return to green after the probe was removed: " + r.stdout + r.stderr

val = pathlib.Path(".planning/phases/16-dimming-and-silence-as-distinct-device-proven-circles/16-VALIDATION.md").read_text()
assert "retired_clause_check.py" in val, "the gate is not registered in the 16-VALIDATION full-suite command"
print("gate is green, names its allowlist and its blind spot, cross-checks the record against the build, demonstrably fires on an injected occurrence, and is registered in the full suite")
PY'</automated>
  </verify>
  <acceptance_criteria>
    - `python3 docs/retired_clause_check.py` exits 0 against the repository as tasks 1 and 2 leave it, and prints its passed line.
    - The gate **demonstrably fires**: injecting a retired phrase into a live file makes it exit non-zero and name that file and line, and removing the injection returns it to green. A guard that cannot be made to fire is decoration; the verify performs this control automatically.
    - The gate reports **every** surviving occurrence in one run, not the first. Confirm by injecting two occurrences in different files and checking both are named; record the output in the SUMMARY.
    - The allowlist is an explicit, commented constant naming the frozen canonical strategy, `.planning/phases/`, `.planning/debug/resolved/`, `.planning/todos/completed/`, `.planning/research/`, `artifacts/` and the gate's own file, each with its reason inline. The breadth of the `.planning/phases/` exclusion is stated honestly in its comment.
    - Every Tier-2 anchored entry, if any were needed, carries a written reason and anchors on content rather than a line number. The SUMMARY lists each one and why it was spared.
    - The gate carries a comment stating plainly that it **cannot** catch non-lexical encodings, citing `docs/phase5_self_check.py` and the zero-match measurement that proves it.
    - The retired-strictness pattern is scoped to a **±6 line** window around a dim-target mention — the measured smallest window that catches all three real sites while excluding the generator's strictly-positive-*epoch* line, per the sweep table in `<retired_vocabulary_inventory>`. The comment beside the scoping cites that false positive by name and records why ±6 rather than a tighter window.
    - The second invariant asserts that `src/CONFIG-BLOCK.md`'s fenced JSON and **both** built forks' Config literals agree on the brightness floor and the dim target, reusing `sequence_dispatch_check.config_literal` rather than a fourth hand-rolled reader, and pinning the **agreement** rather than the values.
    - `16-VALIDATION.md`'s full-suite command includes the gate, with a note that it lands in this plan and is expected absent before wave 4.
    - The plan's own text and this plan's SUMMARY both state that the site list is measured and **not** proven exhaustive, and that the gate is what makes a further undercount visible rather than silent.
  </acceptance_criteria>
  <done>A standalone gate exists that reports every lexical survivor with file and line, carries a visible reasoned allowlist and an honest statement of its own blind spot, mechanically pins the transcription source to the shipped literal, is proven able to fire, and runs in the full suite from now on.</done>
</task>

</tasks>

<reversibility rating="reversible">
Every change in this plan is text in a record or a new standalone checker. Nothing shipped, nothing
signed, no state shape and no device behaviour moves. Reverting is a git revert with no rebuild, no
re-sign and no migration. The decision being recorded (D-01) is itself `costly` and is carried by
plan 16-03; recording it is not.
</reversibility>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a record → a future implementer | A record that states a false property licenses a change a true one would have blocked, and several of these records declare themselves binding on later phases. |
| a green gate → a reader's belief about the class | A lexical gate that does not state its own limits is read as proof of completeness it cannot provide. |
| the frozen historical record → a well-meaning sweep | A sweep that corrects history destroys the audit trail that makes every prior decision reviewable. |
| `src/CONFIG-BLOCK.md` → the shipped Config literal | The file declares itself the transcription source, so drift between the two is a lie about the build, not a stale note. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-22 | Repudiation | a fifth silent undercount of the D-01 site class | high | mitigate | The class has been under-counted four times. This plan pairs a measured human-reasoned list with a repo-scoped gate and states explicitly that neither alone is complete. The gate reports every survivor in one run and is proven able to fire by an injected control, so the next drift is visible rather than discovered by a sixth re-measurement. |
| T-16-23 | Spoofing (of completeness) | a green gate read as proof the class is empty | high | mitigate | `docs/phase5_self_check.py:117` is measured non-lexical — zero grep matches over the whole file while the site was live. The gate carries a comment naming it and stating plainly that non-lexical encodings are outside its reach. |
| T-16-24 | Tampering | the frozen historical record | high | mitigate | The canonical strategy, closed phase directories, resolved debug, completed todos, research and archived artifacts are authored as prohibitions, enforced in both task verifies against the task's own commit, and encoded as the gate's Tier-1 allowlist — so the freeze survives the memory of the person who decided it. Rewriting `artifacts/` would additionally invalidate manifest digests. |
| T-16-25 | Tampering | an amendment that quotes the clause it retires | medium | mitigate | Citation-not-quotation is authored as a prohibition and gated: every touched file is grepped for the retired vocabulary after the amendment, so a supersession note that reproduces what it supersedes fails its own task. |
| T-16-26 | Repudiation | `src/CONFIG-BLOCK.md` drifting from the shipped Config literal | medium | mitigate | The file declares itself the transcription source. The gate's second invariant asserts agreement between it and both built forks on both keys, pinning the agreement rather than the values so it survives future legitimate tuning changes. |
| T-16-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no external packages. The new checker is stdlib only (`pathlib`, `json`, `re`) plus one import of an existing project checker; `16-RESEARCH.md` § Package Legitimacy Audit records the phase as installing nothing. |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces (this plan's share)

**New files**
- `docs/retired_clause_check.py` — the repo-scoped gate, carrying two invariants, a commented two-tier allowlist, and a stated blind spot

**Modified records** (the twenty-one measured sites, mapped to their carriers)
- `src/CONFIG-BLOCK.md` — fenced Config JSON (R1) and both safety table rows in every asserting cell (R2, R3)
- `docs/CAPABILITY-DECISIONS.md` — BD-02 Question, Decision, Consequence and addendum (R4-R7), plus the **new explicit canonical-§21 supersession note**
- `docs/BUILD-NOTES.md` — CAP-16's Fallback cell (R8)
- `.planning/REQUIREMENTS.md` — SAFE-01 amended (R9); CIRC-05's statement clause (R10) and guard parenthetical (R11)
- `.planning/PROJECT.md` — success criterion (R12) and Constraints Safety bullet (R13)
- `.planning/ROADMAP.md` — phase acceptance criterion (R14), Phase 9 criterion 4 (R15), Phase 16 goal block (R16), plan-list line (R17, discharged at plan time)
- `.claude/CLAUDE.md` — Constraints Safety bullet (R18) and capability-audit row 8 (R19)
- `.planning/STATE.md` — Phase 9 decision-log entry superseded in place (R20)
- `.planning/todos/pending/2026-08-16-dimming-and-silence-as-distinct-circles.md` — item recorded as settled (R21)
- `16-VALIDATION.md` — the gate registered in the full-suite command

**No new config keys. No generator, artifact or state-shape change.**
</artifacts_this_phase_produces>

<verification>
- `python3 docs/retired_clause_check.py` exits 0 and prints its passed line
- The gate exits non-zero and names the file and line when a retired phrase is injected into a live file, and returns to green when it is removed
- `src/CONFIG-BLOCK.md`'s fenced JSON parses as JSON and agrees with both built forks on the brightness floor and the dim target
- Every file this plan touches cites D-01 and carries no surviving retired vocabulary
- `.planning/REQUIREMENTS.md` SAFE-01 and CIRC-05 carry visible amendment traces; CIRC-05 is corrected in both of its two places
- `docs/CAPABILITY-DECISIONS.md` BD-02 records the canonical-§21 supersession, the canon retained unmodified, and BD-02 as the governing authority
- Neither task commit touches the canonical strategy, a closed phase directory, resolved debug, completed todos, research, `artifacts/`, `tools/`, `src/PROSOCHE-*.xml` or any pre-existing `docs/*.py` checker
- `python3 docs/manifest_check.py` expected RED (constraint D-MANIFEST), excluded from the chain
</verification>

<success_criteria>
The live record says what the build does: twenty-one measured sites across nine files state the
capture-and-restore property rather than the retired bound, BD-02 carries the explicit supersession
note that keeps the three §21-citing sites coherent, the frozen historical record is untouched and
its freeze is encoded in the gate's allowlist, and a standalone checker now reports every lexical
survivor with file and line — while saying plainly, in its own source, that it cannot see the
non-lexical residue and is therefore not proof the class is empty.
</success_criteria>

<output>
Create `.planning/phases/16-dimming-and-silence-as-distinct-device-proven-circles/16-05-SUMMARY.md` when done.
Record: each of the twenty-one sites and how it was handled; any site found during execution that
this plan's measured list did not contain, called out as a **sixth** undercount if so; the verbatim
gate output from the two-occurrence injection control proving it reports every survivor rather than
the first; every Tier-2 allowlist entry added and its reason; confirmation that the BD-01/BD-02/BD-03
consistency note was re-read and survives; confirmation that R17 was already discharged at plan
time; and an explicit restatement that the site list is measured and not proven exhaustive.
</output>
