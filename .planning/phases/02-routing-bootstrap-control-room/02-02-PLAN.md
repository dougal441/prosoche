---
phase: 02-routing-bootstrap-control-room
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/PROSOCHE-Dumb.xml
  - docs/BUILD-NOTES.md
autonomous: true
requirements: [ROOM-01, ROOM-02, ROOM-03, ROOM-04, ROOM-05, ROOM-06, BOOT-04]
user_setup: []

must_haves:
  truths:
    - "The Control Room Note opens with READ THIS FIRST and explains what PROSOCHĒ is before asking anything of the reader."
    - "A reader who has never seen the project can build Automation A and Automation B from the Note alone, without any other document."
    - "The Note says plainly that the Shortcut cannot install those automations itself and that PROSOCHĒ can be bypassed at any time."
    - "The Note warns against targeting Phone, Maps, Wallet, authenticators, password managers, or other essential apps."
    - "The Note carries the editable MY PHONE, ON PURPOSE proforma with all six prompts from canonical strategy 7.2."
    - "The Note body is provably non-empty and reaches the Note through the camelCase content key, not the short form that validates and renders empty."
  artifacts:
    - src/PROSOCHE-Dumb.xml
    - docs/BUILD-NOTES.md
  key_links:
    - "The Control Room Body Text action -> markdownContents on CreateNoteFromMarkdownLinkAction -> Show Note: the single onboarding path, and the single place a wrong parameter key silently ships an empty product."
    - "The literal strings OPEN and CLOSE in the Automation A/B instructions -> the exact strings plan 02-03's router matches: if the Note tells the user to type something the router does not recognise, every automation the user builds lands in the fail-safe branch."
    - "The shortcut name written into the Automation A/B instructions -> the name Phase 7 passes to sign-shortcut: the Note is telling the user which entry to pick in the Run Shortcut picker."
---

## Phase goal (verbatim from ROADMAP.md)

> A user can import the Shortcut, run it manually for the first time, and get a working state.json plus a fully instructive Control Room Note — and every subsequent invocation routes correctly and never corrupts or duplicates that foundation.

**Success criterion 3 this plan owns:** first manual run creates exactly one non-empty
`PROSOCHĒ — Control Room` Note containing READ THIS FIRST, exact steps for Automation A and
Automation B, a plain statement that the Shortcut cannot self-install these automations and is
bypassable, the essential-apps safety warning, and the editable `MY PHONE, ON PURPOSE` proforma.

## Artifacts this plan touches

| Path | Role |
|------|------|
| `src/PROSOCHE-Dumb.xml` | The `Control Room Body` `Text` action created by 02-01 is expanded in place from its opening section to the full Note. The Note-creation wiring is not changed. |
| `docs/BUILD-NOTES.md` | Appended: the canonical Dumb-fork shortcut name, and any deviation from canonical strategy §17's section headings |

Named shortcut variables introduced by this plan: none. It writes into `Control Room Body`,
declared and owned by 02-01 and registered in `SKELETON.md` §3.

## Decision legend

Full D-14..D-26 legend in `02-01-PLAN.md`. The ones this plan turns on:

- **D-18** — One JSON state file, one Apple Note. The Note is the human store; nothing here is duplicated into JSON.
- **D-20** — PROSOCHĒ is bypassable and the Note must say so plainly.
- **D-21** — The camelCase content key on Note creation. The short form passes the validator and yields an empty body at runtime.
- **D-22** — Display strings must not use the data-flow token serialization.
- **BD-05** — Phase 2 is authorised to build the Control Room on the four Notes actions, gated by UA-01's on-device confirmation, with a named file-based fallback if that gate fails.
- **BD-04** — The Sentient fork's On-Device guarantee is re-planned rather than assumed. Nothing in this Note may claim an AI capability the Dumb fork does not have.

<objective>
Expand the `Control Room Body` `Text` action from the opening section 02-01 wrote into the
complete Control Room Note: every section canonical strategy §17 and §18 name, the exact
Automation A and Automation B build steps, the bypassability and cannot-self-install statements,
the essential-apps safety warning, and the six-prompt `MY PHONE, ON PURPOSE` proforma.

This is the entire onboarding surface of the product. Everything a user needs in order to finish
installing PROSOCHĒ lives here and nowhere else — there is no app, no website, no README the
user will read. A section missing from this Note is a feature the user never discovers.

Purpose: 02-01 proved the Note can be created with a non-empty body and opened. This plan makes
that body worth opening, and adds the explicit gate for the one failure mode that would ship an
empty Note while every automated check stays green.

Output: a `src/PROSOCHE-Dumb.xml` whose Note body is complete and which still validates.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-routing-bootstrap-control-room/02-CONTEXT.md
@.planning/phases/02-routing-bootstrap-control-room/SKELETON.md
@.planning/phases/02-routing-bootstrap-control-room/02-01-SUMMARY.md
@docs/CAPABILITY-DECISIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the complete Control Room Note body</name>

  <precondition>`src/PROSOCHE-Dumb.xml` exists, passes `validate-shortcut --target-macos 26 --target-platform all`, and contains a `Text` action whose output is stored to the variable `Control Room Body` and consumed by the Note-creation action. Halt if 02-01 did not leave that wiring in place — this plan edits that action's literal, it does not create it.</precondition>

  <files>src/PROSOCHE-Dumb.xml, docs/BUILD-NOTES.md</files>

  <read_first>
    - `src/PROSOCHE-Dumb.xml` — specifically the `Control Room Body` `Text` action and the Note-creation action that consumes it
    - `PROSOCHE_Nine_Circles_Canonical_Strategy.md` §17 (the Control Room Note structure, verbatim section list and tone guidance), §18 (the first-run flow and the exact Automation A / Automation B step lists), §7.2 (the `MY PHONE, ON PURPOSE` proforma with all six prompts and their example bullets), and §5.2 (why a shared shortcut cannot fully install the Personal Automations)
    - `.planning/phases/02-routing-bootstrap-control-room/02-CONTEXT.md` — the `<specifics>` section's required Note sections and tone rules
    - `.planning/research/ARCHITECTURE.md` §10 — the Note vs JSON boundary table, and which sections are write-only from the Shortcut's perspective
    - `docs/CAPABILITY-DECISIONS.md` BD-04 (what the product may and may not claim about On-Device AI) and BD-05 (the Notes-actions authorisation and its file-based fallback)
    - `docs/BUILD-NOTES.md` §4 row CAP-08 — the content-key evidence and the recorded failure mode
    - `/Users/dougalhanson/.claude/plugins/cache/shortcuts-playground/shortcuts-playground/1.2.1/skills/shortcuts-playground/VARIABLES.md` — how a `Text` action interpolates variables into its literal, and the display-parameter rule
    - `.planning/research/PITFALLS.md` D3 (over-verbal design), D7 (Note growth), C11 (the user disabling the automation)
  </read_first>

  <action>
Rewrite the literal held by the `Control Room Body` `Text` action so it is the complete Note.
Do not touch the Note-creation action, the `Show Note` action, or any surrounding wiring — the
only structural change permitted is adding `￼` placeholders and their `attachmentsByRange`
entries inside this one `Text` action for the values interpolated below.

Sections, in this order, matching canonical strategy §17:

**Title line** — `# PROSOCHĒ — Control Room`, unchanged from 02-01. This line is load-bearing: it
is what the Note title is derived from and what `Find Notes` searches for in plan 02-04.

**`## READ THIS FIRST` (ROOM-01, ROOM-04, D-20).** Explain in a short, calm paragraph what
PROSOCHĒ is: it notices when you open a chosen app, and the more automatically you have been
reaching for it lately, the more it gets in the way. Then two statements that must be plain and
unhedged, per §5.2 and D-20: this Shortcut cannot create the automations for you — Apple does
not allow a shared shortcut to install a Personal Automation, so the next two sections are
something you do by hand, once. And: PROSOCHĒ is bypassable. You can dismiss any interruption,
and you can turn the automations off in the Shortcuts app whenever you want. It does not claim
otherwise and it is not trying to stop you. Do not soften either statement into a promise the
product cannot keep — PITFALLS C11 and D-20 both turn on this being honest.

**`### Automation A — OPEN` (ROOM-02).** The exact build steps, as a numbered list a reader can
follow with the Shortcuts app in front of them, transcribed from §18: open the Shortcuts app,
go to Automation, create a new Personal Automation, choose the App trigger, select the apps you
want PROSOCHĒ to watch, choose `Is Opened`, set it to run immediately without asking, then add
the `Run Shortcut` action, choose the PROSOCHĒ shortcut by name, and pass the text `OPEN` as its
input. Name the shortcut using the exact string this fork will be signed under, and add that
string to `docs/BUILD-NOTES.md` so Phase 7's signing step uses it verbatim — the Note is telling
the user which entry to pick out of their own library, so the two must agree exactly.

**`### Automation B — CLOSE` (ROOM-03).** The same list with the three differences called out
explicitly: the same apps as Automation A, the `Is Closed` trigger, and the input text `CLOSE`.
Say plainly that both automations are needed, and what is lost with only one: without CLOSE,
PROSOCHĒ cannot measure how long a session actually lasted, so it cannot tell whether a boundary
the user set was kept.

The literal strings `OPEN` and `CLOSE` in these two sections are the exact strings plan 02-03's
router matches. Write them in a way that makes the exact text obvious to a reader who is
retyping it.

**Safety warning (ROOM-05).** Immediately after Automation B, an unmissable short section:
do not point these automations at Phone, Messages, Maps, Wallet, an authenticator, a password
manager, or anything else you might need in a hurry or in an emergency. PROSOCHĒ is for the apps
you open without deciding to, not the ones you need.

**`## MY PHONE, ON PURPOSE` (ROOM-06).** The editable proforma, with all six prompts from §7.2
as their own sub-headings, each with a short instruction and the example bullets §7.2 gives,
left as an editable list the user can type into directly in the Notes app:
what is my phone genuinely for; which apps take more attention than I intend to give them;
what do I actually want the reclaimed attention for; when PROSOCHĒ stops an automatic open,
what would I rather do (naming the six exits — Capture, Coordinate, Create, Connect, Consult,
Close — so the user can choose among them); what does deliberate leisure look like for me;
and an optional sentence to my future self. Say that the user can write naturally and that
nothing here has to be tidy. Add one line stating that PROSOCHĒ only reads this section when the
user explicitly asks it to, from the Control Room menu — which is true, is the Note/JSON boundary
in ARCHITECTURE.md §10, and is what stops a reader assuming the Shortcut is parsing their Note on
every app open.

**`## CURRENT SETTINGS`.** The six values §17 names — fork, profile, sequence, voice, AI, enabled
exits — with the real values known at bootstrap rather than prose about them: fork is the Dumb
fork; profile is the descent chosen at import, interpolated from `Import Descent`; sequence is
Classic; voice is the answer given at import, interpolated from `Import Voice`; AI is not used by
this fork at all (per BD-04 and DUMB-01, and this Note must not imply otherwise); enabled exits
are all six by default. Interpolate the two import answers as `￼` placeholders with
`attachmentsByRange` entries inside this `Text` action, so the composition happens in the text
literal and the Note-creation action receives a single already-composed variable — that keeps the
display-serialization rule (D-22) satisfied at the one place it applies.

**`## CURRENT STATE`.** The heading plus an honest first-run snapshot in words: nothing has been
recorded yet, this is the first run, and this section is rewritten each time PROSOCHĒ is run
manually. Making it actually refresh is ROOM-08 in Phase 7; the section exists now so the Note's
shape is stable and Phase 7 rewrites a section rather than inventing one.

**`## ATTENTION LEDGER`.** The heading plus one line: meaningful events are appended here as they
happen — circle changes, boundaries set and kept or exceeded, redirects, cool-downs, profile
changes — and not every internal calculation. Entries begin in later phases.

**`## VALUE / LIFE RETURNED`** and **`## SUPPORT PROSOCHĒ`.** Each a heading plus a single line
saying the section is reserved and nothing is measured or asked for yet. §17 marks both reserved;
do not invent content for them and do not imply a future paid gate on functionality.

**Tone, binding across every section above** (02-CONTEXT `<specifics>`, PITFALLS D3): calm, clean,
unsentimental. No emoji anywhere. No exclamation marks anywhere. No second-person scolding, no
motivational framing, no diagnosis of the reader, and no claim about what happens inside an app
or about what the reader feels. Short sentences. The reader is setting up a tool, not being
counselled.
  </action>

  <verify>
    <automated>PLUG=/Users/dougalhanson/.claude/plugins/cache/shortcuts-playground/shortcuts-playground/1.2.1; set -e; plutil -lint src/PROSOCHE-Dumb.xml; "$PLUG/bin/validate-shortcut" src/PROSOCHE-Dumb.xml --target-macos 26 --target-platform all; python3 - <<'PY'
import plistlib, sys, re
p = plistlib.load(open('src/PROSOCHE-Dumb.xml','rb'))
acts = p['WFWorkflowActions']

# locate the Text action whose output is stored to Control Room Body
body = None
for i, a in enumerate(acts):
    if a.get('WFWorkflowActionIdentifier') == 'is.workflow.actions.setvariable':
        if (a.get('WFWorkflowActionParameters') or {}).get('WFVariableName') == 'Control Room Body':
            for j in range(i - 1, -1, -1):
                if acts[j].get('WFWorkflowActionIdentifier') == 'is.workflow.actions.gettext':
                    t = (acts[j].get('WFWorkflowActionParameters') or {}).get('WFTextActionText')
                    body = t if isinstance(t, str) else (t or {}).get('Value', {}).get('string')
                    break
            break
if not body:
    print('FAIL: could not locate the Control Room Body text literal'); sys.exit(1)

errs = []
required = ['# PROSOCHĒ — Control Room', '## READ THIS FIRST', 'Automation A', 'Automation B',
            'Is Opened', 'Is Closed', 'OPEN', 'CLOSE', 'Run Shortcut',
            '## MY PHONE, ON PURPOSE', '## CURRENT SETTINGS', '## CURRENT STATE',
            '## ATTENTION LEDGER', '## VALUE / LIFE RETURNED', '## SUPPORT PROSOCHĒ',
            'Wallet', 'password manager', 'authenticator',
            'Capture', 'Coordinate', 'Create', 'Connect', 'Consult', 'Close']
for r in required:
    if r not in body: errs.append(f'missing required content: {r!r}')

if not body.startswith('# PROSOCHĒ — Control Room'):
    errs.append('body does not begin with the exact title line')
if len(body) < 1800:
    errs.append(f'body is only {len(body)} characters; the required section set cannot fit')
if '!' in body:
    errs.append('body contains an exclamation mark (tone rule)')
if re.search(r'[\U0001F300-\U0001FAFF☀-➿]', body):
    errs.append('body contains emoji (tone rule)')
# bypassability and cannot-self-install must both be stated
if not re.search(r'bypass|turn (the )?automations off|dismiss', body, re.I):
    errs.append('body does not state that PROSOCHĒ is bypassable')
if not re.search(r'cannot (create|install)', body, re.I):
    errs.append('body does not state that the Shortcut cannot install the automations itself')
# six proforma prompts
prompts = ['phone genuinely for', 'more attention than', 'reclaimed attention',
           'would I rather do', 'deliberate leisure', 'future self']
for pr in prompts:
    if pr.lower() not in body.lower(): errs.append(f'missing proforma prompt: {pr!r}')

if errs:
    print('NOTE BODY CHECK FAILED'); [print(' -', e) for e in errs]; sys.exit(1)
print(f'NOTE-BODY-OK: {len(body)} characters, all required sections present')
PY</automated>
  </verify>

  <acceptance_criteria>
    - `validate-shortcut src/PROSOCHE-Dumb.xml --target-macos 26 --target-platform all` exits 0 and `plutil -lint` succeeds.
    - The `Control Room Body` text literal begins with the exact line `# PROSOCHĒ — Control Room` and is at least 1800 characters long.
    - It contains all of these headings: `## READ THIS FIRST`, `## MY PHONE, ON PURPOSE`, `## CURRENT SETTINGS`, `## CURRENT STATE`, `## ATTENTION LEDGER`, `## VALUE / LIFE RETURNED`, `## SUPPORT PROSOCHĒ`, plus an Automation A and an Automation B sub-heading.
    - The Automation A section names the App trigger, `Is Opened`, running without asking, `Run Shortcut`, and the input text `OPEN`. The Automation B section names the same apps, `Is Closed`, and the input text `CLOSE`.
    - The shortcut name written into both automation sections is a single consistent string, appears identically in both sections, and is recorded in `docs/BUILD-NOTES.md` as the name Phase 7 must pass to the signer.
    - The body states, without hedging, both that the Shortcut cannot create or install the automations itself and that PROSOCHĒ can be bypassed or turned off.
    - The body names Phone, Wallet, an authenticator, and a password manager among the apps not to target.
    - The `MY PHONE, ON PURPOSE` section contains all six §7.2 prompts and names all six exits by name.
    - The `CURRENT SETTINGS` section interpolates `Import Descent` and `Import Voice` through `￼` placeholders with matching `attachmentsByRange` entries in the same `Text` action, and states that this fork uses no AI.
    - The body contains no exclamation mark and no emoji character.
    - The body makes no claim about what happens inside an app, about what the reader feels, or about an AI capability the Dumb fork does not have.
    - No action was added, removed, or reordered in `WFWorkflowActions` by this task: `WFWorkflowImportQuestions` entry 1 still resolves to index 2 and entry 2 still to index 4, both still carrying `WFTextActionText`.
  </acceptance_criteria>

  <reversibility rating="reversible">This is a text literal inside one already-wired action. Rewording it later costs a re-validate and nothing else.</reversibility>

  <done>A reader who has only this Note can finish installing PROSOCHĒ correctly, knows they can switch it off, knows which apps not to point it at, and has a proforma they can start filling in.</done>
</task>

<task type="auto">
  <name>Task 2: The empty-body trap — prove the content key and the payload, and escalate what only a device can settle</name>

  <files>src/PROSOCHE-Dumb.xml, docs/BUILD-NOTES.md</files>

  <read_first>
    - `src/PROSOCHE-Dumb.xml` — the Note-creation action and the variable reference feeding its content parameter
    - `docs/BUILD-NOTES.md` §4 row CAP-08 in full, including its Parameter shape cell's two prose citations and its Fallback cell
    - `docs/BUILD-NOTES.md` §6 `UA-01` — the existing on-device confirmation item for the four Notes actions, which already names this exact risk
    - `.planning/research/PITFALLS.md` A8 (parameter shape drift, with the named Notes content-key trap) and A9 (validator false-passes, listing this trap among five)
    - `docs/CAPABILITY-DECISIONS.md` BD-05 — the authorisation, the UA-01 gate, and the file-based fallback that applies if the gate fails
    - `/Users/dougalhanson/.claude/plugins/cache/shortcuts-playground/shortcuts-playground/1.2.1/skills/shortcuts-playground/BEST_PRACTICES.md` line 121 and `CHANGELOG.md` line 447 — the two independent statements of the correct content key and of the failure it prevents
  </read_first>

  <action>
This is the single highest-consequence authoring trap in the build, and it is invisible to every
build-machine check except the one this task writes. `docs/BUILD-NOTES.md` CAP-08 records it
verbatim: the shorter content key passes `validate-shortcut` and produces an empty note body at
runtime. Since the Note is the entire onboarding surface, that failure ships a product whose
first run shows the user nothing, with a green validator the whole way.

Do three things.

**First, assert the key and the payload in the file.** Confirm the Note-creation action supplies
its content through the camelCase key recorded in CAP-08, that no other content-key spelling
appears anywhere in the file, and that the parameter is wired to the `Control Room Body`
variable rather than left as a literal or an empty value. Confirm the variable it references is
actually set earlier in the same branch — a content parameter wired to a variable that is never
assigned validates cleanly and produces exactly the same empty note.

**Second, close the loop on what a build machine cannot see.** Nothing here can execute a
Shortcut, so no amount of static checking proves the body arrives. Extend `UA-01` in
`docs/BUILD-NOTES.md` §6 — do not create a competing item, UA-01 already owns the four Notes
actions and already names the empty-body risk — so that its `Exact steps` and
`What to record on completion` fields explicitly cover, for the Phase 2 build as authored: that
after import and one manual run the Control Room Note exists, that its title matches exactly the
title `Find Notes` will be asked for, that its body is populated rather than empty, and that the
markdown headings render as headings rather than as literal hash characters. Record the four
answers as the acceptance evidence for BOOT-04. Keep the document append-only: extend the
existing entry's fields, do not rewrite what Phase 1 wrote.

**Third, record the fallback trigger.** BD-05 names the file-based Control Room as the fallback
if UA-01 surfaces a genuine on-device failure. Add a short note in `docs/BUILD-NOTES.md`, next to
the extended UA-01, stating exactly which observation triggers that fallback (the Note is created
but its body is empty, or the Notes actions fail or stall on a permission prompt during the
manual bootstrap run), and what would have to change: the same `Control Room Body` text goes to a
file through the already-audited Save File path instead of to a Note, with the append-ledger and
proforma round-trip costs BD-05 already enumerates. This is so a future executor hitting that
failure has a decision already made rather than making one under pressure.
  </action>

  <verify>
    <automated>python3 - <<'PY'
import plistlib, sys
p = plistlib.load(open('src/PROSOCHE-Dumb.xml','rb'))
acts = p['WFWorkflowActions']
errs = []

note_idx = [i for i, a in enumerate(acts)
            if a.get('WFWorkflowActionIdentifier') == 'com.apple.Notes.CreateNoteFromMarkdownLinkAction']
if len(note_idx) != 1:
    print(f'FAIL: expected exactly one Note-creation action, found {len(note_idx)}'); sys.exit(1)
i = note_idx[0]
params = acts[i].get('WFWorkflowActionParameters') or {}

if 'markdownContents' not in params:
    errs.append('Note-creation action has no markdownContents parameter')
for wrong in ('markdown', 'contents', 'Contents', 'markdown_contents'):
    if wrong in params:
        errs.append(f'Note-creation action carries an unexpected content key: {wrong!r}')

val = params.get('markdownContents')
if val in (None, '', {}):
    errs.append('markdownContents is empty')

# the content must reference Control Room Body, and that variable must be set earlier
blob = repr(val)
if 'Control Room Body' not in blob:
    errs.append('markdownContents does not reference the Control Room Body variable')
set_at = [j for j, a in enumerate(acts)
          if a.get('WFWorkflowActionIdentifier') == 'is.workflow.actions.setvariable'
          and (a.get('WFWorkflowActionParameters') or {}).get('WFVariableName') == 'Control Room Body']
if not set_at:
    errs.append('Control Room Body is never assigned')
elif min(set_at) > i:
    errs.append('Control Room Body is assigned after the Note-creation action')

if errs:
    print('EMPTY-BODY GATE FAILED'); [print(' -', e) for e in errs]; sys.exit(1)
print('EMPTY-BODY-GATE-OK: camelCase content key present, wired to an earlier assignment')
PY
grep -q 'UA-01' docs/BUILD-NOTES.md && grep -q 'BD-05' docs/BUILD-NOTES.md</automated>
  </verify>

  <acceptance_criteria>
    - Exactly one `com.apple.Notes.CreateNoteFromMarkdownLinkAction` action exists in the file.
    - Its parameters include `markdownContents`, that parameter is non-empty, and no alternative content-key spelling (`markdown`, `contents`, `Contents`, `markdown_contents`) appears on it.
    - `markdownContents` references the `Control Room Body` variable, and that variable is assigned by a `Set Variable` action at a lower index than the Note-creation action.
    - `docs/BUILD-NOTES.md` §6 `UA-01` now names, in its `Exact steps` and `What to record on completion` fields, all four Phase 2 observations: Note exists, title matches exactly, body populated rather than empty, markdown headings render as headings.
    - `docs/BUILD-NOTES.md` records which observation triggers BD-05's file-based fallback and what that fallback changes, referencing `BD-05` by ID.
    - No content that existed in `docs/BUILD-NOTES.md` before this task ran was deleted or reworded — only appended to or extended.
    - `validate-shortcut src/PROSOCHE-Dumb.xml --target-macos 26 --target-platform all` still exits 0.
  </acceptance_criteria>

  <reversibility rating="reversible">A parameter-key assertion and documentation edits; nothing shipped depends on it irreversibly.</reversibility>

  <done>The one failure mode that would silently ship a blank onboarding surface is asserted in the file where it can be asserted, and escalated to a recorded human observation where it cannot.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| The Note body → the user's understanding of what PROSOCHĒ does | This is the product's only claim surface; an overstated claim here is a claim the software cannot keep |
| The Note body → the automations the user builds by hand | The literal input strings and shortcut name in the Note become the configuration of two Personal Automations the software never sees |
| `Control Room Body` variable → `markdownContents` | The one parameter binding where a validator-clean file produces an empty product |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-08 | Tampering | The Note content key | critical | mitigate | Task 2 asserts the camelCase key, rejects every alternative spelling, requires a non-empty value wired to a variable assigned earlier in the same branch, and escalates the runtime half to UA-01 with four named observations |
| T-02-09 | Spoofing | The shortcut name in the Automation A/B steps | high | mitigate | The name is required to be one consistent string appearing identically in both sections and recorded in `docs/BUILD-NOTES.md` as the string Phase 7 signs with, so the user cannot be sent to pick the wrong entry from their library |
| T-02-10 | Repudiation | Claims made in READ THIS FIRST | high | mitigate | Bypassability and the cannot-self-install limitation are both required to be stated without hedging and are asserted by regex; the AI claim is bounded by BD-04 and DUMB-01 |
| T-02-11 | Elevation of privilege | The essential-apps safety warning | high | mitigate | Phone, Wallet, authenticator, and password manager are each asserted present in the body; the warning is placed immediately after the automation build steps rather than at the end where it would be missed |
| T-02-12 | Information disclosure | The proforma as a human-authored free-text surface | medium | mitigate | The Note states that PROSOCHĒ reads that section only when explicitly asked from the Control Room menu, which is the actual Note/JSON boundary; no OPEN/CLOSE path parses it |
| T-02-13 | Denial of service | Note growth over the product's lifetime | low | accept | Phase 2 writes a fixed-size body once; the append-only ledger that could grow without bound is Phase 7's to bound, per PITFALLS D7 |
| T-02-SC | Tampering (supply chain) | package-manager installs | low | accept | No package manifest, no installs; the only external inputs are read-only reference docs inside an already-installed plugin |
</threat_model>

<verification>
- `plutil -lint` succeeds and the validator exits 0 at `--target-macos 26 --target-platform all`.
- The Note-body content check exits 0: every required section, both plain statements, all six proforma prompts, all six exit names, no exclamation mark, no emoji.
- The empty-body gate exits 0.
- `docs/BUILD-NOTES.md` gained an extended `UA-01` and a recorded fallback trigger, and lost nothing.
</verification>

<success_criteria>
The Control Room Note is complete and honest: it teaches both automations exactly, states its own
limits plainly, warns about the apps that must not be targeted, and hands the user an editable
proforma. The one trap that would ship it empty is asserted in the file and escalated where it
cannot be.
</success_criteria>

<output>
Create `.planning/phases/02-routing-bootstrap-control-room/02-02-SUMMARY.md` when done.
</output>
