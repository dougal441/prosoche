---
phase: 12-state-shape-sentinel-gaps-exit-events-and-active-session
plan: 05
type: execute
wave: 5
depends_on: ["12-04"]
files_modified:
  - artifacts/shortcuts/MANIFEST.md
  - docs/BUILD-NOTES.md
  - docs/CAPABILITY-DECISIONS.md
  - .planning/phases/12-state-shape-sentinel-gaps-exit-events-and-active-session/12-UAT.md
autonomous: true
requirements: [SESS-07, STATE-12, EXIT-01, EXIT-02, SAFE-01]

must_haves:
  truths:
    - "Both forks are signed under their exact live display names — PROSOCHĒ — Nine Circles — Core and PROSOCHĒ — Nine Circles — Aware — with no _signed suffix, matching the DISPLAY_NAMES literals docs/manifest_check.py hard-codes."
    - "Every MANIFEST.md row for the two signed artifacts and the two sources carries a size and SHA-256 that match disk, and python3 docs/manifest_check.py exits 0 — a stale row is a false provenance claim, and Phase 10 measured three of six already wrong once."
    - "Gate B is run as an advisory read and reports exactly one line per fork — the com.apple.mobilenotes.SharingExtension / WFCreateNoteInput waiver — and gate B is never &&-chained into any definition of done."
    - "12-UAT.md exists, is cold-runnable by someone with no memory of this phase, and carries an explicit BLOCKED branch that leaves results blank rather than substituting a simulator run or a decrypted-artifact inference for a device observation."
    - "The exit-recording path's device-reachability verdict is recorded as observed — either the UAT was run on a real iPhone, or xcrun devicectl list devices reported no device and the UAT is recorded BLOCKED with every result left blank, following the Phase 10 precedent."
    - "Every deviation and open assumption this phase carries is recorded in docs/BUILD-NOTES.md under a Phase 12 heading: A1 (repeat.each over an empty array, unsettled, rung 2 named), A2 (final-segment dotted reads, inference from two written records), A3 (no installed base re-confirmed before the schema bump), the create_target_url option chosen and why, and the Aware-fork verifier-gap closure outcome."
  prohibitions:
    - "Never record a device-evidence verdict that was not observed on a device — no simulator run, no decrypted-artifact inference, and no fabricated UAT pass may be entered as device evidence. Leave the result blank and mark it BLOCKED."
    - "Never silence a build guard or a checker assertion to make the suite green — no new exemption entry, no skip, no weakened gate that hides a real finding."
  artifacts:
    - path: "artifacts/shortcuts/MANIFEST.md"
      provides: "refreshed size + SHA-256 rows for both signed artifacts and both sources"
      contains: "PROSOCHĒ — Nine Circles — Core"
    - path: ".planning/phases/12-state-shape-sentinel-gaps-exit-events-and-active-session/12-UAT.md"
      provides: "cold-runnable device UAT for the exit-recording path, with an explicit BLOCKED branch"
      contains: "BLOCKED"
    - path: "docs/BUILD-NOTES.md"
      provides: "Phase 12 deviation and assumption record"
      contains: "Phase 12"
  key_links:
    - from: "artifacts/shortcuts/*.shortcut"
      to: "artifacts/shortcuts/MANIFEST.md"
      via: "size + SHA-256 rows verified against disk by docs/manifest_check.py"
      pattern: "manifest_check"
    - from: "12-UAT.md"
      to: "record_exit_and_route() and close_pipeline()'s restore arm"
      via: "the manual test exercises a real leave-and-confirm-exit, the only path no automated check can substitute for"
      pattern: "leave and confirm"
---

<objective>
Ship the phase and record what is actually known.

Three things remain after Plan 12-04: the built forks are not yet signed, so nothing is
installable and `docs/manifest_check.py` is the only checker still red; gate B has not been
read since the emission changes landed, and it is the only channel that would catch a
parameter-key or picker-literal regression introduced by Plan 12-03's nine moved sites; and
the exit-recording path has **zero** device evidence at any rung — the closed OPEN-path
debug session reached breadcrumb J on the OPEN path only, and nothing in
`docs/BUILD-NOTES.md` records an exit-recording device observation.

**The device question is planned with its blocked branch explicit.** Phase 10's device UAT
was blocked (`xcrun devicectl list devices` returned "No devices found."), all ten tests
were left blank, and DIST-03 is still open. That is this project's recorded posture and it
is the right one: a fabricated pass is worse than a blank. `12-UAT.md` must therefore be
written so it is runnable cold at any later date, and the reachability probe must produce a
recorded verdict either way.

**The recording duty.** A probe's result is recorded, not consumed. This phase carries four
open assumptions (A1, A2, A3, A5), one `one-way` decision resolved at a checkpoint, one
scope decision about the Aware fork's verifier set, and one measured correction to the
research (`verify_state_seed()`'s generalisation needs a source-variable filter added, not
only a key-root filter deleted). All of it goes into `docs/BUILD-NOTES.md` and, where it
settles a capability question, `docs/CAPABILITY-DECISIONS.md`.

Binding constraints D-01..D-04 as stated in `12-01-PLAN.md` `<objective>` apply unchanged.
**D-04 correction restated because this is the plan where it bites:** the live display names
are `PROSOCHĒ — Nine Circles — Core` and `PROSOCHĒ — Nine Circles — Aware`, hard-coded as
`DISPLAY_NAMES` in `docs/manifest_check.py`. `.claude/CLAUDE.md` and `12-CONTEXT.md` carry
the stale `Dumb` / `Sentient` literals from before Phase 11 plan 06's rename; the rule is
unchanged, only the two strings moved.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-state-shape-sentinel-gaps-exit-events-and-active-session/12-RESEARCH.md
@.planning/phases/12-state-shape-sentinel-gaps-exit-events-and-active-session/12-04-SUMMARY.md
@.claude/CLAUDE.md
</context>

## Artifacts this phase produces

New and changed artifacts introduced **by this plan** (exclude from drift verification):

| Symbol / path | Kind | File |
|---|---|---|
| `12-UAT.md` | new file | `.planning/phases/12-state-shape-sentinel-gaps-exit-events-and-active-session/12-UAT.md` |
| Phase 12 heading + deviation/assumption record | new section | `docs/BUILD-NOTES.md` |
| Phase 12 capability record (only if a probe settled something) | new section | `docs/CAPABILITY-DECISIONS.md` |
| refreshed size + SHA-256 rows | changed rows | `artifacts/shortcuts/MANIFEST.md` |
| `PROSOCHĒ — Nine Circles — Core.shortcut`, `PROSOCHĒ — Nine Circles — Aware.shortcut` | regenerated signed artifacts | `artifacts/shortcuts/` |

Created by earlier plans in this phase: `EXIT_EVENTS_SEED`, `EXIT_EVENTS_ANCHOR`,
`seed_exit_events()`, `verify_exit_events_seed()`, `ACTIVE_SESSION_SEED`,
`ACTIVE_SESSION_ANCHOR`, `seed_active_session()`, `verify_active_session_seed()`,
`"Session Declared Duration"`, `CREATE_TARGET_URL_SEED`, `CREATE_TARGET_URL_ANCHOR`,
`seed_create_target_url()`, `STATE_READ_SOURCE_VARIABLES`,
`STATE_SEED_COMPOSITE_PREFIXES`, and (option C only) `STATE_SEED_DEFERRED_KEYS`.

<tasks>

<task type="auto">
  <name>Task 1: Sign both forks under their live display names, refresh MANIFEST, and take the gate B advisory read</name>

  <precondition>`git merge-base --is-ancestor 7ca8ebbfe467da38e594bdd41687c094a1f0c678 HEAD` exits 0 (D-01), and the macOS `shortcuts` CLI that `sign-shortcut` wraps is available on this machine (signing is macOS-only and succeeded here in Phase 11).</precondition>

  <files>artifacts/shortcuts/MANIFEST.md</files>

  <read_first>
    - `docs/manifest_check.py` — the `DISPLAY_NAMES` literals it hard-codes, the rows it verifies, and how it computes size and SHA-256, so the refreshed rows match its expectations exactly.
    - `artifacts/shortcuts/MANIFEST.md` — the current six rows and their column format.
    - `.claude/CLAUDE.md` §1 "Exact validator invocation" — the full two-gate rule, including the single permitted gate B waiver line, index-normalised, and the statement that gate B is structurally incapable of being an `&&`-linked build gate because its waiver is permanent.
    - `.claude/CLAUDE.md` §8 — the signing invocation, the filename-discipline rule (no `_signed` suffix), the two known signer quirks that `sign-shortcut` auto-retries, and the `aea decrypt` + `aa extract` recovery workflow.
  </read_first>

  <action>
**1 — Rebuild from a clean tree.** Provenance gate (D-01), then
`python3 tools/build_state_engine.py` and `python3 tools/build_sentient.py`. Confirm the
full checker chain is green before signing anything: a signed artifact built from a source
that fails a checker is a false provenance claim.

**2 — Gate A, mandatory (D-02).** Run
`validate-shortcut src/PROSOCHE-Dumb.xml --target-macos 26 --target-platform all` and the
same for `src/PROSOCHE-Sentient.xml`. Both must print `Validation passed.` and exit 0.
Never pair `--target-platform ios` with `--target-macos 26`: both bundled snapshots are
filtered out and the empty allowlist rejects 3675 of 3675 actions, which is a check that
fails 100% of its inputs and therefore carries zero signal.

**3 — Gate B, advisory read only (D-02).** Run
`validate-shortcut src/PROSOCHE-Dumb.xml --target-macos 27 --target-platform all` and the
Sentient equivalent **as standalone commands**, never `&&`-chained into anything. Each is
expected to exit 1 with **exactly one** line: the
`com.apple.mobilenotes.SharingExtension` / `WFCreateNoteInput` waiver. **Any other line is
a real finding** and must be investigated before the artifact ships — Plan 12-03 moved nine
emission sites, so this is the run most likely to surface one. Record both outputs verbatim
in `docs/BUILD-NOTES.md` with the action indices normalised, so a future run can diff
against them.

**4 — Sign under the exact live display names (D-04).** Sign each fork so the output
basename is exactly the intended shortcut display name with no `_signed` suffix:
`PROSOCHĒ — Nine Circles — Core.shortcut` from `src/PROSOCHE-Dumb.xml`, and
`PROSOCHĒ — Nine Circles — Aware.shortcut` from `src/PROSOCHE-Sentient.xml`. Treating a
`_signed`-suffixed library name as a successful build is an explicit rule violation. If
`shortcuts sign` reports "The file doesn't exist." for a file that does exist, or "isn't in
the correct format." even though gate A and `plutil -lint` both pass, those are the two
known signer quirks and `sign-shortcut` auto-retries both — do not treat either as a real
failure on first sight. Confirm each output is non-zero bytes; a valid XML draft without a
signed `.shortcut` is not a useful stopping point.

**5 — Verify what actually shipped.** Recover each signed artifact's plist through the
AEA1 workflow — extract `SigningCertificateChain`'s leaf certificate, take its public key,
`aea decrypt`, `aa extract`, then convert `Shortcut.wflow` with `plutil` — and confirm the
recovered plist's `WFWorkflowName` matches the artifact basename and that its bootstrap
template carries the Phase 12 shape (`schema_version` 4, the four-leaf `active_session`,
`exit_events: []`). This verifies what shipped rather than trusting the unsigned source
plus a file mtime. If `aea` or `aa` is unavailable, skip this step and record the deviation
explicitly rather than silently.

**6 — Refresh every MANIFEST row.** Recompute size and SHA-256 for both signed artifacts
and both sources and update all six rows in `artifacts/shortcuts/MANIFEST.md`. Phase 10
measured three of six already wrong once, so refresh every row rather than only the ones
you believe moved. `python3 docs/manifest_check.py` must then exit 0.
  </action>

  <verify>
    <automated>git merge-base --is-ancestor 7ca8ebbfe467da38e594bdd41687c094a1f0c678 HEAD && python3 tools/build_state_engine.py && python3 tools/build_sentient.py && for f in state_engine_self_check phase5_self_check phase6_self_check phase7_self_check phase9_self_check sentient_audit_check sentient_core_check environmental_restore_check router_ui_census sequence_dispatch_check note_identity_check manifest_check; do python3 docs/$f.py || exit 1; done && validate-shortcut src/PROSOCHE-Dumb.xml --target-macos 26 --target-platform all && validate-shortcut src/PROSOCHE-Sentient.xml --target-macos 26 --target-platform all && python3 -c "
import pathlib,hashlib
d=pathlib.Path('artifacts/shortcuts')
names=['PROSOCHĒ — Nine Circles — Core.shortcut','PROSOCHĒ — Nine Circles — Aware.shortcut']
man=(d/'MANIFEST.md').read_text()
for n in names:
    p=d/n
    assert p.exists(), 'missing signed artifact: '+n
    assert p.stat().st_size>0, 'zero-byte artifact: '+n
    assert p.read_bytes()[:4]==b'AEA1', 'not an Apple Encrypted Archive: '+n
    h=hashlib.sha256(p.read_bytes()).hexdigest()
    assert h in man, 'MANIFEST sha256 stale for '+n
    assert str(p.stat().st_size) in man, 'MANIFEST size stale for '+n
assert '_signed' not in ' '.join(x.name for x in d.iterdir()), 'a _signed suffix leaked into artifacts/shortcuts/'
print('signed, named and manifested')"</automated>
  </verify>

  <acceptance_criteria>
    - `artifacts/shortcuts/PROSOCHĒ — Nine Circles — Core.shortcut` and `artifacts/shortcuts/PROSOCHĒ — Nine Circles — Aware.shortcut` both exist, are non-zero bytes, and begin with the magic bytes `AEA1`.
    - No file in `artifacts/shortcuts/` carries a `_signed` suffix.
    - Each artifact's SHA-256 and byte size both appear verbatim in `artifacts/shortcuts/MANIFEST.md`.
    - `python3 docs/manifest_check.py` exits 0, and so do the other eleven `docs/*.py` checkers.
    - Both gate A runs print `Validation passed.` and exit 0.
    - Gate B was run **as a standalone command per fork**, not `&&`-chained; each output is recorded verbatim (indices normalised) in `docs/BUILD-NOTES.md`; each shows **exactly one** line and that line names `com.apple.mobilenotes.SharingExtension` and `WFCreateNoteInput`. Any additional line is recorded as a finding and investigated before this task is marked done.
    - The recovered plist from each signed artifact has a `WFWorkflowName` equal to that artifact's basename without the `.shortcut` extension, and its bootstrap template shows `schema_version` 4, a dictionary-valued `active_session`, and `exit_events` equal to `[]` — or, if `aea` / `aa` is unavailable, that deviation is recorded explicitly in `docs/BUILD-NOTES.md`.
  </acceptance_criteria>

  <done>Both forks are signed under their exact live display names, every MANIFEST row matches disk, all twelve checkers are green, gate A is clean on both forks, and gate B's advisory output is recorded verbatim with exactly the one permitted waiver line per fork.</done>
</task>

<task type="auto">
  <name>Task 2: Author a cold-runnable 12-UAT.md for the exit-recording path and record the device-reachability verdict</name>

  <files>.planning/phases/12-state-shape-sentinel-gaps-exit-events-and-active-session/12-UAT.md</files>

  <read_first>
    - `.planning/STATE.md` — the Phase 10 device-UAT block, the exact `xcrun devicectl list devices` result recorded there, and the no-fabrication precedent (all ten tests left blank, DIST-03 still open).
    - `.planning/phases/12-.../12-VALIDATION.md` §"Manual-Only Verifications" — the two manual rows and their stated test instructions, which this document expands into runnable steps.
    - `.claude/CLAUDE.md` §9 — the evidence-escalation ladder, "Rung 2's ceiling" (why `com.apple.mobilenotes` absence, Apple Intelligence, Personal Automation triggers and real-hardware environmental behaviour are all device-gated), the two standing policies, and the recording duty.
    - `tools/build_state_engine.py` — `record_exit_and_route()` and `close_pipeline()`'s owner arm, so the test steps name what should be observed rather than describing it vaguely.
  </read_first>

  <action>
Write `12-UAT.md` so someone with **no memory of this phase** can run it months from now.

**Reachability probe first.** Run `xcrun devicectl list devices` and record the exact
output. Also note whether iPhone Mirroring is live. This produces the document's verdict
header — `REACHABLE` or `BLOCKED` — and it is recorded either way, never omitted.

**Document structure.**

*Header:* phase, date, the commit the artifacts were signed at, the two signed artifact
basenames and their SHA-256s from MANIFEST, and the reachability verdict with the verbatim
probe output beneath it.

*Why device-only:* state plainly that no automated substitute exists. Personal Automation
triggers (App Is Opened / Is Closed) are user-created on the device and cannot be exercised
on a simulator at any effort; `com.apple.mobilenotes` is absent from the booted simulator's
app list, so the Control Room Note path is device-gated by measurement; and real-hardware
brightness and volume capture-and-restore behaviour is rung 3+ by definition. Name this as
the reason a simulator run may not be substituted, so the rule travels with the document.

*Setup:* fresh-install both forks from the signed artifacts; delete
`PROSOCHE/state.json` from the Shortcuts iCloud folder so the run starts from a genuinely
clean state (the schema bump to 4 would also force a rebuild, but deleting the file removes
that variable); create or re-point the two Personal Automations to the installed shortcut's
display name.

*Tests, each with an explicit expected observation and a blank result field.* At minimum:
(1) **First OPEN against clean state** — the bootstrap runs, no "no value was found for
dictionary key" and no "could not evaluate the key path" error, a session starts.
(2) **A genuine leave-and-confirm-exit** — the exit-recording path, which is this phase's
new-risk surface and was **never** exercised by the closed OPEN-path debug session. Choose
each of the six exit routes across separate runs, and specifically include **Create on a
clean install** (the `profile_snapshot.create_target_url` path Plan 12-04 decided): the
expected observation is that the user is *asked* where Create should open, not that a
placeholder URL is opened.
(3) **CLOSE after a session that changed brightness or volume** — confirm
`restore_managed_settings()` is reached and the setting returns to its captured value. This
is the SESS-07 / SAFE-01 test and the whole reason the `active_session` fix is a safety fix.
(4) **A superseded CLOSE** — start a second session before closing the first, and confirm
the older CLOSE writes nothing (the condition-4 ownership compare still holds).
(5) **Second OPEN after an exit** — the `pending_exit` outcome path and the
`exit_stats.<type>` composite reads become reachable only here.
(6) **Control Room after an exit** — the ledger entry appended and the Note renders.
(7) **`exit_events` after several exits** — inspect `state.json` and confirm the array is
newest-first and capped at twenty.

*Blocked branch, explicit:* if the verdict is `BLOCKED`, every result field stays **blank**
— not "assumed pass", not "verified by simulator", not "inferred from the decrypted
artifact". Write the standing instruction into the document itself: this UAT is re-run
unchanged when a device becomes reachable, and until then the phase's device evidence is
recorded as absent. Reference the Phase 10 precedent by name so the next reader sees it is
a policy, not an oversight.

*Blocked follow-up:* if blocked, open a pending todo under `.planning/todos/pending/`
naming this UAT and the artifacts' SHA-256s, so a later run can confirm it is testing the
same build rather than a drifted one.

**Then run it if you can.** If the verdict is `REACHABLE` and iPhone Mirroring is live,
drive the device directly rather than issuing the user a list of taps, and exhaust what can
be observed in the session before handing it back. Record every observation in the document
and, where it settles a capability question, in `docs/CAPABILITY-DECISIONS.md`.
  </action>

  <verify>
    <automated>xcrun devicectl list devices > /tmp/12-device-probe.txt 2>&1; echo "probe exit: $?"; cat /tmp/12-device-probe.txt; test -f .planning/phases/12-state-shape-sentinel-gaps-exit-events-and-active-session/12-UAT.md && python3 -c "
import pathlib,re
p=pathlib.Path('.planning/phases/12-state-shape-sentinel-gaps-exit-events-and-active-session/12-UAT.md')
t=p.read_text()
assert re.search(r'REACHABLE|BLOCKED', t), 'no reachability verdict recorded'
assert 'xcrun devicectl list devices' in t, 'probe command not recorded in the document'
for needle in ('leave and confirm','restore','Create','superseded','Control Room','exit_events'):
    assert needle.lower() in t.lower(), 'missing test: '+needle
assert 'simulator' in t.lower(), 'the document must state why a simulator run may not be substituted'
for sha in ('sha256','SHA-256','SHA256'):
    if sha in t: break
else: raise AssertionError('signed-artifact hashes not recorded in the UAT header')
print('12-UAT.md is cold-runnable and carries a recorded verdict')"</automated>
  </verify>

  <acceptance_criteria>
    - `12-UAT.md` exists at the phase directory path and records the verbatim output of `xcrun devicectl list devices` alongside a `REACHABLE` or `BLOCKED` verdict.
    - The document names the commit and the two signed artifacts' SHA-256s, so a later run can confirm it is testing the same build.
    - It contains at least the seven tests above, each with an explicit expected observation and its own result field, including a **Create on a clean install** step whose expected observation is that the user is asked for a URL rather than a placeholder being opened.
    - It states, in the document itself, why a simulator run may not be substituted — naming Personal Automation triggers, the measured absence of `com.apple.mobilenotes` from the booted simulator, and real-hardware environmental behaviour.
    - If `BLOCKED`: every result field is blank, the standing "re-run unchanged when a device becomes reachable" instruction is present, the Phase 10 precedent is named, and a matching pending todo exists under `.planning/todos/pending/` carrying the artifacts' SHA-256s.
    - If `REACHABLE`: every observation is recorded from the device, and no result field contains an inference drawn from a simulator run or from a decrypted artifact.
  </acceptance_criteria>

  <done>A cold-runnable device UAT for the exit-recording path exists with an explicit reachability verdict, and either the results are recorded from a real device or the document is honestly BLOCKED with blank results and a tracked follow-up.</done>
</task>

<task type="auto">
  <name>Task 3: Discharge the recording duty — deviations, assumptions and decisions into BUILD-NOTES and CAPABILITY-DECISIONS</name>

  <files>docs/BUILD-NOTES.md, docs/CAPABILITY-DECISIONS.md</files>

  <read_first>
    - `docs/BUILD-NOTES.md` — the existing per-phase heading structure and the register format used for prior deviations, so the Phase 12 section matches rather than inventing a new shape.
    - `docs/CAPABILITY-DECISIONS.md` — the BD-record format, and BD-06-A1 Amendment 3 ("there is no installed base to protect"), which the schema bump relied on.
    - The four SUMMARY files from Plans 12-01 through 12-04, which carry the observed guard messages, the selected `create_target_url` option, and the Aware-fork verifier-gap outcome.
    - `.claude/CLAUDE.md` §9 "The recording duty" — probes are cheap to build and expensive to re-run; the record is the whole return on them.
  </read_first>

  <action>
Write one Phase 12 section in `docs/BUILD-NOTES.md` covering everything this phase learned
or deliberately left open. Match the file's existing heading and register format.

**Assumptions carried, each with its status and the rung that would settle it:**
- **A1** — `is.workflow.actions.repeat.each` over an empty array. `[ASSUMED]` a
  zero-iteration no-op. **Unsettled.** Settleable at evidence rung 2 (a simulator probe; no
  Notes app, no Apple Intelligence, no Personal Automation, no real hardware). **Planner
  decision PD-1: deliberately not spent.** The seed fixes the absent case either way, so a
  probe could not change one line of code — it would only change severity framing. Record
  that reasoning, not just the verdict, so a later reader does not re-open it as an
  oversight.
- **A2** — a dotted read whose *final* segment is missing raises identically to one whose
  *intermediate* segment is missing. HIGH confidence, but inference from two written records
  (`.claude/CLAUDE.md`'s "any missing segment" wording and the generator's own comment at the
  `sequences.<Sequence>.<Dispatch Circle>` site), not a fresh device measurement. It is what
  makes `profile_snapshot.create_target_url` a defect rather than tidying.
- **A3** — "no installed base" re-confirmed against BD-06-A1 Amendment 3 before the schema
  bump 3→4. Record what the bump costs if that record is ever wrong: heat, gravity,
  pressure, the rolling windows and every `exit_stats[*].samples`, with no migration, no
  dual-key alias and no read-time normalisation, all three forbidden by name.
- **A5** — `set_value` on a dotted key whose leaf does not yet exist creates the leaf.
  Already relied on in shipped builds; the four-leaf seed removes the dependency.

**Decisions taken by the planner, each with its rationale:**
- **PD-1** — no simulator probe for A1 (above).
- **PD-2** — the `create_target_url` recommendation and the option actually selected at
  Plan 12-04's checkpoint, with the stated reason. This is a `one-way` decision; record it
  where a future phase will find it.
- **PD-3** — the Aware fork's verifier gap was closed completely rather than partially:
  `verify_pending_exit_seed`, `verify_panic_escape_seed`, `verify_compound_value_reads` and
  `verify_conditional_action_string` were armed alongside the two new verifiers. Record the
  measured outcome — all passed, or which one fired and what it said.

**Measured corrections to the phase research, recorded so the next reader does not repeat
the error:**
- `verify_state_seed()`'s generalisation is **not** a pure deletion. The `settings_snapshot`
  root filter was doing two jobs; removing it alone would test 74 `Config` and
  `Previous Session` keys against the bootstrap seed and fail the build. The correct change
  adds a source-variable filter (`STATE_READ_SOURCE_VARIABLES`). Record the measured source
  counts: `State` 141 literal / 6 composite, `Reloaded State` 44 literal, `Config` 30
  literal / 23 composite, `Previous Session` 3 literal.
- `route_exit()`'s Create branch needed **no** gate change under the new shape: a bare
  `active_session.id` dotted read followed by a condition-4 ownership compare is the target
  shape once the container is a seeded invariant, so what the research flagged as a missing
  enclosing gate became correct rather than defective. Record this explicitly so a later
  reader does not "fix" it back.
- `open_pipeline()` writes **four** leaves, not three: `.intention` is cleared to preserve
  the wholesale-replace semantics the old container write provided.
- The product display names are `Core` / `Aware`, not `Dumb` / `Sentient`.
  `.claude/CLAUDE.md` was not updated at Phase 11 plan 06's rename and is the source of the
  stale text that reached `12-CONTEXT.md`.

**Gate B baselines.** Record both forks' verbatim gate B output with action indices
normalised, so a future run can diff against them, and restate that gate B is advisory and
never `&&`-chained into a definition of done.

**`docs/CAPABILITY-DECISIONS.md`.** Add a record **only** if this phase settled a capability
question — i.e. only if Plan 12-05 Task 2's UAT actually ran on a device and observed
something new. If the UAT was BLOCKED, add nothing there and say so in BUILD-NOTES; an
empty capability record is better than a speculative one.
  </action>

  <verify>
    <automated>python3 -c "
import pathlib
bn=pathlib.Path('docs/BUILD-NOTES.md').read_text()
sec=bn[bn.lower().rfind('phase 12'):]
assert sec, 'no Phase 12 section in docs/BUILD-NOTES.md'
required=['A1','A2','A3','PD-1','PD-2','PD-3','STATE_READ_SOURCE_VARIABLES','WFCreateNoteInput','Core','Aware']
missing=[r for r in required if r not in sec]
assert not missing, 'Phase 12 BUILD-NOTES section missing: '+repr(missing)
assert 'rung 2' in sec.lower(), 'A1 record must name the evidence rung that would settle it'
print('recording duty discharged')" && for f in state_engine_self_check phase5_self_check phase6_self_check phase7_self_check phase9_self_check sentient_audit_check sentient_core_check environmental_restore_check router_ui_census sequence_dispatch_check note_identity_check manifest_check; do python3 docs/$f.py || exit 1; done</automated>
  </verify>

  <acceptance_criteria>
    - `docs/BUILD-NOTES.md` has a Phase 12 section naming A1, A2, A3, PD-1, PD-2 and PD-3, each with its status and rationale, and A1's entry names evidence rung 2 as the channel that would settle it.
    - The section records the four measured corrections to the research: the `STATE_READ_SOURCE_VARIABLES` filter requirement with the measured per-source read counts; that `route_exit()`'s Create branch correctly needed no change; that `open_pipeline()` writes four leaves; and the Core / Aware display-name correction.
    - Both forks' gate B outputs are recorded verbatim with normalised indices, alongside the restatement that gate B is advisory and never `&&`-chained.
    - `docs/CAPABILITY-DECISIONS.md` gains a record **only** if the device UAT actually ran and observed something new; if the UAT was BLOCKED, BUILD-NOTES says so explicitly and CAPABILITY-DECISIONS is untouched.
    - All twelve `docs/*.py` checkers still exit 0 after the documentation edits.
  </acceptance_criteria>

  <done>Every assumption, planner decision, measured research correction and gate B baseline this phase produced is recorded where the next reader will find it, and no capability claim was made that a device did not support.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| built source → signed artifact → user's iPhone | The only path by which this phase's code reaches a device. A signed artifact under a wrong filename is a silently dead install; a MANIFEST row that does not match disk is a false provenance claim. |
| written record → future phase | `docs/BUILD-NOTES.md` and `docs/CAPABILITY-DECISIONS.md` are the only memory this project has between phases. A fabricated device verdict recorded here becomes ground truth for every later decision. |

**ASVS L1 applicability** (per `12-RESEARCH.md` §Security Domain, stated not blank):
**V2 Authentication**, **V4 Access control** — inapplicable (no accounts, no privilege
boundary). **V6 Cryptography — delegated, and this is the plan where it applies**: signing
is Apple's `shortcuts sign` / AEA1 and nothing is hand-rolled; verification of what shipped
uses Apple's own `aea` / `aa` tooling against the artifact's embedded
`SigningCertificateChain`. **V5 Input validation**, **V7 Error handling**, **V12 File
handling** — unchanged by this plan.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-24 | Denial of service | a signed artifact shipping under a wrong filename — a silently dead install | high | mitigate | Sign with the exact live display name (`Core` / `Aware`, no `_signed` suffix); the verify block asserts both basenames exist, are non-zero, begin with `AEA1`, and that no `_signed` file leaked into the directory. `docs/manifest_check.py`'s DIST-04 assertion is the standing control. |
| T-12-25 | Repudiation | a MANIFEST row asserting a hash the file on disk does not have | high | mitigate | Every one of the six rows is recomputed, not only the ones believed to have moved; Phase 10 measured three of six wrong at once. The verify block independently recomputes SHA-256 and byte size and greps both out of the MANIFEST text. |
| T-12-26 | Tampering | shipping a plist different from the one that was validated | medium | mitigate | Recover each signed artifact's payload through `aea decrypt` + `aa extract` and confirm `WFWorkflowName` and the Phase 12 bootstrap shape in what actually shipped, rather than trusting the unsigned source plus a file mtime. Unavailability of `aea` / `aa` is recorded as an explicit deviation, never a silent skip. |
| T-12-27 | Repudiation | a device-evidence claim that no device produced | high | mitigate | The reachability probe output is recorded verbatim and the verdict is recorded either way. `must_haves.prohibitions` forbids substituting a simulator run or a decrypted-artifact inference; the BLOCKED branch leaves results blank and opens a tracked todo carrying the artifacts' hashes. |
| T-12-28 | Tampering | a parameter-key or picker-literal regression introduced by the nine moved emission sites | high | mitigate | Gate B's advisory read is taken per fork after the emission changes and compared against the recorded single-line waiver baseline; any additional line is treated as a real finding and blocks the artifact. Gate A remains the mandatory clean gate because gate B at target 27 can accept an OS27-only key iOS 26 does not offer. |
| T-12-29 | Information disclosure | behavioural data leaving the device | low | accept | Structurally impossible; zero network actions in either generator, and this plan adds none. |
| T-12-SC | Tampering | npm/pip/cargo installs | n/a | accept | This phase installs no package (`12-RESEARCH.md` §"Package Legitimacy Audit": every import in scope is Python standard library). No legitimacy checkpoint is raised. |
</threat_model>

<flagged_assumptions>
- **Pitfall 9 stands: the exit path has zero device evidence at any rung.** Everything this phase asserts about runtime behaviour on the exit path is rung-1 (file-level) or rung-2-ceiling-limited evidence. The UAT's blocked branch is planned precisely because that is the likely outcome, and a blank is the honest record.
- **`aea` / `aa` availability was last exercised in Phase 11 and is not re-measured at plan time.** If either is missing, Task 1 step 5 is skipped with an explicit recorded deviation rather than silently.
- **The macOS `shortcuts` CLI is assumed present** because signing succeeded on this machine in Phase 11. There is no fallback: signing is macOS-only and an unsigned XML is not importable. If it is missing the task halts rather than shipping an unsigned artifact.
</flagged_assumptions>

<verification>
- Provenance gate (D-01) exits 0 before every builder invocation.
- Full suite: `python3 tools/build_state_engine.py && python3 tools/build_sentient.py` then all twelve `docs/*.py` checkers — including `manifest_check.py`, which only becomes green after this plan re-signs and refreshes the rows — then gate A on both forks.
- Gate B per fork as a **standalone** advisory command, never `&&`-chained into a definition of done (D-02).
- Phase gate before `/gsd-verify-work`: full suite green, gate A clean ×2, gate B showing exactly the one waived line ×2, both forks signed under the Core / Aware names, `docs/manifest_check.py` green, `12-UAT.md` present with a recorded verdict.
</verification>

<success_criteria>
- Both forks signed under their exact live display names with no `_signed` suffix, each a non-zero-byte `AEA1` archive.
- All six MANIFEST rows match disk and `docs/manifest_check.py` exits 0.
- Gate A clean on both forks; gate B's advisory output recorded verbatim showing exactly the one permitted waiver line per fork.
- `12-UAT.md` is cold-runnable, carries a recorded reachability verdict, and is either run on a device or honestly BLOCKED with blank results and a tracked follow-up.
- Every assumption, planner decision, measured research correction and gate B baseline is recorded in `docs/BUILD-NOTES.md`.
</success_criteria>

<output>
Create `.planning/phases/12-state-shape-sentinel-gaps-exit-events-and-active-session/12-05-SUMMARY.md` when done.
</output>
