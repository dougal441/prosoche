---
phase: 12-state-shape-sentinel-gaps-exit-events-and-active-session
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/build_state_engine.py
  - tools/build_sentient.py
  - src/PROSOCHE-Dumb.xml
  - src/PROSOCHE-Sentient.xml
autonomous: true
requirements: [STATE-12]

must_haves:
  truths:
    - "The bootstrap state.json template declares exit_events as [] and exit_selection_counter as 0, so neither key is ever absent from a freshly built state file."
    - "The exit_events rolling window is bounded at twenty: record_exit_and_route() appends the new event to Exit Events Next first, then copies only prior entries whose Repeat Index is below 20 (condition code 0 against number 20); an entry at exactly index 20 is dropped, not retained."
    - "exit_events ordering is newest-first and stable: the new event is appended before the Repeat With Each replays prior entries in stored order, so two events with the same timestamp keep their stored relative order."
    - "With exit_selection_counter seeded 0, the condition-101 'does not have any value' guards at select_exit() and record_exit_and_route() take their Otherwise (Nothing) arm on clean state and the counter arithmetic proceeds from 0 — no unset-counter branch is reachable on a freshly built state file."
    - statement: "exit_events is seeded [] so Repeat With Each never receives an absent key; whether is.workflow.actions.repeat.each over an empty array is a zero-iteration no-op or a type error remains unsettled ([ASSUMED] A1, settleable only at evidence rung 2)."
      verification: backstop
    - "schema_version is 4 in the emitted bootstrap template and all three coupled generator literals (SCHEMA_VERSION, SCHEMA_VERSION_PREVIOUS, SCHEMA_VERSION_ACCEPTED) move together in one commit."
    - "Both forks rebuild byte-idempotently (docs/phase6_self_check.py builds twice and compares digests) and pass gate A clean at --target-macos 26 --target-platform all."
  prohibitions:
    - "Never seed or clear a state leaf as the empty string — an empty value passes `has any value` (Donor 6.1) and can reach Set Brightness or Set Volume, stranding the user on a dark or silent device."
    - "Never silence a build guard or a checker assertion to make the suite green — no new exemption entry, no skip, no weakened gate that hides a real finding."
  artifacts:
    - path: "tools/build_state_engine.py"
      provides: "EXIT_EVENTS_SEED constant, EXIT_EVENTS_ANCHOR constant, seed_exit_events(), verify_exit_events_seed(), schema literals bumped to 4"
      contains: "def seed_exit_events"
    - path: "tools/build_sentient.py"
      provides: "verify_exit_events_seed armed per fork"
      contains: "verify_exit_events_seed"
    - path: "src/PROSOCHE-Dumb.xml"
      provides: "regenerated Core fork carrying the seeded exit_events/exit_selection_counter and schema_version 4"
    - path: "src/PROSOCHE-Sentient.xml"
      provides: "regenerated Aware fork inheriting the same seeded template"
  key_links:
    - from: "tools/build_state_engine.py seed_exit_events()"
      to: "tools/build_state_engine.py verify_exit_events_seed()"
      via: "both read EXIT_EVENTS_SEED so seeder and guard cannot silently drift"
      pattern: "EXIT_EVENTS_SEED"
    - from: "tools/build_state_engine.py main()"
      to: "fix_state_rebind(actions)"
      via: "seed_exit_events(actions) is called BEFORE fix_state_rebind(actions) — both edit the same template token"
      pattern: "seed_exit_events\\(actions\\)"
    - from: "tools/build_sentient.py verify chain"
      to: "tools/build_state_engine.py verify_exit_events_seed()"
      via: "imported symbol, asserted per fork rather than inferred from Dumb"
      pattern: "verify_exit_events_seed\\(actions\\)"
---

<objective>
TRACER SLICE. Carry one state key end-to-end through every layer this phase touches —
bootstrap template seed → build guard → `main()` registration → schema version bump →
cross-fork arming → regenerated artifact → 12-checker suite → gate A — before the 34-site
`active_session` refactor touches anything.

`exit_events` is the right tracer because it is the *thinnest* key in the phase: a flat
Array seed with **no dotted read and no gate at all** (measured — `12-RESEARCH.md`
§"exit_events: a one-line array seed"). It therefore proves the whole pipeline without
carrying any gate-semantics risk. If `_replace_in_token()` mis-shifts the four
`attachmentsByRange` offsets, if the three-literal schema bump is applied partially, if the
double-build idempotency check breaks on a new seeder, or if the Aware fork fails to
inherit the seeded template — all four fail here, on one commit, instead of on top of a
nine-site emission refactor.

Purpose: close STATE-12's literal claim — "State is persisted as a bounded, versioned JSON
document with rolling windows for sessions, contracts, and per-exit aggregates." The
per-exit rolling window (`exit_events`, cap 20) and its selection counter are the two
windows the versioned document does not currently declare. `recent_sessions` and
`recent_contracts`, its two siblings in `COMPOUND_STATE_KEYS`, are both already seeded `[]`.

Output: two new generator symbols, one bumped schema triple, two regenerated forks.

**Binding constraints carried from `12-CONTEXT.md` `<decisions>` (referenced below as D-01..D-04):**
- **D-01** Build provenance gate — `git merge-base --is-ancestor 7ca8ebbfe467da38e594bdd41687c094a1f0c678 HEAD` must exit 0 before either builder runs; abort the rebuild if it fails.
- **D-02** Two-gate validation — gate A (`--target-macos 26 --target-platform all`) mandatory and clean; gate B (`--target-macos 27 --target-platform all`) advisory, exactly one waived line per fork, **never** chained into a definition of done.
- **D-03** Never fabricate an action identifier, parameter key or enum literal; if it cannot be verified use the safest fallback and record the deviation.
- **D-04** Signed artifacts carry the exact display name with no `_signed` suffix. **Corrected literals:** Phase 11 plan 06 renamed the products — the live names are `PROSOCHĒ — Nine Circles — Core` and `PROSOCHĒ — Nine Circles — Aware`, hard-coded as `DISPLAY_NAMES` in `docs/manifest_check.py`. `.claude/CLAUDE.md` and `12-CONTEXT.md` carry the stale `Dumb`/`Sentient` literals; the *rule* is unchanged, only the two strings moved.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-state-shape-sentinel-gaps-exit-events-and-active-session/12-RESEARCH.md
@.planning/phases/12-state-shape-sentinel-gaps-exit-events-and-active-session/12-PATTERNS.md
@.planning/phases/12-state-shape-sentinel-gaps-exit-events-and-active-session/12-VALIDATION.md
@.claude/CLAUDE.md
</context>

## Artifacts this phase produces

New and changed symbols introduced **by this plan** (the source-grounding pass must treat
these as created here, not as drift):

| Symbol / path | Kind | File |
|---|---|---|
| `EXIT_EVENTS_SEED` | new module constant | `tools/build_state_engine.py` |
| `EXIT_EVENTS_ANCHOR` | new module constant | `tools/build_state_engine.py` |
| `seed_exit_events()` | new function | `tools/build_state_engine.py` |
| `verify_exit_events_seed()` | new function | `tools/build_state_engine.py` |
| `SCHEMA_VERSION` = `"4"` | changed literal | `tools/build_state_engine.py` |
| `SCHEMA_VERSION_PREVIOUS` = `"3"` | changed literal | `tools/build_state_engine.py` |
| `SCHEMA_VERSION_ACCEPTED` = `("1","2","3","4")` | changed literal | `tools/build_state_engine.py` |
| `verify_exit_events_seed` import + call | new import / call site | `tools/build_sentient.py` |
| `verify_pending_exit_seed`, `verify_panic_escape_seed`, `verify_compound_value_reads`, `verify_conditional_action_string` imports + calls | new import / call sites (pre-existing-gap closure, PD-3) | `tools/build_sentient.py` |
| `"exit_events"`, `"exit_selection_counter"` keys | new bootstrap template keys | `src/PROSOCHE-Dumb.xml`, `src/PROSOCHE-Sentient.xml` |

Symbols created by **later plans in this phase** (listed so the drift pass sees the whole
set): `ACTIVE_SESSION_SEED`, `ACTIVE_SESSION_ANCHOR`, `seed_active_session()`,
`verify_active_session_seed()`, `CREATE_TARGET_URL_SEED`, `seed_create_target_url()`,
`STATE_READ_SOURCE_VARIABLES`, and the file
`.planning/phases/12-state-shape-sentinel-gaps-exit-events-and-active-session/12-UAT.md`.

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end "the bootstrap document declares exit_events" — one key through every layer</name>

  <precondition>`git merge-base --is-ancestor 7ca8ebbfe467da38e594bdd41687c094a1f0c678 HEAD` exits 0 (D-01), AND `docs/CAPABILITY-DECISIONS.md` still carries BD-06-A1 Amendment 3's "there is no installed base to protect" record (grep for "no installed base") — the schema bump discards heat, gravity, pressure, the rolling windows and every `exit_stats[*].samples` on any device holding a real state.json, and BD-06-A1 forbids a migration, a dual-key alias and read-time normalisation by name.</precondition>

  <reversibility rating="costly">Changes the on-disk `state.json` contract and forces one device rebuild; undoing it after devices adopt schema 4 needs another bump and another total state loss. Chartered by the ROADMAP phase goal, so recorded but not gated.</reversibility>

  <files>tools/build_state_engine.py, tools/build_sentient.py, src/PROSOCHE-Dumb.xml, src/PROSOCHE-Sentient.xml</files>

  <read_first>
    - `tools/build_state_engine.py` — read `seed_panic_escape()` and its constants `PANIC_ESCAPE_KEY` / `PANIC_ESCAPE_SEED` / `PANIC_ESCAPE_ANCHOR` (the exact insert-after-anchor analog); `verify_panic_escape_seed()` and `verify_pending_exit_seed()` (the verifier analogs, including the two-step placeholder substitution); `_state_template()` and `_replace_in_token()`; the `SCHEMA_VERSION` / `SCHEMA_VERSION_PREVIOUS` / `SCHEMA_VERSION_ACCEPTED` constant block and `fix_state_rebind()`'s docstring; `main()`'s seeder call block and its verify chain; `record_exit_and_route()`'s `get_value("exit_events", …)` read and the `COMPOUND_STATE_KEYS` registry.
    - `tools/build_sentient.py` — the `from build_state_engine import (…)` block and the verify chain around `verify_state_seed(actions)` / `verify_restore_gates(actions)` / `verify_sentinel_gates(actions)`, including the "Sentient INHERITS … so the assertion is the whole point here" comment idiom.
    - `.planning/phases/12-.../12-PATTERNS.md` — §`seed_exit_events()` and §`SCHEMA_VERSION 3→4`, which quote both analogs verbatim.
    - `.claude/CLAUDE.md` — §1 "Exact validator invocation" (the two-gate rule) and §Conventions (the seven parameter-defect axes).
  </read_first>

  <action>
Add the `exit_events` seed to the bootstrap `state.json` template, guard it, register it,
bump the schema, and arm it on both forks — one commit, proven end-to-end.

**1 — Constants.** Add `EXIT_EVENTS_SEED` as a dict mapping both keys to their seeded
values: `exit_events` → `[]` and `exit_selection_counter` → `0`. Add `EXIT_EVENTS_ANCHOR`
= the literal template line `"recent_contracts": [],` — anchor on the neighbouring
compound-key line, never on a line number, exactly as `PANIC_ESCAPE_ANCHOR` does. Place
both immediately above the new seeder, mirroring the placement of `PANIC_ESCAPE_KEY` /
`PANIC_ESCAPE_SEED` / `PANIC_ESCAPE_ANCHOR`. Give `EXIT_EVENTS_ANCHOR` the same
"anchored on the neighbouring line, never a line number" rationale comment
`PANIC_ESCAPE_ANCHOR` carries.

**2 — `seed_exit_events(actions)`.** Copy `seed_panic_escape()`'s insert-after-anchor
mechanics exactly: locate the template with `_state_template(actions)` (content-anchored
on `"schema_version"`, never an index); idempotency early-return on the substring test
`'"exit_events"' in inner["string"]` — a collision-free guard token, unlike `'"id"'`;
derive `indent` from the anchor line rather than hard-coding it; emit **every** text edit
through `_replace_in_token(inner, old, new)`, never `str.replace`. Both keys go in one
`_replace_in_token()` call: the anchor line followed by one line per `EXIT_EVENTS_SEED`
entry, JSON-rendered so that `[]` and `0` stay unquoted (they are an Array and a Number,
not strings — quoting either would make `verify_compound_value_reads()`'s
`COMPOUND_STATE_KEYS` claim about `exit_events` false at the seed).

Docstring: follow `seed_panic_escape()`'s convention — name the mechanism *and* the
failure it prevents. State the honest severity: STATE-12 requires the versioned document
to declare its rolling windows, and `exit_events` is the only member of
`COMPOUND_STATE_KEYS` that is undeclared while `recent_sessions` and `recent_contracts`
are both seeded `[]`. Do **not** claim a hard error: measured, `exit_events` has no dotted
read and no gate, and a flat read of a missing key returns nothing with no error, so the
pre-fix failure mode is `Repeat With Each` over nothing — behaviour that is `[ASSUMED]`
(assumption A1) and settleable only at evidence rung 2. Name that rung in the docstring
rather than asserting a crash. Cite `.claude/CLAUDE.md` §5 on out-of-bounds attachment
ranges as `seed_panic_escape()` does.

**3 — `verify_exit_events_seed(actions)`.** Copy `verify_pending_exit_seed()`'s skeleton:
`_state_template(actions)`, then the mandatory two-step placeholder substitution
(`.replace('"￼"', '"x"')` then `.replace("￼", "0")`) before `json.loads`, then a
`json.JSONDecodeError` arm raising `SystemExit`. Assert **against the constant** — compare
`seed.get(key)` to each `EXIT_EVENTS_SEED` value — so the seeder and the guard cannot
silently agree on being wrong (stated as this project's convention in three separate
docstrings). Raise `SystemExit`, never `assert`; the message is the diagnostic. The
message must cite STATE-12 and the `recent_sessions` / `recent_contracts` precedent, and
must **not** make a crash claim.

**4 — `main()` registration.** Call `seed_exit_events(actions)` in the seeder block
**before** `fix_state_rebind(actions)` — the ordering comment at that call site records why
it is load-bearing ("the rebind pass also edits the same template token, and seeding a new
field is the reason the schema_version bump below exists"). Add
`verify_exit_events_seed(actions)` to the verify chain alongside
`verify_pending_exit_seed(actions)` and `verify_panic_escape_seed(actions)`, so it raises
`SystemExit` before the single `SOURCE.write_bytes()`.

**5 — Schema bump 3→4, all three literals in this commit.** Set `SCHEMA_VERSION = "4"`,
`SCHEMA_VERSION_PREVIOUS = "3"`, `SCHEMA_VERSION_ACCEPTED = ("1", "2", "3", "4")`. The
recognition tuple must admit the literal the transformer is about to write or the **next**
build aborts with "schema version check conditional not found", one build downstream of
the change that caused it. Extend the existing comment above `SCHEMA_VERSION_ACCEPTED`
with a one-line note that the 3→4 move is Phase 12's — new bootstrap fields
(`exit_events`, `exit_selection_counter`, and in later plans `active_session`'s leaves)
only reach a device that already holds a state.json by way of the bump, and BD-06-A1
Amendment 3's "no installed base" record is what licenses the loss.

**6 — Arm on both forks (D-03 discipline: assert per fork, never infer).** In
`tools/build_sentient.py`, add `verify_exit_events_seed` to the
`from build_state_engine import (…)` block, keeping it alphabetically sorted, one symbol
per line. Call it in the verify chain next to `verify_state_seed(actions)`, under a
comment in the file's own established "Sentient INHERITS the seeded bootstrap template
from the built Dumb source rather than re-seeding it, so the assertion is the whole point
here" idiom.

**7 — Rebuild and prove.** Run the provenance gate (D-01), then
`python3 tools/build_state_engine.py` then `python3 tools/build_sentient.py`. Never
hand-edit `src/PROSOCHE-Dumb.xml` or `src/PROSOCHE-Sentient.xml`; both are generator
outputs written exactly once per run, and `build_sentient.py` asserts "frozen Dumb source
changed" if the Dumb source moves during its run.

**8 — Demonstrate guard sensitivity.** A new guard is only credible if it is shown to
fire. Temporarily neutralise the seeder (comment out the `seed_exit_events(actions)` call
in `main()`) against a checkout of the pre-seed `src/PROSOCHE-Dumb.xml`, confirm
`verify_exit_events_seed()` raises `SystemExit` with the STATE-12 message, then restore.
Record the observed message in the commit body.
  </action>

  <verify>
    <automated>git merge-base --is-ancestor 7ca8ebbfe467da38e594bdd41687c094a1f0c678 HEAD && python3 tools/build_state_engine.py && python3 tools/build_sentient.py && python3 docs/state_engine_self_check.py && python3 docs/phase6_self_check.py && python3 -c "import sys;sys.path.insert(0,'tools');import plistlib,pathlib,json,build_state_engine as B;a=plistlib.loads(pathlib.Path('src/PROSOCHE-Dumb.xml').read_bytes())['WFWorkflowActions'];_,i=B._state_template(a);s=json.loads(i['string'].replace('\"￼\"','\"x\"').replace('￼','0'));assert s['exit_events']==[], s['exit_events'];assert s['exit_selection_counter']==0, s['exit_selection_counter'];assert s['schema_version']==4, s['schema_version'];assert B.SCHEMA_VERSION=='4' and B.SCHEMA_VERSION_PREVIOUS=='3' and '4' in B.SCHEMA_VERSION_ACCEPTED;print('ok')" && validate-shortcut src/PROSOCHE-Dumb.xml --target-macos 26 --target-platform all && validate-shortcut src/PROSOCHE-Sentient.xml --target-macos 26 --target-platform all</automated>
  </verify>

  <acceptance_criteria>
    - `python3 tools/build_state_engine.py` exits 0 and `python3 tools/build_sentient.py` exits 0.
    - The emitted bootstrap template in `src/PROSOCHE-Dumb.xml` parses (after the two-step placeholder substitution) to a dict where `exit_events == []`, `exit_selection_counter == 0` and `schema_version == 4`.
    - The same three assertions hold against `src/PROSOCHE-Sentient.xml` — the Aware fork inherited the seeded template.
    - `B.SCHEMA_VERSION == "4"`, `B.SCHEMA_VERSION_PREVIOUS == "3"`, and `"4" in B.SCHEMA_VERSION_ACCEPTED` all hold when `tools/build_state_engine.py` is imported.
    - `python3 docs/phase6_self_check.py` exits 0 — it invokes the builder twice and requires byte-identical output, so this proves `seed_exit_events()`'s substring early-return makes it idempotent.
    - `python3 docs/state_engine_self_check.py` exits 0.
    - Both `validate-shortcut … --target-macos 26 --target-platform all` runs print `Validation passed.` and exit 0 (gate A, D-02).
    - `grep -n 'exit_events' tools/build_state_engine.py` shows `get_value("exit_events", …)` at the `record_exit_and_route()` read site unchanged — the read was NOT routed through `read_value()` (which would fail `verify_compound_value_reads()` with "a compound (Array) state key is read via read_value()").
    - The four `attachmentsByRange` keys in the emitted template still each point at a `U+FFFC` character — enforced automatically by `_replace_in_token()`, which raises `SystemExit("attachment offset … no longer points at a placeholder")` otherwise; a clean build is the proof.
    - Guard sensitivity: with the `seed_exit_events(actions)` call neutralised against a pre-seed template, `verify_exit_events_seed()` raises `SystemExit` and the message names STATE-12. The observed message text appears in the commit body.
    - No fenced-code or hand-authored plist dict was added: every emitted action still comes from `read_value` / `get_value` / `set_value` / `if_block` (D-03).
  </acceptance_criteria>

  <done>The bootstrap state.json declares `exit_events: []` and `exit_selection_counter: 0`, `verify_exit_events_seed()` asserts it on both forks, `schema_version` is 4 across all three coupled literals and the emitted template, both forks rebuild idempotently, and gate A is clean on both.</done>
</task>

<task type="auto">
  <name>Task 2: Close the pre-existing Aware-fork verifier gap (PD-3)</name>

  <precondition>`git merge-base --is-ancestor 7ca8ebbfe467da38e594bdd41687c094a1f0c678 HEAD` exits 0 (D-01). Stated unconditionally even though Task 1 precedes this task in the same plan: `.claude/CLAUDE.md` §Constraints gates *every* builder invocation on it, and this task runs both builders.</precondition>

  <files>tools/build_sentient.py</files>

  <read_first>
    - `tools/build_sentient.py` — the full `from build_state_engine import (…)` block (measured: exactly 13 symbols before this phase) and the whole verify chain, including the three separate "enforced PER FORK rather than inferred for Sentient from Dumb" comments that are this file's stated justification for asserting rather than assuming.
    - `tools/build_state_engine.py` — the docstrings of `verify_pending_exit_seed()`, `verify_panic_escape_seed()`, `verify_compound_value_reads()` and `verify_conditional_action_string()`, so the arming comment can name what each one actually protects on the Aware fork.
    - `.planning/phases/12-.../12-RESEARCH.md` §"Pitfall 6" — the measurement that the Sentient verifier set is a subset and already has a hole.
  </read_first>

  <action>
Measured (`12-RESEARCH.md` Pitfall 6): `tools/build_sentient.py` imports 13 symbols and runs
13 guards, and does **not** import `verify_pending_exit_seed`, `verify_panic_escape_seed`,
`verify_compound_value_reads` or `verify_conditional_action_string`. So the `pending_exit`
seed — the very pattern this phase mirrors — is not asserted on the Aware fork at all.

**Planner decision PD-3: close the gap completely rather than arming only the new
verifiers.** The phase's governing rule is "fix whole classes, never site-by-site"; a
verifier set that asserts the *new* seeds but not the pattern they mirror is exactly the
site-by-site posture the charter forbids. Cost is one import block plus four call lines,
and each guard is a pure assertion over the already-emitted artifact, so arming them cannot
change what ships — it can only reveal a pre-existing defect.

Add all four symbols to the `from build_state_engine import (…)` block (alphabetically
sorted, one per line, matching the block's existing style) and call all four in the verify
chain. Group them under one comment in the file's own "Sentient inherits X from the built
Dumb source, so this asserts the fork did not lose it" idiom, naming per guard what it
protects: `verify_pending_exit_seed` — the `{type, timestamp}` container the Aware fork
inherits and whose absence reproduced the confirmed cycle-16 hard error;
`verify_panic_escape_seed` — the `panic_escape_enabled` seed and its numeric gate;
`verify_compound_value_reads` — that no `COMPOUND_STATE_KEYS` member (`recent_sessions`,
`recent_contracts`, `exit_events`, `exit_stats.<name>.samples`,
`profile_snapshot.enabled_exits`) is read through `read_value()`, which is the guard
directly at risk from this phase's `exit_events` work;
`verify_conditional_action_string` — the ownership-compare `WFConditionalActionString`
shape that Plan 12-03 must preserve at four sites.

**Branch, and it is not optional.** Run the Aware build immediately after arming. If any
newly-armed guard fires, that is a **real finding**, not a reason to weaken the guard
(D-03). Do not delete the guard, do not add an exemption, do not edit the guard's
threshold. Instead: revert only that one guard's arming, record the exact `SystemExit`
message verbatim in `docs/BUILD-NOTES.md` under a Phase 12 heading, open a pending todo
under `.planning/todos/pending/` naming the guard and the message, and keep the other three
plus `verify_exit_events_seed` armed. If all four pass — the expected outcome, since both
seeds are green on the Core fork today — record that measured outcome in the commit body
instead.
  </action>

  <verify>
    <automated>git merge-base --is-ancestor 7ca8ebbfe467da38e594bdd41687c094a1f0c678 HEAD && python3 tools/build_state_engine.py && python3 tools/build_sentient.py && python3 -c "import re,pathlib;src=pathlib.Path('tools/build_sentient.py').read_text();body=[l for l in src.splitlines() if not l.strip().startswith('#')];joined=chr(10).join(body);missing=[g for g in ('verify_exit_events_seed','verify_pending_exit_seed','verify_panic_escape_seed','verify_compound_value_reads','verify_conditional_action_string') if joined.count(g)<2];print('armed-and-imported OK' if not missing else 'MISSING: '+repr(missing));raise SystemExit(1 if missing else 0)" && python3 docs/sentient_core_check.py && python3 docs/sentient_audit_check.py && validate-shortcut src/PROSOCHE-Sentient.xml --target-macos 26 --target-platform all</automated>
  </verify>

  <acceptance_criteria>
    - Each of `verify_exit_events_seed`, `verify_pending_exit_seed`, `verify_panic_escape_seed`, `verify_compound_value_reads`, `verify_conditional_action_string` appears at least twice in `tools/build_sentient.py` outside comment lines — once in the import block and once as a call in the verify chain. (The check filters `#`-leading lines so the arming comment's own prose cannot satisfy it.)
    - `python3 tools/build_sentient.py` exits 0 with all newly-armed guards active — OR, if a guard fired, only that one guard's arming is reverted, its verbatim `SystemExit` message is recorded in `docs/BUILD-NOTES.md`, and a matching pending todo file exists under `.planning/todos/pending/`.
    - `python3 docs/sentient_core_check.py` and `python3 docs/sentient_audit_check.py` both exit 0.
    - `validate-shortcut src/PROSOCHE-Sentient.xml --target-macos 26 --target-platform all` prints `Validation passed.` and exits 0.
    - No guard body in `tools/build_state_engine.py` was edited by this task — `git diff --stat` shows `tools/build_sentient.py` as the only changed source file.
  </acceptance_criteria>

  <done>The Aware fork's verify chain asserts the two seed patterns, the compound-read rule and the conditional-string shape it previously inherited unasserted; either all four pass or each failure is recorded as a finding with the guard left intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `PROSOCHE/state.json` → the running Shortcut | The only persisted input. It is user-reachable through the Files app and survives across runs, so its shape is untrusted at read time. Validated by the three-check gate at emitted actions 27 / 37 / 39 (`schema_version` present, equals `SCHEMA_VERSION`, `profile` non-empty) with rebuild-on-failure. |
| generator source → emitted plist | A hand-authored plist dict bypasses all seven parameter-defect guards. Crossing this boundary is only permitted through `read_value` / `get_value` / `set_value` / `if_block`. |

**Structurally inapplicable ASVS categories** (reused from `12-RESEARCH.md` §Security
Domain — stated, not left blank): **V2 Authentication** — no accounts, no login, no
identity. **V4 Access control** — single user, single device, no privilege boundary.
**V6 Cryptography** — delegated entirely to Apple's `shortcuts sign` / AEA1; nothing
hand-rolled. There is no network, no server, no auth and no secret anywhere in this
project, so the injection / transport / secret-management families do not apply either.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-01 | Tampering | bootstrap `state.json` template in `src/PROSOCHE-Dumb.xml` action 75 | high | mitigate | Seeder and guard are separate symbols reading one shared constant (`EXIT_EVENTS_SEED`), so template and generator cannot silently drift; `verify_exit_events_seed()` raises `SystemExit` before the single `SOURCE.write_bytes()`. |
| T-12-02 | Tampering | `attachmentsByRange` offsets in the edited template token | high | mitigate | Every edit routes through `_replace_in_token()`, which shifts all four offsets past the edit and re-asserts each still lands on `U+FFFC`. `.claude/CLAUDE.md` §5 records that an out-of-bounds range can crash Shortcuts on import. Hand-computing offsets or using `str.replace` is prohibited by Task 1. |
| T-12-03 | Denial of service | `schema_version` recognition tuple | high | mitigate | All three coupled literals move in one commit; the emitted conditional literal is asserted by the Task 1 verify block. A partial bump does not fail this build — it fails the *next* one, which is why the assertion is on the emitted artifact, not only on the module constants. |
| T-12-04 | Tampering | Aware fork inheriting an unasserted seeded template | medium | mitigate | Task 2 arms `verify_exit_events_seed` plus four previously-unarmed guards per fork rather than inferring Aware's state from Core. |
| T-12-05 | Information disclosure | behavioural data leaving the device | low | accept | Structurally impossible: measured zero HTTP/network actions in either generator; `searchweb` / `searchmaps` / `openurl` are user-initiated app launches, not data egress. This plan adds none. |
| T-12-06 | Repudiation | a build artifact whose provenance cannot be established | medium | mitigate | D-01 provenance gate runs before either builder as a `<precondition>`; the signed-artifact hash refresh is Plan 12-05's `docs/manifest_check.py` gate. |
| T-12-SC | Tampering | npm/pip/cargo installs | n/a | accept | **This phase installs no package.** `12-RESEARCH.md` §"Package Legitimacy Audit" records that every import in scope is Python standard library (`json`, `plistlib`, `pathlib`, `hashlib`, `uuid`, `re`, `subprocess`, `tempfile`, `os`), verified by reading both generators' import blocks. No `[ASSUMED]` / `[SUS]` package exists, so no legitimacy checkpoint is raised. |
</threat_model>

<probe_edge_accounting>
## Spec-less probe fallback — full 11-row reconciliation (phase-wide)

This phase has no `12-SPEC.md`, so `## Edge Coverage` and `## Prohibitions` were absent and
the orchestrator ran the deterministic edge probe instead. It surfaced **11 applicable
edges, all `unresolved`**. This table is the denominator, recorded once here for the whole
phase so a reader can check the no-silent-drop equality rather than inferring it: **11
probe-surfaced items == 8 plain `truths` strings + 1 `backstop` marker + 2 flagged
assumptions.**

| # | Req | Category | Resolution | Authored in | The predicate |
|---|---|---|---|---|---|
| 1 | STATE-12 | adjacency | covered → plain truth | 12-01 `must_haves.truths` | Rolling window bounded at twenty; an entry at exactly Repeat Index 20 is dropped, not retained (condition code 0 against number 20). |
| 2 | STATE-12 | empty | **backstop marker** | 12-01 `must_haves.truths` | Seed makes the *absent* case unreachable; `Repeat With Each` over an *empty array* stays unsettled (A1, rung 2). |
| 3 | STATE-12 | ordering | covered → plain truth | 12-01 `must_haves.truths` | Newest-first and stable: the new event is appended before the repeat replays prior entries in stored order. |
| 4 | SESS-07 | unclassified | **flagged assumption** | 12-02 `<flagged_assumptions>` | Never auto-`backstop`ped. Reviewed manually: the SESS-07 edge is the three-arm nesting depth of the restore. |
| 5 | EXIT-01 | unclassified | **flagged assumption** | 12-03 `<flagged_assumptions>` | Never auto-`backstop`ped. Reviewed manually: EXIT-01 has no numeric/temporal/collection edge of its own in this phase. |
| 6 | EXIT-02 | adjacency | covered → plain truth | 12-03 `must_haves.truths` | Exit routing matches exact route names via condition 4, so prefix-sharing names never collide. |
| 7 | EXIT-02 | empty | covered → plain truth | 12-01 `must_haves.truths` | With `exit_selection_counter` seeded 0, both condition-101 guards take their Otherwise (Nothing) arm on clean state. |
| 8 | EXIT-02 | ordering | covered → plain truth | 12-03 `must_haves.truths` | The six exit routes and their state keys survive the refactor; `docs/phase6_self_check.py` passes. |
| 9 | SAFE-01 | adjacency | covered → plain truth | 12-03 `must_haves.truths` | The boundary value zero is never written; the floor is the device's true minimum per the BD-02 addendum, which this phase does not re-litigate. |
| 10 | SAFE-01 | empty | covered → plain truth | 12-03 `must_haves.truths` | A cleared or absent captured value never reaches Set Brightness: leaves are seeded `CLEARED_SENTINEL`, never `""`, and `"null"` coerced to a number is false. |
| 11 | SAFE-01 | ordering | covered → plain truth | 12-03 `must_haves.truths` | `restore_managed_settings()` is emitted before `notification()` and `save_state()`. |

**Prohibitions (§B, in-prompt two-stage pass).** Stage 1 over-produced ~10 raw candidates;
stage 2 dropped routine-engineering ones (coupled-edit hygiene, anchor re-pointing, checker
literal moves — all owned by the edge probe or by build guards) and kept four
values/safety/ethics items, distributed across the plans that can violate them and authored
**descriptor-less** into `must_haves.prohibitions` (never `truths`, never a `check_*`
scalar): never seed or clear a leaf as `""` (12-01/02/03/04); never silence a guard or
checker to go green (12-01/02/03/04); never weaken the session-ownership compare (12-03);
never record device evidence no device produced (12-05).

**Canon-referral drop, with breadcrumb.** Candidates in the canon security/compliance
families (OWASP injection, path traversal, prototype pollution, GDPR, generic fairness) were
recalled and **dropped, not minted**: this project has no network, no server, no auth, no
secrets and one fixed file path. `12-RESEARCH.md` §Security Domain records the
category-by-category inapplicability that each plan's `<threat_model>` restates.

**One extra `backstop` beyond the 11.** 12-04 carries a second
`{statement, verification: backstop}` truth covering `read_value()` of a JSON-`null` leaf.
It is **not** a probe row — it is a consequence of the option-B branch at that plan's
`checkpoint:decision` — and is excluded from the equality above by design.
</probe_edge_accounting>

<flagged_assumptions>
Surfaced explicitly rather than silently resolved (spec-less probe fallback §A/§C —
`unclassified` rows are never auto-`backstop`ped, and no probe-surfaced item is dropped):

- **A1 — `Repeat With Each` over an empty/absent input.** `12-RESEARCH.md` records this as the one genuinely open runtime question in the phase. It is settleable at evidence rung 2 (a simulator probe: no Notes app, no Apple Intelligence, no Personal Automation, no real hardware). **Planner decision PD-1: do not spend a probe.** The fix is byte-identical either way, so a probe cannot change one line of code; building, validating, signing and importing a probe artifact is a new defect surface spent purely on prose framing; and the seed makes the *absent* case unreachable, leaving only the empty-array case. The `.claude/CLAUDE.md` §9 rule — never climb higher than the open question requires — applies. Recorded as `[ASSUMED]` in `verify_exit_events_seed()`'s message and carried as a `verification: backstop` truth above, with rung 2 named as the channel that would settle it.
- **A3 — "no installed base" still holds.** Re-confirmation is a `<precondition>` on Task 1, checkable by grepping `docs/CAPABILITY-DECISIONS.md` for BD-06-A1 Amendment 3. If a device does hold a real `state.json`, the bump silently discards heat, gravity, pressure, the rolling windows and every `exit_stats[*].samples`, and BD-06-A1 forbids a migration by name.
- **Prohibition recall breadcrumb.** Canon security/compliance candidates (OWASP injection, path traversal, prototype pollution, GDPR) were recalled and **dropped, not minted**: this phase has no network, no server, no auth, no secrets and one fixed file path. `12-RESEARCH.md` §Security Domain records the category-by-category inapplicability.
</flagged_assumptions>

<verification>
- Provenance gate (D-01) exits 0 before every builder invocation.
- Quick chain after the task commit: `python3 tools/build_state_engine.py && python3 tools/build_sentient.py && python3 docs/state_engine_self_check.py && python3 docs/phase6_self_check.py`.
- Full 12-checker chain plus gate A on both forks at wave merge (command verbatim in `12-VALIDATION.md` §Test Infrastructure).
- Gate B (`--target-macos 27 --target-platform all`) is run as an **advisory read only** and is never `&&`-chained into any definition of done (D-02). Expected: exit 1 with exactly one line per fork — the `com.apple.mobilenotes.SharingExtension` / `WFCreateNoteInput` waiver. Any other line is a real finding.
</verification>

<success_criteria>
- `exit_events` and `exit_selection_counter` are declared in the emitted bootstrap template on both forks.
- `verify_exit_events_seed()` exists, asserts against `EXIT_EVENTS_SEED`, is registered in `main()`'s verify chain and armed on the Aware fork, and is demonstrated to fire against a reverted seed.
- `schema_version` is 4 in all three generator literals and in the emitted template.
- `tools/build_sentient.py` imports and calls five verifiers it did not before.
- Both forks pass gate A clean; `docs/phase6_self_check.py` proves byte-idempotency.
</success_criteria>

<output>
Create `.planning/phases/12-state-shape-sentinel-gaps-exit-events-and-active-session/12-01-SUMMARY.md` when done.
</output>
