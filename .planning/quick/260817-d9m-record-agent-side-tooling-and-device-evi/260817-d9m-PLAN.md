---
phase: quick-260817-d9m
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/CLAUDE.md
autonomous: true
requirements: [QUICK-260817-d9m]

must_haves:
  truths:
    - "A reader of .claude/CLAUDE.md can learn, without asking, that a booted iOS 26.5 simulator with Shortcuts installed is an available runtime evidence channel for this project."
    - "That same reader learns the simulator has no Notes app, so every Control Room Note behaviour still requires the owner's iPhone."
    - "The simulator's rank against the existing four-tier Evidence hierarchy is stated explicitly, not left to inference."
    - "iPhone Mirroring is recorded as request-only — the agent must ask the user to bring it up and must name what needs observing."
    - "/ponytail is recorded as sanctioned, bounded by the generator guards and the do-not-fabricate protocol."
  artifacts:
    - ".claude/CLAUDE.md contains a `## 9. Agent-side tooling and device-evidence channels` section"
  key_links:
    - "New §9 sits between `## 8. Signing and AEA1 round-tripping` and `## Recommended Stack` — the numbered run 1..8 continues unbroken and nothing above or below is renumbered."
    - "§9's hierarchy text keys off the existing `### Evidence hierarchy` list under `## Conventions` (donor > golden corpus > ToolKit catalog > inference) rather than defining a competing scheme."
    - "§9 extends the existing `## Conventions` rule that on-device inspection is a first-class evidence channel, and does not weaken it."
---

<objective>
Record the three agent-side tooling channels now available to this project — the `/ponytail` skill, the iOS Simulator, and iPhone Mirroring — as a new numbered section in `.claude/CLAUDE.md`, with the simulator slotted explicitly into the file's existing Evidence hierarchy and its hard limits stated.

Purpose: the project has depended entirely on the owner's iPhone for every runtime observation, and the standing device backlog is blocked on DIST-03. A booted simulator that can import and run Shortcuts is a genuinely new evidence channel. Left unrecorded, a future agent either does not know it exists or — worse — over-trusts it and substitutes simulator output for a Notes / Apple Intelligence / Personal Automation finding it cannot settle.

Output: one new `## 9.` section in `.claude/CLAUDE.md`. Exactly one file changes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

@.claude/CLAUDE.md
@docs/BUILD-NOTES.md
</context>

<measured_facts>
Confirmed by direct command run during planning on 2026-08-17. The executor MAY re-run these to
confirm; the executor MUST NOT replace any of them with a guess or an inference.

| Command | Result |
|---|---|
| `xcrun simctl list runtimes` | exactly one runtime: `iOS 26.5 (26.5 - 23F77)`, `com.apple.CoreSimulator.SimRuntime.iOS-26-5` |
| `xcrun simctl list devices available` | iPhone 17 Pro (`79A84C29-DB62-40A2-AC3F-CCB5F8192F86`) **Booted**; also available and shut down: iPhone 17 Pro Max, iPhone 17e, iPhone Air, iPhone 17, plus five iPads |
| `xcrun simctl listapps 79A84C29-DB62-40A2-AC3F-CCB5F8192F86` | 25 apps; `com.apple.shortcuts` **present**; `com.apple.mobilenotes` **absent** |

Session tool: `mcp__Claude_Code_iOS_Simulator__control` — actions `attach`, `launch`, `screenshot`,
`tap`, `swipe`, `text`, `button`, `open_url`, `detach`. Available alongside `xcrun simctl` from Bash.
</measured_facts>

<placement_hazard>
`## 8.` and `## Recommended Stack` both sit **inside** the GSD-managed block delimited by
`<!-- GSD:stack-start source:research/STACK.md -->` (line 29) and `<!-- GSD:stack-end -->` (line 254).
A future regeneration of that block from `.planning/research/STACK.md` would drop the new §9.

Mirroring §9 into `.planning/research/STACK.md` is **out of scope** for this task — the brief says
exactly one file changes. Do not edit STACK.md. Record the hazard in the SUMMARY so the user can
decide whether to mirror it later; do not act on it here.
</placement_hazard>

<tasks>

<task type="tracer">
  <name>Task 1: Insert §9 — Agent-side tooling and device-evidence channels</name>
  <files>.claude/CLAUDE.md</files>
  <read_first>
    `.claude/CLAUDE.md` lines 187-207 (all of `## 8.` through the `## Recommended Stack` heading) to
    fix the exact insertion point, and lines 320-360 (the `### Evidence hierarchy`, `### Verified iOS
    Shortcuts runtime semantics`, and `### Operator/operand type validity is invisible in the plist`
    subsections under `## Conventions`) to match their wording and citation discipline. Also skim
    `docs/BUILD-NOTES.md` §3 "Evidence protocol" (binding citation rule) and §11 "Device evidence"
    (how a primary-device finding is written up) so the register matches.
  </read_first>
  <action>
    Insert a new section into `.claude/CLAUDE.md` between the final `## 8.` bullet (the "Known signer
    quirks" line) and the `## Recommended Stack` heading. Use `Edit` with a scoped replacement anchored
    on the `## Recommended Stack` heading — do not rewrite the file with `Write`, and do not renumber,
    reword, reorder, or reflow any existing section.

    The heading must be exactly: `## 9. Agent-side tooling and device-evidence channels`

    Target 25-45 lines total for the section. Tight prose, tables where the file already uses tables.
    This is a tooling record, not an essay. Structure it as:

    1. **One-line lead** stating what the section records and the date it was measured (2026-08-17).

    2. **A tool table** with columns roughly `Tool | How it is reached | Availability`, one row each for:
       - `/ponytail` — the anthropic-skills skill (laziest-solution-that-works: YAGNI, stdlib and
         native-platform-first, minimal diffs). Sanctioned on this project. Add one sentence, in the
         table or immediately under it, bounding it: prefer the minimal change, but laziness never
         licenses skipping the seven parameter-defect axes under `## Conventions` or the
         do-not-fabricate protocol in `docs/BUILD-NOTES.md` §2.
       - **iOS Simulator** — via this session's `mcp__Claude_Code_iOS_Simulator__control` tool
         (actions `attach`, `launch`, `screenshot`, `tap`, `swipe`, `text`, `button`, `open_url`,
         `detach`) and `xcrun simctl` from Bash. Always available on this Mac.
       - **iPhone Mirroring** — real-device UAT on the owner's iPhone. **Not always live**; the user
         sets it up on request. State plainly that the agent must ask for it rather than assume it,
         and that the request should name specifically what needs to be observed.

    3. **The measured simulator inventory**, stated as measured on 2026-08-17, citing the three
       `xcrun simctl` commands from `<measured_facts>` above as the evidence: exactly one runtime,
       iOS 26.5 (26.5 - 23F77); iPhone 17 Pro `79A84C29-DB62-40A2-AC3F-CCB5F8192F86` booted, with
       iPhone 17 Pro Max / 17e / Air / 17 and five iPads also available; 25 apps installed, of which
       `com.apple.shortcuts` is one and `com.apple.mobilenotes` is not. Name the iOS version
       explicitly against the project's declared "iOS 26.x" target so a future reader knows which OS
       produced any simulator evidence.

    4. **Hierarchy placement**, written as an extension of the existing `### Evidence hierarchy` under
       `## Conventions`, not a parallel scheme. State the ordering: the real device (owner's iPhone —
       exported-and-decrypted donor shortcuts, or iPhone Mirroring UAT) outranks the simulator, which
       outranks all file-level analysis (validator, ToolKit catalog, decrypted plist). The reason to
       give: the simulator is a *runtime* channel, so it can settle things no file-level check ever
       can — tie this to the existing `## Conventions` rule that operator/operand type validity is
       invisible in the plist and that inspecting the imported shortcut is a first-class channel, not
       a fallback. The simulator extends that rule; it does not replace the device.

    5. **What the simulator cannot settle** — a short bullet list, each with its reason:
       - the Control Room Note path in full (no Notes app installed), so every
         `com.apple.mobilenotes.SharingExtension`, `appendnote`, `filter.notes` and `shownote`
         behaviour still needs the real iPhone;
       - Apple Intelligence — the simulator is not Apple-Intelligence-capable hardware, so the
         Sentient `Use Model` / On-Device path (CAP-26, BD-04-R2) still needs the owner's iPhone;
       - Personal Automation triggers (App Is Opened / Is Closed);
       - real-hardware environmental behaviour — brightness and volume capture-and-restore.

    Keep every claim traceable to either a named command or a named existing section. Do not
    introduce a new verdict vocabulary, do not assign any capability a verdict, and do not restate or
    revise any existing CAP / BD / DEV / UA row.

    Optionally add ONE line to the existing `## Sources` list citing the `xcrun simctl` commands as
    the evidence for the simulator inventory. Add nothing else to `## Sources`.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && test "$(grep -c '^## 9\. Agent-side tooling and device-evidence channels$' .claude/CLAUDE.md)" -eq 1 && awk '/^## 8\. Signing and AEA1 round-tripping$/{a=NR} /^## 9\. Agent-side tooling and device-evidence channels$/{b=NR} /^## Recommended Stack$/{if(!c)c=NR} END{exit !(a && b && c && a < b && b < c)}' .claude/CLAUDE.md && echo PLACEMENT_OK</automated>
  </verify>
  <done>`.claude/CLAUDE.md` has exactly one `## 9. Agent-side tooling and device-evidence channels` heading, positioned after `## 8. Signing and AEA1 round-tripping` and before `## Recommended Stack`. The section names all three tools, records the measured simulator inventory including the iOS 26.5 version and the absent Notes app, states the device > simulator > file-level ordering against the existing Evidence hierarchy, lists the four things the simulator cannot settle, and states that iPhone Mirroring must be requested from the user.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the edit is purely additive and single-file</name>
  <files>.claude/CLAUDE.md</files>
  <action>
    Run the scope gates below against the working tree. If the additive gate reports any removed line,
    inspect `git diff -U0 -- .claude/CLAUDE.md`, restore whatever Task 1 displaced, and re-run — do not
    relax the gate. If the section length falls outside 25-45 lines, tighten or expand the prose in
    §9 only; do not satisfy the bound by editing any other section.

    Then commit. The commit touches `.claude/CLAUDE.md` alone.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && test "$(git diff --name-only)" = ".claude/CLAUDE.md" && test "$(git diff -U0 -- .claude/CLAUDE.md | grep -E '^-' | grep -cv '^--- ')" -eq 0 && L=$(awk '/^## 9\. Agent-side tooling and device-evidence channels$/{f=1} f&&/^## /&&!/^## 9\./{exit} f{n++} END{print n+0}' .claude/CLAUDE.md) && test "$L" -ge 25 && test "$L" -le 45 && echo "SCOPE_OK section_lines=$L"</automated>
  </verify>
  <done>`git diff --name-only` names `.claude/CLAUDE.md` and nothing else; the diff removes zero lines (purely additive, so no existing section was renumbered, reworded, or reflowed); §9 measures between 25 and 45 lines inclusive. Change committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author → `.claude/CLAUDE.md` | This file is standing instruction content for every future agent in this repo. Text written here is executed as guidance, not merely read, so an inaccurate or over-permissive claim propagates silently into later phases. |
| simulator output → project evidence record | A new, weaker evidence channel entering a project whose existing rules were written assuming only device-or-file evidence exists. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-d9m-01 | Tampering | `.claude/CLAUDE.md` §1-§8, `## Conventions`, `## Sources` | high | mitigate | Task 2's purely-additive gate (`git diff` removes zero lines) makes any renumber, reword, or reflow of an existing section a hard failure. |
| T-d9m-02 | Elevation of Privilege | the Evidence hierarchy | high | mitigate | §9 must rank the simulator strictly below the real device and must enumerate the four classes it cannot settle (Notes, Apple Intelligence, Personal Automation triggers, environmental capture/restore), so it can never be cited to close a device-gated finding. |
| T-d9m-03 | Information Disclosure | device UDID + simulator container paths | low | accept | The UDID is a local CoreSimulator identifier on the owner's own machine, already present in the repo's operating context; it carries no credential and no behavioural data. Record the UDID, not container paths. |
| T-d9m-SC | Tampering | npm/pip/cargo installs | — | n/a | No package installs in this plan. No package-manager step is planned or permitted; the legitimacy gate is not engaged. |
</threat_model>

<verification>
- `grep -n '^## ' .claude/CLAUDE.md` shows the numbered run 1..9 unbroken with §9 immediately before `## Recommended Stack`, and every other heading unchanged from the pre-change list.
- `git diff -U0 -- .claude/CLAUDE.md` shows insertions only.
- `docs/BUILD-NOTES.md`, `docs/CAPABILITY-DECISIONS.md`, `.planning/research/STACK.md`, `src/`, and `tools/` are untouched.
- Every simulator claim in §9 is re-derivable by running the three `xcrun simctl` commands named in the section.
</verification>

<success_criteria>
- One file changed: `.claude/CLAUDE.md`.
- §9 exists, is 25-45 lines, and is placed between §8 and `## Recommended Stack`.
- All three tools recorded; `/ponytail` bounded by the generator guards and the do-not-fabricate protocol.
- Simulator inventory recorded as measured (runtime `iOS 26.5 (26.5 - 23F77)`, booted iPhone 17 Pro `79A84C29-DB62-40A2-AC3F-CCB5F8192F86`, 25 apps, `com.apple.shortcuts` present, `com.apple.mobilenotes` absent), with the commands named as evidence.
- Simulator ranked below the real device and above file-level analysis, written as an extension of the existing `### Evidence hierarchy`.
- The four device-only classes listed with reasons.
- iPhone Mirroring recorded as request-only, with the instruction to name what needs observing.
- Diff is purely additive; no existing section renumbered or reworded.
</success_criteria>

<output>
Create `.planning/quick/260817-d9m-record-agent-side-tooling-and-device-evi/260817-d9m-SUMMARY.md` when done.

The SUMMARY must record the `<placement_hazard>` finding — that §9 lives inside the GSD-managed
`stack` block sourced from `.planning/research/STACK.md` and would be lost on a regeneration of that
block — as an open observation for the user, explicitly noting that mirroring was out of scope here.
</output>
